# P3 — Walking Skeleton (first conversion end-to-end)

> **One trivial conversion, fully through the real architecture, on all 3 OS.** P3
> drives the **in-core CSV→TSV** path (`EngineProgram::InProcessNative`, §3.5.6 — no
> sidecar binary) end-to-end: **drop → detect → pick target → convert → atomic,
> no-overwrite publish → result UI**. It proves the Tauri + atomic-publish + IPC stack
> early, before any heavy engine, so P5–P7 broaden coverage on a proven harness instead
> of discovering architectural problems late (README "walking skeleton first").
>
> Derives from [01-conversion-pipeline](../spec/01-conversion-pipeline.md) (§1.2 layered
> detection, §1.7 dispatch incl. the `InProcessNative` sub-case, §1.8–§1.12 the convert
> spine), [02-guarantees](../spec/02-guarantees.md) (§2.0–§2.7 the no-harm
> guarantees: §2.3 link-safety [T7], §2.4 frozen set [T8],
> §2.5 re-run detection, §2.6 cleanup/temp-ownership, §2.7 destination/divert incl. the
> §2.1.2/§2.7.2 FAT/exFAT divert), [03-engines-and-bundling](../spec/03-engines-and-bundling.md)
> (§3.5.6 native CSV/TSV engine, §3.2 the `Engine`/`Invocation`/`EngineProgram` seam),
> and [05-ui-ux](../spec/05-ui-ux.md) (the minimal walking-skeleton screen path).
> Index: [plan/README.md](README.md). Box format: [`_format.md`](_format.md).
>
> **This is the v0 base** — the smallest atomic `[ ]` boxes below; a later adversarial
> review deepens/splits/completes them. Built by the loop (P1..P11 range) strictly top to
> bottom, deps resolved in place (DECISION C).
>
> **What P3 builds vs defers (the boundary, decided in plan/README.md):**
> - **`crate::fs_guard` is BUILT HERE** (the real atomic-publish OS primitives + FAT/exFAT
>   divert) — it has **no engine dependency** and is exercised by the walking-skeleton
>   publish; P4's §02 reference covers only the §2.12 isolation wrapper + §2.13 app-fault,
>   **not** a re-implementation of `fs_guard` (P4 must not rebuild it).
> - **`crate::isolation` + the §0.9 pool are compile-time SHELLS here** — the §1.7 dispatch
>   enum reaches the `InProcessNative` branch only; if its types reference `crate::isolation`
>   / the pool envelope, P3 creates **interface-only shells so the InProcessNative path
>   compiles without spawning anything**. At runtime CSV→TSV genuinely bypasses
>   isolation/pool (§2.12.4 absolute: pure in-core memory-safe Rust, no subprocess), so this
>   is a compile-time stub, **not** a forward dependency on P4. **P4 fills these shells**
>   (it does not build them from scratch).
> - **No image-worker / libvips / any subprocess engine here** — the first real sidecar is
>   validated in P4/P5.
>
> **Activation targets for P0 (named box-ids):** several P0 boxes carry `→ activated in P3` /
> `→ activated in P3+` notes — the concrete P3 boxes they activate against are:
> G31/G32 output-validity (P0.5.5) → **P3.38/P3.62**; the §1.2 detection-fuzz KAT
> (P0.5.7) → **P3.30** (the KAT [TEST] box, `needs: P3.29`); the detect/`fs_guard` fuzz targets + the replay convention
> (P0.4.3 / P0.5.8) → **P3.29** (`crate::detection` body, P3.26–P3.29; P3.30 is the KAT landmark)**/P3.6/P3.8/P3.41 + P3.67**; the §2.1.3
> atomicity-under-interruption home (P0.5.9) → **P3.19.1**, the §2.1.2 Windows-AV-retry
> fault-injection home (P0.5.9) → **P3.19.2** (the two split into separately-faileable sub-boxes
> under the P3.19 grouping shell); the §2.14.1 temp-ownership / lock-before-part MECHANISM (P0.5.9) →
> **P3.20/P3.21** with its dedicated security-TEST home → **P3.71**; the T7/T8
> link-safety/self-feeding homes (P0.5.9) → **P3.64**;
> `cargo-mutants` over the `fs_guard`/`detect`/`outcome` kernel (P0.5.10) → the runnable
> first-informational-pass [GATE] box **P3.72** (`needs: P0.5.10` + the **P3.6/P3.8/P3.18 /
> P3.29 / P3.46 / P3.68/P3.69** kernel-body boxes it injects mutants into; the §2.8.2/§2.9.1
> catalogs are the `crate::outcome` string-table leg) — the box that RUNS the gate +
> initialises the per-crate ratchet + the `gate-status.md` entry, mirroring P3.67's
> fuzz-replay activation. Both P3.67 (`needs: P0.5.8`) and P3.72 (`needs: P0.5.10`) carry the
> explicit P0→P3 edge; the other activation edges resolve trivially since P0 is `[x]` before
> the loop reaches P3.

## Boundaries (decided, see plan/README.md)

- **P2 → P3:** P2 declared the pipeline **contracts + domain types** (§0.6 `DroppedItem`/
  `CollectedSet`/`OutputPlan`/`RunResult`/`JobStage`/`JobState`, the §0.4 IPC
  command/event signatures, the §1.1 intake state machine, the C12 `EngineHealth`
  contract). **P3 IMPLEMENTS** the bodies behind them for the CSV→TSV slice. A box that
  needs a P2-declared type/handler-stub carries an explicit `needs: P2.<n>` edge; the
  load-bearing P3→P2 edges are wired on the boxes (the type-consuming ones) and owned by
  the P3.70 reconciliation box so no P3→P2 dependency is left implicit.
- **P3 → P4:** P3 establishes the `crate::isolation` + pool **interface shells**; P4
  **expands** them into the real §2.12 wrapper + §0.9 pool. P3 builds the **real**
  `crate::fs_guard`.
- **No-clobber/atomic kernel is content-independent:** every `fs_guard` box is fully
  buildable now (a real temp FS + a real publish primitive), independent of any engine.

---

### Module skeletons & the InProcessNative dispatch seam

**Goal:** the Rust modules the walking skeleton lives in exist and the §1.7 dispatch
reaches the `InProcessNative` branch — with `crate::isolation` and the §0.9 pool present as
**compile-time interface shells** so the in-core path compiles without spawning anything.

- [x] **P3.1** [RUST] Scaffold the `crate::fs_guard` / `crate::run` / `crate::outcome` module roots with their public surface · §2.0 §0.7
  needs: P2.137
  > stand up the three §2.0 trust-kernel leaf-module roots (no dependency on UI/IPC/engine-registry; the layer never calls back up, §2.0 dependency direction) as three INDEPENDENT sub-boxes — they have no mutual dependency at scaffold time, so each is worked + checked off on its own (the loop works them top-to-bottom; a body issue in one root does not couple to the others). `#![deny(unsafe_code)]` at the crate root (no FFI in any P3 module). Downstream boxes that consume one root `needs: P3.1` (the parent gates on all three roots existing — each downstream box needs only the root it names in its own body).
  - [x] **P3.1.1** [RUST] Scaffold the `crate::fs_guard` module root + its public function shells · §2.0 §0.7
    > `src-tauri/src/fs_guard/` with public shells only (`atomic_publish`, `output_name`, `resolve_identity`, `is_safe_output`, `check_path_limit`, `location_status`) wired so the later `fs_guard` boxes (P3.6–P3.19, P3.31–P3.40) fill the bodies. **[Reconcile: P3.1.1 — "public shells" = the documented public-surface CONTRACT MAP, not callable bodies]** no callable body has an honest value at scaffold time (CLAUDE §5/G8; the P2.74 `resolve_identity` precedent — author the type, never a half-body), so P3.1.1 delivers the module-doc surface map + the tier-2-leaf framing, and each function's signature+body lands together in its named fill-box (P3.6–P3.19, P3.31–P3.40).
  - [x] **P3.1.2** [RUST] Scaffold the `crate::run` module root + its public function shells · §2.0 §0.7
    > `src-tauri/src/run/` with public shells only (`cleanup_item`/`cleanup_run`/`sweep_stale` + the run-lifecycle ordering seam) wired so the later `run` boxes (P3.20–P3.25) fill the bodies; no dependency on `fs_guard` to compile its root. **[Reconcile: P3.1.2 — "public shells" = the documented public-surface CONTRACT MAP, not callable bodies]** same rule as P3.1.1: a no-op `cleanup_run`/`sweep_stale` would be a dishonest "cleaned/swept" claim (CLAUDE §5/G8), and no run-owned temp exists to clean at scaffold time, so P3.1.2 delivers the module-doc surface map (the ordering seam included) and each signature+body lands in its fill-box (P3.20–P3.25).
  - [x] **P3.1.3** [RUST] Scaffold the `crate::outcome` error-taxonomy module root · §2.0 §0.7 §2.8
    > `src-tauri/src/outcome/` as the §2.8 taxonomy + `OutcomeMsg`/`CleanupResidue` string home (the `From<ConversionErrorKind>` projection seam P3.46 fills; the §2.8.2 message catalog P3.68 + the §2.9.1 lossy-note catalog P3.69 author the string TABLES) wired so the later boxes fill the bodies; no dependency on `fs_guard`/`run` to compile its root. **[Reconcile: P3.1.3 — the taxonomy ROOT is already type-complete (P2.18/P2.19/P2.20/P2.39.1/P2.73); this box authors NO new type/impl]** it records the root as scaffolded + maps the remaining STRING TABLES (§2.8.2 catalog P3.68, §2.9.1 lossy catalog P3.69) and the P3.46 render seam. TWO sub-clauses corrected: (a) `CleanupResidue` — outcome is the STRING home only (the §2.8.2 catalog ROW for the existing `ConversionErrorKind::CleanupResidue` variant + the §2.6.4 "With residue" tail, both P3.68); the `CleanupResidue { item, residue_path }` STRUCT is orchestrator's (tier 1, P2.12) — a tier-2 leaf cannot reference it (§0.7). (b) the `From<ConversionErrorKind>` projection seam is VACUOUS under the §2.8.2 option-1 alias (`pub type ErrorKind = ConversionErrorKind`, P2.18): `ErrorKind::from(kind)` is the std reflexive `From<T> for T` (identity), and a first-party `impl From<ConversionErrorKind> for ConversionErrorKind` is un-writable (E0119) — nothing for P3.46 to fill; P3.46.2's real work is rendering via the P3.68 catalog.
- [x] **P3.2** [RUST] Build the `crate::isolation` compile-time interface shell (no spawn, no FFI) · §2.12.1 §2.12.4 §0.7
  needs: P3.1
  > the §2.12 decoder-isolation wrapper as an **interface-only shell** so the §1.7 dispatch enum's `Subprocess` arm type-checks; it spawns nothing in P3. CSV→TSV genuinely bypasses it at runtime (§2.12.4 absolute: pure in-core memory-safe Rust, no third-party C/C++ decoder), so this is a compile-time stub, NOT a forward dependency on P4. P4 expands this shell into the real wrapper (it must not rebuild it from scratch). **[Reconcile: P3.2 — "interface-only shell" = the documented public-surface CONTRACT MAP, not a callable spawn symbol]** the confined-spawn entry `run_confined` has no honest non-spawn body (the P2.25 "same value the real body returns" + P2.62 "genuinely-correct zero result" honesty checks both fail — a confined runner that runs nothing is broken, not correct; the P3.1.1 / P2.74 no-half-body rule) AND its §1.7 `EngineInvocation` / `InvocationResult` types are authored by P3.4 AFTER this box, so its signature+body co-land at P4.13. P3.2 delivers the module-doc contract map + the §2.12.3 two-tier design-of-record + the §2.12.4 bypass note; **no `SandboxTier` / policy type is planted** (no P3 consumer; the tier value's shape is a P4 choice made with its P4.18 consumer — CLAUDE §5 no-premature-commitment). The §1.7 dispatch's subprocess-class arms — `EngineProgram::Sidecar` / `ResourceBin` (**`EngineProgram` has no `Subprocess` variant**; `Subprocess` is the §0.6 `EngineKind` — the box title's "Subprocess arm" is shorthand) — are unreachable-by-construction in P3 and return the honest `InvocationResult::Failed(InternalError)` (the P2.25 precedent), referencing no isolation symbol; P4.13 fills the shell.
- [x] **P3.3** [RUST] Build the §0.9 subprocess-pool interface shell + the in-core worker-thread permit lane · §0.9 §1.7
  needs: P3.1
  > the §0.9 pool as an interface shell carrying the **global-degree** permit model; expose the **`spawn_blocking`-style worker-thread lane** the `InProcessNative` engine actually uses (it holds a global-degree permit, has **no** `serialised_only` lane, runs on dedicated worker threads so the synchronous CSV loop never blocks the Tokio runtime, §0.9 native-CSV/TSV row / §1.7 concurrency-permit model). The subprocess permit/semaphore machinery is shelled (P4 fills it); only the in-core lane is live in P3.
  > **[P3.3 dep + reconcile notes]** the live lane promotes `tokio` to a FIRST direct dependency (`tokio::sync::Semaphore` + `tokio::task::spawn_blocking`; prod features `rt`+`sync`, dev `macros`, no `net`); the paired §0.8 drift-floor row is the [!extern] L(-1) box **P3.82** (Co-Pilot, `needs: P3.3`, mirroring tokio-util P2.133/P2.134). The §0.9 `clamp(physical_cores−1,1,4)` literal is reconciled to `std::thread::available_parallelism()` (std has no physical-core API) in this box's commit (§0.9 `[DECIDED: P3.3]` note + the P4.20 reuse-reconcile, DoD item 2). P3.3 authors NO §0.9 `pub const` (`MAX_LO_CONCURRENCY`→P4.22, native timeout→P3.45, subprocess watchdog→P4.20).
- [x] **P3.4** [RUST] Wire the §1.7 dispatch enum reaching the `InProcessNative` branch only · §1.7 §3.2 · G29
  needs: P3.2, P3.3
  > the §1.7 `EngineInvocation` dispatch envelope `(JobId, EngineId, Invocation, CancellationToken)` and the match on `Invocation.program: EngineProgram` (§3.2). In P3 only the **`InProcessNative(EngineId)`** arm is implemented; the `Subprocess`/`ResourceBin` arms compile against the P3.2 isolation shell but are unreachable in the walking skeleton. Exhaustive-match deny on `EngineProgram` (no `_ =>` catch-all, the §1.2/G29 deny set, P0.4.1) so a future engine arm cannot be silently dropped. **[Reconcile: P3.4 — "compile against the P3.2 isolation shell" = the `needs: P3.2` module-presence/authored-contract edge, NOT a called isolation symbol]** P3.2 is a DOC-ONLY contract map (its `run_confined` signature+body co-land at P4.13), so the `Sidecar`/`ResourceBin` arms reference NO `crate::isolation` symbol in P3 — being unreachable-by-construction (only the in-core engine is wired in the walking skeleton) they return the honest `InvocationResult::Failed(InternalError)` (the P2.25 precedent). Naming: the variants are `EngineProgram::Sidecar` / `ResourceBin` (§3.2.2 has NO `Subprocess` variant; `Subprocess` is the §0.6 `EngineKind`). P4.13 authors the real `run_confined` and rewrites these arms to route through it.
  > **[Reconcile: P3.4 ↔ P4.6 — the type-authoring home]** this box is the SOLE author of the §1.7 `EngineInvocation` + `InvocationResult` types, at their spec-fixed FULL shape (§1.7 `[DECIDED]`: `Failed(ConversionErrorKind)` — the §2.8 kind authored at P2.18 — plus `Succeeded`/`Cancelled`), not a P3-slim variant; P4.6 (`needs: P3.4`) is the P4-side reconcile seat that verifies them against §1.7 and wires the P4 consumers (its note carries the resolution).
  > **[Reconcile: P3.4 ↔ P4.2/P4.3 — the §3.2.2 transitive hull, resolved 2026-07-07 (Co-Pilot; the Loop's P3.4 hard-stop escalation)]** the §1.7 envelope transitively embeds the §3.2.2 plan-seam cluster (`EngineInvocation.plan: Invocation`; the dispatch matches `Invocation.program`/`Invocation.progress`), so P3.4 ALSO authors — in `crate::engines`, at their §3.2.2 literal shapes — `Invocation`, `EngineProgram`, `StdinPlan`, `TempPath` (= `tempfile::TempPath`), `PlanError` (the P3.5 `plan()` signature type; `kind: ConversionErrorKind` from P2.18) and `ProgressModel`. `ProbeOutput` stays P4-authored (P4.2): the §3.2.1 two-phase probe leg is P4-only — neither the envelope nor P3.5's `plan()` references it. P4.2/P4.3 are the P4-side reconcile seats (their notes carry the split); their former `needs: P4.1` edges were compiler-inverted (the §3.2.2 trait REFERENCES these types; the types reference no trait). Supply-chain: `TempPath` promotes `tempfile` dev→prod — move the row from `[dev-dependencies]` to `[dependencies]` in THIS box's commit (Loop-doable: already in `Cargo.lock`, no new package, `Cargo.toml` is not L(-1)); the §0.8 floor needs NO work — the `PINNED_FLOORS` row exists since P1.59 (reconcile-to-present; the walkdir precedent) and §0.8 already lists tempfile (the "FS guarantees … exact" row), the §3.2.2 `TempPath` block itself records "tempfile is already in §0.8"; the floor-row comment refresh ("via dev-deps") is a Co-Pilot L(-1) touch-up once this box is `[x]`. All hull types are core-internal (no serde/specta) — no `bindings.ts`/G19 surface.
- [x] **P3.5** [RUST] Define the `Engine` trait `plan()` for the native CSV/TSV engine (Pure, no I/O) · §3.2 §3.5.6
  needs: P3.4, P2.8, P2.13
  > the §3.2 `Engine::plan(&job) -> Result<Invocation, PlanError>` impl for the native engine: returns an `Invocation { program: InProcessNative(EngineId::NativeCsvTsv), out_tmp: Some(<dest-dir publish temp>), progress: ProgressModel::InProcessFraction, .. }` (§3.2.2). `plan()` is **Pure** (no I/O, no spawn); single-step engine so `plan_encode` is never reached (the default-impl InternalError, §3.2). `EngineId::NativeCsvTsv` is absent from the §3.3.3 `EngineId → binary-name` table (no sidecar to resolve, §3.5.6).
  > **[Reconcile: P3.5 — the minimal trait home; P4.1 EXPANDS it]** this box authors the `Engine` TRAIT itself in its minimal walking-skeleton form (the §3.2.2 `plan()` signature) together with the native impl; P4.1 (`needs: P3.75`) EXPANDS the same trait to the full §3.2.2 surface (descriptor/capabilities/plan_encode/classify_failure) — never a second trait. `Invocation`/`PlanError`/`ProgressModel::InProcessFraction` come from the P3.4 hull.
  > **[Decision: P3.5 — the 2026-07-07 plan-seam ruling (Co-Pilot; resolves the Loop's P3.5 hard-stop)]** three spec corrections, landed in the ruling's commit (§3.2.1/§3.2.2/§1.7 + the §0.6 FFprobe note + spec-README annotations): **(1) `out_tmp` ownership** — `plan()`/`plan_encode()` BORROW `out_tmp: &TempPath` (the borrow exists only for argv embedding) and construct the returned `Invocation` with `out_tmp: None`; §1.7 OWNS the temp and populates `Some(temp)` on the ENCODE invocation after the call returns. The §3.2.2 `TempPath` lifecycle block was already `[DECIDED]` this way ("OWNED by the §1.7 invocation; the engine … never owns deletion") — the removed "built from the passed temp" doc phrase was the stale literal (literal→normative, the 1c8e8 class); a by-value param provably breaks the P4.9 probe under the shared trait signature (the probe engine's `plan()` would drop the encode temp — file deleted — with `plan_encode` still ahead of it). **(2) tier-3 params** — `fn plan(&self, item: &DroppedItem, target: TargetId, input: &Path, out_tmp: &TempPath)`: `crate::engines` (tier 2) cannot take the tier-1 orchestrator-homed `ConversionJob` (§0.7); §1.7 projects. `input` is the effective read path §1.7 hands in (P3: the §2.3-resolved source; the §3.5.0 macOS staging hands the scratch copy from P4 on) — argv embeds `input`, never an `item`-derived path. **(3) the probe discriminator** — `plan()` returns `Result<PlanOutcome, PlanError>`, `PlanOutcome::{Encode(Invocation), Probe(Invocation)}` (§3.2.2): both shapes construct `out_tmp: None`, so `out_tmp.is_some()` cannot mark the probe; probe-ness is per-JOB (the same FFmpeg engine encodes audio single-step and probes video), so it is NOT an `EngineDescriptor` flag. THIS box authors `PlanOutcome` with the trait (exhaustive-match, no catch-all); the box literals above (`plan(&job) -> Result<Invocation, PlanError>`, `out_tmp: Some(<dest-dir publish temp>)`) are the pre-ruling shorthand — the `Some` literal describes the SPAWN-TIME shape after §1.7 populates, not `plan()`'s return. The P3 caller matches `Probe(_)` to the honest `InternalError` arm (unreachable-by-construction: the only P3 engine is single-step; the P2.25/P3.4 precedent). The owner-populates step is NOT the §3.2.1-banned struct mutation (§3.5.1: "no placeholder-then-mutate"; that ban protects the ENGINE-computed `duration_us` — `out_tmp` is §1.7's OWN resource; §3.2.2 `fn plan` states the boundary); its Rust realization is the builder's choice — in-place assignment or functional record update (`Invocation { out_tmp: Some(temp), ..planned }`) — the spec fixes the SEMANTICS (the owner attaches its resource before dispatch), not the form. In THIS box's commit, also reconcile the P3.4-authored `crate::engines` prose to the ruling (DoD item 2): the `Invocation.out_tmp` prose at BOTH sites (the struct-header block AND the field comment) and the plan-seam-fields test's assertion message (the `engines` unit-test module) still phrase the encode=`Some`/probe=`None` dichotomy without the "plan() constructs `None`; §1.7 populates at spawn time" framing — restate them at spawn-time framing (their spawn-time invariant itself stays true).

---

### `crate::fs_guard`: resolved-identity, link-safety & path-limit (the no-harm kernel)

**Goal:** the load-bearing "same file?" / "would this write touch an original?" / "would
this name overflow the path limit?" predicates exist on all 3 OS — the §2.3/§2.2.3 half of
`fs_guard` the atomic publish sits on top of. These are the BRANCH-coverage-floored
kernel functions (P0.4.8) and `cargo-mutants` targets (P0.5.10).

- [x] **P3.6** [RUST] Build `fs_guard::resolve_identity` — canonical path + OS file identity, per OS · §2.3.1 §2.3.4 · G9
  needs: P3.1, P2.74
  > `resolve_identity(path) -> io::Result<FileIdentity>` (the `FileIdentity` TYPE is authored at **P2.74**, §2.3.1 — this box fills the FUNCTION body, it does NOT re-define the type): `std::fs::canonicalize` primary; **Unix** `(st_dev, st_ino)` via `MetadataExt`; **Windows** `(volumeSerialNumber, fileIndexHigh, fileIndexLow)` via `GetFileInformationByHandle` (catches hardlinks + junctions `canonicalize` misses, §2.3.4); **macOS** `(st_dev, st_ino)` + Finder-alias-is-a-data-file note. Display/comparison form normalised via `dunce::canonicalize` so a `\\?\`-prefix difference compares equal (§2.3.1). G9 invariant (d) no raw `127.0.0.1`/`localhost` — N/A here but the module joins the grep scope.
  > **Signature note (option A):** the real signature is fallible `io::Result<FileIdentity>` (canonicalize is fallible; §2.3.2 retries on the parent when the path does not exist) — the §2.3.1 `-> FileIdentity` literal is the infallible shorthand. Reconcile the §2.3.1 spec literal to the fallible form in THIS box's commit (DoD item 2; G68 doc-graph sweep for any other `-> FileIdentity` literal). P3.1.1 sets the fs_guard public-surface CONTRACT MAP (a documented surface, no callable body — the P2.74 no-half-body precedent generalised); this box adds `resolve_identity`'s signature + body into that map's named slot.
  > **[Resolution: P3.6 hard-stop — dunce/winapi-util floors + the §2.3.1 stable-toolchain correction, 2026-07-07 (Co-Pilot)]** the Loop's escalation (dunce floor row = unplanned L(-1)) was CORRECT and is resolved by **P3.83** (both floor rows landed, reconcile-to-present). Its "safe-std, no FFI" premise was HALF right: no unsafe FFI is needed, but NOT via std — `MetadataExt::volume_serial_number()`/`file_index()` are nightly-gated (`windows_by_handle`, rust-lang #63010; E0658 proven on the pinned stable toolchain 1.96.0), and `same_file::Handle` exposes no Windows identity numbers (`dev()`/`ino()` are Unix-only, verified in the 1.0.6 source). The §2.3.1 Windows identity therefore goes through **`winapi-util`** (`file::information(handle)` → the two `u64`s `FileIdentity` stores; safe wrapper, no `unsafe` in the core) — §2.3.1 + the §0.8 FS-guarantees row corrected accordingly in the P3.83 commit. THIS box's commit does both `Cargo.toml` promotions itself (`dunce` under `[dependencies]`, `winapi-util` under `[target.'cfg(windows)'.dependencies]` — `Cargo.toml` is not L(-1); the tempfile/P3.4 precedent) AND reconciles the `fs_guard` module-doc contract map to this ruling (DoD item 2): the header's "per-OS handle FFI … single allow-listed `crate::platform` shim (P3.6/P3.9)" sentence over-claims for P3.6 (identity is unsafe-free via `winapi-util`; only P3.9's dir-handle primitive remains the FFI case), and `resolve_identity`'s documented dep list gains `winapi-util` beside `dunce`. The box literal "via `GetFileInformationByHandle`" stays true — `winapi-util` wraps exactly that syscall.
- [x] **P3.7** [RUST] Build the resolved-identity de-dup of the frozen set · §2.3.2 §2.4.1
  needs: P3.6, P2.76
  > key every frozen entry by `FileIdentity`; two dropped paths resolving to the same inode/file-index collapse to **one** `DroppedItem` (converted once, SSOT); the retained representative is the **first-seen** path (deterministic), but identity — not the path string — is the dedup key (§2.3.2). Applied as the set is built in §1.1/§2.4 (P3.5).
  > **Reconcile (option A — dup with P2.76):** the PURE de-dup fold (first-seen-wins over `FileIdentity`) is homed at **P2.76**; this box is its REAL-FS integration — call P3.6 `resolve_identity` per candidate → key by `FileIdentity` → the P2.76 fold — the hardlink/two-paths→one-member behavior a synthetic-`FileIdentity` unit test cannot exercise. `needs: P2.76` (the fold). Part of the systemic P2↔P3 §1.1 reconciliation (see the P2.74 systemic flag); finalise the build-vs-integrate split when this box is built.
- [x] **P3.8** [RUST] Build `fs_guard::is_safe_output` — write-target link-safety against the frozen source set · §2.3.3 · G9 G48
  needs: P3.6
  > reject any candidate `final` whose resolved identity equals a frozen **source FILE**, or whose resolved parent resolves onto/into a source file's resolved path (§2.3.3 rule 2); the frozen set holds **files only**, so landing beside-source **inside a dropped folder is the normal correct case** and must NOT be rejected (§2.3.3 — guard is "resolves onto an original file?", not "under a dropped folder?"). On reject → divert (§2.7, P3.6), and the divert target is re-checked. Closes T7 (input-side symlink/junction); a G48 in-core fuzz target (P0.4.3) — incl. the §2.3.3 dangerous-path classes.
- [x] **P3.9** [RUST] Build the parent-directory-handle safety primitive (TOCTOU-closed) · §2.3.3 · G29 G48
  needs: P3.8
  > open the parent dir handle FIRST (`std::fs::File::open` Unix / a `FILE_FLAG_BACKUP_SEMANTICS` dir handle Windows; a `stat` type pre-check rejects a FIFO/device/non-dir parent BEFORE the open so it cannot hang), verify the **open handle's** identity is not inside the frozen set (§2.3.1 resolved-identity / canonical-path **equality** on the handle's real path — NOT an ancestor-prefix test), then publish the leaf **relative to that same handle** — so the parent cannot be swapped to a symlink between check and write. The publish primitives (P3.12–P3.14) accept this dir fd/handle; `fs_guard::atomic_publish(verified_parent, tmp, leaf)` encapsulates the per-OS form (§2.3.3). G29 SAST (the `std::process`/path-validation rules N/A; the module is unsafe-free — the Windows handle identity is read via `winapi-util`'s SAFE wrapper, no raw FFI; the raw publish FFI `renameat2`/`renameatx_np`/`NtSetInformationFile` is homed in the allow-listed `crate::platform` by P3.12/P3.14, P0.4.2).
- [x] **P3.10** [RUST] Build `fs_guard::output_name` — verbatim-stem + space-paren numbering candidates · §2.2.1 §2.10.1
  needs: P3.1
  > `output_name`: `stem` taken **byte-for-byte** (§2.10.1 — `OsString`, no `to_string_lossy` for any operation, no transliteration/ASCII-fold/emoji-strip), `ext` = target's canonical lowercase extension (`tsv`/`csv`); first candidate `stem.ext`, then `stem (n).ext` (n=1,2,3…) — the SSOT space-paren shape, never `_1`/`-1`/a hash; multi-dot stems preserved (`my.report.final` → `…final.tsv`). Candidates produced **lazily** for the §2.2.2 exclusive-publish loop (P3.15), never a directory-list max+1 pre-scan.
- [x] **P3.11** [RUST] Build `fs_guard::check_path_limit` — per-OS component + total limits, fail-never-truncate · §2.2.3 §2.10.1 · G48
  needs: P3.10
  > validate the **resolved final** path length before the exclusive create: **Windows** total ≤ 260 (`MAX_PATH`, no long-path-aware assumption) + component ≤ 255 UTF-16; **macOS** component ≤ 255 UTF-8 bytes, total ≈ `PATH_MAX` 1024; **Linux** component ≤ 255 (`NAME_MAX`), total ≤ 4096 (`PATH_MAX`). When appending `(n)`/swapping the extension would overflow → emit `PathTooLong` (§2.8), **never truncate**. Windows: prepend `\\?\` manually for constructed numbered candidates (no "dunce inverse" — `dunce` only strips), but a user-facing path > 260 still fails clearly. Runs on the fully-resolved path **including any §2.7 divert** (P3.6). A G48 in-core fuzz target (NUL-path / `PATH_MAX`+1 bound-firing fixtures, P0.4.3).

---

### `crate::fs_guard`: the atomic, no-clobber publish OS primitives + FAT/exFAT divert

**Goal:** the real `fs_guard::atomic_publish` — the no-placeholder exclusive-rename per OS,
the durability fsync, the FAT/exFAT third-fallback that triggers a divert, and the §2.1.3
two-state crash invariant. This is the heart of what the walking skeleton proves.

- [x] **P3.12** [RUST] Build the Unix single-call no-replace publish primitive (Linux/macOS, named) · §2.1.1 §2.1.2 · G48
  needs: P3.9
  > the dir-handle-relative create-only publish: **Linux** `renameat2(olddirfd, tmp, dirfd, leaf, RENAME_NOREPLACE)` (fails `EEXIST`); **macOS** `renameatx_np(olddirfd, tmp, dirfd, leaf, RENAME_EXCL)` (the macOS equivalent; macOS has NO `renameat2`/`RENAME_NOREPLACE` — the Linux spelling must not be used). Chosen at **runtime per destination** (an unsupported `EINVAL`/non-`VOL_CAP_INT_RENAME_EXCL` filesystem falls back, P3.13), never a build-time switch. No 0-byte placeholder at `final` ever (§2.1.2). G48 fuzz target on `is_safe_output`/`atomic_publish` (P0.4.3).
  > **[Decision: P3.12 hard-stop — the syscall crate is `rustix`, floored at P3.84, 2026-07-07 (Co-Pilot)]** the escalated crate choice is ruled for the whole §2.1 publish cluster: **`rustix` 1.1.4** (the Loop's recommendation CONFIRMED by source-verification, not pattern-match: `rustix::fs::renameat_with(olddirfd, tmp, dirfd, leaf, RenameFlags::NOREPLACE)` maps to `renameat2`/`RENAME_NOREPLACE` on Linux and to `renameatx_np`/`RENAME_EXCL` on apple — BOTH box spellings behind ONE safe dirfd-relative call (`AsFd`, fits the P3.9 verified parent handle); `libc` rejected — raw `unsafe` per call site). THIS box's commit promotes it (`[target.'cfg(unix)'.dependencies] rustix = { version = "1.1.4", features = ["fs"] }` — `Cargo.toml` is not L(-1); already in the lock via `tempfile`, the P3.6 class). **Premise correction:** P3.12 is NOT the first unsafe FFI in `crate::platform` — rustix's API is SAFE, so P3.12/P3.13/P3.16/P3.18 land with ZERO `unsafe` in the core; the first real unsafe externs arrive at P3.14 (`windows-sys`, see its note). The runtime `EINVAL`-class fallback detection stays reactive per the box text (rustix surfaces the same `io::Errno` values).
- [x] **P3.13** [RUST] Build the Unix `link`+`unlink` fallback + its success-window residual handling · §2.1.2 §2.1.3
  needs: P3.12
  > the portable POSIX fallback `link(tmp, final)` then `unlink(tmp)` (fails `EEXIST`), used when the single-call no-replace primitive is unavailable/unsupported. Handle the **success-window residual** (after `link` succeeds, before `unlink` — both `final` and `*.part` briefly exist; the §2.1.3 link-form sub-state): `final` is complete+durable (Success); a failed `unlink` leaves a residual `*.part` reclaimed by the §2.6.4 sweep (annotated, not an item failure). On NFS treat an ambiguous rename result as name-may-be-taken → re-pick (§2.1.2).
- [x] **P3.14** [RUST] Build the Windows dir-handle-relative create-only publish (`FileRenameInformationEx`) · §2.1.2 §2.3.3
  needs: P3.9
  > `NtSetInformationFile(tmpHandle, …, FileRenameInformationEx)` with `RootDirectory` = the verified parent-dir HANDLE, `FileName` = `leaf`, `Flags` **OMITTING** `FILE_RENAME_REPLACE_IF_EXISTS` → `STATUS_OBJECT_NAME_COLLISION` on collision (the Ex-class bitfield, not the boolean `ReplaceIfExists`). Closes the parent-swap race (plain path-string `MoveFileExW` re-resolves by path and does NOT). Atomicity comes SOLELY from the no-replace move; `MOVEFILE_REPLACE_EXISTING`/`ReplaceFileW` have **no caller** in v1. Bounded short-backoff AV-retry on transient `STATUS_ACCESS_DENIED`/`STATUS_SHARING_VIOLATION` → then `WriteFailed` (§2.8).
  > **[Provisioned: P3.14 crate — `windows-sys` 0.61.2, floored at P3.84 (the P3.12 ruling, 2026-07-07)]** the Windows externs ride **`windows-sys` 0.61.2** (verified in the registry source: the documented kernel32 shim `SetFileInformationByHandle(FileRenameInfoEx = 22, FILE_RENAME_INFO { Flags, RootDirectory, FileName })` — the exact Ex-class no-replace move this box describes at the NT level — AND the Wdk `NtSetInformationFile` route; the builder picks within the one crate). THIS box's commit promotes it (`[target.'cfg(windows)'.dependencies] windows-sys = { version = "0.61.2", features = [...] }`; already in the lock via `tempfile`). Feature fact (G1-verified in the 0.61.2 source): `FILE_RENAME_INFO`/`SetFileInformationByHandle`/`FileRenameInfoEx` live under `Win32_Storage_FileSystem` (+ `Win32_Foundation`), but the Ex-class `FILE_RENAME_FLAG_*` bitfield constants live in `Win32_System_WindowsProgramming` — the feature list must include it (or the NT route rides `Wdk_Storage_FileSystem`, whose `FileRenameInformationEx = 65` is a DISTINCT enum from the Win32 `FileRenameInfoEx = 22`). These externs are the FIRST real `unsafe` calls in `crate::platform` — the module-inner `#![allow(unsafe_code)]` + per-call `// SAFETY:` comments per G29/`check-unsafe-policy` (the allow-list already seeds `src-tauri/src/platform/*.rs`).
- [x] **P3.15** [RUST] Build the numbering ↔ no-clobber retry loop (one bounded loop) · §2.1.2 §2.2.2
  needs: P3.12, P3.13, P3.14, P3.10
  > the single bounded loop where §2.2 numbering and the absolute no-clobber guarantee are the SAME thing: hand each lazy `output_name` candidate to the dir-handle-relative exclusive publish; on the exists-error bump the `(n)` suffix and yield the next candidate; cap ≈10 000 variants → too-many-collisions/`PathTooLong` failure (§2.8). The directory's real state at the instant of the kernel publish decides — not a stale scan (an optional cheap `symlink_metadata` pre-check may skip obviously-taken low numbers, but the **authority is always the kernel publish**, §2.2.2).
- [x] **P3.16** [RUST] Build the durability sequence — `sync_all` + post-publish directory fsync · §2.1.1
  needs: P3.15
  > step 3 `tmp.sync_all()` (`fsync`/`FlushFileBuffers`) so bytes are durable **before** the rename; step 6 on Unix **fsync the containing directory** after the rename (and on the `link`+`unlink` path after `link`, the same dir-fsync — bytes already durable via the shared inode); Windows dir-fsync is a no-op (NTFS journaling), `MOVEFILE_WRITE_THROUGH` is best-effort metadata flush only. Atomicity does NOT depend on `WRITE_THROUGH` (§2.1.1).
- [x] **P3.17** [RUST] Build the §2.14.3 EXDEV cross-volume fallback inside `fs_guard::atomic_publish` (copy-exactly-once → same-volume exclusive publish) · §2.14.3 §2.1.2 §2.8 · G48 G31
  needs: P3.16, P3.15, P3.20, P3.21
  > **Forward-ref note (DECISION-C ordering inversion):** `needs: P3.20`/`P3.21` point at the publish-temp naming model (`.convertia-<InstanceId>-<RunId>-<jobId>-<rand>.part`, P3.20) + the lock-before-part run-lifecycle invariant (P3.21) defined later in document order — the EXDEV fallback places its cross-volume intermediate under the per-run scratch root using that `.part` naming so §2.6.3 can reclaim it, so DECISION C builds both first; the edge is acyclic and valid, the inversion documented at the `needs:` line.
  > the §2.14.3 `[DECIDED]` reactive cross-device fallback `fs_guard::atomic_publish` tries the direct intra-volume no-placeholder publish (P3.14/P3.15) first and only on **EXDEV/cross-device** runs: (a) detect the cross-device failure; (b) place the engine output on a **named, swept other-volume kind-2 temp** (under the per-run scratch root, or carrying the `.convertia-<InstanceId>-<RunId>-<jobId>-<rand>.part` naming so §2.6.3 reclaims it — never an anonymous `$TMPDIR` `tempfile`), the lock-before-part ordering (P3.21) covering it; (c) **re-check `final`-volume free space against the intermediate (≈ output) BEFORE the copy** and fail `OutOfDisk` (§2.8) if it won't fit (this path's ~2× destination-volume peak the §1.10/§2.14.4 preflight does NOT model — mirror §2.7.2's late-divert "never assume it fits"); (d) copy the cross-volume temp into a **new same-volume temp** + `sync_all()` it; (e) publish that same-volume temp → `final` with the no-placeholder exclusive-rename (P3.12/P3.14), the §2.2 numbering retry **re-renaming the SAME already-copied intermediate** on a collision (the expensive cross-volume copy happens **EXACTLY ONCE**; only the cheap intra-volume exclusive-rename loops); (f) dir-fsync; the extra copy removed by §2.6. The cross-volume step is a **copy**, never a cross-volume `rename`; the only rename is intra-volume + exclusive. (The §2.14.2 `0o700` per-run kind-2 scratch-ROOT creation primitive this temp lands under is owned by P3.21's run-lifecycle ordering — the run-start `run-<RunId>/` create-then-lock step.)
- [x] **P3.18** [RUST] Build the FAT/exFAT-class detection + `DivertReason::NoAtomicPublish` (Unix-only) · §2.1.2 §2.7.2 · G48
  needs: P3.13
  > detect a destination filesystem that supports **neither** the no-replace rename **nor** hardlinks (FAT/exFAT — `link()` → `EPERM`/`ENOTSUP`): via the OS filesystem-type/`statfs`-class query OR a one-shot capability probe (no-replace-rename → `EINVAL`/unsupported AND `link()` → `EPERM`/`ENOTSUP`). On Unix this is a per-location **DIVERT trigger** carrying `DivertReason::NoAtomicPublish` (§0.6) — the full §2.1 chain then runs on the hardlink-capable system disk (P3.6). **Windows is NOT diverted** — `MoveFileExW`-without-`REPLACE_EXISTING` is a true create-only move on FAT/exFAT too. A G48 bound-firing target.
  > **[Decision: P3.18 hard-stop — the READ-ONLY `statfs`-class realization, 2026-07-07 (Co-Pilot; resolves the Sonnet-P1 probe-residue fork)]** the box's FIRST option is ruled in: the detection is the read-only `statfs`-class query — Linux `rustix::fs::statfs(dest)` → `StatFs = statfs64` `f_type` ∈ {`libc::MSDOS_SUPER_MAGIC` 0x4d44 (covers vfat — the kernel has no separate VFAT magic), 0x2011BAB0 exFAT (NOT libc-exported — a project constant with a magic.h citation)}; macOS the same call → `StatFs = c::statfs` whose **public `f_fstypename: [c_char; 16]`** ∈ {"msdos" (uniform for FAT12/16/32), "exfat"} (the escalation's "rustix exposes no clean macOS `f_fstypename`" premise is FALSE — verified in the rustix 1.1.4 + libc 0.2.186 registry sources; rustix's apple `StatFs` IS the raw libc struct). NO file is written, so the Sonnet-P1 unreclaimable-residue defect is structurally impossible and the A–D naming-ownership fork dissolves. The one-shot WRITE probe stays a spec alternative but is bound by §2.7.2 to "**in the probe dir**" — i.e. the P3.33/P3.36 probe infrastructure (`fs_guard::location_status` performs the create; `crate::run` owns the naming grammar + reclaim, P3.24) under the reclaimable `.convertia-<InstanceId>-probe-<rand>.part` form (the escalation's option B/D ownership, already the plan's design) — NEVER a standalone `fs_guard`-named probe file in the user's destination (the discarded working-tree draft's `.convertia-fscap-*` grammar; preserved off-repo as a scratchpad patch, superseded). List-miss honesty: an exotic no-rename/no-hardlink FS not matching the FAT/exFAT class is caught EXACTLY by the already-built reactive §2.1.2 third-fallback arm at the real publish (`PublishOutcome::NoAtomicPublishSupport`, P3.12–P3.17 — its temp is P3.20-reclaimable by construction), so the `statfs` list is the proactive per-location planning heuristic, not the correctness backstop. G48 bound-firing target unchanged (magic/name-list boundary fixtures instead of probe-error fixtures).
- [x] **P3.19** [TEST] Assert the two independent fault-injection invariants — the §2.1.3 all-OS crash/power-loss two-state invariant + the §2.1.2 Windows-only AV-retry · §2.1.3 §2.1.2 · G31 G15
  needs: P3.16, P3.14, P0.5.9
  > grouping shell over **two independent, separately-faileable** fault-injection scenarios that exercise different failure modes on different OS paths via different `#[cfg(test)]` seams and fail for independent reasons (a broken Windows AV retry must NOT block debugging the cross-OS crash invariant) — split per the same discipline as P3.46 (.1 FSM / .2 projection, "a bug in one is independent from a bug in the other; each fails a different check"). The parent is `[x]` only when both sub-boxes are (_format.md §2). This box (via its sub-boxes) is the activation target for BOTH P0.5.9 homes — the §2.1.3 atomicity-under-interruption home (→ P3.19.1) AND the §2.1.2 Windows-AV-retry fault-injection home (→ P3.19.2): this is the P3 box the P0.5.9 `→ activated in … P3` atomicity/AV-retry edge points at (`needs: P0.5.9`, the P0 home is `[x]` before the loop, mirroring the P2.127/P4.18.1 back-reference pattern). (`needs: P3.16` for the durability sequence the crash invariant is premised on + `P3.14` for the Windows publish path the AV-retry seam injects into.)
  - [x] **P3.19.1** [TEST] Assert the §2.1.3 crash/power-loss two-state invariant — kill in the post-`sync_all`-pre-`rename` window (all 3 OS) · §2.1.3 · G31 G15
    needs: P3.16
    > a `#[cfg(test)]` fence in `fs_guard::atomic_publish` injecting a kill **specifically between `sync_all()` and the rename** (all 3 OS, premised on the P3.16 durability sequence) and asserting the on-disk state is **exactly one of** the §2.1.3 states (complete `final`, OR no `final` + a discardable `*.part`, OR — link-fallback only — `final` + a leftover `*.part`) — **never** a truncated/0-byte `final`. G31 source-unchanged leg corroborates no original was touched. The activation target for the P0.5.9 §2.1.3 atomicity-under-interruption home. (`needs: P3.16` for the durability sequence this invariant is premised on.)
  - [x] **P3.19.2** [TEST] Assert the §2.1.2 Windows-only AV-retry fault-injection — transient `STATUS_SHARING_VIOLATION`/`STATUS_ACCESS_DENIED` → bounded retry → `WriteFailed` · §2.1.2 §2.8 · G31 G15
    needs: P3.14
    > a `#[cfg(test)]` seam in the Windows `FileRenameInformationEx` path (P3.14) injects a transient `STATUS_SHARING_VIOLATION`/`STATUS_ACCESS_DENIED` and asserts the **bounded short-backoff retry** fires and, when the violation persists past the cap, the publish surfaces `WriteFailed` (§2.8) — never a silent overwrite, never an unbounded spin. Windows-specific (`cfg(target_os="windows")`). The activation target for the P0.5.9 §2.1.2 Windows-AV-retry fault-injection home. (`needs: P3.14` for the Windows publish path the seam injects into.)
    > **[RECONCILE: P3.19.2 delivered-at-P3.14, 2026-07-10 (Loop; both G1 reviewers verified)]** the §2.1.2 Windows AV-retry fault-injection was ALREADY fully covered at P3.14 by the real-lock tests `fs_guard::publish_rename_windows_tests::a_persistent_lock_exhausts_the_av_retry_to_writefailed` (a persistent no-`FILE_SHARE_DELETE` handle → real `ERROR_SHARING_VIOLATION` → the bounded short-backoff retry exhausts → §2.8 `WriteFailed`, no clobber, tmp intact) and `…::the_av_retry_recovers_when_the_lock_clears_mid_retry` (a transient lock cleared mid-retry → `Published`). REAL locks are the test-strategy §0.1 preferred form ("never mock the FS under test") — SUPERIOR to the injected `#[cfg(test)]` seam this box's original phrasing suggested, so no seam was added. `STATUS_ACCESS_DENIED` coverage-equivalence: `crate::platform::rename_noreplace_at` collapses `STATUS_ACCESS_DENIED | STATUS_SHARING_VIOLATION` (the NT move) and `ERROR_ACCESS_DENIED`/`ERROR_SHARING_VIOLATION` (the `tmp`-open) into ONE `WindowsRenameOutcome::Retryable` value BEFORE `publish_rename_windows`'s retry loop runs, so exercising the real `SHARING_VIOLATION` exercises the identical retry code path `ACCESS_DENIED` would — no distinct behaviour is left untested. **Forward-guard:** if that OR-arm is ever SPLIT into per-status branches, a real-`ACCESS_DENIED` trigger must be added (the equivalence is proven by reading the shared arm, not a second live fault). No new code — P3.19.2 is `[x]` by reconcile.

---

### `crate::run` & `crate::fs_guard`: temp ownership, cleanup & free-space restoration

**Goal:** run-owned `*.part` namespacing, the startup sweep that never deletes a live
instance's temp, and honest residue reporting — so a killed/failed/cancelled walking-skeleton
item leaves nothing (or surfaces the residue). Activation target for P0.5.9 temp-ownership.

- [x] **P3.20** [RUST] Build the publish-temp naming + ownership model (`InstanceId`+`RunId`-encoded) · §2.6.1 §2.14.1 §3.5.6
  needs: P3.1
  > the kind-1 publish temp is a uniquely-named **sibling dotfile** in the destination dir — `…/<dest_dir>/.convertia-<InstanceId>-<RunId>-<jobId>-<rand>.part` (`tempfile::NamedTempFile::new_in(final_dir)` / a `TempPath` rooted in `final_dir`), on `final`'s volume by construction (§2.14.1), **never** a system-temp file (§3.5.6 — the native engine's `out_tmp` is this dest-dir temp). `InstanceId`+`RunId` encode ownership so cleanup can tell its own temps from a concurrent instance's and resolve the exact owning lock (§2.6.1).
- [x] **P3.21** [RUST] Build the lock-before-part run-lifecycle ordering invariant · §2.6.3 §2.14.1
  needs: P3.20
  > `crate::run` at run start: mint `RunId` → create `run-<RunId>/` under the central scratch root → acquire + OS-lock `.lock` → **only then** write the first `*.part`. This is the premise that makes "absent lock ⇒ dead ⇒ reclaimable" SAFE (so a concurrent sweeper never deletes a live foreign `*.part`, §2.6.3) — a §6 property-test target.
- [x] **P3.22** [RUST] Build `crate::run::cleanup_item` / `cleanup_run` — own-prefix-scoped cleanup on every exit path · §2.6.2 · G31
  needs: P3.20
  > the per-exit-path cleanup table (§2.6.2): item-success (single-call → nothing to remove; link-fallback → `unlink(tmp)`); item-failure → remove that `tmp`; out-of-disk → remove partial + `OutOfDisk` (§2.8), batch continues; run-end → remove the recorded `final_dir` set's temps **by exact own prefix `.convertia-<thisInstanceId>-<thisRunId>-*.part`**, **never a bare `*.part` glob** (which would delete a concurrent foreign instance's live temp — SSOT). Recording the actual `final_dir` per written item (incl. divert/cross-volume) is what makes run-end enumerate every dir a temp landed in.
- [x] **P3.23** [RUST] Build `crate::run::sweep_stale` — startup sweep with held-lock as the sole delete gate · §2.6.3
  needs: P3.21
  > on startup glob `convertia/scratch/<*>.<*>/run-*` across ALL instance dirs; liveness via a **NON-BLOCKING try-lock** (Unix `flock(LOCK_EX|LOCK_NB)`/`fcntl(F_SETLK)`; Windows `LockFileEx` with `LOCKFILE_FAIL_IMMEDIATELY|LOCKFILE_EXCLUSIVE_LOCK` — bare forms BLOCK, wrong here). Would-block ⇒ LIVE ⇒ untouched; immediate-acquire ⇒ DEAD ⇒ removed (then release). Held-lock is the **sole** delete predicate — never mtime/PID alone (PIDs are reused, §7.1.2). Close the create-then-not-yet-locked window: skip lockless run dirs within a short mtime grace window.
- [x] **P3.24** [RUST] Build the opportunistic destination-resident `*.part` reclaim (cross-instance lock-addressable) · §2.6.3
  needs: P3.23
  > because kind-1 `*.part` live in destination dirs (not the central scratch root) and §7.4 persists no destination set, reclaim them at (a) run-end/same-session retry and (b) **opportunistically** before any later write into a dest dir: remove a sibling stale `.convertia-*.part` only when its owning run is **dead** — resolve the exact owning lock `convertia/scratch/<InstanceId>.*/run-<RunId>/.lock` from the embedded ids (held ⇒ keep; free/stale/**absent** ⇒ dead ⇒ reclaim). Recognise the **pre-RunId probe residue** `.convertia-<InstanceId>-probe-<rand>.part` (no RunId/jobId) → resolve liveness by `InstanceId` alone (§2.6.3 / §2.7.2).
- [x] **P3.25** [RUST] Build cleanup-failure honesty — `CleanupResidue` surfacing, never a silent clean success · §2.6.4 §1.12 §2.8.2 · G31
  needs: P3.22, P3.68, P3.76
  > **Forward-ref note (DECISION-C ordering inversion):** `needs: P3.68` points at the §2.8.2 message catalog at end-of-P3 (later document order) — the `CleanupResidue`/"With residue" strings this box surfaces live in that catalog — and `needs: P3.76` at the core-owned-path wire layer (the 2026-07-06 owner ruling) whose display-typed `CleanupResidue { item, residue_display }` this box constructs (the landed P2.12 shape still carries the pre-ruling `residue_path: PathBuf`; P3.76 re-types it); DECISION C builds both first; the edges are acyclic and valid, the inversion documented at the `needs:` line.
  > **[Resolution: P3.25 hard-stop — the missing P3.76 edge was a plan-wiring bug, 2026-07-07 (Co-Pilot; option A)]** the box's prose has described the POST-ruling display form since the 4217659 revision, but the `needs:` line and the cluster-intro consumer list were missed in that commit's wiring pass (the sibling P3.50 got the `needs:` edge; the intro list had missed P3.50 too — fixed in the same pass). Option B (build against the pre-ruling `residue_path: PathBuf` wire shape, re-type at P3.76) is REJECTED — it would land a NEW raw-path wire emission against the 2026-07-06 owner ruling and force the correct prose backwards. The same wiring pass missed **P3.48** (it streams `ItemStarted` + `ItemFinished { outcome: ItemOutcome }` + the live `IpcError` — all in P3.76's re-type list) — both edges + the consumer list fixed in this ruling's commit; the class was swept (P3.46.2 is path-free kind/text-only; the P3.5x UI chain inherits P3.76 transitively — since the 2026-07-12 P3.81 re-ordering via P3.53 → P3.78 → P3.77 → P3.76, originally via P3.54 → P3.81).
  > if removing a temp fails: success-with-undeletable-`tmp` → success stands + a `residue` annotation; a failed item whose partial couldn't be cleaned → reported **Failed** WITH `CleanupResidue` naming the path (never a clean success); a Cancelled item whose publish temp survived the §1.7 bounded group-kill confirm-wait → carries a `CleanupResidue` + the §2.8.2 "With residue" tail (§2.6.4 case 3). `CleanupResidue { item, residue_display }` flows into `RunResult.cleanup_incomplete` (§1.12; the wire carries the display form per the 2026-07-06 core-owned-paths ruling — the real residue `PathBuf` stays `RunResultStore`-side for the C9 `Residue(ItemId)` reveal). The `{path}`-substituted `CleanupResidue` string + the "With residue" tail live in `crate::outcome` (the §2.8.2 catalog row authored in **P3.68** — `needs: P3.68`, followed in place by DECISION C).

---

### `crate::detection`: the §1.2 layered-detection framework bootstrap (CSV/TSV path)

**Goal:** the §1.2 detection framework exists (magic-sniff + container/text/encoding
classification scaffolding) and classifies the walking-skeleton types — enough to drive
drop→detect→group. P5–P7 later add only per-format signatures. Activation target for the
P0.5.7 KAT convention and the P0.4.3 detect fuzz target.

- [x] **P3.26** [RUST] Build the layered-detection dispatcher skeleton (magic → container → text → structural-peek) · §1.2 §2.12.4 · G29
  needs: P3.1
  > the §1.2 strategy order as a dispatcher: (1) magic-byte/signature sniff on a bounded **first-4-KiB** header window; (2) container introspection seam (ZIP/OLE/`ftyp`/gzip — stubbed for P3, filled by P5–P7); (3) text classification; (4) bounded structural-peek for `notes`/`dims`. All steps are bounded reads in **memory-safe Rust** with no third-party C/C++ decoder, so detection runs **in-core** (§2.12.4 absolute satisfied) — no isolation subprocess for a sniff. Only the text-classification path needed for CSV/TSV is live in P3; the rest are typed seams.
- [x] **P3.27** [RUST] Build the text/encoding classification (BOM → UTF-8 → codepage fallback) · §1.2 §2.10.2
  needs: P3.26
  > confirm bytes decode as text (BOM → strict UTF-8 → single-byte codepage fallback, e.g. `chardetng`); the encoding heuristic stays **in-core** (memory-safe, bounded, no C/C++ decoder, §2.12.4). Produces `CollectedSummary.encoding_hint` (e.g. "Windows-1252") for the §1.4 summary line. Text encoding is detected, **never assumed from the extension** (§2.10.2).
- [x] **P3.28** [RUST] Build CSV-vs-TSV delimiter detection (content over name) · §1.2 §2.10.2
  needs: P3.27
  > delimiter sniff over the bounded sample: a consistent tab-delimited file is **TSV** even if named `.csv` (content over name, §1.2/spreadsheets.md); a consistent comma file is CSV; produce `CollectedSummary.delimiter_hint`. Ambiguous (no consistent delimiter) → `Uncertain` (never silently extension-fall-back). Grouping keys on the resulting **`UserFacingFormat`** (CSV ≠ TSV, delimiter-determined, §1.3).
- [x] **P3.29** [RUST] Populate the `DetectionOutcome` result model + outcome rules · §1.2
  needs: P3.28, P2.15
  > emit `DetectionOutcome::Recognized { format, confidence: Confidence, dims: None }` for CSV/TSV (non-raster → `dims: None`); `UnsupportedType { detected }` / `Uncertain { best_guess }` / `Empty` / `Unreadable { reason: ReadFailure }` for the others. Outcome rules: unsupported/uncertain/empty/unreadable are **never** offered a target list and never extension-fall-back (§1.2); a `.csv`-that-is-really-TSV converts as its **detected** type. `Confidence { High, Low }` — `Low` never silently falls back to the extension.
- [x] **P3.30** [TEST] Stand up the §1.2 detection KAT first entries (CSV/TSV) · §1.2 §6.4.1 · G15
  needs: P3.29, P0.5.7
  > add the first `tests/detect-kat.toml` entries pinning canonical CSV/TSV (and a `.csv`-that-is-TSV, an ambiguous→`Uncertain`) files to their exact `FormatId`, read by the G15 unit test so §6.4.1's claim is machine-enforced at L2. This is the P3 box the P0.5.7 `→ activated in P3` KAT-convention edge points at (`needs: P0.5.7`, the P0 home is `[x]` before the loop, mirroring the P3.67 `needs: P0.5.8` activation-target pattern). A pure-Rust detection-fuzz target on `crate::detection` is the P0.4.3/G48 leg (registered there).

---

### The §1.1/§2.4 freeze (`crate::orchestrator`, §0.7 conductor) + §2.7 destination & per-location divert (`crate::fs_guard`)

**Goal:** the frozen source set (§2.4), the recursive folder walk (§1.1), and the §2.7
destination model incl. per-location writability/ephemeral/FAT-exFAT divert — so the walking
skeleton can ingest a CSV folder and land output beside-source or diverted, on all 3 OS.

- [x] **P3.31** [RUST] Build the §1.1 recursive folder walk + hidden/system filter + per-item-failure-continues · §1.1 §2.4.1
  needs: P2.64
  > **Reconciled `[x]` — DUP of the already-built P2 walk (systemic §1.1 pass, Co-Pilot, owner-ratified):** the §1.1 recursive-walk PRIMITIVE (depth-first `walkdir`, symlinked-dirs-not-followed for loop-safety; the dotfiles + `.DS_Store`/`Thumbs.db`/`desktop.ini` + Windows hidden/system ignore constant; per-item read-failure → `SkippedItem`, walk CONTINUES; fatal-walk-root stop; retain dropped roots for §2.7) is ALREADY built + unit-tested in `crate::orchestrator` (`walk_intake_roots`, delivered by **P2.64/P2.65/P2.66/P2.67/P2.68**, all `[x]`, dead-code pending wiring). §0.7 homes §1.1 in orchestrator (the "§01 conductor builds the queue" role), NOT fs_guard — the walk is NOT re-homed (§0.7 > the plan-heading, owner-ratified). No distinct deliverable remains: the primitive is P2.64-68; the per-item-DETECT-failure + the end-to-end walk→detect→de-dup→freeze wiring is **P3.49** (which already references the P2.64 walk). `[Reconcile: systemic-§1.1-pass — dup-delivered-by-P2.64-68; orchestrator-home per §0.7]`
- [x] **P3.32** [RUST] Build the §2.4 freeze-point (eager immutable snapshot) in `crate::orchestrator` · §2.4.1 §2.4.2 §1.1
  needs: P3.7, P2.4
  > snapshot the set **eagerly and once** into an immutable `Vec<DroppedItem>` (conversion iterates the snapshot, never re-reads the dir — the structural no-self-feeding defence, §2.4.2); de-dup by `FileIdentity` (P3.7); apply the P2.73 zero-byte/unreadable-at-intake → `Skipped(Empty|Unreadable)` classification at the snapshot (distinct from turn-time gone/unreadable = `Failed`). Closes T8 (no self-feeding) at the freeze. Homed in `crate::orchestrator` (§0.7 conductor — the frozen set becomes the queue), co-located with the P2.64-68 walk + the P2.76 de-dup, NOT fs_guard.
  > **Reconcile (systemic §1.1 pass, owner-ratified):** re-scoped to the freeze-point PRIMITIVE. The zero-byte-at-intake = `Skipped` vs turn-time = `Failed` classification LOGIC is already built by **P2.73** (`[x]`) — dropped from the title (was a dup); this freeze APPLIES it. `needs:` redirected P3.31 → **P3.7** (the real-FS de-dup this freeze folds; P3.31 is the reconciled-out walk dup). The end-to-end walk→detect→de-dup→freeze wiring stays **P3.49**.
  > **[Correction — executed at P3.40 (the 2026-07-07 frozen-model-identity ruling):** `freeze_snapshot` as built DISCARDED each survivor's `FileIdentity` after the de-dup (moving only `canonical_path` into `ItemPaths.resolved_path`) — under-implementing the §0.4.4 `[DECIDED]` "identity evidence" content item. The P3.40 Decision note rules the retention (an orchestrator-side `identities` table beside the snapshot, §0.7-tier-clean); the code correction lands with P3.40's fill.]
- [x] **P3.33** [RUST] Build `fs_guard::location_status` — writability + ephemeral classification (cached per-dir) · §2.7.2 · G31
  needs: P3.1
  > the **writable** test: `create_new` a throwaway probe file (`.convertia-<InstanceId>-probe-<rand>.part`, pre-RunId, §2.7.2) then remove it — confirms the dir accepts a create (NOT the §2.1 publish primitive; do not share the helper). Probe **lazily, cache per-directory** within the run. **Ephemeral** test: under `%TEMP%`/`GetTempPathW` (Win), `$TMPDIR`/`/tmp`/`/var/folders` (macOS), `$TMPDIR`/`/tmp`/`/var/tmp`/`/run/user/<uid>` (Linux) → divert. Probe-cleanup-failure → still **writable**, logged only, never a divert. The per-dir cache is a planning **hint**, not a commitment (P3.36 re-checks at write).
- [x] **P3.34** [RUST] Build the §2.7 destination modes — beside-source + user-chosen-root subtree re-creation (create-only, ancestor-by-ancestor) · §2.7.1 §1.8
  needs: P3.9, P3.33
  > **beside-source (default):** output in the source's parent dir. **User-chosen root `D`:** re-create the dropped-root-relative subtree `D/sub/dir/file.<tgt>` (never flattened); each missing ancestor created **create-only** (`mkdir`, never `mkdir -p`-that-accepts-an-existing-file), ancestor-by-ancestor, then the deepest-created dir's handle is opened + link-safety-verified (P3.9) before the leaf publish (§2.7.1 / §2.3.3 ordering). The common root = deepest dir containing all frozen sources (computed at freeze).
- [x] **P3.35** [RUST] Build the §2.7.3 divert target resolution + re-test of the divert root · §2.7.3 §2.7.4
  needs: P3.33
  > on an unwritable/ephemeral/`NoAtomicPublish` location, divert that source's output (per-location, not whole-batch) to **Downloads → Documents fallback** via `PathResolver` (`download_dir()`/`document_dir()`), overridable by the user-chosen root. The divert target is itself run through `location_status` (incl. the FAT/exFAT test, P3.18) — if it too is ephemeral/unwritable/FAT-exFAT → fail clearly `WriteFailed` (§2.8), never divert onto a purgeable/another-FAT volume. Diverted outputs de-collided by the same §2.2 numbering; summary maps each output to its source (§2.7.4).
- [x] **P3.36** [RUST] Build the late-divert path — post-probe read-only flip re-runs the full safety chain · §2.7.2 §2.7.5 · G31
  needs: P3.35, P3.15, P3.11
  > when the real §2.1 publish fails for a **writability** reason (USB pulled / share dropped / permission flip after the cached probe), treat the location as unwritable and **late-divert** to the §2.7.3 target **before** reporting failure — re-running the full chain on the divert target: §2.3.3 `is_safe_output`, §2.2.3 path-limit re-checked against the divert **absolute path**, §2.14.4 free-space re-checked against the divert **volume**, then the §2.1 publish. A non-writability error (`OutOfDisk`) is NOT a divert trigger. The divert path is **not degraded** — every guarantee runs identically (§2.7.5, the SSOT Principle-5 assertion this proves end-to-end).

---

### The §2.1.1 write-sequence assembly (`crate::orchestrator`, §0.7) + the `crate::fs_guard`/`run` §1.8 OutputPlan legs (CSV→TSV)

**Goal:** the §1.8 `OutputPlan` computation and the §2.1.1 7-step write sequence wired
together, plus the §2.5 re-run detection — the orchestration that consumes detect+plan and
produces an atomic publish for the walking-skeleton job.

- [x] **P3.37** [RUST] Build the §1.8 `OutputPlan` computation (directory-based, no pre-baked `final_path`) · §1.8 §2.7
  needs: P3.34, P3.35
  > compute `OutputPlan { job, final_dir, diverted: Option<DivertReason>, base_name, extension, publish_temp_dir }` per job before any write, applying the §2.7 rules: resolve `final_dir` (beside-source or diverted), set `publish_temp_dir = final_dir` (the sibling-dotfile temp on the same volume, §2.14.1). **Directory-based by design** — the exact final name + `(n)` numbering is resolved at write time on the resolved real file (P3.15), **never** pre-baked into a `final_path` string (a pre-numbered path reintroduces the §2.1.2 TOCTOU race). No `crosses_volume` field (EXDEV detected reactively at publish, §2.14.3).
- [x] **P3.38** [RUST] Assemble the §2.1.1 per-item write sequence (pick-temp → engine-writes → sync → resolve-late → publish → dir-fsync → cleanup-on-error) · §2.1.1 · G31 G32
  needs: P3.37, P3.16, P3.22
  > wire the 7 steps in order: (1) pick the publish-temp on `final`'s volume (P3.20); (2) the native CSV/TSV engine writes into `tmp` (P3.8); (3) `tmp.sync_all()`; (4) resolve `final` + the no-clobber decision **as late as possible**; (5) the no-placeholder exclusive-rename publish (P3.15); (6) durability dir-fsync (P3.16); (7) on any error in 3–6, remove `tmp` — `final` was never created (P3.22). Exit-verification: success **only if** the temp output exists and is non-empty (§1.7). G31/G32 source-unchanged + output-validity bind to this pair (activation target for P0.5.5).
  > **[Decision: P3.38 home = `crate::orchestrator`, 2026-07-07 (Co-Pilot; the Loop's home escalation — §0.7 wins over the plan heading)]** the sequence composes `run` (temp/cleanup) + `fs_guard` (publish) + the engine step — and §0.7 is decisive: the tier-1 orchestrator "*sequences*" the tier-2 leaves, `fs_guard` depends DOWN only, and `run`/`fs_guard` are mutually-independent siblings, so ONLY the orchestrator can compose all three (a deliberate `run`→`fs_guard` edge was rejected — it breaks the §0.7 sibling structure for one box's convenience). The cluster heading was a loose module label, now corrected (the 77b3731 §1.1-freeze precedent: §0.7 > a plan-heading mis-file; the P2.10 ‡ type-lift is the same rule). Side-points ratified with it: **(a)** the engine step is a callable seam (a closure/small trait writing into `tmp`) — `needs:` correctly excludes P3.41; the real native engine binds at P3.41/P3.48; **(b)** the two gate legs activate at DIFFERENT times: **G32(a) source-unchanged** binds NOW (the sha256 before==after check is assertable with the stub seam), while **output-validity — G31's per-pair structural readers, reused by G32(b)** — binds when the REAL engine + corpus land (P3.41 → P3.62/P3.63); the `· G31 G32` markers stay as the P0.5.5 activation target (gate-leg attribution G1-corrected against the build-gates.md rows — an earlier draft swapped the two ids). **Prevention sweep (same class, this ruling's commit):** P3.39 clarified by note (the compute/store split honouring §2.5.2 — see there); P3.40/P3.44/P3.45 verified leaf-local; P3.25's boundary was already built correctly at P3.22 (`cleanup_run` returns `Vec<PathBuf>`, the orchestrator projects, §0.7-cited in `run/mod.rs`); the P4 §1.7 lifecycle boxes (P4.7/P4.11) carry a forward note.
  > **[Reconcile: P3.38 — the P3.48 composition ruling, 2026-07-12]** this box's "the real native CSV/TSV engine binds at P3.41/P3.48" seam expectation predated the P3.43–45 §1.7 lifecycle fills (built to the §3.2.2/P3.5 out_tmp ownership). The P3.48 ruling composes the run path as *pick-temp → await `dispatch` → publish legs* (§2.1.1 step 2 = the awaited §1.7 dispatch — mandatory for the P4+ subprocess engines, whose step 2 is inherently async): `write_item`'s step 1 moves conductor-side, its `engine_write` seam is SUPERSEDED on the run path, and steps 3–7 are re-cut into the publish-leg fn — every §2.1.1 guarantee stays live; the G31/G32(a) tests migrate under `[Test-Change: P3.48]` tags. See the P3.48 Decision note.
- [x] **P3.39** [RUST] Build the §2.5 re-run equivalence key + in-session ledger (the sole firing signal) · §2.5.1 §2.5.2
  needs: P3.37
  > `EquivKey = hash(source_identity, target_format, effective_settings_canon)` — **no destination component** (v1 verdict destination-independent, §2.5.1); `effective_settings_canon` is the fully-defaulted option set serialised order-independently. `crate::run` keeps an in-memory `HashSet<EquivKey>` (cleared on quit, nothing persisted, §7.4) — a second identical drop **same session** → the prompt fires. Disk presence is a **corroborator only, never fires alone** (an existing same-named file is an ordinary collision → silent numbering across sessions, §2.5.2). Accept the documented vanished-output / changed-destination edges.
  > **[Clarification: the compute/store SPLIT — `crate::orchestrator` COMPUTES the key, `crate::run` STORES the ledger (§2.5.2 verbatim) — the P3.38 prevention sweep, 2026-07-07 (Co-Pilot; G1-corrected)]** the KEY composes `FileIdentity` (a `fs_guard` type) + target + settings — computing it in `run` would need the `run`→`fs_guard` sibling edge §0.7 rejects, and the orchestrator already holds all three inputs (the frozen set carries the identities, C4/C6 carry target+settings) — so the `EquivKey` COMPUTATION homes in `crate::orchestrator`. The `HashSet<EquivKey>` STORAGE stays in `crate::run` exactly as §2.5.2 `[DECIDED]` states ("Within the current app session, `crate::run` keeps an in-memory `HashSet<EquivKey>`") behind a small opaque-key API (`run::has_seen(key)`/`run::record(key)` shape — the orchestrator calling DOWN into the leaf is an ordinary tier-1→tier-2 edge; `run` never sees `FileIdentity`, only the finished hash, so no sibling edge arises and NO spec edit is needed). An earlier draft of this note re-homed the whole ledger to the orchestrator — both G1 reviewers NOGO'd it against the untouched §2.5.2 `[DECIDED]` (a plan note cannot override normative spec; SSOT > spec > plan) and the split above is the spec-consistent resolution.
  > **[Decision: the `effective_settings_canon` canonicalisation = a `std::hash::Hash` fold over the existing ordered structures — NOT serde_json, 2026-07-07 (Co-Pilot; the Loop's scoping question)]** derive `Hash` on the ~6 domain types the key folds (`OptionKey`/`OptionValue`/`OptionValues`, `TargetId`/`UserFacingFormat`/`CrossCatOp`) and feed ONE hasher in fixed field order — the pieces were BUILT for this: `OptionValue`'s §1.6 INVARIANT excludes floats (Int(i64)/Bool/Enum/Color — all Hash-derivable, Eq already derived everywhere), `OptionValues` is a `BTreeMap` whose `Ord`-derived `OptionKey` doc names "the §2.5 EquivKey canonicalisation" as its purpose, and BTreeMap iteration IS §2.5.1's "sorted keys" order-independent form; `FileIdentity` carries `Hash` since P2.74. The minimal-derive policy's "Hash only when something keys on it" condition is now met — update any "NO Hash" rationale comments on the touched types in the SAME commit (DoD 2), with `[Build-Session-Entscheidung: P3.39]` tags at the derive sites. **serde_json canonicalisation REJECTED**: it would couple the key to serde attributes/JSON formatting for zero gain — the P2.22 §0.4.5 forward-guard already characterises `EquivKey` as a core-internal hash-style u64 that is NEVER on the wire. Constraints for the fill (G1-precised): the ledger is session-only (§2.5.2 cleared-on-quit, §7.4 persists nothing) so NO cross-run/cross-version hash stability is needed — the seed-stable hasher is the COMPUTE-side one: ONE process-lifetime `BuildHasher` instance held with the orchestrator's `compute_equiv_key` state produces the u64 `EquivKey` (never a fresh `RandomState` per call — two computes of the same job must agree); the run-side `HashSet<EquivKey>`'s own bucket hasher is INDEPENDENT (any single long-lived std `HashSet` suffices). `UserFacingFormat` already derives `Hash` (P2.3) — the new derives are OptionKey/OptionValue/OptionValues/TargetId/CrossCatOp. A u64 collision's worst case is one spurious RerunPrompt (a hint dialog — §2.5's disk-presence corroborator + §2.1 exclusive-create make data loss impossible regardless). Prevention sweep: P3.40 CONSUMES this key (no second canonicalisation site exists — §7.4 persists no options); no other planned box extends domain derives.
- [x] **P3.40** [RUST] Wire re-run detection into C4 + the §2.5.3 never-overwrite fallback · §2.5.2 §2.5.3 §1.8
  needs: P3.39
  > compute re-run equivalence during **C4 `plan_output`**, returned in `OutputPlanPreview.rerun` (so the UI enters RerunPrompt before Convert); C6 carries the user's `RerunDecision` (Skip default / FreshCopy → ordinary §2.2 numbering, **never** a replacing publish). When equivalence can't be determined (renamed/moved prior output, new session) → fall through to §2.2 silent next-free-variant numbering — the failure mode is a harmless extra numbered copy, **never** an overwrite (which §2.1's exclusive-create makes impossible regardless, §2.5.3).
  > **[Decision: the §2.5.1 `source_identity` input = the freeze-RETAINED identities — §0.4.4 had already decided this, 2026-07-07 (Co-Pilot; the Loop's frozen-model-identity escalation)]** Option A, and NOT as an open fork: §0.4.4's `[DECIDED 2026-07-06]` registry content list already names "**the §2.3 identity evidence**" beside the path pairs and the real roots — the built model under-implements that enumeration (`freeze_snapshot` destructures each survivor and DROPS the `(dev, inode)` numbers after moving `identity.canonical_path` into `ItemPaths.resolved_path`), so per SSOT > spec > code the CODE yields. Option B (re-resolve at C4) would leave the registry without the mandated evidence AND reintroduce per-item plan-time stat IO + the freeze-to-plan TOCTOU + the FIFO-hang class (P3.9) on already-frozen sources; option C (path-keyed) contradicts §2.5.1 verbatim ("Source **identity** (not path)" — the hardlink/two-paths match is the point). **Home (§0.7-decisive — the P3.38/77b3731 class):** NOT a field on the tier-3 `domain` model — `FileIdentity` is a tier-2 `fs_guard` type, so `ItemPaths`/`FrozenCollectedSet` embedding it would be an upward edge; the evidence rides ORCHESTRATOR-side: `FrozenSnapshot` (P3.32) gains an `identities` table (`BTreeMap<ItemId, FileIdentity>` shape) over every RESOLVED survivor — eligible AND detect-ineligible skipped alike (both exit the same resolve+de-dup pass WITH a §2.3.1 identity, and §0.6 invariant 6 frames the frozen set as "ALL dropped items — eligible AND skipped alike"); only a walk/resolve-FAILURE skip has none (`resolve_identity` failed — a physical fact, not a scoping choice; an existing such file is still protected by the §2.1 exclusive-create like any pre-existing file). `freeze_snapshot` RETAINS each survivor's identity (clone the `canonical_path` into `ItemPaths.resolved_path`, move the identity into the table), and the §0.4.4 registry value becomes an orchestrator composite record (the domain `FrozenCollectedSet` + the identities table; naming = the fill's `[Build-Session-Entscheidung]` — the registry is already orchestrator-homed, P2.44). Consumers served: C4 folds `identities[item]` into `compute_equiv_key` (P3.39); the §2.1.1 `write_item.frozen_sources` / §2.3.3 comparison set = the FULL resolved-survivor table — §2.3's promise is unqualified ("never writes to, through, or as a target that resolves (via **symlink, alias, junction or hardlink**) onto any source in the frozen set"), a final resolving onto ANY dropped original (eligible or skipped) is §2.3.3's divert case, and §2.2 numbering covers collisions with UNRELATED pre-existing files. (An R1 draft of this note scoped both to eligible-only — G1-NOGO'd against §0.6 invariant 6 + the unqualified §2.3/SSOT promise: a plan note cannot narrow normative spec — the same class as the P3.39 R1 ledger re-home.) Executed WITH this box's fill (a spec-conformance correction of built code — no new box; the retained-but-unwired parts ride the established dead-until-P3.49 shell pattern); in the same commit, sweep the doc-comment CLASS calling `resolved_path` "the §2.4 de-dup identity" (the `(dev, inode)` key is the identity, the path is the §2.3.2 representative — G68 described-the-old-way; the phrasing recurs across `domain/mod.rs` — grep for it rather than enumerating sites). §0.4.4 gains a `[CLARIFIED]` carrier sentence in this ruling's commit. **Prevention sweep (the class: a box implements the fields its TITLE/headline names while the spec § ENUMERATES more content in prose):** the other §0.4.4 stores audited — the picked-destination registry (`DestinationId → PathBuf`) and `RunResultStore` (outputs/`common_root`/`divert_root` `PathBuf`s, P2.43) match their content lists; near-members verified NON-members: the real §2.7.1 rel-path (derivable offline from the stored `raw_path.strip_prefix(root)` — nothing discarded), the §2.7 common root (computed at plan time over the stored roots, pure), the §2.3.3 dir-HANDLE identity (fresh-by-design from the OPEN handle, never frozen data). Forward: the P3.49/P3.78 registering fills implement §0.4.4's FULL enumeration — this note is their pointer.

---

### The native CSV→TSV engine (§3.5.6, in-core MIT Rust) + §1.7 InProcessNative lifecycle

**Goal:** the actual in-core transform — single streamed pass, CSV-injection-safe, RFC-4180
re-quoting — and its §1.7 `InProcessNative` lifecycle (self-reported progress, cooperative
cancel, wall-clock timeout). This is the one engine the walking skeleton runs.

- [x] **P3.41** [RUST] Build the streamed CSV/TSV transform pass (encoding-normalise → delimiter-swap → RFC-4180 re-quote) · §3.5.6 §2.10.2
  needs: P3.28, P3.5
  > a single streamed pass: detect encoding/delimiter (P3.27/P3.28) → re-encode to **UTF-8 (no BOM default)** → swap delimiter → **RFC-4180 re-quote** where a field contains the new delimiter/quote/newline → write to `out_tmp`. Use a real RFC-4180 reader (the `csv` crate). MIT (own code, no §3.6 concern). Both directions (CSV→TSV and TSV→CSV); the offered non-diagonal default for a CSV source is **TSV** (same-format CSV diagonal excluded from tiles, §1.5).
- [x] **P3.42** [RUST] Build the CSV-injection-safe literal-preservation rule · §3.5.6 · G31 G32
  needs: P3.41
  > leading `= + - @` stay **literal text** (never re-interpreted as a formula) — the CSV-injection-safe guarantee (§3.5.6); the G32 output-validity reader asserts CSV-injection literal-preservation (P0.5.6) over the corpus, so this is the behaviour that gate binds to.
- [x] **P3.43** [RUST] Build the §1.7 `InProcessNative` self-reported progress (`progress_tx` → `ItemProgress`) · §1.7 §1.11 §3.2.2
  needs: P3.41, P3.4
  > no stdout to line-read → §1.7 attaches **no** line-reader and instead passes a bounded `tokio::sync::mpsc::Sender<f32>` (`progress_tx`) into the `spawn_blocking` executor; the sync loop `blocking_send(bytes_processed / source_size)` at each N-KB chunk; §1.7 forwards every received fraction as one `ConversionEvent::ItemProgress { runId, itemId, fraction, stage }` — wire-indistinguishable from every other engine (§1.11). Sub-100-KB inputs → a single `1.0` start→done tick (indistinguishable from `CoarseSpawnDone`). Bounded channel = natural back-pressure.
- [x] **P3.44** [RUST] Build the §1.7 cooperative cancel (poll token at chunk boundary, drop `out_tmp`) · §1.7 §2.1
  needs: P3.43
  > the sync loop polls the job's `CancellationToken` at every N-KB chunk boundary; on cancel it stops mid-stream, **drops the `out_tmp` `TempPath`** (deleted on drop, §3.2.2) → `Cancelled` with no partial leftover — the "cleanly discards the one in progress" guarantee reached **cooperatively** (no kill step to sequence; the §2.6 group-kill step is a no-op for this engine, §1.7 InProcessNative sub-case).
- [x] **P3.45** [RUST] Build the §1.7 wall-clock timeout + wedged-uninterruptible-read bound · §1.7 §0.9 §2.12.4
  needs: P3.44
  > a §0.9-owned wall-clock timeout (tight for this light engine) wraps the sync call; on expiry the loop is cancelled cooperatively → `Failed(EngineHang)`, run **CONTINUES**. The wedged-uninterruptible-read caveat: the abandoned thread MUST NOT exhaust the `spawn_blocking` pool — the pool is **bounded with headroom** above the global degree, AND/OR reads go through a **bounded chunked reader with a short per-read deadline**, so a handful of wedged reads degrade gracefully (those items fail, the batch finishes). In the P3 slice, the **§1.7 wall-clock timeout** bounds runaway CSV-expansion (in-core untrusted-byte but pure bounded Rust, §2.12.4) and the wedged-read bound is the **bounded-pool-headroom leg** (the Decision note below); the §1.10 input-size *budget* proper (the est-output/scratch-byte ceilings) arrives with the §1.10 engine **P4.72** (not built in P3 — see the P3.49 §1.10-seam slice-stub note), which is then validated/calibrated in P9.41.
  > **[Decision: P3.45 pre-build ruling — ① the `time` feature-add is gate-inert; ② the P3 wedged-read mechanism = the bounded-pool-headroom leg of the §1.7 AND/OR, 2026-07-11 (Co-Pilot; the Loop's tail-of-turn pause)]** **①** the wall-clock timeout is `tokio::time::timeout` (the built P3.3/P3.43 code already plans it — `pool::run_in_core`'s doc and the `engines` dispatch doc name P3.45's wrapper verbatim); the prod feature set grows `["rt", "sync"]` → `["rt", "sync", "time"]`, Loop-executable at this box (Cargo.toml is not L(-1)) and VERIFIED gate-inert: the registry source `tokio-1.52.3/Cargo.toml` defines `time = []` — a bare in-crate flag with NO optional dependency — so the feature-add resolves NO new package and `Cargo.lock` stays byte-identical; G18/G18a run unchanged and the P3.82 floor is version-keyed (features orthogonal): NO floor action, NO L(-1) touch (the "touches Cargo.lock" premise is refuted at the source). Update the dep-row rationale comment in the same commit (`rt` + `sync` + `time` — still NOT `net`, the no-network dependency surface intact); if deterministic timeout tests want `tokio::time::pause`/`advance`, the DEV-dep may add `test-util` (dev-only; the resolver-2 no-unify precedent the `macros` dev-feature comment records) — the fill's call. The §0.9 timeout `pub const` homing in `crate::pool` is spec-mandated wording ("named `pub const`s in this §0.9 pool module", imported by the §6.7.2 harness) — no choice site. **②** §1.7's `[DECIDED]` wedged-read requirement ("the abandoned thread MUST NOT exhaust the blocking pool") offers its mechanism as an explicit AND/OR ("Either way a handful of wedged reads degrade gracefully"); the P3 slice takes the FIRST leg — bounded-pool-with-headroom — which IS the already-built architecture: P3.3's `run_in_core` releases the global-degree permit on abandonment precisely so the wedged thread "parks in the pool's headroom" (its doc), and the Tauri-managed tokio runtime's default blocking pool is bounded (512 threads at the pinned 1.52.3) with ample headroom above the §0.9 global degree — the fill DOCUMENTS this reliance at the pool module (`[Build-Session-Entscheidung: P3.45]` tag). The per-read-deadline leg is deliberately NOT built (a spec-sanctioned OR-choice, not a deferral): §2.12.4 demands no per-read deadline (its CSV-expansion bound = the wall-clock timeout plus the §1.10 budget at P4.72), the freeze already stat-prechecks + resolves every source (P3.9/§2.3.1) so a wedge is post-freeze device death — rare, one item `Failed(EngineHang)`, run continues — and a genuine per-read deadline on blocking file IO would restructure the whole read path against that residue. This box's own earlier slice sentence mis-attributed the per-read deadline to "bound runaway CSV-expansion" — corrected in place (this commit): the deadline leg addresses wedged READS; expansion is bounded by the timeout + §1.10 (§2.12.4). No spec edit (the AND/OR is a sanctioned-choice site, not a contradiction). **Prevention sweep (the class: a doc asserts the per-read-deadline leg as P3.45-delivered):** swept by MECHANISM phrasing (`per-read` / `chunked reader` / `read deadline` over docs/ + src-tauri/src), NOT the AND/OR token alone — an R1 token-only grep claimed "only consumer" and BOTH G1 reviewers refuted it with two siblings (the doc-staleness discipline's map-ALL-phrasings rule, re-learned): the box's own slice sentence (corrected above), **P3.61**'s bound-firing fixture body (corrected — the fixture exercises the wall-clock timeout), and **P4.23**'s lane recap (corrected — bounded-pool-headroom). §1.7:991 stays the spec's only AND/OR site; after these corrections no plan/spec/code site names the per-read deadline as built (zero `src-tauri/src` hits; the spec's own AND/OR offer stands as authored).

---

### Orchestrator & IPC: the §1.9 lifecycle + the §0.4 command/event wiring (slice scope)

**Goal:** the orchestrator that builds the batch from the frozen set, drives `JobState`,
fans progress over the Channel, and the §0.4 commands the slice needs — so a real
drop→convert round-trip runs through the typed IPC surface.

- [x] **P3.46** [RUST] Build the §1.9 job/batch lifecycle FSM + the Running→Failed wire-ErrorKind projection · §1.9 §2.8
  needs: P3.45, P3.38
  > the two independent concerns decomposed into sub-boxes (a bug in the FSM transitions is independent from a bug in the internal-kind→wire-kind projection; each fails a different check). The parent is `[x]` only when both sub-boxes are (_format.md §2). A worker-thread panic is caught at the §2.13 boundary as a clean per-item `Failed` (the panic-boundary body is P4; P3 wires the per-item isolation seam). The forward `needs: P3.68` edge (the §2.8.2 catalog this maps INTO) is carried **only** on the .2 sub-box that renders into the catalog — not duplicated on this parent (the structured Forward-ref note sits on .2). **[Build-Session-Entscheidung: P3.70]** the P3.70 reconciliation edges for this box (`P2.18` `ConversionErrorKind`/`ErrorKind` + `P2.20` `OutcomeMsg`) follow that same rule and sit on **.2** — the projection sub-box is the box that consumes those two contracts, while .1 is pure FSM transitions and consumes neither; P3.70 names "P3.46" at box granularity, and this file's own convention resolves it to the consuming sub-box rather than duplicating the edge upward.
  > **[Decision: P3.46 home = `crate::orchestrator` — §1.9's "crate::run" was a spec-internal mis-attribution, reconciled literal→normative, 2026-07-11 (Co-Pilot; the Loop's SPEC-CONTRADICTION hard-stop)]** §1.9 named the transition/Batch owner "`crate::run`" (01-pipeline 799/1125/1130/1139 + the 1135 contrast + 1158) while §0.7 normatively homes "queue + job lifecycle (§1.9)" + `JobState` in the tier-1 `crate::orchestrator` and defines `crate::run` as the tier-2 scratch/cleanup LEAF that depends DOWN only (its committed P3.1.2 module contract states this verbatim). **§0.7 wins** — the 77b3731 precedent (§0.7 > a lower-source mis-file; there the §1.1-freeze home vs a plan mis-file, here vs the pipeline-spec prose) and the 1c4c8e8 class (illustrative literal reconciled to the normative invariant); the mis-naming was also unbuildable as written: the projection composes `InvocationResult` (tier-2 `engines`) + `JobState` (tier-1) + `crate::outcome`, none of which the `run` leaf may reference. Fixed in this ruling's commit: the six 01-pipeline sites (one `[CORRECTED]` tag at the §1.9 block), the README resolved-log rows (the §1.7-envelope row and the mapping-home row), the TWO code echoes (the `engines/mod.rs` `InvocationResult` doc-comment — no specta derive, `bindings.ts` unaffected — and the `orchestrator/mod.rs` `JobState::Failed` doc-comment, found by the G1 R1 reviewers after a head-capped sweep grep missed it; `JobState` IS a specta wire type, so its mirrored `bindings.ts` doc line is REGENERATED via `cargo run -p xtask -- codegen` in this same commit — the R2 convergent finding, the bindings-doc-mirror class), and the .2 sub-box body above. Both sub-boxes home in `crate::orchestrator`; the .2 mechanics (std reflexive `ErrorKind::from` identity-cast, the `crate::outcome`-owned alias, the P3.68 catalog render) stand exactly as written. **Prevention sweep (the class: a doc names the ORCHESTRATOR role `crate::run`):** full `crate::run` grep over docs/spec + docs/plan + `src-tauri/src`, every hit classified — the scratch/temp/ledger/cleanup mentions (§2.6/§2.5.2/§2.14 in 02, the §3.2.2 temp-picking in 03 and 01-line-772, README's tree listing, the P1.11.3/P3.21–P3.24/P3.39 plan rows) are GENUINE `crate::run` roles and stay; the orchestrator-role sites enumerated above were the complete member set — after this commit the mechanism-grep finds zero remaining, and the §1.9 `[CORRECTED]` tag anchors the reconciliation for G68.
  - [x] **P3.46.1** [RUST] Build the §1.9 job/batch FSM (Pending→Running→{Succeeded|Failed|Cancelled|Skipped}) + deterministic queue order · §1.9
    needs: P3.45, P3.38
    > pure orchestration: drive `JobState` transitions (`Pending → Running → {Succeeded|Failed(kind)|Cancelled}`; `Skipped` set at construction, never enters the queue, terminal); deterministic collected/traversal queue order, no reordering (§1.9). No taxonomy/serialization here — the internal-kind→wire-kind projection is P3.46.2.
  - [x] **P3.46.2** [RUST] Build the Running→Failed internal-`ConversionErrorKind` → wire-`ErrorKind` projection (rendered by the P3.68 catalog) · §2.8
    needs: P3.46.1, P3.68, P2.18, P2.20
    > **Forward-ref note (DECISION-C ordering inversion):** `needs: P3.68` points at the §2.8.2 message catalog at end-of-P3 (later document order) — this projection renders INTO that catalog's `OutcomeMsg.text`, so DECISION C builds P3.68 first; the edge is acyclic and valid, the inversion documented at the `needs:` line.
    > the taxonomy/serialization leg: `crate::orchestrator` maps `InvocationResult::Failed(kind)` (internal `ConversionErrorKind`) to the wire `ErrorKind` via `ErrorKind::from(kind)` (the std reflexive identity under the §2.8.2 option-1 `pub type ErrorKind = ConversionErrorKind` alias — `crate::outcome` owns no such `From` impl and none can be written, E0119; the internal kind IS the wire kind, reconciled at P3.1.3) and renders the per-item `OutcomeMsg::Failure { text }` from the **§2.8.2 catalog (P3.68)** **before** the state is recorded / a row or event emitted (§1.9). Sits between the FSM (P3.46.1) and the P3.68 string catalog — a missing enum variant fails here, a wrong transition fails in P3.46.1. (`needs: P3.68` — the §2.8.2 string catalog this projection renders into `OutcomeMsg.text`, built at end-of-P3 + followed in place by DECISION C.)
- [x] **P3.47** [RUST] Materialise pre-flight skips into the batch at C6 construction (non-queue `Skipped` records) · §1.9 §1.12
  needs: P3.46, P3.32
  > at C6 the orchestrator builds the `Batch` from the frozen `CollectedSet`, creating for **every `SkippedItem` in `CollectedSet::Single.skipped`** a `ConversionJob` with `JobState = Skipped(reason)` set at construction (reason copied from `SkippedItem.reason`) over the §1.1 single id space — these never enter `Pending`, receive **no** Channel events, and are terminal at construction. This is the single anchor preventing a skip from being lost between the `CollectedSet` and the §1.12 projection (§1.9).
  > **[Decision: the Skip-job shape = a §0.6 `JobSource` SUM TYPE `{ Eligible(DroppedItem), Skipped(SkippedItem) }` on `ConversionJob.source` — the escalation's option ① sharpened, 2026-07-11 (Co-Pilot; the Loop's P3.47 hard-stop)]** option ② (a separate `Vec<SkippedRecord>` on `Batch`) is REJECTED as spec-contradicting — §1.9 `[DECIDED]` says "creates … a `ConversionJob` record" and the committed `Batch.jobs` doc says "carries BOTH"; option ③ (synthesise a `DroppedItem`) is REJECTED as data-invention (a skip has no `DetectionOutcome`/`size_bytes` to invent — §1.4/§1.12 honesty). Option ① is ruled in its SHARPENED form: NOT `Option<DroppedItem>` (a None-with-meaning convention that models a skip as "an eligible job missing its source" and would give the P2.14 invariant a queued-only carve-out) but the sum type — a skip is its OWN kind carrying its OWN frozen record. Verified consequences: the P2.14 invariant STRENGTHENS to a uniform `item == source.item()` (both arms carry `item`) plus the NEW coupling invariant `source is Skipped(_) ⟺ state is JobState::Skipped(_)` (both property-tested; the P2.14 test evolution carries `[Test-Change: P3.47]` tags at EVERY touched assertion/constructor site — the invariant is asserted at multiple sites (the unit test + the property test + the `job_in`-style constructors), G70); the §1.9 "skips survive C6" anchor gains FULL fidelity (the registry is evicted at C6 — the `Batch` is the sole post-C6 carrier, and now of the COMPLETE skip record incl. `source_display`); §1.12 needs nothing more — `ItemResult` carries NO display field (P2.12: the WebView joins by `ItemId` over its C1 data, for eligible items identically), so the Skipped row builds from `item` + `state` + `OutcomeMsg::Skipped { reason, text }` (the `text` sourcing is the P3.50 ruling); P3.48's conductor dispatches `Pending` jobs only and reads the eligible arm behind an accessor (naming = the fill's `[Build-Session-Entscheidung]`); `ConversionJob` is core-INTERNAL (P2.10, no specta) so `bindings.ts` is untouched — but the fill's doc-comment edits must re-check per-TYPE for specta mirrors (the P3.46 R2 lesson). §0.6 + §1.9 spec edits land in THIS ruling's commit; the code (enum + field + P2.14 evolution + accessor) lands with the fill. **Prevention sweep (the class: a P2-authored §0.6 type whose P3 consumer must represent a case the type cannot):** the other skip-touching consumers audited — P3.48 emits NO events for skips (nothing to construct), P3.50's `ItemResult` is skip-constructible as verified above, `Totals.skipped` exists (P2.12), `ItemOutcome` is never built for a skip (no live event) — `ConversionJob` was the sole member; near-member checked NON-member: `ConversionJob.plan: Option<OutputPlan>` stays an `Option` (absence = not-yet-planned, a TEMPORAL state — a legitimate Option, unlike a kind-by-absence).
- [x] **P3.48** [RUST] Wire the C6 `start_conversion` run + the `ConversionEvent` Channel fan-out · §0.4.1 §0.4.2 §1.11
  needs: P3.46, P3.50, P3.76, P2.29, P2.37
  > **Forward-ref note (DECISION-C ordering inversion):** `needs: P3.76` points at the core-owned-path wire layer (the 2026-07-06 owner ruling, later document order) — the `ItemStarted` / `ItemFinished { outcome: ItemOutcome }` / live-`IpcError` shapes this fan-out streams are in P3.76's re-type list (the landed `ItemOutcome::Succeeded` still carries the pre-ruling `output_path: PathBuf`) — and `needs: P3.50` at the §1.12 projection (later document order): the terminal `RunFinished(RunResult)` this fan-out emits must carry the FULL §1.12 projection (pre-flight skips + the batch-summary string — P3.50's deliverable; an under-projected `RunResult` still type-checks, so the compiler canNOT backstop this ordering, G1-convergent 2026-07-07). DECISION C builds both first; the edges are acyclic (P3.50 does not need P3.48) and valid, the inversions documented at the `needs:` line (added with the P3.25 hard-stop resolution, 2026-07-07 — the 4217659 wiring pass missed this box too).
  > C6 creates a `RunId`, enqueues the batch (§0.9), spawns the in-core worker, returns the `RunId` immediately, and streams `ConversionEvent`s: `RunStarted { totalItems = QUEUED-eligible count, willReencode: false }` (CSV/TSV is never re-encode), `ItemStarted`, `ItemProgress` (P3.43), `ItemFinished { outcome }`, `BatchProgress { done, total }` (denominator = queued items only, so a skip never holds the bar below 100%), terminal `RunFinished(RunResult)`. Pre-flight skips emit **no live** `ItemFinished{Skipped}` — terminal-projection only (§0.4.2).
  > **[Decision: the C6 per-item composition = pick-temp → AWAIT `dispatch` → publish legs (the escalation's option ②) — canonical for EVERY engine incl. P4+, 2026-07-12 (Co-Pilot; the Loop's P3.48 hard-stop)]** Option ② is over-determined, not a taste call: **(a)** the BUILT §1.7 lane already implements it — `run_native_csv_tsv` REQUIRES `plan.out_tmp` populated (`InternalError` otherwise) and its closure doc says verbatim "the §2.1 atomic publish CONSUMES it on success" (the P3.41–45 fills were built to the §3.2.2/P3.5 plan-seam ruling: §1.7 owns+populates `out_tmp`, picked inside the destination volume §2.14.4 — same-volume, EXDEV-safe); **(b)** §2.1.1 step 2 runs the engine "through the §2.12 isolation wrapper" — for every P4+ subprocess engine that step is inherently ASYNC (spawn/kill/cancel), which a synchronous `FnOnce(&Path)` seam can never host, so option ① (the `write_item` spine) dies at P4 — exactly the cross-phase bind the escalation named; **(c)** the §0.9 wall-clock timeout is the ENGINE bound — ① would wrap the publish/divert IO in it (a slow destination would mis-taxonomize as `Failed(EngineHang)` instead of the §2.8 `WriteFailed`/divert taxonomy) and let a timeout-abandonment race the publish steps on the detached thread; **(d)** tier-clean: the tier-1 conductor picks the temp (`RunScratch::publish_temp`, §2.14.1), populates `Invocation.out_tmp`, awaits `dispatch` (tier-2; progress → `ItemProgress`, cancel/timeout inside), and ONLY on `Succeeded` runs the §2.1.1 publish legs (steps 3–7 incl. late-divert → `WriteOutcome`); `Cancelled`/`Failed` drop the temp (§3.2.2) and project directly (P3.46.2). **`write_item` is RE-CUT, not deadened** (the owner-rule concern): step 1 moves conductor-side (the same `RunScratch::publish_temp` call), step 2 is superseded by `dispatch` on the run path (P3.38's "binds at P3.41/P3.48" seam text predated the P3.43–45 lifecycle — reconciled by the P3.38 note), steps 3–7 are refactored into the publish-leg fn (a `publish_written_temp` shape consuming the written temp; naming = the fill's `[Build-Session-Entscheidung]`) — every guarantee-bearing piece (sync, resolve-late, exclusive publish, dir-fsync, cleanup-on-error, late-divert) stays LIVE on the run path; the P3.38 G31/G32(a) tests migrate onto the publish leg + the conductor path under `[Test-Change: P3.48]` tags (G70). The §2.1.1 `[CLARIFIED]` sentence (step 2 = the awaited dispatch, composed tier-1) lands with this ruling. **Secondary scope rulings:** **(1) Target:** NO synthesized `Target` (it carries `label`/`lossy`/`availability` — §0.6 data; faking them is the P3.47-class invention) — P3.48 builds the SHARED §1.5 `TargetId → Target` lookup over the slice offer source (CSV↔TSV) and C6 validates+constructs through it; P3.49's C3 REUSES the same helper (single source — no double-build, no DECISION-C pull of the P3.78-blocked P3.49). **(2)** P3.48 = the C6 CONDUCTOR, tested over directly-registered frozen sets (the established build-vs-wire split; the full C1→C6→summary E2E is P3.49/P3.63); P3.48 owns the `.manage` of the four stores it first consumes (`CollectedSetRegistry`, `Pool`, `EquivKeyComputer`, `RerunLedger`) and BUILDS the per-item `RerunDecision` applier (C6's §2.5.3 semantics: Skip default / FreshCopy → ordinary §2.2 numbering — the applier's exact JobState mapping is the fill's `[Build-Session-Entscheidung]` against §2.5.3/§1.9).
  > **[Decision addendum: the rerun-skip representation = `SkipReason::AlreadyConverted` — the fill's G1-NOGO fork resolved, 2026-07-12 (Co-Pilot; the Loop's in-fill escalation)]** the delegated "applier JobState mapping" turned out NOT loop-decidable: §2.5 (Skip = no new output) + §1.12 `[verified normative]` ("skipped items appear as a distinct outcome, not a failure") + §0.6 (four detection-only `SkipReason`s, §1.9 "Skipped = detection-ineligible") were not simultaneously satisfiable, and the fill's interim DROP violated §1.12 (both fill-reviewers converged; a full-rerun batch would read "All 0 files converted"). Ruled: option A in the shape that PRESERVES the P3.47 machinery — ONE new wire variant `SkipReason::AlreadyConverted` (§0.6, landed this commit) assigned by the applier at C6 construction; the item rides the EXISTING skip machinery end-to-end (`JobState::Skipped(AlreadyConverted)`, `ItemResult` row, `OutcomeMsg::Skipped`, `Totals.skipped`, no live events, terminal at construction — §1.9 `[CLARIFIED]`). The P3.47 coupling invariant is REFINED, not broken: `source is JobSource::Skipped(_) ⟺ state is Skipped(<detection reason>)`; `Skipped(AlreadyConverted)` ⟹ source ELIGIBLE (the item keeps its real `DroppedItem` — no `SkippedItem` is fabricated; §1.4 stays detection-only). Its §2.8.2 line ("This file was already converted in this session, so it was skipped.") renders DIRECTLY — never through the `SkipReason→ErrorKind` bridge (detection-only): the bridge's forced exhaustive `AlreadyConverted` arm is a CONCRETE never-taken arm returning a real kind (`InternalError`-class), documented never-rendered — NEVER `unreachable!()` (the production anti-pattern; the P2.25 honest-seam precedent). Constraints for the fill: the P2.14/P3.47 property tests evolve under `[Test-Change: P3.48]` (the refined coupling), plus a property/unit assertion that the FREEZE never mints `AlreadyConverted` (the construction-only invariant, type-unenforced); `bindings.ts` regen (additive wire variant); the stale detection-only enumerations update in the same commit (G68): the domain `SkipReason` doc ("the four detection-INELIGIBLE classes"), its `bindings.ts` mirror (via the regen), the P2.20 bridge doc, and the `Totals.skipped` field doc ("pre-flight detection-ineligible items") — while §1.12's freeze-time trivial-copy parenthetical STAYS at four (correct-as-scoped: a freeze-time `SkippedItem` genuinely never carries `AlreadyConverted`). Option B (keep the drop + weaken §1.12) REJECTED — it deletes a normative §1.12 sentence and ships the "All 0 files converted" absurdity against SSOT *Fail clearly*. **Fill obligations (G1-precised):** the §1.7 non-empty exit-verification currently INSIDE `write_item` (the §2.8 `Empty`/`InternalError` mapping between its steps 2 and 3) RELOCATES onto the `Succeeded` path — it must move, not drop; and the src/ doc-comments still describing the superseded shape ("the P3.46/P3.48 conductor wires `write_item`" verbatim, plus the engine-binds-into-the-`engine_write`-seam paraphrases — sites across `orchestrator`/`run`/`fs_guard` doc-comments and dead-code `reason` strings) are KNOWN-STALE and are reconciled by the fill in the SAME files it re-cuts (DoD 2). **Prevention sweep (the class: two built subsystems textually claiming the same flow segment):** the only other candidate pairs audited — the P3.36 late-divert lives INSIDE the publish legs (no overlap), the P4.13 isolation wrapper plugs into `dispatch`'s existing subprocess arms (seamed, no second claimant), and NO docs/spec text pins the `engine_write` seam (the claimants are P3.38's plan text — reconciled here — plus the known-stale src/ comments above, a G1-R1 convergent find after a docs-only sweep grep; the code leg of the sweep standard applies); the composition ruled here binds the P4+ run path by construction (P4.13/P4.20/P4.23 all route through `dispatch`), so the class is closed forward — `write_item`-seam ↔ `dispatch` was the sole member.
- [x] **P3.49** [RUST] Implement C1 `drain_intake` / C3 `get_targets` / C4 `plan_output` for the slice · §0.4.1 §1.3 §1.4 §1.5
  needs: P3.32, P3.29, P3.40, P2.64, P3.78, P3.48, P2.22, P2.25, P2.26
  > **C1** `drain_intake` is the universal intake-completion door (P3.78): every intake origin — core-side drop, picker, launch-arg, second-instance — lands in the core-side `PendingIntake` buffer, and C1 drains it by `collectingId` into the single freeze (P3.32), returns `CollectedSet` (`Single`/`Mixed`/`Unsupported`/`Uncertain`/`Empty`) projected per the §1.3 `group()` rule — incl. the lone-Unsupported / lone-Uncertain specificity + the `EmptyReport→skipped` projection. **C3** resolves the CSV/TSV `TargetOffer` (the offered set, the one pre-highlighted default = TSV for a CSV source, lossy flag, availability) from the registry (§1.5) — REUSING the shared §1.5 `TargetId → Target` lookup P3.48 builds (the `needs: P3.48` edge; single source, no second offer table — the P3.48 ruling). **C4** computes the `OutputPlan` preview + `rerun` (P3.40) + the §1.10 preflight verdict; eager on `3→4`, debounced re-call on change (§0.4.1).
  > **§1.10-seam slice-stub note (DECIDED — mirrors the P3↔P4 UI-seam supersede model):** C4's §1.10 preflight verdict here is the **trivial walking-skeleton `PreflightVerdict`** (the CSV→TSV footprint is negligible ⇒ `up_front_fail: None` by construction) — NOT a real §1.10 estimator. The real §1.10 estimation+decision ENGINE is **P4.72** (which `needs: P4.20`, the real pool, so it cannot be DECISION-C-pulled into P3 without the forbidden P3→P4 inversion); P4.72 **SUPERSEDES** this stub behind the same C4 contract, exactly as the P4 UI primitives supersede the P3 slice renderers. The Build-Loop must NOT build a real §1.10 estimator in P3 (a double-build with P4.72) — P3's C4 returns the trivial verdict only.
  > **`on_scan` `ScanProgress` emit (§0.4.2) — runtime home `[Co-Pilot reconcile]`:** C1's throttled (~2/s, coalesced) `ScanProgress { scanned }` emit over the `on_scan` Channel during the §1.1 walk is realized HERE — the type is authored at P2.7, the `Channel<ScanProgress>` arg at P2.22, and the walk that produces the count at P2.62/P2.64; **no separate P2 box schedules the runtime emit/throttle**, so this C1 end-to-end box owns it (best-effort, monotonic, dies with the C1 call). This corrects the prior `on_scan`-pump mis-attribution to P2.69 (P2.69 is cooperative ingest cancellation, not the emit).
- [x] **P3.50** [RUST] Implement C8 `get_run_summary` + the §1.12 `RunResult` projection (incl. pre-flight skips + the batch-summary string) · §0.4.1 §1.12 §2.8.2
  needs: P3.47, P3.25, P3.68, P3.76
  > **Forward-ref note (DECISION-C ordering inversion):** `needs: P3.68` points at the §2.8.2 message catalog at end-of-P3 (later document order) — the §1.12 batch-summary strings this projection assembles live in that catalog — and `needs: P3.76` at the core-owned-path wire layer (the 2026-07-06 owner ruling) whose display-typed `RunResult`/`ItemResult` this projection emits; DECISION C builds both first; the edges are acyclic and valid, the inversion documented at the `needs:` line.
  > the §1.12 run-end projection: `RunResult { collected_set_id, run_id, items, totals, cleanup_incomplete, common_root_display, divert_root_display }` (the real root `PathBuf`s stay in `State<RunResultStore>` for the C9 `OpenTarget` resolution, per the revised §0.6); map each output back to its source by `ItemId`; a fully-failed batch is a clear failure (derived `failed == total && total > 0`). Project **pre-flight skips** into `RunResult.items` as `ItemResult { item, state: Skipped(reason), output_display: None, reason: OutcomeMsg::Skipped{reason} }` (trivial copy, no lossy reverse map), counted in `Totals.skipped` (never `failed`); assemble the §1.12 **batch-level summary string** (All succeeded / Partial / All failed / Cancelled + the With-residue tail) from the §2.8.2 catalog (P3.68). C8 is the idempotent re-fetch (mirrors the terminal `RunFinished`). (`needs: P3.68` for the batch-summary strings + `P3.76` for the display-typed wire shapes, both followed in place by DECISION C.)
  > **[Decision: the Skip-`text` source = the SAME §2.8.2 catalog via the P2.20 bridge + RETAIN `detected_display` on `SkippedItem` — the escalation's option (B) precisely scoped, NOT the slot-free option (A), 2026-07-11 (Co-Pilot; the Loop's P3.50 hard-stop)]** the recommendation inverts on the SSOT read: **SSOT principle 6 MANDATES the surface** ("When detection identifies a real but unsupported type, ConvertIA says so plainly — 'can't convert this type — **detected: X**'"), so a slot-free UnsupportedType line (option A) would be an SSOT violation, not a neutral choice — and (B) is NOT invention but RETENTION, the exact P3.40 class: detection COMPUTES the name (`DetectionOutcome::UnsupportedType { detected: String }` — non-optional — and `Uncertain { best_guess: Option<String> }`), the `SkippedItem` projection DISCARDS it, an SSOT-mandated consumer needs it. Precisely scoped: **(a)** §0.6 `SkippedItem` gains `detected_display: Option<String>` (Some(detected) for UnsupportedType — always; the best_guess for Uncertain when named; None otherwise — display-only, never a path; landed in §0.6 this commit); **(b)** the freeze partition RETAINS it from the verdict instead of dropping; **(c)** `crate::outcome` gains the `skipped_message(reason, detected_display) -> OutcomeMsg::Skipped` renderer over the EXISTING P2.20 `skip_reason_to_error_kind` bridge + the EXISTING §2.8.2 rows — **ONE new string** (G1-adopted — the R1 Sonnet P2 refuted the draft's "future richer row" prose as a quiet deferral of SSOT-6's "names what it believes" arm): Empty→Empty row, Unreadable→Unreadable row (both already intake-worded), guessless Uncertain→Unrecognized row (the "or that it can't tell" arm; the hardcoded outcome-test string IS that row verbatim), Uncertain WITH a named guess→the new §2.8.2 mapping-paragraph line "ConvertIA isn't sure what kind of file this is — it might be {guess} — so it can't convert it.", UnsupportedType→its row with `{detected}` filled from the retained field (the §2.8.2 mapping paragraph landed this commit); **(d)** NO new box — executed WITH this box's fill (the P3.40 retention precedent: one coherent fill incl. the wire-type field + freeze retention + renderer + projection; `SkippedItem` IS a specta wire type → `bindings.ts` regen + golden/round-trip test updates under `[Test-Change: P3.50]` tags — the additive `Option` field deserialises absent→None, so the wire stays compatible). The box body's `OutcomeMsg::Skipped{reason}` is shorthand — the wire variant carries `{ reason, text }` (§2.8.2) and this ruling sources the `text`. The `Totals`→`BatchSummary` `{n}`-semantics stay the fill's own `[Build-Session-Entscheidung]` as proposed (the attempted-items default is consistent with §1.12's skip≠fail posture; decide at the fill). **Prevention sweep (the P3.40 class, third member: freeze-computes-then-discards vs a mandated consumer):** the remaining `SkippedItem`-discarded detection content audited — `size_bytes` (no § mandates a skip's size: §1.4 sizes the eligible set, §1.10 budgets eligible conversions — non-member), the `Unreadable { reason: ReadFailure }` fine-grain (gone/locked/permission — the §2.8.2 Unreadable row is deliberately ONE generic line, no § mandates the split on the skip surface — non-member), `Uncertain.best_guess` (a member — resolved by (a)); the P3.47 sweep verified skip-consumer SHAPE constructibility but not CONTENT sources — this ruling closes the content leg; the SkippedItem projection drops nothing else.
- [x] **P3.51** [RUST] Wire C9 `open_path` live — `AppHandle` + `RunResult` from `State<RunResultStore>` + the P2.101–103 membership gate + `OpenerExt` invoke + refusal log + `Ok(())` + E2E · §0.4.1 §7.7.1 §7.7.3 §7.5 §2.7.4
  needs: P3.50, P2.100, P2.103, P3.79
  > **Reconcile-note (P2↔P3 §7.7 build-vs-wire split — Co-Pilot-ratified, Option ①):** the C9 mapping + membership-gate LOGIC is built PURE in P2 (P2.100 the `OpenerOp` mapping — re-keyed to `OpenTarget` by P3.79 · P2.101 the §7.7.3 membership validate over the real P2.43 `RunResultStore` · P2.102 the two-rule split · P2.103 split-output), dead-until this wire box (the §1.1-walk / §7.8.1-funnel dead-code pattern). This box is the **live wire, NOT a re-implementation**: it gives the handler the `AppHandle`, fetches the current `RunResult` from `State<RunResultStore>` (P2.43), calls the P2.101–103 gate, and on a validated member invokes `OpenerExt` (reveal/open, §7.7.1); a non-member is refused + logged (§7.5) and a validated open returns `Ok(())` — then proven end-to-end in the walking skeleton.
  > the DoD one-click open-folder/open-file action: "open folder" opens `common_root`, and when `divert_root` is `Some(..)` a second affordance opens the divert root (§2.7.4 / §1.12). `OpenTarget` is the ID-keyed wire form (common-root / divert-root / per-item / residue targets, P3.79) — the WebView names a run-scoped target, never a path.
- [x] **P3.52** [RUST] Wire C7 `cancel_run` / C13 `cancel_ingest` to the cooperative cancel + ingest-token · §0.4.1 §1.1 · G54
  needs: P3.48, P3.31
  > **C7** trips the run's §0.4.4 cancellation token → the in-core cooperative cancel (P3.44); already-finished items kept, in-progress discarded cleanly. **C13** trips the ingest-scoped `CancellationToken` keyed by the frontend-generated `CollectingId` (registered at C1 handler entry, dropped on **every** exit branch — no token leak) → the §1.1 walk stops cooperatively, discarding the partial un-frozen set (no cleanup obligation, no temp written during ingest). G54 governs the gate-plane integrity these handlers run under (no security claim of its own).

---

### Minimal walking-skeleton result UI (§5 minimal)

**Goal:** the minimal React/TS screen path that drives and renders the slice —
Idle/DropZone → Collecting → Confirm → Targets (TSV) → Destination → Converting → Summary —
exercising the generated `bindings.ts` IPC door. Full polish + the rich components are P4/P8.

> **P3↔P4 UI-seam model (DECIDED — the recommended model, stated in both phases):**
> P3 builds **intentionally-minimal, slice-only renderers** of these components (just
> enough to drive the CSV→TSV vertical slice end-to-end and prove the IPC door). P4's
> generic UX-correctness primitives (P4.64–P4.70 + P4.74/P4.75) **SUPERSEDE**
> them — P4 rebuilds each into the generic, option-declaration-driven, fully-a11y
> component P5–P7 register against. P4 does **NOT** extend the P3 renderers in place;
> it replaces them (the P3 versions are deliberately throwaway slice scaffolding, not a
> foundation P4 builds on). Each P4 UI box names the P3 box it supersedes + carries the
> `needs: P3.5x` edge so the loop builds the P3 slice-renderer first (it is the live UI
> until P4) and the supersede is explicit, never a silent double-build. This keeps the
> walking-skeleton philosophy literally true (P3 proves the slice; P4 generalises) and
> is the single owner of the answer to "does P4 rebuild or extend P3's UI?" — it
> **rebuilds (supersedes)**.
> **[Carve-out: the DestinationBar, 2026-07-15 (the P3.56 item-1/item-2 ruling)]** the
> DestinationBar has NO P4 rebuild box and none is warranted — nothing about it is
> option-declaration-generic (no `OptionDecl` dispatch, no per-engine variance); the
> P3.56 build IS the durable component (§5.3:301's full contract incl. the
> persisted-destination flow), which **P4.69 EXTENDS** with the up-front-fail `Note`
> leg and **P4.70.3** a11y-wires. The rebuild answer holds for every OTHER slice
> renderer (DropZone P3.54, FormatPicker P3.56, ProgressList+Cancel P3.58,
> ResultSummary+OpenActions P3.59, fault screens P3.60); the P4-header statement
> carries the mirrored carve-out.

- [x] **P3.53** [UI] Build the §5.2 walking-skeleton state machine (the slice subset) · §5.2 §5.8 · G57
  needs: P3.48, P3.78, P3.80, P3.40, P2.39, P2.120
  > a finite-state reducer over the slice states: `Idle (1) → Collecting (2) → Confirm (3) → Targets+Destination (4/5) → [RerunPrompt (6)] → Converting (7) → Summary (8)`, plus the pre-flight `MixedDropRefusal (9)` / `Unsupported (10)` branches and the global `app://fault → AppFault (12)` wildcard edge. Driven by inbound IPC results/events (§5.8); the backend is the source of truth for facts. All user-facing literals via `strings/ui.ts` (English-only, G57, P0.4.6).
  > **[Decision: the FULL slice reducer builds HERE (the Loop's option ①) — the machine is the flow-SSOT, the screens stay presentational, 2026-07-13 (Co-Pilot; the Loop's P3.53 scoping question)]** over-determined: **(a)** this box's own body enumerates the COMPLETE slice machine (incl. the RerunPrompt branch and the wildcard edge) — option ② (happy-path core now, guards scattered into the screen boxes) would under-fill the box's enumeration (the box-fill-under-enumeration class — the P3.32/P3.76 freeze under-fill corrected at P3.40); **(b)** §5.2 is normative for the derivation: "Phase 3 derives the machine from these [state/transition text+tables], not the art" and "the wildcard is recorded here so a Phase-3 reducer wires the global [fault edge]" — EVERY transition cell of the slice states — incl. the state-1 specials (picker-cancelled → stays `Idle`, modelled as its OWN Msg, never routed through the Empty arm; launch-with-files → INITIAL state `Collecting` via the store init), the **7a Converting(Cancelling) sub-state arm** (enter-on-cancel, second-Esc-ignored, → Summary(partial) — P3.58 dispatches into it, so the arm lives HERE; P4.67 later supersedes/extends the button+semantics), the Targets→Confirm Back, the Rerun-Esc return (P3.57's committed edge) and the state-9 re-drop (P3.60's committed edge) — becomes a reducer arm + test; **(c)** §5.3: "each component is presentational + wired to the store/machine" — transition logic in screens would violate the screens' own spec posture. Constraints for the fill: `machine.ts` stays PURE (a `transition(state, msg)` reducer — NO command call inside it; the command-firing effects live in the screen/hook wiring, and the machine consumes only results/events as Msgs), exhaustiveness-guarded over the state×msg union (no catch-all beyond the documented fault wildcard), store integration per the P1.31.2/P2.114 shape, comprehensive per-transition tests. The exact Msg-set naming is the fill's `[Build-Session-Entscheidung]` (the ~18-msg sketch is a faithful projection of the §5.2 tables). The screen boxes P3.54–P3.60 then render + dispatch only — their remaining content (components, wiring, a11y, effects) is untouched by this pull. P4.78 (the 12-state completion) is reconciled in this commit: its "slice-omitted edges" list predated this ruling AND was already stale against P3.57/P3.60's own committed edges — its genuine remainder is the non-slice states (11, the full 7a semantics via P4.67/P4.67.1) + the P4-screen cells + the slice-arm audit.
- [x] **P3.54** [UI] Build the DropZone + C2a intake wiring (drag-over affordance, click-to-browse, choose-folder) · §5.3 §5.4 §0.4.1
  needs: P3.53, P3.77
  > **DropZone** in `Idle` (1) + the state-9 re-drop: the native file-drop is handled core-side (`WindowEvent::DragDrop` → the §7.8.1 funnel, P3.77) — the DropZone renders the drag-over affordance only, no drop path ever reaches the WebView; click/Enter/Space → **C2a `pick_for_intake { kind: 'files' }`**; the "or choose a folder" affordance → **C2a `{ kind: 'folder' }`** (no `dialog:allow-open` grant — Rust-side `DialogExt`, §5.4/§0.10). No raw FS path transits the WebView for intake. A cancelled picker → stays `Idle` as its OWN machine Msg (a clean no-op per the §5.2 state-1 row — NEVER routed through the `CollectedSet::Empty` arm, which is the Unsupported(10) path; the 2026-07-13 P3.53 ruling's distinct-Msg constraint).
  > **[Build-Session-Entscheidung: P3.54 — scope = the intake TRIGGER, not the §5.8 machine CONSUMPTION (the dual-review divergence resolution)]** P3.54's refs are §5.3 (DropZone) / §5.4 (native-drop boundary) / §0.4.1 (C2a): it builds the DropZone (the §5.4/§5.6.1 `role="button"` drop surface + the secondary choose-folder button, both native `<button>`s so Enter/Space activate and the visible label is the accessible name) and FIRES C2a. Driving the §5.2 machine from the drained set — dispatching `startCollecting`/`scanTick`/`collected` — is the **§5.8 drain CONSUMPTION** (§5.4 names it "the §5.8 C1 `drain_intake` consumption"), which rides with the SCREEN boxes that make each target state reachable: **P3.55 (Confirm) is the first `collected`→Confirm consumer**, P3.60 the Mixed/Unsupported arms, P3.56–P3.59 their own transitions. So after P3.54 the picker opens + the nudge→drain runs, and the machine advances only once P3.55+ land (no blank advance into an unbuilt screen). This resolves the P3.54 dual review (Sonnet NOGO/P1 "the P3.53 machine has no callers" vs Opus GO/P3 "defensible if a named box owns it"): the named owner is the §5.8/screen boxes, spec-anchored by §5.4→§5.8 — not P3.54.
- [x] **P3.55** [UI] Build the Confirm gate (BatchSummary + FileList skip rows) · §5.3 §1.4
  needs: P3.54
  > **BatchSummary** renders the mandatory pre-convert gate (state 3): detected format + count ("N CSV files"); when `skipped` is non-empty, the passive one-line tally *"M file(s) weren't recognized and will be skipped"* (never blocks confirm, never silent). **FileList** (behind a "Show N files" disclosure) is the single owner of the per-item detail — eligible rows plain, skipped rows visually marked with their §2.8 reason; virtualised. `sampleNames`/`displayName` are display-only, never re-submitted as intake (§5.3).
  > **[Consumption seam — inherited from the P3.54 scope note]** as the first box whose screen needs the machine past `Idle`, P3.55 also wires the **§5.8 intake→machine consumption** so state 3 is reachable + testable: on the `app://intake` nudge + the mount-drain, dispatch the C1-drain `CollectedSet` into the §5.2 machine (`startCollecting`/`scanTick`/`collected`, per §5.4→§5.8 and the P3.53 Msg set) — the completion of the P3.54 DropZone trigger. P3.60 later adds the Mixed/Unsupported arms; the launch-vs-drop asymmetry (a plain-launch `Empty` stays `Idle`, a drop `Empty` → Unsupported) is the §5.2 `fromIdle`/`fromCollecting` routing already built in P3.53.
  > **[Decision: FileList virtualisation builds IN P3.55 — mechanism = a hand-rolled fixed-row-height windowing hook in `src/hooks/`, NO new dependency, 2026-07-15 (Co-Pilot; the Loop's P3.55 pacing checkpoint floated a "defer to P9/§1.10" scope)]** the deferral premise is refuted at every layer: §1.10 explicitly delegates the component ("the UI list is virtualized (§5 owns the virtualization component)"), §5.3's FileList row carries `virtualized: true` + "Virtualised (§1.10) for thousands of files" as its component contract, this box's own body says "virtualised" — and NO subsequent box owns it: P3.55 is FileList's ONLY builder (unlike the P3.56/P3.58 slice renderers it has no P4-SUPERSEDES successor, and P9's §1.10 work is resource-budget calibration (P9.41), not list virtualisation), so dropping it would be the quiet-deferral/under-fill class with no named, scheduled owner. The mechanism is ruled WITH it (the "otherwise a new dependency" fork): FileList rows are fixed-height read-only display strings, so windowing is a genuinely small visible-range slice (scrollTop → start index + overscan) that must honour §5.6's never-focus-a-not-yet-mounted-virtual-row rule — a hand-rolled hook, because a new JS dep would open a permanent supply-chain surface (version pin + `PINNED_FLOORS_JS` floor row = the dep↔floor two-box split incl. an L(-1) Co-Pilot leg) that a fixed-height list does not justify. The hook is authored as the SHARED virtualisation primitive — its subsequent consumer candidates are P4.66's ProgressList (whose §5.3 row mandates "virtualised for large batches"; the P4.66 body's `(§1.10 virtualisation)` parenthetical locally names the sibling selector-granularity technique, which windowing composes with, not replaces) and the Summary results list (virtualised per §5.6's never-focus-an-unmounted-row rule — its §5.3 row carries no explicit flag); the concrete reuse shape lands in their own boxes. Hook naming/API = the fill's `[Build-Session-Entscheidung]`; a genuine insufficiency (dynamic row heights, scroll anchoring) escalates instead of quietly pulling a dep (a dep-add is structurally a Co-Pilot floor action anyway).
- [x] **P3.56** [UI] Build the FormatPicker (TSV target) + DestinationBar (will-save-to + Change + preflight) · §5.3 §1.5 §2.7
  needs: P3.55, P3.80
  > **FormatPicker** shows the offered target tile(s) with the one pre-highlighted **default = TSV** for a CSV source (descriptors from C3, no platform matrix hardcoded). **DestinationBar** always visible before Convert (state 5): the "**will save to …**" line (beside-source default / divert noted, from the C4 plan); **initial state for a returning user = the persisted `lastDestinationMode`** via the P3.80 resolver hand-off (§5.3:301 — the item-2 Decision note below); the **Change destination** button (C2b `pick_destination` → C5 `set_destination`); and the **Convert** button **disabled** when `preflight.up_front_fail` is `Some(kind)` (the §5.3 fails-fast disable; the passive §2.8-string `Note` is the P4.69/P4.72 leg — the item-1 Decision note below). The `preflight.up_front_fail` this reads is the **P3.49 walking-skeleton stub** (`None` for CSV→TSV by construction); the real §1.10 verdict arrives when **P4.72** supersedes the stub behind the same C4 contract (the P3.49 §1.10-seam slice-stub note) — this box's disable behaviour reads whatever C4 returns, unchanged across the supersede.
  > **[Decision: item 1 — the P3 up-front-fail surfacing = DISABLE-ONLY; the verbatim-§2.8 `Note` rides P4.69 (UI) + P4.72 (wire text) — the Loop's option A precised, 2026-07-15 (Co-Pilot; the Loop's P3.56 in-fill escalation)]** the fill's chrome-worded batch-level Note strings are REMOVED — they paraphrase §2.8: §5.7's owner column reads "**§2.8 string**, here (chrome)", i.e. the failure STRING is §02-owned and rendered verbatim (§5.7:800, §5.3:301) and only the surrounding chrome is §5.7's — the fill-review Sonnet P1 stands, the chrome-ownership reading does not. The verbatim Note is NOT honestly buildable in P3: the wire (`PreflightVerdict.up_front_fail: Option<ErrorKind>`, §0.6) carries the KIND only; the §2.8.2 `TooBig`/`OutOfDisk` rows are ITEM-scoped while `up_front_fail` is the WHOLE-BATCH verdict (§0.6's own scope comment); a frontend §2.8-catalog copy would fork the single-source catalog (`crate::outcome`, P3.68) — so the string must arrive BACKEND-rendered on the wire (the `OutcomeMsg { reason, text }` pattern, the P3.50 precedent), and its batch-scoped §2.8.2 wording belongs to the verdict's real producer (P4.72's §1.10 design domain; the P3.49 slice-stub note already forbids pre-building that domain in P3 — and `up_front_fail` is `None` by construction here, so the Note is unreachable this phase regardless). This is the P4.65 class verbatim (the slice FormatPicker "had no lossy-note slot"; the note rides the P4 box owning its string machinery): P3.56 ships the disable behaviour only; **P4.72** authors the batch-scoped §2.8.2 row(s) + the additive backend-rendered verdict text (its body now carries the leg); **P4.69** renders it as the passive `Note` (its body now carries the leg + the `needs:` edges). NO spec edit — §5.3:301/§5.7:826 describe the END state, exactly as §5.7:821 always described the P4.65 lossy note. Options B (keep the paraphrase) and C (frontend catalog / a P3 wire re-cut inside P4.72's domain) REJECTED on those grounds.
  > **[Decision: item 2 — the persisted-destination flow builds IN P3.56 via the hand-off direction (the Loop's option A); the §5.8/§0.4.1 OPEN wire form is RESOLVED as the pre-C4 hand-off, 2026-07-15 (Co-Pilot; the Loop's P3.56 in-fill escalation)]** §5.3:301 makes the returning-user initial state part of the DestinationBar's component contract (the binding enumeration per the box-fill rule — the box headline is not the ceiling), P3.56 is the DestinationBar's ONLY builder (no P4 rebuild box exists and none is warranted — the seam-model header carve-out landed with this ruling; P4.69 extends it, P4.70.3 wires a11y), and the P3.80 resolver code's own docs name the P3.53+/P3.56 screens as its consumer (`orchestrator/mod.rs` — its fallback comment even assigns the passive fallback note to "the P3.56+ screens'") — option B would strand a built, tested resolver behind an invented owner box (the deferral class; CLAUDE §5). Ruled: the P3.80 OPEN point resolves in exactly the direction its note enabled — at the Confirm→Targets advance the frontend calls a NEW small §0.4.1 query command (naming = the fill's `[Build-Session-Entscheidung]`; the §0.4.1 row + bindings/golden land in the fill's commit) whose handler runs `resolve_persisted_destination`: a re-validated path arm returns the registered `DestinationPicked { destination, display }` pair, otherwise a beside-source outcome (exact return form = the fill's decision; no path outbound ever) — and the FIRST C4 call passes ordinary `ChosenRoot(id)` (or `BesideSource`), keeping the `[DECIDED 2026-07-06]` §0.6 2-variant `DestinationChoice` AND the id-less `OutputPlanPreview` permanently (no `Last` variant, no mirror-back field — the two amendments the P3.80 option-② rejection refused). The re-validation-failure fallback note (§5.8's promise; the resolver's own comment assigns it to these screens): the query return distinguishes the FELL-BACK outcome STRUCTURALLY from a plain beside-source pref (exact form = the fill's decision — a fact on the wire, never a string/path), and the DestinationBar renders the passive fallback note from it as a §5.7:825 chrome string; the §2.7 divert/writability surfacing on the RESOLVED destination rides the C4 plan as ever (G1 Opus P2 adopted: without the structured outcome the note would be silently lost whenever beside-source itself is writable — the resolver alone knows the fallback happened). **Spec re-cut obligation (same commit, DoD 2/G68):** §5.8's "Persisted `lastDestinationMode`" block (05-ui-ux:903–929) + the §5.3:301 parenthetical still sketch the `{ mode: "last" }`-style-intent-on-C4 + C4-mirror-back literals — re-cut BOTH to the built hand-off form (the invariants hold: core-side read + re-validate + beside-source fallback, no stored path on the wire, the frontend holds the id+display pair only — the C2b posture — and the no-harm machinery is untouched; §5.8's fallback-note sentence re-cuts to the structured-outcome + chrome-note channel above, preserving the surfacing itself).
- [x] **P3.57** [UI] Build the RerunPrompt interstitial (Skip default / Fresh copy / Cancel) · §5.3 §2.5
  needs: P3.56
  > the one batch-level prompt (state 6), entered **only** from the C4 `rerun` flag (destination-independent, never re-entered on a C5 change): *"Already converted with these settings."* — **Skip (default, focused)** / **Make a fresh copy** / **Cancel** (Esc → back to Destination with the held plan intact). The choice becomes the `RerunDecision` carried into C6. Rendered as a focus-trapped `role="alertdialog"` over the inert-but-mounted Targets/Destination.
- [x] **P3.58** [UI] Build the Converting screen (ProgressList real per-item + aggregate bar + Cancel) · §5.3 §1.11 §0.4.2
  needs: P3.57
  > **ProgressList** keyed by `itemId` over the `ItemProgress` payloads: real determinate per-item progress (never a bare spinner — the native engine self-reports a real fraction or an honest start→done, §1.11) + the aggregate `BatchProgress` bar; rows transition to terminal `Succeeded`/`Failed`/`Cancelled`/`Skipped`. **Cancel** button → C7 `cancel_run` → the `Converting (Cancelling…)` (7a) sub-state (button disabled, label "Cancelling…", a second Esc ignored) → `Summary` (partial) on backend confirm.
- [x] **P3.59** [UI] Build the Summary screen (ResultSummary + OpenActions, split-divert two-button) · §5.3 §1.12 §2.6.4
  needs: P3.58
  > **ResultSummary** renders `RunResult`: per-item success/fail/skip with §2.8 reason strings, output→source map, fully-failed banner (never a quiet "done"); a residue item (`IpcError.residueDisplay != None` / in `cleanup_incomplete`) rendered **per its §2.6.4 case — the terminal state is NEVER rewritten** — with its core-rendered §2.8.2 line verbatim + an optional "reveal residue" link (C9 `Residue(ItemId)`, P3.79; the 2026-07-16 Decision note below re-cut this sentence's original "rendered Failed" literal).
  > **[Decision: residue rendering = §2.6.4 per-case (terminal state never rewritten); the case-1/case-3 string = a new `OutcomeMsg` residue variant riding `ItemResult.reason` (option (a) in its `Lossy`-precedent shape); the banner line = the P3.50 `batch_summary_line` finally wired onto the wire, 2026-07-16 (Co-Pilot; the Loop's P3.59 NOGO/NOGO spec-contradiction hard-stop)]** §5.3:303's blanket "rendered as Failed (not Succeeded)" is the self-refuting literal (its own case-3 sentence keeps Cancelled) and loses to §2.6.4's normative three-case posture ("the success stands", 02:942-944) — reconciled literal→normative, WITH the string source this time (the NOGO's core: the fill's earlier reconcile orphaned case 1's string and forced chrome authorship against §5.7:799/:830): **(1)** §2.8.2 gains the case-1 row by PROMOTING §2.6.4's own already-authored copy ("converted — a temporary file may remain at {path}", 02:944 — verbatim modulo catalog capitalization); §02 keeps ownership — option (b)'s chrome owner-split is REJECTED (the exact P3.56-item-1 class) and option (c)'s `CleanupResidue.text` is REJECTED (`CleanupResidue` stays the STRUCTURAL annotation — item + `residue_display` + the C9 reveal membership; a text field would double-carry case 2, whose `Failure { kind: CleanupResidue }` row already IS the complete line naming {path}). **(2)** the wire slot = a new NON-FAILURE `OutcomeMsg` residue-annotation variant (naming = the fill's `[Build-Session-Entscheidung]`) in `ItemResult.reason` for **case 1 ONLY** — the EXACT `Lossy` precedent (a §02-rendered note on an otherwise-successful item; the field's own doc reads "None for a plain success with no lossy note"). **Case 3's `reason` stays `None` — the shipped arm is RATIFIED:** §2.6.4 case 3 authors NO per-item sentence (its complete surface = the STRUCTURAL `CleanupResidue` annotation — the rendered `residue_display` + the C9 reveal link — plus the batch-level With-residue tail, which 02:1266/:1274 homes under the BATCH summary strings; the G1-R1 convergent Opus-P2/Sonnet-P1: routing that tail per-item would double-render it against point (3) and its pathless "see details" wording cannot satisfy §5.7:830's "with where residue remains"). Case 2 UNCHANGED (`Failure` carries the full `cleanup_residue` row). **This EXPLICITLY SUPERSEDES the Succeeded arm of the shipped P3.25 `residue_item_reason` (orchestrator/mod.rs `ResidueDisposition` docs + the `Succeeded | Cancelled => None` arm; landed 7b08f7b, live-wired through `project_run_result` by the P3.50 fill) — no silent reversal, this note IS the escalated reconciliation (the R1 Sonnet P0):** that arm's rationale ("the terminal `state` already carries the meaning … neither adopts the §2.8.2 `CleanupResidue` *failure* string") was CORRECT against the failure-worded row but never carried §2.6.4:944's OWN authored non-failure case-1 annotation, leaving it carrier-less — the exposed gap that forced the fill's chrome authorship; spec > code. The Succeeded arm now returns the new variant with the promoted row; the Cancelled + Failed arms stand exactly as built; the `ResidueDisposition`/`residue_item_reason` docs + their tests evolve under `[Test-Change: P3.59]` tags in the fill's commit. The UI renders `reason` VERBATIM with no additional chrome path line (which also resolves the double-render defect). §0.6 + §2.8.2 + §5.3:303 edits land with the fill (same commit, DoD 2) — the §5.3:303 re-cut covers BOTH the blanket-Failed literal AND its case-3 "shows the tail" sentence (precised to the BATCH-level append it names), and the `ItemResult.reason` field doc ("None for a plain success with no lossy note" + its §2.8/§2.9/§1.1 source enumeration) gains the case-1 residue source with its `bindings.ts` mirror regenerated (the G1-R2 Opus P3); bindings regen + golden; the reveal-residue link keys off `cleanup_incomplete`/`residueDisplay` presence, unchanged. **(3)** the fully-failed banner / status line (the convergent Opus-P2/Sonnet-P1 finding): `RunResult` gains the core-assembled summary-line display field wired from the EXISTING `batch_summary_line` (P3.50 built it — it has NO production caller today, grep-verified; that wire gap is exactly why the fill authored "No files were converted" in chrome) — the UI renders it verbatim and the chrome banner string is removed; `summaryStatusLabel` drops its fake exhaustiveness (a non-terminal state is unrepresentable in a terminal `RunResult` row — assert, never invent "Failed", §5.2). **Forward (named owner):** a lossy note AND a residue annotation on ONE item share the single `reason` slot — a consequence of THIS ruling's case-1 carrier choice (pre-ruling, `reason` never carried a residue note), not constructible in P3 (the slice emits no `Lossy`); P4.69's audit owns the composition rule (its body now names it). **Prevention sweep (the class: a site asserting residue⇒Failed):** exactly three carriers — §5.3:303 (the fill re-cuts), P4.69's body + THIS box's body (re-cut in this ruling's commit); zero code/README echoes (mechanism-grep over src-tauri/src + src/ + the READMEs); §5.7:830's full clause "not a clean success **with where residue remains** — never a green done" survives and is satisfied per case: case 1 via the promoted row's `{path}`, case 2 via the `cleanup_residue` row's `{path}`, case 3 via the rendered structural `residue_display` + reveal link. **OpenActions** (Summary-only, not mid-run): "Open folder" → C9 on the common-root `OpenTarget` (P3.79); on `divert_root = Some(..)` render TWO buttons ("Open source folder" + "Open saved-to folder") with the connector line — labels are real `strings/ui.ts` entries (§5.3). "Convert more" → `Idle`.
- [x] **P3.60** [UI] Build the pre-flight refusal + fault screens (MixedDropRefusal, Unsupported, AppFault) · §5.2 §5.3 §2.13
  needs: P3.54
  > **MixedDropRefusal** (state 9): hard refusal listing formats+counts, an **active DropZone** as the primary re-drop action (→ `Collecting`) + Dismiss → `Idle`; no subset-convert. **UnsupportedNotice** (state 10): four variants (`Unsupported`/`Uncertain` + its note/`Unreadable`/`Empty` + skip tally), `aria-live="assertive"` heading, focus on Dismiss. **AppFaultNotice** (state 12): renders the machine-held `AppFault.message` **verbatim** (the §2.13.3/§7.2-owned pre-localised calm line — the 2026-07-16 Decision note below) under a chrome heading + **Start over** (Ctrl/⌘+N); no stack trace, never fabricates per-item outcomes (§2.13); wires the `app://fault` listener → the §5.2 `appFault` wildcard dispatch (the run-path `onRunFault` leg is P4.50's). A pre-run C3/C4/C5 reject never reaches state 12 — it renders via the `CommandError` inline slot (§5.3:317, built by P4.69; this box honours the routing posture only).
  > **[Decision: AppFaultNotice = the Loop's option A (render `AppFault.message` VERBATIM; §5.3:309 props re-cut to `fault, onStartOver`) + the run-path wire = option 1 (P4.50), 2026-07-16 (Co-Pilot; the Loop's P3.60 pre-fill escalation)]** **(1) the message:** §2.13.3's P2.109 `[DECIDED]` block (02:1900-14) is the normative owner — `EngineMissing`/`BundleDamaged` "present over the §0.4.2 `app://fault` event on the §5.8 WebView screen", i.e. ON this screen, and the payload's `message` field exists precisely for that (its field doc: "the §2.13.3 pre-localised, plain-English, trace-free calm message"): the UI renders it verbatim under the §5.7:799 doctrine (§7.2/§2.13.3 own the WORDS; the heading + Start-over are chrome). Option B (chrome-only, §5.3:309 taken literally) REJECTED: `crate::outcome`'s one-string-one-home comment deliberately refuses §2.8.2 homing for the three app-level kinds ("render via the §2.13.3 app://fault catalog"), so a payload-dropping screen would leave the §7.2 strings with NO renderer anywhere — the exact orphaned-string class the P3.59 NOGO named — and would show a damaged-bundle user the factually wrong "the conversion stopped". §5.3:309's `onStartOver`-only props cell + §5.2:148's single italic literal are the P3.59 literal-vs-normative class (single-class cells predating state 12's `app://fault` trigger), re-cut to the owner. The literal's TRUE scope is the run-path class (Channel silence / C6-C7 reject), whose copy is CHROME by construction — the core is dead and can author nothing; §2.13.3:1920 delegates "§5.8 owns the UI handling" — so frontend authorship there is spec-assigned, NOT the P3.56-item-1 catalog-fork class (no §02 catalog exists for it to fork). **(2) the run-path wire:** P4.50 owns it (its body already names the WebView/backend-disconnect class; the leg note lands with this ruling) — option 2 (wire it in P3.60) REJECTED because under (1) it would force the frontend to SYNTHESIZE a wire `AppFault` (authoring a fact client-side, against P3.53's "the backend is the source of truth for facts"), and option C (a nullable-`fault` re-cut of the shipped P3.53 machine) REJECTED for P3: it pre-decides P4.50's presentation split incl. the run-path copy harmonization (§5.8:1021's literal vs §2.13.3:1920's "reconnecting / restart" affordance — P4.50's design domain; the P3.49/P3.56 don't-pre-build-a-later-box's-domain posture); machine.ts's non-null state-12/`runFault { fault: AppFault }` typing stands UNCHANGED in P3 (every P3 entry carries a real DTO). **This EXPLICITLY SUPERSEDES the `onRunFault`-supplier attribution of the shipped P2.124 comments** — FOUR `events.ts` sites name the supplier as P3.53 or P3.60: the `ConversionRunHandlers` doc's supplier clause "UNSET in P2: the P3.53 FSM supplies it … and adds the §5.8 mid-run channel-silence watchdog" (its render parenthetical "dispatch → state 12 / `AppFaultNotice`, P3.60→P8" is the `app://fault` RENDER chain and STANDS — reconfirmed by the subscribe-doc's "`AppFaultNotice` render is the P3.60 slice → P8.19.1 copy"), the `onRunFault` field doc "UNSET in P2 (P3.53 supplies it)", the `startConversionRun` caller doc "The CALLER (the §5.2 Convert transition, P3.53) … supplies `onRunFault`", and the `runConversion` routing line "…(P3.60 AppFault / P4.69 CommandError), which supply `onRunFault` then" (its AppFault half only — the P4.69 CommandError half stands) — no silent reversal: the fill re-cuts all four to name P4.50 as the `onRunFault` supplier (incl. the watchdog clause), citing this note; the P2.124 C6-rejection detection SOURCE stays live, firing into the optional-absent handler as a documented no-op until P4.50. In P3, state 12 is therefore runtime-reachable only via `app://fault` — whose production EMIT is itself the P4 readiness/`PendingFault` body (main.rs `present_startup_fault`: "BOTH presentation bodies are P4") — so this box lands the seam those P4 bodies light up, exercised by tests. **Spec re-cuts (same commit as the fill, DoD 2/G68):** §5.3:309 (props `fault, onStartOver` + the verbatim-render contract); §5.2:148 (primary content per class: `app://fault` → `message` verbatim; run path → the chrome literal, renderer = P4.50); §5.7's ownership table gains the missing **app-level fault** row (message = §7.2/§2.13.3 verbatim, chrome + the run-path line = here — closing the ownership gap this escalation exposed); §5.8:1024-27 ("outside this state machine (the UI may never even mount)" — over-broad vs the P2.109 block: `EngineMissing`/`BundleDamaged` DO route into state 12 via the wildcard; precise it to the WebviewFault-at-startup/native case); §5.8:1035 ("render `AppFaultNotice` … per the disconnect handling" — the `app://fault` class renders `message`, not the disconnect literal); §5.3:317's "(full-screen, run-path only)" parenthetical (stale vs §5.2:148's `app://fault` trigger). **Prevention sweep (the class: a state-12/AppFault site still describing the pre-P2.109 single-class shape):** the six spec sites above (fill) + P4.69's title/body "§2.8 strings" mis-catalog and P8.19.1's "verbatim from the §2.8.2 catalog" mis-catalog (both re-cut in THIS ruling's commit — AppFaultNotice's body is the §2.13.3 catalog, never §2.8.2) + the two `events.ts` supplier comments (the supersession above); mechanism-grep over the repo (`AppFaultNotice` / the state-12 literal / "run-path only") found no further carrier (05:69's dir-sketch comment names both classes and stands).

---

### Walking-skeleton end-to-end proof on all 3 OS

**Goal:** the slice is proven green end-to-end on Windows, macOS and Linux — the per-pair
integration test, the output-validity readers binding, and the cross-OS run that de-risks the
whole Tauri + atomic-publish + IPC stack early (the README walking-skeleton purpose).

- [x] **P3.61** [TEST] Author the CSV→TSV / TSV→CSV corpus fixtures + the bound-firing CSV-expansion fixture · §6.4.5 §6.4.2 · G24a G16
  needs: P3.42, P0.5.11
  > committed corpus fixtures for both pairs (incl. a Windows-1252-encoded CSV, a quoted-field/embedded-delimiter CSV, a CSV-injection `=…`/`@…` leading-token file, a CJK/RTL-content file) under the §6.4.5 conventions; a deterministic **bound-firing** fixture exercising the P3-slice CSV-expansion bound — the **§1.7 wall-clock timeout** (P3.45; the per-read-deadline leg is deliberately not built — the P3.45 Decision note), NOT the §1.10 input-size budget (which arrives with the §1.10 engine P4.72 and is corpus-validated in P9.41 — the P3.49 §1.10-seam slice-stub note). Each fixture's SHA-256 joins the §6.4.5 corpus manifest **via the `stage-corpus` generator (P0.5.11) in the same commit** (G24a integrity, P0.5.4 — never a hand-computed hash); single-source helper, no inline duplication (test-strategy §3). (`needs: P0.5.11` for the manifest generator the corpus landing invokes.) *(§-cites corrected 2026-07-17, the Loop's P3.61 R1-opus P2 routed up: the pre-correction prose said "§6.4.2" for both the manifest and the helper rule — §6.4.2 is the property/fault-injection level and owns neither; the manifest is §6.4.5's, the helper convention test-strategy §3's. The refs-line's `§6.4.2` STANDS — it legitimately covers the bound-firing fixture's fault-injection level. Class-swept repo-wide: this line was the only prose carrier; the fill's six code cites were corrected in 115cf84.)*
  > **[Decision: corpus fixture files are text-hygiene-EXEMPT — `.editorconfig` gains a `[tests/corpus/*.{csv,tsv}]` block unsetting ALL FOUR axes (the binary-block analogue, deliberately broader than the requested charset-only) + `.gitattributes` gains the paired `tests/corpus/*.csv|*.tsv -text` rows, 2026-07-17 (Co-Pilot, owner-acked L(-1); the Loop's P3.61 pre-fill escalation)]** the blocking premise held at every layer: this box mandates a **Windows-1252-encoded CSV** while `[*]` asserts `charset = utf-8` and G52's editorconfig-checker skips only NUL-byte binaries — a single-byte-encoded TEXT fixture is scanned and reds; no workaround exists (`.csv` is the detection-bearing extension, the small tier is deliberately non-LFS, `check-editorconfig` is L(-1), no `.ecrc`). Ruled BROADER than the ask on the class ground: **fixture bytes are the test subject — no hygiene axis may assert anything about them** (a no-final-newline / trailing-space / CRLF fixture is the same fixture-sovereignty class; §6.4.5 realism will demand them), so all four axes unset, exactly the existing binary-block form. The `.gitattributes` leg closes the same class's SILENT face: the global `* text=auto eol=lf` would EOL-rewrite a future CRLF fixture at add/checkout and break its G24a manifest sha256 — `-text` (not `binary`; text diffs stay) pins byte-exactness; today's LF-only fixtures are unaffected, the hazard dies before it exists. Scope kept minimal: flat-path + extension-pinned, `manifest.toml` (first-party) stays under `[*]` and the global text rule; P5-P7 corpus text formats extend both blocks consciously per phase (an L(-1) act each). Adjacent scanners checked: G51 typos scans an explicit prose allowlist (corpus out of scope), G2 gitleaks unaffected by the enumerated fixture content (a future FP routes L(-1) as ever). **Staleness sweep (the class: "corpus/manifest not yet landed" era-text; live since P3.30):** TEN carriers re-cut in the ruling's commit — the two the Loop routed (g24-stage-corpus.py:154, ci.yml G24a step name) + the sibling self-test leg g24-corpus-integrity.py:191 + the check-corpus-integrity TARGET-ABSENT docstring + the two manifest-less-root runtime messages (check-corpus-integrity / stage-corpus, reworded era-independent — the arm itself stays correct for bare self-test roots) + BOTH lefthook.yml G24a slot comments (the L1 pre-commit AND the L2 pre-push slot — the second carried a DIFFERENT phrasing, "target-absent until P3-P7") + build-gates.md's G24a-row "(once the manifest lands P3-P7)" verification label and "Activated as the corpus fills" tail + the plan-lint docstring's G22/G23/G24a target-absent grouping (G24a split out as flipped-live at P3.30). Carriers 7-10 were the G1-R1/R2 sonnet P1s: the first sweep grep was PATH-FILTERED (scripts/ .github/ docs/security/ docs/process/) and so missed the repo-root lefthook.yml while mis-calling docs/security clean; the R1 fix then matched ONE phrasing and missed the same file's second slot with a variant wording — the sweep discipline is a repo-wide grep over ALL phrasings (here: the era-marker "P3-P7" itself), then triage. CONSCIOUSLY EXCLUDED (forward-valid or frozen, so a later reader need not re-open): P0.5.4's Delivered record (a frozen historical box — it describes the gate as built, when the manifest genuinely did not yet exist) and every "fixtures fill P3-P7"-form forward statement (the .gitattributes reserved-LFS-tier comment, the CLAUDE §1a map row, spec §0.7, ci.yml's G22/G23/G30 step names) — those assert the corpus KEEPS filling through P7, not that the manifest is absent.
  > **[Decision: the §6.4.5 CSV/TSV sample list BINDS this box — the four not-yet-built samples (semicolon European-decimal-comma, pipe, UTF-8-BOM, UTF-16) build HERE, 2026-07-17 (Co-Pilot; the Loop's P3.61 fixture-scope question)]** the deferral premise ("§6.4.5 groups CSV/TSV under Spreadsheets = P7; the corpus fills P3-P7") fails on the decisive layers: **(a) capability + code path** — the samples exercise P3-BUILT behaviour on the NATIVE pair: the engine's `Delimiter::Semicolon`/`Pipe` arms, the §2.10.2 BOM strip, and `classify_encoding`'s UTF-16LE/BE detection are live, unit-tested code (incl. the dedicated semicolon-decimal-comma / pipe-hint / UTF-16LE delimiter-sniff tests), and the native CSV↔TSV pair's FIRST corpus validation is P3.62's — its walking-skeleton reliability binds THIS phase (P7.62 later RE-sweeps `CSV↔TSV` inside the spreadsheet-category per-pair run over the broadened corpus — a re-validation, not the missing author; deferring the samples there would leave the shipping native pair corpus-unvalidated against them for four phases): P7's own header states P7 only *broadens* the P3-built engine (P7.54 passes "the **P3-sniffed** delimiter + encoding" into the SEPARATE LibreOffice CSV→workbook pairs), and **P9.25** only ASSERTS the samples "come through" (a consumer, not an author); **(b) ownership, precisely** — **P7.61** (the spreadsheet-corpus staging box) DOES list plain CSV/TSV samples incl. semicolon-European + Windows-1252, but explicitly as a **"Reuses the P3 CSV/TSV fixtures where they already cover a case — no inline duplication, single-source helper"** box: the AUTHORING home is P3.61 (authoring semicolon/W-1252 first in P7.61 would fork the single source the native pair needs earlier anyway), and **pipe / UTF-8-BOM / UTF-16 appear in NO authoring box at all** (P7.61's list omits them; P9.25 consumes them) — for those three the quiet-deferral/no-named-owner class applies outright; **(c) the box-fill rule** — the box's "(incl. …)" is inclusive and §6.4.5:1024-27's prose list is the binding enumeration for the CSV/TSV samples. The seven already-built fixtures stand ("tab" = the TSV fixtures; property-combining per file, e.g. ragged+leading-zero, is fine; the injection file's leading `-` alongside `=`/`+`/`@` is §3.5.6's full four-token set — the spec-§ the engine implements, not an extra); the per-file `exercises` record + `covers`/expected-outcome land with the P3.62 pair-binding (this box's manifest rows carry the G24a-required path/sha256/licence/provenance set); UTF-16 LE-vs-BE / one-vs-two files = the fill's `[Build-Session-Entscheidung]` (the §1.2 KATs already pin both BOMs at unit level). G52/G24a interplay verified: a UTF-16 fixture contains NULs, so editorconfig-checker auto-skips it as binary AND git `text=auto` never EOL-normalises it — the hygiene exemption covers it either way. **Forward note (layout):** §6.4.5 homes these under `tests/corpus/spreadsheets/` in the per-source-format END-state layout, and **P7.61 is the box that stages that directory** — the P3 corpus is deliberately FLAT (the P3.30 precedent — G69's dir-map + §0.7 home no corpus subdirectories yet, and the 2026-07-17 hygiene-exemption globs are flat-scoped), so the first corpus-subdirectory box (P7.61, or P5's image staging if earlier) owns the re-organisation as a structural act: the §0.7 + §1a rows AND the `.editorconfig`/`.gitattributes` glob extension in the SAME owner-acked L(-1) commit (whether the P3 fixtures then MOVE under the new layout or stay flat-referenced is that box's decision; a move regenerates the manifest `path` rows via `stage-corpus`).
- [x] **P3.62** [TEST] Bind the G31/G32 output-validity + source-unchanged readers to the CSV/TSV pairs · §6.4.3 §2.5 · G31 G32
  needs: P3.61, P3.38, P0.5.5, P0.5.6
  > activate the P0.5.5/P0.5.6 invariants on these pairs (this is the FIRST binding of P0.5.5 source-unchanged + P0.5.6 output-validity to real CSV/TSV data — `needs: P0.5.5, P0.5.6`, the P0 homes are `[x]` before the loop, mirroring the P4.59 `needs: P0.5.6` activation pattern): **(a) SOURCE-UNCHANGED** — `sha256` of every corpus source unchanged before/after (the no-harm proof, G32(a)); **(b) OUTPUT-VALIDITY** — the produced output passes a **real RFC-4180 reader** (the `csv` crate) + CSV-injection literal-preservation (NOT bare field-count) + non-empty/output≠input/size-plausibility (G31, reused by G32(b)). Register CSV↔TSV in `tests/corpus/manifest.toml` with its byte-stable/lossy disposition + the determinism sub-assertion (`sha256(out1)==sha256(out2)`).
- [x] **P3.63** [TEST] Author the per-pair integration runner end-to-end pass (drop→…→publish→summary) — activates the G23 conversion-command→test gate (keyed to `start_conversion`) · §6.4.3 §6.5 · G15 G31 G23
  needs: P3.62, P3.50, P0.4.11
  > the §6.4.3 per-pair integration test driving the **real** vertical slice — frozen-set freeze → detect → C3/C4 → C6 convert → §2.1 atomic publish → `RunResult` summary — against the real temp FS (never mock the no-harm/`fs_guard` layer, the thing under test, P0.5.1); assert the published output exists at the expected beside-source/diverted name, the no-clobber numbering on a pre-existing collision, and the summary maps output→source. G15 unit+integration; the §6.5 ledger marks the pair `reliable` once it passes (the walking-skeleton's first ledger entry). **This is the partner test for the §0.4.1 conversion command `start_conversion` (the C6 run path wired in P3.48 + the slice CSV/TSV transform P3.41) and completes the G23 activation PAIR** (the P0.4.11 contract, re-keyed by the 2026-07-17 Decision note below): the fill lands this test FIRST — in a `tests/`-path file, green under the still-`convert_*`-keyed gate — the owner-acked L(-1) re-key then flips G23 live against it (no matrix row or bijection script needed — distinct from the G22 schema-parity leg, which first fail-closes in P4.60); the check-off follows the re-key's green CI.
  > **[Decision: G23 is RE-KEYED to the real §0.4.1 conversion command `start_conversion` (the Loop's option A); sequencing = test FIRST (the fill), owner-acked L(-1) re-key SECOND, check-off THIRD, 2026-07-17 (Co-Pilot; the Loop's P3.63 SPEC↔GATE↔PLAN hard-stop)]** premise CONFIRMED at all three layers: §0.4's normative invariant 1 (00:1264-68) enforces one-target-per-batch BY "the absence of any per-item-target IPC command" — a `convert_*` command class is spec-FORBIDDEN (option B rejected); the §0.4.1 set holds exactly ONE conversion command (C6 `start_conversion`, the real `#[tauri::command]` in `ipc/conversion.rs`); and `check-completeness`' `_CMD_RE` matches only `fn convert_\w+`, with a g24 self-test leg PINNING that `start_conversion` is ignored — so G23 as delivered can NEVER fail-close (a permanently-dormant guarantee; the P0.4.11-era sketch assumed per-pair `convert_*` handlers, superseded by the single-C6 + §3.2 engine-registry design the spec froze). Ruled: **(1)** the gate re-keys to the §0.4.1 conversion-command set = {`start_conversion`} — a named set. A future spec-level conversion-command addition is itself gated (the §0.4.1 row + plan-lint check 12's command-surface drift assertion, which rejects any unenumerated `#[tauri::command]`), but NO existing check auto-syncs G23's keyed set to the §0.4.1 table — so this note RECORDS the standing obligation: a spec act that adds a conversion command MUST extend G23's keyed set (+ its g24 legs) in the same owner-acked L(-1) act; the guarantee becomes "the conversion-run command is driven by ≥1 real integration test" — per-PAIR completeness lives in G22 + the §6.4.3a `covers` bijection, NOT in G23's walk (the P6/P7 completeness boxes' "`convert_*`/engine command" tokens re-cut in this commit). **(2) Fail-closed-safe SEQUENCING:** no `tests/`-path file references `start_conversion` today, so re-keying first would flip G23 live+RED — therefore (i) the fill authors THIS box's E2E test first (a `tests/`-dir / `*_test(s).rs` file per the gate's `_TEST_PATH_RE`, genuinely DRIVING the C6 path — G23's name-reference match is the mechanical floor, the E2E drive is this box's own bar) and pushes green under the still-dormant gate; (ii) the Co-Pilot lands the owner-acked L(-1) re-key — `check-completeness` `_CMD_RE` (which must tolerate the real attr-arg form `#[tauri::command(rename_all = …)]`) + its docstrings/messages, the g24-completeness legs (incl. INVERTING the "start_conversion-is-ignored" leg), the build-gates G23 row, both lefthook completeness-slot comments, the ci.yml step name, gate-status.md:78 — flipping G23 live AND green against the landed test; (iii) the check-off follows the re-key's green CI. **Prevention sweep (the class: "`convert_*` is the conversion-command shape"):** the five L(-1) carriers ride the re-key commit; this box's title/body + P0.4.11's Delivered prose (correction marker — its forward claim "fail-closes when the first `convert_*` handler lands" WAS the defect premise) + P6:421/P7:484 + P2:105's historical shorthand are re-cut in THIS commit; repo-wide uncapped grep over `convert_*`/`fn convert_`/`convert_\w` found no further carrier (the g24 self-test's `convert_csv_tsv` SAMPLES are the gate's own fixtures and re-key with it).
- [x] **P3.64** [TEST] Add the §2.3/§2.4 link-safety + frozen-set + no-self-feeding integration cases · §2.3.2 §2.3.3 §2.4.2 §2.4.3 · G31 G48
  needs: P3.63, P0.5.9
  > integration cases proving the kernel on real links: a source reached via a symlink + its target both dropped → de-duped to one (§2.3.2); an output dir that is a symlink resolving onto a source → diverted, never clobbered (§2.3.3, T7); an output landing in a watched source folder does NOT expand/restart the batch (§2.4.2 snapshot, T8) and a concurrent/launch-time hand-off cannot inject into the frozen set (§2.4.3, T8); a hardlink/junction-to-source identity caught by dev+inode/file-index. These are the activation targets for the P0.5.9 T7/T8 homes (§2.4.2/§2.4.3) + the P0.4.3 `is_safe_output` fuzz — this is the P3 box the P0.5.9 `→ activated in … P3` T7/T8 link-safety/self-feeding edge points at (`needs: P0.5.9`, the P0 home is `[x]` before the loop, mirroring the P2.127/P4.18.1 back-reference pattern).
- [x] **P3.65** [TEST] Add the FAT/exFAT-divert + atomicity-under-interruption cross-volume integration case · §2.1.2 §2.7.2 §2.14.3 · G31
  needs: P3.64, P3.18, P3.17
  > an integration case on a FAT/exFAT-class destination (Unix): the publish diverts with `DivertReason::NoAtomicPublish` to the hardlink-capable system disk and the full §2.1 chain holds there (Windows-FAT is NOT diverted and keeps the guarantee by construction, §2.1.2); a cross-volume (`EXDEV`) publish detected reactively exercises the §2.14.3 copy-exactly-once fallback **built in P3.17** (asserting the intermediate is named/swept, the destination-volume free-space re-check fires, and a numbering collision re-renames the already-copied intermediate, never re-copies); plus the §2.1.3 kill-in-the-rename-window assertion (P3.19) wired into the cross-OS run so the two-state invariant is proven on a real second volume.
- [x] **P3.66** [TEST] Prove the full slice green on Windows + macOS + Linux (the cross-OS de-risk run) · §6.1.3 §6.7.1 · G15 G30
  needs: P3.65, P3.59
  > run the P3.63 integration runner + a thin smoke of the C1→C6→C8 round-trip on **all 3 OS** in the Lane-A matrix (G30 build-matrix, P0.4.10) — the README walking-skeleton purpose: de-risk the Tauri + atomic-publish + IPC stack early on every platform before any heavy engine. Assert the per-OS publish primitive (Linux `renameat2`/macOS `renameatx_np`/Windows `FileRenameInformationEx`) is exercised and green, and the slice **builds** on each OS — a debug `tauri build --no-bundle`, the §6.7.1-step-5 Lane-A shape (no engine/sidecar dependency to stage, §3.5.6). *(Corrected 2026-07-18, the P3.66 ruling: the pre-correction "builds + **bundles** on each OS" contradicted §6.7.1 step 5 ("`cargo check` / a debug `tauri build`") and §6.7.2 stage 1, which homes the WiX/dmg/AppImage bundling in the tag-triggered Lane B — owned by P10.9/P10.11/P10.12 and already recorded at P1.58's `--no-bundle`. Spec > plan: the box text yields, no `.github/**` edit exists to make.)*
  > **[Decision: the C1→C6→C8 smoke homes in `crate::ipc` on `pub(super)` helpers (NOT `pub(crate)`); the primitive clause means EXERCISED-THROUGH-THE-PRODUCT-PATH per OS leg, with the single-call-first ORDERING pinned structurally and NO production change for observability, 2026-07-18 (Co-Pilot; the Loop's P3.66 scout escalation)]** the scout's three findings are confirmed first-hand (the 3-OS `gate-tooling`/`compile-sanity` legs already run the suites ungated; §6.7.1 step 5 vs §6.7.2 stage 1 resolves the bundling clause above; `publish_once` maps BOTH `PublishAttempt::Published` and `LinkPublishAttempt::Published` to `SinglePublish::Published { residual_tmp: false }`, and `renameat2`/`renameatx_np` are one `rustix::renameat_with` resolved at COMPILE time — so neither is runtime-distinguishable). **(1) Visibility — widen, but to `pub(super)`:** `drain_to_collected_set` (`ipc::intake`) and `resolve_run_summary` (`ipc::conversion`) are SIBLINGS under `crate::ipc`, so `pub(super)` (visible in `crate::ipc` + its descendants) is sufficient AND strictly narrower than `pub(crate)`, which would expose an IPC-funnel internal to tier 1/2 (`orchestrator`/`engines`) against the §0.7 tier model — take the narrow one (the same-parent-module `pub(super)`-reuse pattern the P3.63 harness established over its P3.48-tagged items — a TEST-tree precedent for the MECHANISM; that `pub(super)` suffices for a sibling-descendant test module is independent and was compile-verified in the G1 review). The widening is *fulfilling* these helpers' documented design, not bending it: both doc comments already declare unit-testability as the reason they exist ("Takes plain `&` state (NOT the `AppHandle`) so it is fully unit-testable"; "Separated from the `#[tauri::command]` handler so the mapping is directly unit-testable"), which is exactly the §1.1a boot-glue split owner-decision A mandates in place of a `tauri::test` mock. **Home = `crate::ipc`** (a new `#[cfg(test)]` module in the `ipc` tree, naming = the fill's `[Build-Session-Entscheidung]`): `ipc/mod.rs` ALREADY homes five cross-cutting C-surface test modules (`c_surface_scan`, `responsiveness_contract`, `error_shape_contract`, `camel_case_wire_contract`, `ipc_boundary_proptest`), so a round-trip spanning C1+C6+C8 belongs at that span's own level — homing it inside `conversion` to save one keyword would mis-file it (the §0.7/P2.10 home-at-the-owning-level discipline). **Middle leg = `crate::orchestrator::run_conversion`** (already `pub(crate)` — the entry the P3.63 harness itself drives): C6's own inner `start_run` is AppHandle-coupled boot-glue (G28 signature-exempt) and is NOT `cargo test`-callable, so the smoke drives the conductor directly and EXACTLY these two widenings suffice — `build_batch`/`resolve_slice_target` are already crate-visible, there is no hidden third (the G1-review Opus P3). **(2) The primitive clause = the scout's reading (a), SHARPENED:** "exercised and green" binds at THIS box's level — the cross-OS run — so it means *the OS's primitive is reached through the PRODUCT path on its own leg*, which the new round-trip delivers (C6 publishes through `atomic_publish`) and which the existing per-primitive `unix_realfs_tests`/`windows_realfs_tests` suites do NOT by themselves satisfy: pointing at them alone would be the assert-over-built-layers shape the scout rightly flagged. Reading (b) is REJECTED as a runtime bar — it is unobservable by construction, and the fallback branch it would target is reachable only on a FAT/exFAT destination that §2.7.2 diverts up front (the bound P3.65 recorded, its real verification homed at P11.25). **NO production-code change to make it observable:** adding a discriminating `SinglePublish` arm purely for a test would be test-driven production complexity for a distinction the product does not act on — `residual_tmp` already carries the only §2.1.3-behavioural difference. Instead the single-call-FIRST ordering (try `publish_noreplace`, fall back ONLY on `Unsupported`) is pinned by a **source-scan assertion** — the §1.1a boot-stage pattern already sanctioned for the unreachable-from-`cargo test` surfaces — so a silent re-order to fallback-first reddens, which is the invariant reading (b) was really after.
- [x] **P3.67** [TEST] Stand up `tests/fuzz_replay.rs` — replay every `fuzz/crashes/`+`fuzz/corpus/` file through the now-real detect + fs_guard (+ CSV/TSV) fuzz-target functions on the STABLE toolchain · §6.4.2 · G48 G24
  needs: P0.5.8, P3.30, P3.8, P3.41
  > the activation target for the P0.5.8 fuzz-crash replay convention (`→ activated in P3`): now that the real fuzz-target function BODIES exist — `crate::detection` (P3.30 KAT/sniff), `crate::fs_guard::resolve_identity`/`is_safe_output` (P3.6/P3.8), and the in-core CSV/TSV transform (P3.41) — wire `tests/fuzz_replay.rs` as a plain `cargo test` integration test feeding every committed `fuzz/crashes/`+`fuzz/corpus/` file directly to those target functions with **NO libFuzzer harness**, so it compiles + runs on EVERY platform incl. Windows under the **STABLE** toolchain (only the instrumented `cargo-fuzz` leg needs nightly — Linux/macOS, the P0.4.3/G48 leg). The G24 planted-positive (a committed crash fixture MUST fail the replay if its fix is reverted) binds here. This is the P3 box the P0.5.8 `→ activated in P3` edge points at (`needs: P0.5.8`, the P0-authored convention, satisfied since P0 is `[x]` before the loop).
  > **Delivered (P3.67, 1204fbf) [Build-Session-Entscheidung: P3.67]:** the harness is `crate::fuzz_replay` (`src-tauri/src/fuzz_replay.rs`), a crate-root `#[cfg(test)]` module — NOT a cargo `tests/` integration target: `convertia-core` is a BINARY crate (no `lib.rs`) and the P1.6 root manifest is VIRTUAL, so a `tests/`-dir target links no library and cannot reach `crate::detection`/`fs_guard`/`engines` at all; the G48/P0.5.8/test-strategy-§1.5 name `tests/fuzz_replay.rs` denotes the plain-`cargo test` suite as opposed to `fuzz/` (the **P2.126 precedent** — the `IPC_PROPTEST_TARGETS`, contracted identically as "in `tests/`, NOT under `fuzz/`", landed as a `#[cfg(test)]` module in `ipc/mod.rs`), and the crate-root placement follows `crate::test_corpus` (P3.61)/`crate::test_volumes` (P3.65) — a FILE, never a directory, so §1a/§0.7 (G69) is untouched. Drives the FOUR G48 in-core targets whose bodies exist (`detect`/`fs_guard_resolve_identity`/`fs_guard_is_safe_output`/`csv_tsv`); `zip_slip`/`imgworker_ffi` join at P7.50.1/P4.35.1. Creates NO `fuzz/` (which would fail-close `check-fuzz-contract` against the not-yet-built harnesses, CLAUDE.md §1a) — so the replay is kept ARMED against an empty corpus by a G24 planted-positive over the real replay engine, a vacuity guard, and an adversarial-shape leg over every G48-named path/byte shape (all three mutation-verified). **Co-Pilot item (L(-1), non-blocking):** the `tests/fuzz_replay.rs` path string in `build-gates.md` G48 + `test-strategy.md` §1.5 is L(-1) and was left untouched per the L(-1) doc-sync split — it needs either a ruling ratifying the P2.126 reading (the path string is a NAME for the plain-`cargo test` suite) or a correction, after which the P0.5.8/P4.205/P7.337 forward-refs re-cut with it. *(Resolved 2026-07-20 — CORRECTION ruled, not ratification: a factually-unrealizable path in the L(-1) gate catalogue is the described-the-old-way class even wrapped in a "it's just a name" gloss, and no gate keys on the string (grep-verified, prose-only). The G48 row (both mentions + the planted-positive line), build-gates:1296 and test-strategy §1.5 now name `crate::fuzz_replay` with the bin-crate rationale; the P0.5.8 box carries a correction marker; the P0.4.3 activation map + P4.35.1/P7.50.1 forward-refs + the two in-code doc comments (main.rs / fuzz_replay.rs, tense-fixed so they do not describe the docs the old way) re-cut in the same owner-acked commit. CONSCIOUSLY LEFT (the same-class sibling, resolved the other way): the G48 row's "a `proptest` in `tests/`" phrase for the P2.126 IPC legs stays — it is an area-CONTRAST (`tests/` vs `fuzz/`), not a concrete unrealizable filename, and the P2.126 in-crate landing was ratified then; one class, two genuinely different shapes.)*

---

### `crate::outcome`: the §2.8.2 message catalog + §2.9.1 lossy-note catalog (the single string home)

**Goal:** the actual canonical-English STRING TABLES `crate::outcome` is the "single
source of every conversion-outcome string" for (§2.0 line 43). P2 authored the
`ConversionErrorKind` ENUM (P2.18/P2.18.1), the `OutcomeMsg` TYPE + projection helper
(P2.20), and the `LossyKind` ENUM (P2.8.2); these two boxes author the kind→string
TABLES themselves — without them `OutcomeMsg::{Failure,Lossy}.text` has nothing to
produce. They sit at end-of-P3 (the outcome cluster's string-table leg) rather than
re-numbering the whole phase; the consumers (P3.25/P3.46/P3.50 + P4.65/P4.69/P8.19/
P8.20 + every per-pair fail-clearly / lossy-iff-flagged test) carry the forward
`needs:` resolved in place by DECISION C.

- [x] **P3.68** [RUST] Author the §2.8.2 `ConversionErrorKind` → canonical-English message catalog (all item kinds + the 5 batch-summary strings + `{detected}`/`{platform}`/`{path}` substitution) · §2.8.2 §2.8 · G23 G57
  needs: P3.1, P2.18, P2.20
  > the §2.8.2 single-home string table in `crate::outcome` (the home §2.0 names): one canonical-English row per `ConversionErrorKind` item/app kind (`Corrupt`/`Empty`/`Unrecognized`/`UnsupportedType`/`UnsupportedPair`/`Unreadable`/`Gone`/`PasswordProtected`/`NoAudioTrack`/`TooBig`/`OutOfDisk`/`WriteFailed`/`PathTooLong`/`TooManyCollisions`/`EngineCrash`/`EngineHang`/`EngineError`/`PlatformUnavailable`/`QuarantinedByOs`/`CleanupResidue`/`InternalError`) with the `{detected}`/`{platform}`/`{path}` runtime-substitution wiring applied, PLUS the 5 **batch-level summary strings** (All succeeded / Partial / All failed / Cancelled / With-residue tail, assembled by §1.12). Produces `OutcomeMsg::Failure { kind, text }` (and the per-item `Skipped`/run-level chrome rows that ride the catalog) — the table P3.46 maps INTO via `ErrorKind::from`, P3.25 reads for the `cleanup_residue`/`CleanupResidue` row, P3.50 reads for the §1.12 projection, and P4.69/P8.19 render verbatim (UI never re-authors the string). Tone: plain/calm/never-blaming (SSOT *Fail clearly*); English-only (G57). A `#[test]` asserts every `ConversionErrorKind` variant has a non-empty catalog row (no unhomed kind). (`needs: P2.18` for the enum + `P2.20` for `OutcomeMsg`; the consumers carry the forward edge to this box per DECISION C.)
- [x] **P3.69** [RUST] Author the §2.9.1 `LossyKind` → canonical-English note catalog (all variants + the `{w}`×`{h}` substitution) · §2.9.1 §2.9 · G23 G57
  needs: P3.1, P2.8.2, P2.20
  > the §2.9.1 single-home lossy-note table in `crate::outcome` (§2.9 "single home of every lossy-note string"): one calm canonical-English note per `LossyKind` variant (`image_lossy_codec`/`image_palette`/`image_downscale`/`image_alpha_flatten`/`image_animation_flatten`/`image_svg_raster`/`doc_pdf_reflow`/`doc_pdf_to_text`/`doc_html_render`/`doc_to_text`/`doc_simplified`/`sheet_to_delimited`/`xls_legacy_limits`/`text_encoding_narrowed`/`slides_to_pdf_flatten`/`office_roundtrip_approx`/`pptx_to_ppt_legacy`/`audio_lossy_target`/`audio_transcode`/`audio_lossy_origin`/`audio_bitdepth`/`audio_tags_dropped`/`video_reencode`/`video_alpha_lost`/`video_subs_dropped`/`video_to_gif`/`audio_downmix`), incl. the **`image_svg_raster` `{w}`×`{h}` substitution**; produces `OutcomeMsg::Lossy { kind, text }`. The table P4.65 surfaces verbatim in FormatPicker, P8.20 polishes the presentation of, and every P5/P6 lossy-iff-flagged test (P5.49/P5.50/P5.52/P5.73, P6.29/P6.74, …) asserts FIRES against. English-only (G57). A `#[test]` asserts every `LossyKind` variant has a non-empty note row. (`needs: P2.8.2` for the `LossyKind` enum + `P2.20` for `OutcomeMsg`; the consumers carry the forward edge per DECISION C.)

---

### Cross-phase reconciliation (the deferred P3→P2 contract `needs:`)

- [x] **P3.70** [GATE] Wire the deferred P3→P2 contract reconciliation `needs:` edges — domain types, detection-outcome, error model, IPC commands/events, state machine · §0.6 §0.4 · G7 G20
  needs: P2.4, P2.8, P2.13, P2.15, P2.18, P2.20, P2.22, P2.25, P2.26, P2.29, P2.37, P2.39, P2.120
  > the P3 instance of the cross-phase reconciliation obligation (the master plan-lint forbidden-string check is P4.77): P3 IMPLEMENTS the bodies behind P2's contracts, so every P3 box that consumes a P2-declared type/handler must carry the edge — P3.5 plan() reads the `Target`/`EngineId`/`EngineDescriptor` types (**P2.8/P2.13**); P3.29 populates `DetectionOutcome` (**P2.15**); P3.32 freezes into `Vec<DroppedItem>` (**P2.4**); P3.46 maps internal kind → wire `ErrorKind` via the `From`/`OutcomeMsg` projection (**P2.18/P2.20** — the edge sits on the consuming sub-box **P3.46.2**, this file's stated convention, per the `[Build-Session-Entscheidung: P3.70]` on P3.46's note); P3.68 authors the §2.8.2 catalog over `ConversionErrorKind`/`OutcomeMsg` (**P2.18/P2.20**) and P3.69 the §2.9.1 catalog over `LossyKind`/`OutcomeMsg` (**P2.8.2/P2.20**); P3.48 C6 fan-out over the `ConversionEvent` Channel (**P2.29/P2.37**); P3.49 C1/C3/C4 contracts (**P2.22/P2.25/P2.26**); P3.53 the §5.2 state machine over the three `app://` listeners (**P2.39/P2.120**). `needs:` these P2 boxes here so the §6 selection builds the P2 contract first (P2 is `[x]` before the loop reaches P3, so trivially satisfied — but the edge must RESOLVE, not dangle); no P3 box `>`-note defers a `needs:` with the P4.77-forbidden phrasing.

---

### `crate::run`/`crate::fs_guard`: the §2.14.1 temp-ownership security-test home

**Goal:** the [TEST] box that EXERCISES the §2.14.1 temp-ownership security invariants the
P3.20/P3.21 MECHANISM builders produce — the Unix mode bits, the Windows per-run scratch-root
DACL, cleanup-on-fault/kill leaving no orphan, and the Windows AV-lock-during-cleanup path. It
is the activation target for P0.5.9's §2.14.1 temp-ownership home (P3.20/P3.21 remain the
MECHANISM home). Placed at end-of-phase (after the P3.68/P3.69 string-table leg + the P3.70
reconciliation box, the file's established end-of-P3 placement convention) so it stays a
single gap-free id (`.71`) rather than re-numbering the whole P3 sequence + every cross-phase
reciprocal reference to P3.70.

- [x] **P3.71** [TEST] Assert the §2.14.1 temp-ownership security invariants (grouping shell — four independent, separately-faileable OS-specific assertions) · §2.14.1 §2.6.3 §2.6.4 · G31 G15
  needs: P3.20, P3.21, P3.22, P3.23, P3.24, P0.5.9
  > grouping shell over the security-TEST home that EXERCISES the §2.14.1 temp-ownership invariants the P3.20 publish-temp-ownership + P3.21 lock-before-part MECHANISM builders produce (those stay the mechanism home). The four assertions target **different OS + syscall surfaces and fail independently** (a Unix mode-bit bug is unrelated to a broken Windows DACL or an orphaned-scratch failure), so each is its own sub-box per the same discipline as P3.46/P3.19/P1.62; the parent is `[x]` only when all four sub-boxes are (_format.md §2). The activation target for the P0.5.9 §2.14.1 temp-ownership security-TEST home — this is the P3 box the P0.5.9 `→ activated in … P3` §2.14.1 temp-ownership security-TEST edge points at (`needs: P0.5.9`, the P0 home is `[x]` before the loop, mirroring the P2.127/P4.18.1 back-reference pattern). G31 source-unchanged corroborates no original is touched. (`needs: P3.20`+`P3.21` for the ownership/lock mechanism, `P3.22`+`P3.23`+`P3.24` for the cleanup/sweep/reclaim paths the sub-boxes assert.)
  - [x] **P3.71.1** [TEST] Assert the Unix `0o700`/`0o600` mode bits on per-run scratch + publish temps · §2.14.1 · G31 G15
    needs: P3.20, P3.21
    > **(a) Unix mode bits** — the per-run kind-2 scratch ROOT is created `0o700` and every kind-1 `*.part` publish temp is `0o600` (assert the on-disk `st_mode` permission bits, all Unix); independent of the Windows DACL/AV-lock paths.
    > **[RECONCILE: P3.71.1 = a delivered dup, marked `[x]` without new code, 2026-07-20 (Build-Session)]** the two assertions this leg demands ALREADY exist, authored alongside the P3.20/P3.21 MECHANISM with explicit `P3.71` forward-refs in their own comments (the dup-box pattern): `crate::run::run_scratch_tests::run_dir_is_owner_only_0o700_on_posix` (the kind-2 scratch root == `0o700`) and `crate::run::publish_temp_tests::create_in_is_owner_only_0o600_on_posix` (the kind-1 `.part` == `0o600`) — both `#[cfg(unix)]`, both reading the on-disk `st_mode & 0o777`, both green. "Every kind-1 temp is `0o600`" holds by the **choke-point** argument: every publish temp mints through the one `PublishTemp::create_in` path (`0o600` via its `tempfile::Builder`), so the one asserted temp is representative. No new code; the P3.71 security-TEST home's genuinely-new legs (b)/(c)/(d) are the `crate::run::temp_ownership_security_tests` module (fea200c / e9355f4). Verified: both cited tests exist + assert exactly `0o700`/`0o600` (both G1 reviewers confirmed the reconcile honest).
  - [x] **P3.71.2** [TEST] Assert the Windows per-run scratch-root effective DACL grants no broad principal · §2.14.1 · G31 G15
    needs: P3.21
    > **(b) the Windows per-run scratch-root DACL** — the run-start `run-<RunId>/` scratch root's **effective** security descriptor grants **no broad principal** (Everyone / `BUILTIN\Users` / Authenticated Users) an ACE, delivered by the §2.14.1-`[DECIDED]` mechanism: the scratch lives under the per-user profile (`app_local_data_dir()`, wired at the C6 handler since P3.48), whose **inherited default ACL** is the `0o700` equivalent — "no explicit broadening", the P3.21 `acquire` create path unchanged. Assert at **SID level** (the security-descriptor/`icacls` query by well-known SID, never a localized principal-name string match — a German-locale Windows prints `VORDEFINIERT\Benutzer` for `BUILTIN\Users`); the machine-trust principals (SYSTEM/Administrators) retain their inherited ACEs — just as `0o700` never excludes root on POSIX. Windows-specific, test-only.
    > **[Decision: P3.71.2 pre-fill ruling — assertion re-cut to the spec-`[DECIDED]` mechanism, 2026-07-20 (Co-Pilot)]** the box formerly demanded access "only to the current-user SID" "via an explicit restrictive DACL at create" — both contradict spec §2.14.1's `[DECIDED]` mode-bits block ("On Windows the equivalent is the **default per-user ACL** (the scratch lives under the user profile / `app_local_data_dir()`; no explicit broadening)"), which outranks this plan text and test-strategy §6 (spec > security/process docs > plan): the delivered P3.21 `acquire` (plain `create_dir_all` under the profile) is spec-conformant verbatim, and the repo holds zero explicit-DACL code (`SetNamedSecurityInfo`/`SetEntriesInAcl`/`SECURITY_ATTRIBUTES`: 0 hits). The escalation's option B (an explicit-DACL rebuild of P3.21) is REJECTED as amending a spec-`[DECIDED]` mechanism without a spec-level re-decision nothing motivates — SYSTEM/Administrators cannot be meaningfully excluded (ownership takeover), so the literal "only the current-user SID" is unsatisfiable even under B and B's whole residual delta is profile-config independence. Honest residual, stated: the guarantee inherits from a correctly-configured per-user profile ACL; compensating controls = the §2.14.1 positional decision (`app_local_data_dir()`) + this leg failing on any explicit broadening. Same-commit class sweep: test-strategy §6's mechanism parenthetical corrected (L(-1)) + P0.5.9's frozen prose carries an appended correction marker; the run/mod.rs comments already state the delivered mechanism and stand. The assertion tooling (`icacls` vs a PowerShell `Get-Acl`/SDDL query) is the fill's [Build-Session-Entscheidung] within this frame; a spawned inspection process in a `#[cfg(test)]` leg may trip the CI-only G29 SAST — pre-flight semgrep locally, `// nosemgrep: <id>` with rationale if needed.
  - [x] **P3.71.3** [TEST] Assert cleanup-on-fault/kill leaves no orphaned scratch dir (all OS) · §2.6.3 §2.6.4 · G31 G15
    needs: P3.22, P3.23, P3.24
    > **(c) cleanup-on-fault/kill leaves no orphaned temp** — inject a fault/kill on a representative exit path and assert `cleanup_item`/`cleanup_run` (P3.22) + the startup sweep (P3.23) + the opportunistic reclaim (P3.24) reclaim every own-prefix temp, leaving zero orphan (own-prefix-scoped, never a bare `*.part` glob that would touch a foreign live temp). All OS.
  - [x] **P3.71.4** [TEST] Assert the Windows AV-lock-during-cleanup path — single-attempt residue honesty + post-release sweep reclaim · §2.6.4 §2.6.3 · G31 G15
    needs: P3.22, P3.23
    > **(d) the Windows AV-lock-during-cleanup path** — a temp held open during cleanup (a no-`FILE_SHARE_DELETE` handle, the AV-scanner class → `ERROR_SHARING_VIOLATION`) is handled by the §2.6.4-`[DECIDED]` model the P3.22/P3.23 mechanism delivers: `cleanup_run` makes its **single bounded attempt** and surfaces the undeletable path as **residue** (never a silent leak, never an in-process retry spin), and after the handle releases the **§2.6.3 sweep reclaims it** (`sweep_stale_within` with an injectable grace for the central `run-<RunId>/` surface; the §2.6.3 opportunistic reclaim owns the destination-resident `.part` surface) → **zero orphan**. Assert against the real mechanism on the central `run-<RunId>/` scratch surface: plant the hold, `cleanup_run` → residue names the path + no spin; release the handle → sweep → gone. Windows-specific.
    > **[Decision: P3.71.4 pre-fill ruling — re-cut to the §2.6.4-`[DECIDED]` residue+sweep model, 2026-07-20 (Co-Pilot; the P3.71.2 class, second instance)]** the box formerly demanded asserting "the bounded retry-after-release OR the `MoveFileEx(MOVEFILE_DELAY_UNTIL_REBOOT)` deferred-delete fallback fires" — a mechanism existing nowhere in spec or code (`MOVEFILE_DELAY_UNTIL_REBOOT`: zero repo hits outside this prose class): spec §2.6.4 `[DECIDED]` names "a lock held by AV software" as its FIRST example and decides cleanup failure → honest `CleanupResidue` + the §2.6.3/§2.6.4 sweep reclaim, and §2.6.2's Cancel row equally defers a wedged temp "to the §2.6.4 sweep". Under spec > security/process docs > plan, the P0.5.9/test-strategy-§6 "mirroring the §2.1.2 publish-path AV-retry" symmetry assumption is the corrected lower layer — the asymmetry is DELIBERATE: the publish path retries in-process because the item's success depends on immediacy (§2.1.2); the cleanup path does not, because reclaim has a decided asynchronous home (the sweep). The escalation's option B (building the retry loop or the deferred-delete) is REJECTED as amending a spec-`[DECIDED]` model without a re-decision nothing motivates — supporting observation: `MOVEFILE_DELAY_UNTIL_REBOOT` writes HKLM `PendingFileRenameOperations` and requires administrator rights, unavailable to an unprivileged desktop app. `needs:` gains P3.23 (the post-release reclaim leg exercises the sweep). Same-commit class sweep: test-strategy §6's AV-lock bullet corrected (L(-1)) + a second P0.5.9 correction marker (homes list + activated-in map); build-gates:1315's "Windows AV-lock cleanup-on-fault sub-case (P0.5)" is a frozen r-note idea-ledger NAME carrying no mechanism claim — classified not-stale, stands.

---

### `cargo-mutants` first informational pass over the no-harm/atomicity/no-misroute kernel

**Goal:** the [GATE] box that RUNS the P0.5.10-authored scoped `cargo-mutants` gate now that
the kernel crate BODIES exist — the first informational pass over `crate::fs_guard` +
`crate::detection` + `crate::outcome`, initialising the per-crate ratchet + the `gate-status.md`
informational entry. It is the activation target for P0.5.10's `→ activated in P3+` note
(`needs: P0.5.10`), mirroring the P3.67 fuzz-replay activation box: P0 authored the
CONTRACT + ratchet shape; this box is the runnable first pass against the real bodies that
the kernel-body boxes build, so the gate is enforced rather than authored-then-never-run.
Placed at end-of-phase (after the P3.68/P3.69 string-table leg + P3.70 reconciliation +
P3.71 temp-ownership test home, the file's established end-of-P3 convention) so it stays a
single gap-free id (`.72`) rather than re-numbering the whole P3 sequence.

- [ ] **P3.72** [GATE] Run the first informational `cargo-mutants` pass over `crate::fs_guard` + `crate::detection` + `crate::outcome`, initialise `max_survived_mutants.toml` per crate + the `gate-status.md` informational entry · §6.4 · G15 G24
  needs: P0.5.10, P3.6, P3.8, P3.18, P3.29, P3.46, P3.68, P3.69
  > the activation target for the P0.5.10 scoped mutation-testing gate (`→ activated in P3+`, "needs the kernel crates"): now that the no-harm/atomicity/no-misroute kernel BODIES exist — `crate::fs_guard` (`resolve_identity`/`is_safe_output`/`atomic_publish`/the FAT-exFAT divert, P3.6/P3.8/P3.18 + P3.15–P3.16), `crate::detection` (P3.29), and `crate::outcome` (the §2.8.2/§2.9.1 string-table leg P3.68/P3.69 + the §2.8 projection P3.46) — run the FIRST **informational** `cargo-mutants` pass scoped to those three crates, emitting a per-crate survived-mutant report and **initialising the decrease-only `max_survived_mutants.toml` ratchet per crate at the first-run count** (P0.5.10's activation criteria), plus writing the dated `informational` entry in `docs/process/gate-status.md` (plan-lint check 23). Line coverage proves a line ran, not that a test would CATCH a regression there — this is the kernel gate that proves the no-harm tests bite. Owner-decidable informational→required like G17b (the owner flips to required when the survived count reaches **0** for `crate::fs_guard` + `crate::detection`, P0.5.10). Release-tier — runs on the release-confirmation leg, never per-push (the run cost). (`needs: P0.5.10` — the P0-authored contract + ratchet shape, satisfied since P0 is `[x]` before the loop reaches P3; the kernel-body boxes P3.6/P3.8/P3.18/P3.29/P3.46/P3.68/P3.69 for the real bodies the mutants are injected into, mirroring the P3.67→P0.5.8 activation pattern.)

---

### The instrumented-nightly `cargo-fuzz` legs for `crate::fs_guard` + the in-core CSV/TSV transform

**Goal:** the [CI,TEST] box that INSTANTIATES the instrumented-nightly `cargo +nightly fuzz run`
legs for two of P0.4.3's primary named in-core G48 targets — `crate::fs_guard::resolve_identity`/
`is_safe_output` and the in-core CSV/TSV transform — now that their real fuzz-target function
BODIES exist (P3.6/P3.8/P3.41). P3.67 is the STABLE-toolchain replay (`NO libFuzzer harness`);
this is its instrumented-nightly counterpart, the peer of the DETECT instrumented-nightly leg
(P9.35), the imgworker-FFI leg (P4.35.1), and the zip-slip leg (P7.50.1) — plus, separately, the
IPC serde/numeric-overflow legs (P2.126), which are **G16 `proptest`s in `tests/`, NOT
instrumented-nightly libFuzzer legs** (the trusted WebView→Rust door). Without it, fs_guard + CSV/TSV would have ONLY the stable replay and no box wiring
`cargo +nightly-YYYY-MM-DD fuzz run` for them into `.github/workflows`. Placed at end-of-phase
(after P3.67/P3.72, the file's established end-of-P3 convention) so it stays a single gap-free
id (`.73`) rather than re-numbering the whole P3 sequence + every cross-phase reference.

- [ ] **P3.73** [CI,TEST] Instantiate the P0.4.3 instrumented-nightly `cargo-fuzz` legs for `crate::fs_guard::resolve_identity`/`is_safe_output` + the in-core CSV/TSV transform (date-pinned nightly, pinned libFuzzer bounds, G56 `timeout-minutes`) · §6.4.2 §1.2 · G48 G56
  needs: P0.4.3, P3.6, P3.8, P3.41
  > the activation target for the P0.4.3 `→ activated in P3` in-core fuzz legs whose instrumented-nightly instantiation no box homed (P0.4.3 names `crate::detection` → activated as the instrumented-nightly leg in **P9.35**; the serde/IPC boundary → **P2.126** (a G16 `proptest`, NOT an instrumented-nightly libFuzzer leg — the trusted WebView→Rust door); the imgworker-FFI surface → **P4.35.1**; the zip-slip archive-entry-name target → **P7.50.1**; but `crate::fs_guard::resolve_identity`/`is_safe_output` and the in-core CSV/TSV transform had ONLY the stable-toolchain replay **P3.67** — explicitly `NO libFuzzer harness` — with no instrumented-nightly leg). Now that the real bodies exist (`crate::fs_guard` `resolve_identity`/`is_safe_output` P3.6/P3.8 incl. the Windows dangerous-path classes + NUL-path/`PATH_MAX`+1 bound-firing fixtures; the in-core CSV/TSV transform P3.41), wire `cargo +nightly-YYYY-MM-DD fuzz run crate_fs_guard_resolve_identity` / `crate_fs_guard_is_safe_output` / `csv_tsv_transform` into `.github/workflows` (Linux+macOS instrumented nightly; per-push = the P3.67 stable replay / saved-crash-corpus replay), **date-pinned nightly channel** + **pinned libFuzzer resource bounds** (`-rss_limit_mb`/`-max_len`/`-timeout`/`-max_total_time`, an OOM/timeout a committed FINDING never a runner kill) + **G56 `timeout-minutes`**; every crash minimized + committed under `fuzz/corpus/`+`fuzz/crashes/` (then replayed cross-platform by P3.67). This is the P3 box the P0.4.3 fs_guard + CSV/TSV instrumented-nightly edge points at (`needs: P0.4.3`, the harness contract `[x]` before the loop; the kernel-body boxes P3.6/P3.8/P3.41 for the real targets the instrumented run exercises). → activates the P0.4.3 fs_guard + CSV/TSV in-core fuzz legs.
  > **[Forward note — the lib-target precondition, VERIFIED 2026-07-20 (Co-Pilot; the Loop's P3.67 scouting): the fill MUST escalate this as a pre-fill owner question BEFORE building.]** The whole `cargo-fuzz` plan presupposes a LIBRARY target `convertia-core` does not have: `fuzz/fuzz_targets/*.rs` import the crate under test through the fuzz crate's path dependency, a path dependency resolves the LIB target only, and `convertia-core` is a binary crate (no `src-tauri/src/lib.rs`, verified) — so `use convertia_core::fs_guard::…` cannot compile today. This blocks P3.73 + the sibling instrumented legs P4.35.1/P7.50.1/P9.35 (NOT P3.67 — the stable replay is in-crate by the 2026-07-20 homing ruling). The resolution is a genuine architecture fork, NOT decided here: (i) a bin+lib split of `convertia-core` (spec §0.7 + the G53 core-deps walk + the G27 coverage-domain map all touch), (ii) `#[path]`-including the module sources into the fuzz targets (fragile, duplicated cfg context), (iii) re-homing the fuzzed kernels into a workspace lib crate (a §0.7 restructure). Whichever wins is a spec-level act with its own ruling — this note converts the surprise into a planned escalation.

---

### The `RunEvent::Exit` best-effort scratch-cleanup wiring — deferred from P2.81

> P2.81 built the `App::run` lifecycle handlers (`ExitRequested` `prevent_exit` + `Exit` → `flush_logs` +
> the `_ =>` non-exhaustive arm) but DEFERRED the `best_effort_scratch_cleanup` call inside the `Exit` arm.
> **[Mechanism CORRECTED at P3.74 — see the box's [Decision] note.]** The frozen P2.81 premise — "§7.3.2
> mandates it IS the §2.6 `cleanup_run` path … wires the `Exit` arm → `crate::run::cleanup_run`" — is
> superseded: `cleanup_run` consumes a `RunScratch` the P3.48 conductor owns in its run future, so the Exit
> arm reaches no `RunScratch`; the reachable §2.6.3 primitive is `sweep_stale` (the box wires that).
> Placed at end-of-phase (after P3.72/P3.73, the file's established convention) so it stays a single
> gap-free id (`.74`) rather than re-numbering P3 + every cross-phase reference.

- [ ] **P3.74** [RUST] Wire `best_effort_scratch_cleanup` (= the §2.6.3 `sweep_stale` path, §7.3.2 `[DECIDED, mechanism CORRECTED]`) into the P2.81 `RunEvent::Exit` arm — best-effort + non-blocking, deferring a wedged `.part` to the §2.6.3 next-launch sweep · §7.3.2 §2.6
  needs: P2.81, P3.23
  > the one-call wiring the P2.81 Scope-DECISION deferred: inside the `RunEvent::Exit` arm call `crate::run::sweep_stale` over the launch's `app_local_data_dir()` scratch base — reclaim the DEAD runs' central `run-<RunId>/` dirs across every instance dir, held-lock-gated (a concurrent LIVE instance is never touched, §2.6.3) — **best-effort and non-blocking** (a bounded synchronous sweep; must not stall app exit; §7.3.2). A wedged descendant still holding a `.part` (§1.7 group-kill timed out) is left to the SAME §2.6.3 sweep next launch (P3.23), never blocking exit. NOT a separate cleanup implementation (§7.3.2 `[DECIDED]`) — `sweep_stale` IS §2.6.3 machinery.
  > **[Decision: P3.74 pre-fill ruling = A — the §7.3.2 mechanism re-cut from `cleanup_run` to `sweep_stale`, 2026-07-21 (Co-Pilot; the Loop's P3.74 hard-stop)]** an exhaustive scout proved `cleanup_run` is structurally uncallable from `RunEvent::Exit`: it consumes a `RunScratch` by value, and the §1.9/P3.48 C6 conductor OWNS the live run's `RunScratch` inside its spawned `run_conversion` future (cleaned per-run by `finish_run` there) — none of the app-managed State stores holds one, so `&AppHandle` reaches no `RunScratch`. The §7.3.2 `[DECIDED]` "cleanup_run" predates the P3.48 ownership design. Ruled A: the mechanism clause alone is superseded (all intent clauses stand — `sweep_stale` fulfils each verbatim, incl. "not a separate implementation": it IS §2.6.3 machinery); `best_effort_scratch_cleanup(&AppHandle)` → `sweep_stale(app_local_data_dir())`, the CENTRAL scratch root across all instance dirs, lock-gated. Option B (store the run `RunScratch` app-side so `cleanup_run` is callable) REJECTED — a P3.48-conductor rework against the §0.7 tier model / "scratch owned by the future". **Honest residual (spec-synced):** destination-resident `.part`s are the §2.6.3(b)/P3.24 surface, not this central sweep; per-run cleanup stays `finish_run`; the §7.3.3 confirmed-quit cancel→cleanup→exit flow (the only mid-run-scratch case) is **P4.67**, so today `Exit` fires only idle → this backstop mainly reclaims a prior/foreign crashed run (an early run of the next-launch sweep). Carrier sweep (this commit — the class: every site naming `cleanup_run`/§2.6 for the §7.3.2 Exit mechanism, or a "dead until §7.2 startup" attribute now live via P3.74): §7.3.2 (07:620-625, mechanism CORRECTED marker) + the 07 code-block comment (§2.6→§2.6.3) + the spec README:279 change-log line; this box title+body+`needs:` (P3.22→**P3.23**, the real `sweep_stale` dep); the P2.81 scope-note above (correction marker) **and the P2.81 box's own Scope-DECISION in `P2-app-shell-contracts.md`** (correction marker); `main.rs` — the `Exit`-arm comment, the `dispatch_run_event` fn-doc (cleanup_run→sweep_stale), + the new `best_effort_scratch_cleanup` fn; `run/mod.rs` — the module-doc wiring line (cleanup_run@P3.48 / sweep_stale@P3.74), the caller-note, and the dead-code attributes **removed** from every now-live member (`sweep_stale`+`sweep_stale_within`, their callees `sweep_verdict`/`probe_lock`/`dir_age`, the `LockState`/`SweepVerdict` enums, and `remove_own_temps_in`); AND the P3.20/P3.48 `cleanup_run`-lineage sites the opus round-2 pass caught — `run_prefix`'s vestigial `allow` removed (live since P3.48 via `cleanup_run`, its false "dead until the P3.74 run-lifecycle wiring" reason gone), and the `cleanup_run` doc/comment/allow-reason prose re-pointed from "the P3.74 run-lifecycle teardown / future additional caller" to the truth (`finish_run` is its SOLE production caller; the P3.74 Exit backstop uses `sweep_stale`, not `cleanup_run`; `cleanup_run`'s own `allow` + its `[Test-Change: P3.48]` tag left intact — prose only). Prod-build verified warning-free after every removal; both G1 reviewers' NOGO carrier findings resolved (repo-wide grep: no site ties `cleanup_run` to P3.74/Exit, zero "dead until §7.2/P3.74" reasons). **FLAG:** `sweep_stale`'s intended §7.2.5 startup caller (the `prepare_scratch_and_log` orphan-reclaim slot, P2.106.5) has **no named owning box** (P2.106.5 says only "body P3/P4"; no P4 box names it) — recorded for the Co-Pilot, not blocking P3.74 (which is `sweep_stale`'s first production caller).

---

### The core-owned-path wire revision (owner ruling 2026-07-06) — IDs + display strings on the wire

> **The owner ruling (2026-07-06): no FS path ever crosses the IPC wire or an `app://` event, in either direction** — the wire carries **IDs + lossy display strings** only; the real `PathBuf`s live in core-side registries (`FrozenCollectedSet`, `RunResultStore`, the `DestinationId` session registry). This cluster owns the revision: P3.76 re-types the wire, P3.77 moves the native drop core-side behind the payload-less `app://intake` nudge, P3.78 renames C1 to the universal `drain_intake` completion door, P3.79/P3.80 re-key C9 and the destination legs to ID-resolution, and P3.81 consolidates the frontend re-wire. **Per-box live-consumer rule (the 2026-07-12 P3.77 ruling):** each wire-changing box here carries the MINIMAL reconciliation of every consumer LIVE-MOUNTED at its build time in the same commit (green-per-box; no correctness gap) — P3.77 (nudge-drain + drop-ingest removal) and P3.78 (the remaining two `events.ts` call sites) are the members, P3.79/P3.80 verified consumer-free until the P3.56/P3.59 screens (the P3.81 ruling re-ordered the consolidation box post-screens). Placed after P3.74 per the file's established end-of-phase convention so the ids stay gap-free (`.76`–`.81`) rather than re-numbering P3 + every cross-phase reference; the P3.25/P3.48/P3.49/P3.50/P3.51/P3.53/P3.54 consumers carry the forward `needs:` edges onto this cluster (P3.25/P3.48 added 2026-07-07 — the P3.25 hard-stop resolution; the original wiring pass missed the two non-C-command wire emitters — and P3.50, which always carried the `needs:` edge, joined this intro list in the same fix), resolved in place by DECISION C (the P3.75 sweep box stays the physically-last section — its "every other P3 box" mandate auto-covers these boxes).

- [x] **P3.76** [RUST] Build the core-owned-path wire layer — the display-string projections + `DestinationId` session registry · §0.6 §0.4.3 §2.10.1
  needs: P2.4, P2.7, P2.12
  > re-type every outbound wire field to *Display strings (`DroppedItem { item, displayName, relPathDisplay? }`, `SkippedItem`, the roots, `OutputPlanPreview`, `DestinationResolved`, `RunResult`, `ItemResult`, `CleanupResidue`, `ItemOutcome::Succeeded`, `ItemStarted`, `IpcError.pathDisplay`/`residueDisplay`); the real `PathBuf`s stay core-side in `FrozenCollectedSet`/`RunResultStore`; stand up the minimal `DestinationId` session registry for core-picked roots; regenerate `bindings.ts` (G19). The §2.10.1 byte-for-byte filename guarantees hold on the core-side `PathBuf`s — a *Display string is the lossy presentation projection only, never re-submitted as input.
  > **[Decision: P3.76↔P3.77 scope — the `IntakePayload` retire moves to P3.77 (option ①), 2026-07-07 (Co-Pilot; the Loop's scope escalation)]** "retire `IntakePayload`" is ATOMICALLY the same change as P3.77's title move ("the payload-less `app://intake` nudge") — the type's SOLE constructor is that event's emit (no command signature carries it; verified) — and removing it forces the emit-through-`PendingIntake` routing (P3.77's core) plus the frontend drain-on-nudge consumption (the MINIMAL form at P3.77, the consolidation at P3.81 — the 2026-07-12 P3.77 ruling). So the retire bullet is re-homed to P3.77 (title de-scoped here; option ② would have pulled P3.77/P3.81 cores into the already-largest box, option ③ found no broader mis-cut — the remaining P3.76 deliverables are box-local). `IntakePayload` (P2-landed `crate::domain` + its `.types()` registration + G15 wire-form test) therefore SURVIVES P3.76's bindings regen — pre-existing P2 stock living until its own box, NOT a new raw-path wire emission (the P3.25 option-B rejection concerned building NEW emissions against the 2026-07-06 ruling; the cluster P3.76–P3.81 as a whole realizes the ruling, and the "no inbound path DTO survives" assertion holds from P3.78 on — the payload dies at P3.77, C1's `paths`/`origin` args at P3.78).
- [x] **P3.77** [RUST] Move the native drop core-side + the payload-less `app://intake` nudge (retiring `IntakePayload`) — `WindowEvent::DragDrop` → the §7.8.1 funnel; `PendingIntake` = the single hand-off buffer · §7.8.1 §5.4 §7.1.1
  needs: P3.76, P2.55, P2.58
  > the native drop is handled in Rust (`WindowEvent::DragDrop`) and funnels into the §7.8.1 intake funnel exactly like launch-arg/second-instance paths — no drop path ever reaches the WebView; `app://intake` becomes a payload-less nudge and `PendingIntake` (P2.58) becomes the single hand-off buffer for ALL intake origins (drop, picker, launch-arg, second-instance), the P2.55 refuse-busy gate unchanged upstream of it. **Retire `IntakePayload` in THIS box** (moved from P3.76 — the 2026-07-07 option-① Decision on P3.76): the payload-less re-emit IS the retire — remove the `crate::domain` type, its `.types()` registration and its G15 wire-form test, regenerate `bindings.ts` (G19), and route the emit's former path cargo through `PendingIntake` so no path is lost.
  > **[Decision: the wire-change box CARRIES its live consumers' minimal reconciliation — the cluster-sequencing gap closed per-box, 2026-07-12 (Co-Pilot; the Loop's P3.77 hard-stop, options A/B convergent)]** the plan deferred the frontend re-wire wholesale to P3.81 while P3.77's spec-mandated wire changes BREAK the live-mounted P2.120/P2.121 frontend three ways (the retired `IntakePayload` import reds `tsc`; the payload-less nudge nulls the listener's `event.payload`; the core-side `DragDrop` handler + the live `useNativeDragDrop` ingest DOUBLE-ingest every drop) — an unfixable-at-P3.81 sequencing gap, since green-per-box (tsc is a hard gate) and the no-correctness-gap bar bind EVERY push. Ruled (the escalation's A, generalised — B converges): **each wire-changing box in this cluster carries the MINIMAL reconciliation of every consumer that is LIVE-MOUNTED at its build time, in the same commit; P3.81 remains the comprehensive consolidation.** For THIS box: the `app://intake` listener consumes the nudge payload-LESS and issues the drain (§7.8.1 "the frontend issues the drain on every nudge" — via the CURRENT C1 drain form until P3.78 renames it), the frontend drop-INGEST path is removed (killing the double-ingest; the enter/over/leave drag-affordance rendering stays until the P3.54 DropZone replaces it), the dead `IntakePayload` import goes with the regen. **(b)** the `[RUST]` tag names the box's PRIMARY deliverable, not a file-type exclusivity — the minimal frontend reconciliation + `bindings.ts` regen ride the box (every regen box already touches `src/`). **(c)** the cluster intro + P3.81 are precised in this ruling's commit. **Prevention sweep (the class: a wire-changing box whose live-mounted consumer's fix the plan defers):** verified against the MOUNTED surface (`App.tsx` hooks → `events.ts`): **P3.78 IS a member** (the C1→`drain_intake` rename strands the live `ingestPaths` call sites — the remaining TWO after this box's own drop-ingest removal; its box now carries the reconciliation clause); **P3.79/P3.80 are NON-members** (grep-verified: NO mounted code calls C9/`open_path` or the C2b destination legs — their first live frontend consumers are the P3.56/P3.59 screens (originally worded "P3.81"; re-ordered by the P3.81 ruling), no gap); P3.76 already landed green (display-projection additions, no breaking wire removal). Class closed for the cluster; the rule binds any future wire-changing box the same way.
- [x] **P3.78** [RUST] Rename C1 → `drain_intake { collectingId, onScan }` — the universal intake-completion door (walk+freeze inside; golden + bindings + contract tests in the same commit) · §0.4.1 §1.1
  needs: P3.77, P2.60
  > C1's `paths`/`origin`/`drainPending` args are retired entirely (the P2.60 drain flag is subsumed — draining `PendingIntake` IS the command): the WebView calls `drain_intake { collectingId, onScan }` on the nudge, the core runs the §1.1 walk + freeze over the buffered core-side paths and returns `CollectedSet`; C2a `pick_for_intake` sheds its `collectingId`/`onScan` args in the same move (per the revised §0.4.1 row — the picker only fills the §7.8.1 funnel and returns `()`; C1 is the sole `onScan` carrier); the C1/C2a golden + `bindings.ts` + contract tests are updated in the same commit — AND the remaining TWO live `events.ts` call sites (the mount-drain + the nudge-drain — P3.77's reconciliation already removed the third, the drop-ingest) move onto `drain_intake` in the same commit (the cluster's per-box live-consumer reconciliation rule, the P3.77 ruling).
- [x] **P3.79** [RUST] Re-key C9 to `OpenTarget` ID-resolution against `State<RunResultStore>` · §0.4.1 §7.7.2 §7.7.3
  needs: P3.76, P2.100, P2.103
  > C9 becomes `open_path { target: OpenTarget }` — a run-scoped ID form (common root / divert root / per-item / residue by `ItemId`) the core resolves against `State<RunResultStore>` to its own recorded `PathBuf`; the WebView cannot name a path at all; the P2.100 `OpenerExt` mapping + the P2.101–103 membership gate re-key onto the resolved entry (the §7.7.3 membership check becomes the ID-resolution itself).
- [x] **P3.80** [RUST] Re-key the destination legs — C2b returns `{ destination: DestinationId, display }`; `DestinationChoice::ChosenRoot(DestinationId)`; core-resolved persisted last-destination · §0.4.1 §5.8 §7.4
  needs: P3.76, P2.23
  > C2b's Rust-side picker registers the picked root in the P3.76 `DestinationId` session registry and returns `{ destination: DestinationId, display }` (no path string outbound); `DestinationChoice::ChosenRoot(DestinationId)` resolves core-side at C5/C6; the §7.4 persisted last-destination is loaded + validated core-side into the registry — the WebView only ever handles the ID + display pair.
  > **[Decision: leg 3 = the CORE persisted-last resolver, option ① — the §5.8 `{ mode: "last" }` C4-intent wire stays the §5.8-owned OPEN point, 2026-07-12 (Co-Pilot; the Loop's P3.80 scoping escalation)]** legs 1+2 build exactly as scoped (the §0.6 2-variant `DestinationChoice`, the escalation's PROPOSED core-internal resolved form — a `ResolvedDestination { BesideSource, ChosenRoot(PathBuf) }` shape, naming the fill's `[Build-Session-Entscheidung]` — for the pure orchestrator legs, the C2b shell return, the C4/C6 registry resolution with §0.4.3 refusal, `.manage` + boot-scan, serde/bindings/golden). Leg 3 = a REAL, tested core resolver (`resolve_persisted_destination` shape): read the §7.4.1 `lastDestinationMode` pref via the P2.85-built `LastDestinationMode { BesideSource, ChosenPath(PathBuf) }` — its persisted VALUE is a union `"beside-source" | "<absolute path>"` (§7.4.1's table row; the apparent §7.4.1-"mode" vs §5.8-"stored absolute path" conflict is a KEY-NAME-vs-VALUE non-finding, steelmanned + resolved: the ChosenPath-arm value IS the path) — re-validate a path arm as writable (§7.4.1 "always re-validated as writable at use time"; §2.7 fallback semantics, the P3.33 `location_status` machinery), mint + register a `DestinationId`, yield the `DestinationPicked` pair; a failed re-validation → beside-source fallback, nothing registered. Fill doc-sync obligation (G1-R2): the `prefs.rs` consumer-map doc (the P2.88 `[Build-Session-Entscheidung]` block) still says `last_destination_mode` is frontend-consumed / "never via Rust" — TRUE until this box, falsified by this resolver: reconcile it in the SAME commit (DoD 2/G68; the P2.88 plan note already carries the [Superseded] pointer). Consumer-free until the P3.56/P3.59 screens (re-worded by the P3.81 re-ordering ruling; originally "until P3.81") — the C9 PRECEDENT verbatim (P3.79 = re-key + pure resolver, P3.51 = live wire; the `DestinationRegistry` itself was dead-until-P3.80), and per the P3.77 per-box rule P3.80 is a verified NON-member (no live consumer to reconcile). **Option ② REJECTED**: a `Last` variant + an `OutputPlanPreview` id-field would AMEND the `[DECIDED 2026-07-06]` §0.6 2-variant shape and INVENT a wire form §0.4.1 — the explicitly-named owner ("the exact wire form is §0.4.1's", §5.8) — has never defined (the DECISIVE ground: §0.4.1's own C4 row types the `destination` argument as the plain 2-variant `DestinationChoice` and returns the id-less `OutputPlanPreview` — the reserved form is literally blank), for a frontend consumer that does not exist before P3.81 (the P2-callback-seam class). **Option ③ REJECTED**: it leaves an explicit box clause unfulfilled (the deferral class). The §5.8 intent-wire resolution — the exact wire form of "use my persisted choice" at C4, and whether a mirror-back field is even needed versus a mount-time `DestinationPicked` hand-off (§5.8 sketches THAT the resolved id reaches the frontend; only the form is delegated-open) — stays OPEN with §5.8/§0.4.1 as owners, resolved when the screens wire the persisted-choice flow (the P3.53+/P3.56 mount/DestinationBar leg) / the §5.8 box **(RESOLVED 2026-07-15: the P3.56 item-2 Decision — the hand-off direction, a new §0.4.1 query command; §5.8/§5.3:301 re-cut with the fill)** — NOTE the direction ① enables without binding it: the ①-resolver's `DestinationPicked { destination, display }` handed to the frontend at mount would let it pass ordinary `ChosenRoot(id)` on C4, needing NO `Last` variant and NO preview id-field, keeping §0.6 2-variant permanently. **Prevention sweep (the class: a plan leg building against a §5.8 flow whose wire form is an open §0.4.1 point):** plan-wide grep for the `{ mode: "last" }` / §5.8-intent references — P3.80 is the SOLE member; P3.81's consolidation consumes whatever the owner-point resolution lands, no other box pre-builds it.
- [ ] **P3.81** [UI] Consolidate + VERIFY the frontend on the ID+display wire — the post-screens render-audit (no raw path anywhere), the C9/Destination consumer verification, the P3.77-interim retirement · §5.3 §5.4 §5.8
  needs: P3.53, P3.54, P3.55, P3.56, P3.57, P3.58, P3.59, P3.60
  > the POST-SCREENS consolidation/verification box (re-ordered by the 2026-07-12 P3.81 ruling — see the Decision note): **(a)** the render-AUDIT — no raw path is rendered or held anywhere in the frontend (grep `src/` for path-shaped state + verify every screen renders *Display strings only, §2.10.1); **(b)** verify the screens' C9-by-`OpenTarget` (P3.59 Summary) and `DestinationId`+display (P3.56 DestinationBar) consumers go through the `bindings.ts`/façade door only (§0.4.5 — no stray `invoke`); **(c)** retire the P3.77-interim drag-affordance listener in favour of the P3.54 DropZone's own DOM affordance (the hand-off completion); **(d)** unify any residual per-screen façade helpers into `events.ts`/`commands.ts` (one façade, §5.1). A real audit+consolidation deliverable — not a seam stub.
  > **[Decision: P3.81 is a POST-screens box — the inherited `needs:` edges were inverted, 2026-07-12 (Co-Pilot; the Loop's P3.81 ordering escalation — options ②+③ convergent, ① rejected)]** all three remaining deliverables referenced screens built AFTER it (the render surface is P3.53+; the C9 consumer is the P3.59 Summary, the Destination consumer the P3.56 DestinationBar; the affordance hand-off target is P3.54) while P3.53/P3.54 carried `needs: P3.81` — an ordering inversion. GENESIS (recorded honestly): the ORIGINAL P3.81 ("re-wire the frontend") correctly preceded the screens — they needed a re-wired frontend; the 2026-07-12 P3.77 ruling moved the re-wiring per-box and re-scoped P3.81's CONTENT to consolidation, but did NOT re-examine its inherited INCOMING edges — this ruling completes that re-scope (the class: a box re-scope must sweep the box's EDGE-SET, not only its text). Resolution: P3.53's edge → `needs: P3.48, P3.78, P3.80` (the real wire dependencies — drain door + id-form destination; P3.76/P3.77 transitive), P3.54's → `needs: P3.53, P3.77` (the affordance hand-off source); P3.81 gains `needs: P3.53–P3.60` and becomes the genuine post-screens audit above. **Option ① REJECTED**: UNSET façade seams for same-phase consumers a few boxes later are process filler — the P2.120 seam pattern exists for CROSS-phase gaps, and the screens shape their own helpers when they consume (the box would be "thin/awkward", the escalation's own reading — confirmed). The screens build DIRECTLY on the landed ID wire (P3.76–P3.80 all `[x]`; the live frontend is already ID-clean via the P3.77/P3.78/P3.80 reconciliations — verified: `events.ts` holds only drain/startConversion/subscribe helpers, no path-shaped state). **Prevention sweep (the class: a consolidation box ordered BEFORE its subject via stale inherited edges):** `grep needs:.*P3\.81` — exactly P3.53/P3.54 carried it (the other screens chain linearly; P3.63/P3.64 E2E do not reference it); the only other cross-cutting box, the P3.75 phase-end sweep, is correctly physically-last with no incoming inversion; the P3.77-ruling class-note is extended by this edge-sweep rule. No further member.
- [x] **P3.82** [CI] Add `tokio` to `check-supply-chain`'s §0.8 `PINNED_FLOORS` (`tokio = 1.52.3`, g24-auto-covered like `tokio-util`/`proptest`) — the §0.8 drift-floor row for the P3.3 direct-dep promotion · §0.8 §0.9 · G18 G18a
  needs: P3.3
  > **[!extern] (L(-1)):** `scripts/check-supply-chain` + `scripts/gate-selftests/**` are L(-1)-caged — Co-Pilot-authored under owner-ack (G71); the loop skips + collects it. The paired §0.8 floor for the **P3.3** `tokio` direct-dep promotion (§0.9/§1.7 in-core permit lane — `Semaphore` + `spawn_blocking`), mirroring the tokio-util **P2.133 dep / P2.134 floor** split. **MUST follow P3.3** (`needs: P3.3`): `_pinned_floor_assertion()` is reconcile-to-present — a `PINNED_FLOORS` crate ABSENT from `Cargo.lock` FAILS G18 (the "relied-upon dep vanished" leg), so the floor row cannot precede the dep landing in the lock (P3.3 lands green WITHOUT it: a new direct dep NOT in `PINNED_FLOORS` does not red G18 — the floor is a present-crate check, not an all-direct-deps-listed check). Adds the `PINNED_FLOORS` entry (`tokio = 1.52.3`, the resolved P3.3 lock version). §0.8 already lists `tokio` ("exact", 00-architecture §0.8), so **no §0.8 table edit is needed** — only the floor row + g24 coverage. No dedicated g24 leg: the existing `_all_at_floor` + real-lock legs auto-cover it (like `tokio-util`/`proptest`).
- [x] **P3.83** [CI] Add `dunce` + `winapi-util` to `check-supply-chain`'s §0.8 `PINNED_FLOORS` (`dunce = 1.0.5`, `winapi-util = 0.1.11`, g24-auto-covered like `tokio`) — the §0.8 drift-floor rows for the P3.6 direct-dep promotions · §0.8 §2.3.1 · G18 G18a
  > **[!extern] (L(-1)):** `scripts/check-supply-chain` is L(-1)-caged — Co-Pilot-authored under owner-ack (G71); the Loop hard-stopped + escalated (the P3.6 hard-stop, 2026-07-07). The paired §0.8 floors for the **P3.6** promotions: `dunce` (§2.3.1 `\\?\`-prefix display normalisation) and `winapi-util` (§2.3.1 Windows file-identity — the safe `GetFileInformationByHandle` wrapper; `same-file` exposes no Windows identity numbers, see the §2.3.1 `[CORRECTED 2026-07-07]`). Unlike P3.82 the floors PRECEDE the dep-add: both crates are ALREADY in `Cargo.lock` transitively (`dunce` 1.0.5 via `tauri`, `winapi-util` 0.1.11 via `walkdir`→`same-file`), so `_pinned_floor_assertion()`'s present-crate check holds before P3.6 lands (the P3.82 "cannot precede the dep landing in the lock" caveat is satisfied by the transitive presence; reconcile-to-present, the tempfile/P1.59 class). The §0.8 FS-guarantees row is corrected in the same commit (`same-file`→`winapi-util`). No dedicated g24 leg: the `_all_at_floor` + real-lock legs auto-cover (the tokio precedent). Authored AND executed by the Co-Pilot in one commit.
- [x] **P3.84** [CI] Add `rustix` + `windows-sys` to `check-supply-chain`'s §0.8 `PINNED_FLOORS` (`rustix = 1.1.4`, `windows-sys = 0.61.2`, g24-auto-covered like `tokio`) — the §0.8 drift-floor rows for the P3.12/P3.14 publish-primitive direct-dep promotions · §0.8 §2.1.1 §2.1.2 · G18 G18a
  > **[!extern] (L(-1)):** `scripts/check-supply-chain` is L(-1)-caged — Co-Pilot-authored under owner-ack (G71); the Loop hard-stopped + escalated (the P3.12 hard-stop, 2026-07-07). The syscall-crate ruling for the WHOLE §2.1 publish cluster (P3.12–P3.18, both OS halves — the P3.6 lesson: chase the full cluster, not the escalated half): **`rustix` 1.1.4** for the Unix primitives (SAFE API, verified in the registry source: `renameat_with(RenameFlags::NOREPLACE)` = Linux `renameat2`/`RENAME_NOREPLACE` and apple `renameatx_np`/`RENAME_EXCL` incl. the weak-symbol pre-10.12 handling — moot under the §0.3.1 macOS-11 floor; `linkat`/`unlinkat` for P3.13; `fstatvfs`/`statvfs` for free-space) — **`libc` REJECTED** (raw `unsafe` at every call site vs rustix's zero-unsafe-in-core). **`windows-sys` 0.61.2** for the Windows publish/free-space externs (verified: kernel32 `SetFileInformationByHandle` + `FileRenameInfoEx = 22` + `GetDiskFreeSpaceExW` in `Win32_Storage_FileSystem`, and the Wdk `NtSetInformationFile` route also present — P3.14's builder picks within the one crate). **`fs2` is RETIRED** — never becomes a dependency (unmaintained; free-space rides the same two crates; the §0.8 row corrected in this commit). Both floors reconcile-to-present (BOTH crates are pulled transitively by `tempfile` — rustix 1.1.4 unix-side, windows-sys 0.61.2 windows-side), so the floors PRECEDE the dep-adds safely (the P3.83 transitive-presence class); the multi-version windows-sys lock state (0.45/0.59/0.60.2/0.61.2) is handled by `_pinned_floor_assertion`'s documented ANY-copy-≥-floor semantics. No dedicated g24 leg (the `_all_at_floor` + real-lock legs auto-cover). Authored AND executed by the Co-Pilot in one commit; the same commit carries the P3.9-Nachtrag precision fix (the G29/build-gates + `check-unsafe-policy` illustrative raw-FFI example lists corrected to the post-P3.6/P3.12 reality).
- [x] **P3.85** [CI] Add `chardetng` + `encoding_rs` + `csv` to `check-supply-chain`'s §0.8 `PINNED_FLOORS` (`chardetng = 1.0.0`, `encoding_rs = 0.8.35`, `csv = 1.4.0`, g24-auto-covered like `tokio`) — the §0.8 drift-floor rows for the P3.27/P3.41 direct-dep promotions · §0.8 §1.2 §3.5.6 · G18 G18a
  needs: P3.27, P3.41
  > **[!extern] (L(-1)):** `scripts/check-supply-chain` is L(-1)-caged — Co-Pilot-authored under owner-ack (G71); the Loop's P3.41 session flagged the missing `csv` floor, and the standing prevention-sweep rule surfaced the two siblings: the P3.27 `chardetng`/`encoding_rs` Cargo.toml comments declare the SAME pending Co-Pilot follow-up (the class: a direct-dep promotion whose paired floor box was never authored — P3.82–84 covered the tokio/dunce+winapi-util/rustix+windows-sys promotions; P3.27/P3.41 had none). All three floors reconcile-to-present (`chardetng` 1.0.0 / `encoding_rs` 0.8.35 / `csv` 1.4.0 sit in `Cargo.lock` since their dep boxes landed — the P3.82 "the floor cannot precede the dep landing in the lock" ordering is satisfied dep-first here), and the §0.8 "Additional crates" rows landed with the dep boxes (DoD 2), so **no §0.8 table edit is needed** — only the three floor rows + the three Cargo.toml follow-up comments re-pointed at this box. No dedicated g24 leg (the `_all_at_floor` + real-lock legs auto-cover, the tokio precedent). Sweep close-out: the Cargo.toml comments declaring a pending floor follow-up ↔ the `PINNED_FLOORS` dict now reconcile — no comment-declared pending floor remains in the workspace (`tauri`/`serde`-class deps are DELIBERATELY un-floored: "PINNED_FLOORS lists only the codegen/runtime-pinned crates", the tauri dep-row comment). Authored AND executed by the Co-Pilot in one commit.
- [x] **P3.86** [CI] Refine G70's removed-assertion tag window to the contiguous deletion RUN (the whole-unit tombstone) — `check-test-suppression` + 3 `g24` deletion-run legs · §6.4 · G70 G24
  > **[!extern] (L(-1)):** `scripts/check-test-suppression` + `scripts/gate-selftests/**` are L(-1)-caged — Co-Pilot-authored under owner-ack (G71); the Loop hard-stopped + escalated (the P3.77 G70 hard-stop, 2026-07-12; independently confirmed by its build-subagent's STOP-and-report). The per-assert ±6 window was designed for in-place edits; git's diff geometry emits every `-` of a change region before any `+`, so the buried assertions of an ATOMICALLY-DELETED test unit — a retired symbol's whole `#[test]`/`describe` block (the P3.77 `IntakePayload`/`ingestFromIntakeEvent`/`ingestFromDrop` retires) or a full-body rewrite's deletion run (`stash_vs_drain_race`) — were STRUCTURALLY untaggable: every G70=0 path was a hack-class construct (a redundant sibling test, a drain-mirror, a reused retired name, a gratuitous `{}`-scope for the diff tool), all owner-rule-rejected. Refinement: a removed assertion's tag window = ±6 around the BOUNDARIES of its maximal contiguous `-`-run — the justification belongs to the deletion EVENT (the G8 site-adjacency philosophy); a 1-line removal degenerates to the historical rule EXACTLY; ADDED markers / commented-out assertions keep per-line ±6; NO opener-gating (a mid-test gutted run still demands its own adjacent tag — G1 keeps the semantic judgement, the gate stays flag+justify, never weaker; corollary both G1 reviewers verified + accepted: two back-to-back deleted units with NO surviving line between them form ONE run, so one tombstone may span both — the tag stays mandatory and the whole run is diff-visible to G1). `g24-test-suppression.py` 52→55 legs (buried-assert run untagged ⇒ fail; ONE adjacent tombstone ⇒ pass; a tag beyond run±6 ⇒ still fail); the G70 build-gates row carries the `[Refined]` sentence (same commit, G68). Empirically validated against the live P3.77 staged diff: the 5 structurally-untaggable flags clear under the Loop's ALREADY-PLACED adjacent tombstone tags with zero code change. Authored AND executed by the Co-Pilot in one commit.

---

### The phase-end Co-Pilot hardening sweep — the standing phase-close box

> The standing test-strategy §11 phase-close box (owner directive, recorded 2026-07-06):
> Co-Pilot-executed — never the Build-Loop; mandate, level and evidence rules in
> [test-strategy §11](../process/test-strategy.md#11-the-phase-end-co-pilot-hardening-sweep).

- [!extern] **P3.75** [TEST] Run the phase-end Co-Pilot hardening sweep over the whole P3 delivery — adversarial re-test at the hardest technically-possible level · §6.4
  > **[!extern] (Co-Pilot-executed — the standing test-strategy §11 phase-close sweep, never the Build-Loop):** runs once every other P3 box is `[x]`; the phase's whole delivery is adversarially re-tested at the hardest technically-possible level with unrestricted session tooling (Docker, WebDriver/Playwright, property/fuzz/mutation probes, real-OS live runs); findings are fixed with tests as normal dual-reviewed commits before this box flips `[x]`.
  > **Boundary stop:** P4.1 carries `needs:` on this box — a `[!extern]` prerequisite of a non-extern box is a loop STOP (`_format.md` §2/§6), so the loop hard-stops at the P3→P4 boundary and hands off to the Co-Pilot until the sweep is `[x]`.
