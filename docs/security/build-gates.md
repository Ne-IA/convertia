# ConvertIA — Build-Gate Catalogue (living)

> The operational catalogue of every guardrail: **when** it runs, **what** it
> blocks, **which tool** enforces it, and its **fail posture**. Companion to the
> [security-concept.md](security-concept.md) (the *why*). Living document — same
> conflict order and update rules as the concept doc.
>
> Gate IDs are stable (`Gnn`). The plane column maps to the
> [defense-in-depth planes](security-concept.md#3-defense-in-depth--the-enforcement-planes)
> L0–L5.

## 0. Policy

- **Two planes.** Each gate marked *(mirror)* runs both as a local git-hook
  (L1/L2/L3) **and** in CI (L4). Local = realtime; CI = clean-checkout backstop.
- **Performance budgets.** pre-commit (L1) **soft** target **< 10 s** (a soft target, not a
  hard contract — §2's reading note names the heavy contributors `actionlint`→`shellcheck`,
  cold-crate clippy; the heavy additions are diff/glob-scoped to hold it); pre-push (L2) target
  **< 3 min**; anything heavier is **CI-only (L4)** or **release-only (L5)**.
- **Hooks run in parallel** within a plane; gates sharing a resource (e.g. one
  test DB-equivalent / one build dir) are merged into a single sequential wrapper.
- **Fail-closed by default.** A gate blocks on doubt. Only the explicitly listed
  gates are **fail-open** (skip when a prerequisite is genuinely absent), and only
  because another plane guarantees enforcement.
- **No bypass.** `--no-verify`, force-push, disabling a required CI check, **and
  `core.hooksPath` redirection** are forbidden (CLAUDE.md). The local L1–L3 plane is
  entirely git-hook-based, so `git -c core.hooksPath=<elsewhere>` (or a persisted
  `git config core.hooksPath`) silently disables ALL local hooks WITHOUT `--no-verify` —
  a functionally identical un-named bypass — so it is added to the forbidden set
  explicitly, machine-checked by P0.6 step-0 (`git config --get core.hooksPath` unset or
  == the lefthook path) and by G54 (which resolves the EFFECTIVE hooks dir, not a
  hardcoded `.git/hooks/`). A red gate is fixed, not bypassed.
- **Severity ≠ exit code** for custom gate scripts: *any* finding fails the gate;
  severity only drives output formatting.
- **Custom gate scripts are themselves tested** (positive **and** negative cases —
  every custom gate and fastpath detector ships a self-test that proves it FAILS on
  a planted violation, not only that it passes clean) under a narrowly-scoped
  self-test gate (G24).
- **Structural parsing over regex.** A gate that consumes a generated/structured
  file (JSON/TOML/YAML/lockfile/SBOM) **parses** it (`jq`/`serde_json`/a real YAML
  reader), never a regex — and a gate that **cannot resolve or parse its input fails
  CLOSED in CI** (a missing/unparseable target is a gate failure, not a skip).
- **Offline tolerance.** Any gate with a network step (advisory-DB refresh, rule
  fetch) **decouples** the refresh (warn-only) from the check (hard-fail against
  the local/vendored DB), and honours an offline env flag.
- **The dual review (G1) is a quality amplifier, NOT a security control.** Only the
  deterministic gates — **every `Gnn` except G1** — are security controls. The
  `Dual-Review:` trailer is self-attested and unverifiable, so a gamed trailer cannot
  ship insecure code — the gates either pass on a clean checkout or they do not. (The
  range is phrased "every gate except G1" rather than a frozen *G2–G50* bound, which
  drifts each time a gate is added; the catalogue is the authoritative span. plan-lint
  check 5 asserts the prose never claims a span narrower than `max(Gnn)`.)
- **Gate-toolchain is pinned + verified as a class.** Every gate/SAST/SBOM/lint tool
  (Semgrep, Syft, `gitleaks`, `zizmor`, `actionlint`, `typos`, `osv-scanner`/`grype`,
  `cargo-fuzz`, the CycloneDX tools, `cdxgen`, `editorconfig-checker`, **`lefthook`
  (G54), `cargo-auditable` (G55), `minisign` (G39)**, …) is pinned by
  **exact version AND verified by checksum / image digest at install** — vendored
  binaries / digest-pinned containers preferred over `cargo install`/`npm -g` from a
  live registry (a poisoned/typosquatted gate tool both misses a real finding and can
  read the CI secret). **Trust-role of the explicitly-named tools (r7 — the named set
  spans two distinct trust roles, stated so the list does not overstate any one tool's
  position):** `cargo-auditable` (G55) + `minisign` (G39) touch the **minisigned bytes /
  the signing step** and are squarely in that trust boundary; **`lefthook` (G54)** is in
  the boundary for a different reason — it is the program that EXECUTES the entire L1–L3
  local plane and is re-invoked in CI by the G54 two-plane parity, so a poisoned/
  typosquatted `lefthook` is **strictly more damaging than a poisoned linter** (it can
  silently no-op EVERY local hook — the functional equivalent of the `core.hooksPath`
  bypass §0 forbids — while reporting success, and it installs from a live registry,
  exactly the path §0 rejects), hence a **G24 wrong-checksum negative self-test** on its
  install like every other pinned tool; whereas **`cargo-mutants` (G15)** and
  **`diffoscope` (G60)** are **release-tier INFORMATIONAL tools** (a mutation-score, a
  reproducibility delta) that produce no signed artifact — pinned per this same §0
  discipline but **NOT inside the minisign trust boundary**. **The fetch-and-verify mechanism is itself a first-class
  self-tested control (not merely "spot-checked"):** a script downloads each pinned
  tool, verifies its checksum/digest, and ships a **G24-style negative self-test — a
  deliberately-wrong checksum MUST fail the install**; any `pip`-installed gate tool
  (e.g. Semgrep) uses **`pip install --require-hashes -r requirements-ci.txt`** (entries
  in `pkg==ver --hash=sha256:...` form — a bare `pip install semgrep==X.Y.Z` verifies
  only the version string, not content), with a planted-positive that a hashless install
  fails the lint. **Checksum-VALUE authenticity (r6 — the self-test proves the verify
  MECHANISM works but NOTHING about whether the committed checksum was obtained
  authentically; a poisoned tool whose real malicious hash is committed sails through —
  the self-test rejects a *different* hash, the install *matches* the committed malicious
  one — and these tools both read the CI secrets AND are what would catch a real finding,
  the exact threat the G37 engine policy hardens against, left open one tier down while §0
  asserts the symmetry):** apply the **G37 acquisition-mode discipline to the gate-tool
  pins** — a committed **gate-tool source allow-list** (per-tool upstream origin), and at
  **pin-establishment corroborate each recorded checksum against the tool's OWN published
  signature** (GitHub-release attestations / `cosign` sigs / a maintainer-signed checksums
  file) **OR ≥ 2 independent origins** (not a bare hash of one download). A gate-tool
  checksum edit is a **hard Co-Pilot escalation like an `engines.lock` SHA edit**. The Rust
  toolchain itself is pinned via a committed
  `rust-toolchain.toml` (an exact stable version + a **date-pinned** nightly channel
  `nightly-YYYY-MM-DD` for `cargo-fuzz` G48 — a bare floating `nightly` is a daily-moving
  input), asserted not-floating. The **build toolchain that produces the shipped artifact**
  is in the same boundary — the CI base image/container is digest-pinned, the C/C++
  toolchain and the Tauri CLI/bundler are version/digest-pinned (P0.2); if engines are
  from-source, the source tarballs are hash-pinned + the build container digest-pinned
  (spec §3.8). The trust boundary is "everything that touches the bytes that get
  minisigned".

## 1. L0 — Build-Loop per box (the dual review, "holy grail")

| ID | Gate | Tool / mechanism | Blocks | Scope / fail-mode |
|---|---|---|---|---|
| **G1** | Opus + Sonnet pre-commit dual review | two model reviewers critique the staged diff; recorded as a `Dual-Review: opus=… sonnet=…` commit trailer (presence/well-formedness gated by G12) | commit (self-gate) | always-on; **quality amplifier, NOT a security control** (only the deterministic gates G2+ are security controls — §0). The prose contract is below. |

> **G1 has a catalogue ROW (above) AND the prose contract (below).** Every gate
> cited in a box header `· <Gnn>` ref must resolve to a `| **Gnn** |` row
> (`_format.md` §3.1/§7); the P0 dual-review boxes (P0.1.3 / P0.6.1–P0.6.3) cite
> `G1` as a header ref, so the row is mandatory. The row is the machine target; the
> prose paragraphs that follow are the human contract.

**G1 — Opus + Sonnet pre-commit dual review.** *Plane L0.* Before each build
commit, two model reviewers (`opus` + `sonnet`) critique the **staged diff**
(`git diff --cached`, inline — not a SHA) for completeness, correctness, and
spec-conformance, plus the relevant spec `§§` + this catalogue.
- Output per reviewer: findings ranked **P0→P3**, each with a one-line reason +
  spec-`§`/file ref; convergence/divergence stated explicitly.
- **P0/P1 → fix in the working tree, re-stage, re-review** (loop). **No push
  between fix and re-review** (no fix-push cycle). **P2/P3 →** documented in the
  commit body + a follow-up box if structural.
- Recorded as a machine-readable commit trailer of the **exact** form
  `Dual-Review: opus=GO sonnet=GO` (or `NOGO`) — the *evidence of review* is
  mandatory; each reviewer's findings + convergence/divergence are recorded verbatim
  in the commit body (so a "both GO, 0 findings" on a non-trivial diff is an
  auditable smell for Co-Pilot spot-audit).
- **Staged-diff sanity (the trailer attests *this exact staged diff*).** Immediately
  before `git commit`, `git diff --cached --stat` MUST match the file set the two
  reviewers saw at GO; any file added/removed after GO **requires re-review** (no
  silent post-review staging).
- **Test-integrity scrutiny (HIGH-SCRUTINY — the "no green-by-rewrite" semantic
  leg).** A diff that **MODIFIES / RELAXES / SKIPS / DELETES a test**, or flips a test
  **red→green** (a rewritten/weakened assertion, an added `#[ignore]`/`it.skip`/
  `describe.skip`/`.only`/`test.skip`/`xfail`, a `#[should_panic]` added to a
  previously-real assertion, or a removed/commented-out assertion in a changed test),
  is an **explicit reviewer scrutiny item**: both reviewers verify it is **NOT
  suppressing a real regression** (the default is the CODE is wrong, not the test) and
  that the **(1) old-expectation-genuinely-obsolete (spec-`§`/decision cited) + (2)
  new-expectation-correct (verified vs the spec / by reading the real result back, not
  "it's green now")** justification is **present in the commit body**. A red→green test
  edit lacking the (1)+(2) justification is a **P0/P1** finding. This is the
  **SEMANTIC** half of the discipline; the **mechanical** signal that an unjustified
  test-suppression marker entered the diff is **G70** (the G8-deferral analogue for
  tests — same diff-scan posture as G8/G21; see its own L2 row below for the marker
  set + the `[Test-Change]` tag form). G70 flags + requires the justification, G1 judges whether the
  rewrite is legitimate — together they ensure the ability to change a test is
  **preserved but verified** (a justified change passes normally). Doctrine:
  [test-strategy.md](../process/test-strategy.md) §8.
- **Definition-of-Done.** A box is "done" only when it satisfies the ConvertIA DoD
  (authored in [build-loop.md](../process/build-loop.md), P0.6): (a) spec-`§`
  referenced or marked tooling-only; (b) spec synced in the same commit; (c) tests
  at the highest sensible level green; (d) hard gates green with **no** `--no-verify`;
  (e) dual-review done + trailer present; (f) inline `[Build-Session-Entscheidung]`
  tags at non-spec choice sites; (g) `engines.lock` + SBOM row if a new engine was
  staged; (h) §0.11 + security-concept §5 row if a new threat class was introduced.
- **Skipped only** for: (a) check-off commits with no code/config diff; (b)
  `[!extern]` boxes (nothing built).
- *Blocks:* the build commit (self-gate; the trailer is also checked at pre-push by
  G12).

## 2. L1 — pre-commit (cheap, < 10 s, parallel)

> *(Reading note: gate IDs are stable, so a gate appears at its **enforcement plane**,
> not in numeric order — the higher-numbered G47/G49/G51/G52 below sit in L1 because
> that is where they run, even though their IDs were assigned later. The `< 10 s`
> budget is now a **soft** target: `gitleaks` + the G47 jq parse + `actionlint`
> (shells out to `shellcheck`) + `typos` + `editorconfig-checker` + the
> diff-scoped fmt/clippy/tsc/eslint were all added since the budget was written, and
> `actionlint`+`shellcheck` or clippy-on-a-cold-changed-crate can each approach 10 s;
> the heavier additions are diff/glob-scoped to hold the budget.)*

| ID | Gate | Tool / mechanism | Blocks | Scope / fail-mode |
|---|---|---|---|---|
| **G2** | Secrets / credential scan *(mirror)* | **`gitleaks`** (pinned version) — entropy + curated ruleset covering cloud keys, GitHub PATs (`ghp_`/`github_pat_`), PEM/private-key blocks, generic high-entropy literals; **plus a committed `.gitleaks.toml` CUSTOM RULE for the minisign secret-key shape** (the `untrusted comment:` header co-occurring with a long base64 blob, and/or banning a `*.key` from being staged) — a minisign secret key is **NOT** a PEM block (no `-----BEGIN … PRIVATE KEY-----` envelope), so the default PEM rule **cannot** catch the project's single most damaging secret (`MINISIGN_SECRET_KEY`; `MINISIGN_PASSWORD` is the companion) and the custom rule is the real guard. **Current subcommands (gitleaks v8.19+ deprecated `protect`/`detect` in favour of `git`/`dir`/`stdin`** — invoking the deprecated form under §0's pin-and-verify discipline is a footgun once a pin bump removes it; **re-verify against the pinned version's `--help` on every bump). NB `--staged` is a FLAG on the `git` subcommand (`gitleaks git --staged`), NOT a separate subcommand — so a future bump must not regress to the deprecated `gitleaks protect --staged` form. FOUR legs:** L1 = **`gitleaks git --staged`** (pre-commit snapshot); **L2 = `gitleaks git` over the unpushed range `@{u}..HEAD`** (the last LOCAL catch before a secret goes public — a secret introduced in an earlier unpushed amend/rebase-squash/out-of-hook commit never appeared in a single `--staged` snapshot L1 saw, so without L2 the first catch is L4 = already public; **this L2 leg is EXCLUDED from the docs-only fastpath skip — a secret in a `.md` counts**; **when `@{u}` is unset (no upstream), the L2 leg follows the same fastpath fallback chain `@{u}` → `origin/<branch>` → `origin/main` → `origin/HEAD` and finally falls back to `gitleaks git HEAD`** so the first-push case still scans — with a G24 self-test of that first-push case); L4 = full-tree **`gitleaks dir`** (working tree); **release-tier (L4 nightly / L5)** = full-**history** **`gitleaks git`** over the commit log. **The full-history leg's CI job MUST `actions/checkout` with `fetch-depth: 0` (r7):** GitHub Actions checkouts default to `fetch-depth: 1` (shallow), which would silently make the full-history leg scan only the tip — a green-but-empty result of exactly the silent-no-op class §0 warns against (the only backstop for "a once-committed-then-removed secret stays live forever in a PUBLIC repo with no PR-review backstop"); a **G56/G49 workflow-lint sub-assertion** asserts the job running the full-history leg sets `fetch-depth: 0`. **The full-history scan runs on an explicit `on: schedule:` (daily/nightly) trigger distinct from the release trigger (r7)** so a secret committed-then-removed mid-phase is not invisible for months until the next release. Committed `.gitleaks.toml` allowlist + one-time baseline for test-fixture keys so the first history run doesn't flood. **Allowlist/baseline growth-lint (the suppression-ledger discipline applied uniformly — a real secret silently added to the allowlist would be un-scanned forever, the single highest-value cheat in an autonomous single-session loop, identical to the silent-`GO` cheat G12 was hardened against):** every `.gitleaks.toml` allowlist/baseline entry carries a **box-id + Dual-Review note + a content-fingerprint that ROTATES (re-requires justification) when the matched file changes** (so a suppression cannot silently outlive the bytes it was reasoned about — same fingerprint-rotation as the G29 Semgrep suppression ledger); **allowlist growth is a Co-Pilot review item**, and `.gitleaks.toml` + the baseline join the L(-1) security-critical-file set (P0.6). **Reconciled with the G24a corpus manifest** so a secret-shaped adversarial fixture is allowlisted ONLY if it is a provenance-vouched manifest entry — a G24 self-test asserts a secret-shaped string OUTSIDE the manifested corpus paths still FAILS G2 (the allowlist cannot be widened to cover un-manifested bytes). **CI uses the pinned, vendored, checksum-verified `gitleaks` BINARY directly (per §0), NOT the `gitleaks/gitleaks-action` Marketplace action** — since `gitleaks-action` v2.0.0 that action requires a keyed COMMERCIAL license for **ORGANIZATION-owned** repos (free only for a personal account), and ConvertIA is under the **Ne-IA org**, so a fill-pass reaching for the Marketplace action would break the EULA (verified against gitleaks-action licensing); the pinned-binary path also keeps the §0 pin-and-verify discipline. **Delivered P0.3.1 (as-built refinements):** the minisign rule catches the secret-key **base64 BLOB by its `RW` algo-prefix + a ≥180-char length floor** (KDF-independent — verified against a real minisign-0.12 key, both `RWQA…`/`RWRT…` variants; the length floor excludes the ~56-char PUBLIC key), which catches a leaked key VALUE even with no `untrusted comment:` header (an env-var leak); the L2 first-push fallback `gitleaks git` (no range) ≡ full-history `gitleaks git HEAD`; the `fetch-depth: 0` requirement is enforced at RUNTIME (`run-gitleaks --history` refuses a shallow checkout — stronger than a static YAML lint); the growth-guard (`check-gitleaks-allowlist`) is a **path-freeze** (the allowlist `paths` == a frozen set in the L(-1) check, value-level `regexes`/`stopwords` blankets forbidden, baseline capped) since the allowlist is path-based — the per-matched-file content-fingerprint rotation + the G24a-corpus-manifest reconciliation are deferred to G24a / a value-allowlist; the self-test already asserts a secret OUTSIDE the allowlisted fixture path still FAILS | commit / push (range leg: L2; history leg: release-tier) | always-on (no glob); fail-closed |
| **G3** | Format check *(mirror)* | `cargo fmt --check` + `prettier --check` (TS/CSS/JSON; check-only, no auto-write) — Biome's linter, if wanted, belongs in G5, not here. **Delivered (P0.4.1):** the Rust leg — `rustfmt.toml` (frozen by `scripts/check-rust-lint-contract`) + the live `cargo fmt --check` (target-absent until the P1 crates); the prettier leg is P0.4.7 | commit | by file glob; fail-closed |
| **G4** | Lint — Rust *(mirror)* | `cargo clippy -D warnings` — **plus the no-panic-sloppiness policy** (`clippy::unwrap_used` / `expect_used` / `panic` at **deny** in production crates, `+ indexing_slicing` for `crate::detect`; allow-listed in `#[cfg(test)]`, with a `// PANIC:`-justified escape — same allow-listed-module pattern as G29's unsafe policy). The §1.2 detect path runs untrusted bytes **outside** the §2.12 boundary (principle 9), so a stray `.unwrap()` there is a **guaranteed in-core DoS (T5)** — the lint prevents the class, G48 fuzz finds the residue. Also set **`#[deny]` on non-exhaustive matches** (no `_ =>` catch-all) for the dispatch enums `FormatId`/`EngineProgram`/`PatentDisposition` + the error taxonomy, so adding a registry format/variant without updating every dispatch arm is a **compile error**, not a misroute caught late by a P5–P7 corpus test. **Plus `#![deny(clippy::arithmetic_side_effects)]` on the IPC command-handler module (r7 — allow-listed in `#[cfg(test)]`):** a `MAX_USIZE` integer IPC field is valid JSON that deserializes without serde error then can overflow in a post-deserialization preflight (a `width*height*bpp` buffer estimate) driving a bad allocation (T10) — the deny makes unchecked arithmetic on the WebView→Rust numeric boundary a compile warning before the G48 serde-target fuzz runs (the per-numeric-arg boundary-value `proptest` is in P0.4/G48 target (5)). **Delivered (P0.4.1):** `clippy.toml` (test-allowances + MSRV) + the `REQUIRED_ATTRS` deny-set contract in `scripts/check-rust-lint-contract` (the no-panic + `indexing_slicing`-on-detect + `arithmetic_side_effects`-on-IPC + `wildcard_enum_match_arm`-exhaustive-match denies), frozen now + asserted at each module root + run live (`clippy -D warnings`) once the P1 crates land | commit | changed-crate scope; fail-closed |
| **G5** | Lint — TS/React *(mirror)* | `eslint` (flat config) + `stylelint` (CSS). **Delivered (P0.4.7):** `scripts/check-ts-gate` — the TS half of the build-quality plane (Rust half = G3/G4 `check-rust-lint-contract`). Two tiers like P0.4.1: a **structural freeze** (live now) of the eslint project-local-rule contract (`@typescript-eslint/no-explicit-any` + the `fc.gen()`-shrink-wrapper ban, paired with G9 invariant (f)) + the tsconfig strict flags + the tool-wiring; a **target-absent live tier** (once a frontend `package.json` lands in P1: assert the eslint flat config carries the rules + run `eslint`/`stylelint`/`prettier --check`). Contract-only (no config files authored — they are §0.7-homed P1 frontend-build files, mirroring P0.4.2). `g24-ts-gate-contract.py` = 33 legs; canary 28→29; L1+L2+L4. G1 (volle Härte) GO, P2/P3 fixed (token-proximity eslint off-detection, extends-array+multi-level follow, symmetric fc.gen freeze guard). | commit | by glob; fail-closed |
| **G6** | Type-check (fast) *(mirror full at L2)* | `tsc --noEmit` on changed scope; (`cargo check` is implicit in clippy). **Delivered (P0.4.7):** the `tsc --noEmit` strict-flag contract (TSCONFIG_REQUIRED: `strict` + `noUncheckedIndexedAccess`/`noImplicitOverride`/`noFallthroughCasesInSwitch`) is frozen by `check-ts-gate`; live `tsc --noEmit` runs once the P1 toolchain lands. | commit | by glob; fail-closed |
| **G7** | Doc-consistency `plan-lint`/`spec-lint` *(mirror)* | custom stdlib-only script (see §6) `--quiet`. **Delivered P0.3.5** (`scripts/plan-lint`): 35 checks (9 format from `_format.md` + 26 doc from §6) over 991 plan boxes + the doc set; 22 active run, 13 target-absent skip until their P0.6/P1 source lands; the linter found+fixed real bootstrap doc-drifts (6 heading-skips, a malformed line-style ref, two broken README links); the G68 freshness leg (a doc still naming a removed/renamed gate/threat-class fails) is implemented over the gate-id + threat-class axes (fail-closed from P0); `g24-plan-lint.py` = 51 legs; L(-1)-caged; wired L1 (`--quiet`) | commit | on docs glob; any finding = exit 1 |
| **G8** | Deferral / dead-marker gate *(mirror)* | diff-based scan for `TODO`/`FIXME`/`unimplemented!`/`todo!`/`unreachable!`/`dbg!`/`println!`/`console.log`/`": any"`/`as any`/inline `style=`/`style:`/"stub"/"placeholder"/"phase 2"/`compile_error!` **plus the broadened semantic-deferral vocabulary a prior-project audit (which found 308 untracked gaps) proved escapes a `TODO`-only scan** — "later"/"for now"/"temporary"/"not yet"/"will add"/"comes in P<n>"/"deferred to"/"once <phase>"/**"currently absent"** (the last added in r5 so a leftover "(build-loop.md, currently absent)"-style annotation fails G8 once the named procedure is authored) — exactly the phrasings a phase-sequenced autonomous loop writes instead of a literal `TODO`) — in **new** production lines lacking a **box-id or `[Build-Session-Entscheidung]`** suppressing marker within ±6 lines (the `any`/inline-CSS markers encode the CLAUDE.md hard rules; a correctly-placed `[Build-Session-Entscheidung]` tag is a documented choice site, not a deferral, so it suppresses). **The `[!extern]` suppressor is restricted to `.md` files under `docs/plan/` (r7 nit — a bare `[!extern]` in a `.rs`/`.ts` comment could otherwise suppress a "currently absent"-class deferral in production code): in production code ONLY a `[Build-Session-Entscheidung: box-id]` tag suppresses, never a bare `[!extern]`; a G24 negative self-test plants `[!extern]` in a `.rs` comment beside a deferral marker and asserts it STILL FAILS G8** | commit | new-marker only; **fail-open** if no diff base (mirror at L4 is **fail-closed**, full-tree, no diff-base excuse). **Delivered P0.3.4** (`scripts/check-deferral`): scope = PRODUCTION code only (`src/`, `src-tauri/src/` + the §0.7 outliers `build.rs`/`index.html`/`vite.config`, minus tests/fuzz) so the scanner never flags its own marker list / the spec anti-pattern catalogues; HARD markers match anywhere; the SOFT phrasings match ONLY comment text via a **stateful, string-aware** extractor (a `/* */` body line with no leading `*` IS scanned — G1 P1 fix; a `//` inside a `"…"` URL is NOT a comment — G1 P2 fix; an unbalanced quote before a `//` — a regex `/don't/` or an unterminated string — does NOT swallow the comment, so the deferral is still caught — G1 re-review P1 fix; a `placeholder=` attribute is not flagged); `[Build-Session-Entscheidung]` ±6 lines suppresses, `[!extern]` only in `docs/plan/*.md`; `--diff` (L1) scans the STAGED blob; `--full` (L2+L4); target-absent until P1; `g24-deferral.py` = 40 legs |
| **G9** | Repo-invariant grep gate *(mirror)* | a **committed, non-empty** invariant list (`scripts/repo-invariants.sh` / `g9.toml`) — NOT a placeholder — with a **G24 positive+negative self-test**. Initial committed invariants: **(a)** no hardcoded colour outside `design/tokens.css` (CSS+TS+TSX — the CLAUDE.md no-inline-CSS rule + the §5.5 design-token home, mirroring the `no-hex-outside-primitives.sh` pattern); **(b)** no `std::process::Command::new` outside `crate::isolation` (belt-and-suspenders to the G29 Semgrep rule (c)); **(c)** no `unsafe impl Send/Sync` outside the allow-listed FFI module; **(d)** no raw `127.0.0.1`/`localhost` literal outside `#[cfg(test)]`; **(e)** no `cargo vet update` / `cargo vet sync` / `cargo vet import` invocation in any `.github/workflows/*.yml` or `scripts/` file (only a Co-Pilot-reviewed `imports.lock` edit may change the cargo-vet trust set — a CI/script auto-refresh of the committed trust DB is banned, pairing with G18a's `imports.lock`-in-the-diff check; a G24 positive+negative self-test plants a workflow line invoking `cargo vet update` and asserts it FAILS); **(f)** no `fc.gen(` outside the approved shrink-wrapper pattern in `*.test.ts` (the §P0.5 "ban `fc.gen()` without a shrink wrapper" rule was a prose convention with no enforcing gate — valid TS that passes ESLint/tsc/G5 — so this G9 grep invariant, or an equivalent project-local ESLint rule, makes it machine-enforced; G24 positive+negative self-tested). **The "LibreOffice carve-out" is defined PRECISELY here, not left open (an undefined carve-out fails open or hides an untested allow-list):** the exception is scoped to **bundled third-party config files inside the LibreOffice program tree** (which may legitimately reference a loopback UNO endpoint) — those paths are EXCLUDED from the G9 scan glob via the **concrete pinned platform-specific exclusion globs `bundle/LibreOffice/**`, `bundle/LibreOffice.app/**`, `bundle/libreoffice/**`** (named here so the carve-out can't be authored too broad/narrow), so **first-party Rust/TS production code remains localhost-banned with NO exception** (the G29 no-`--accept` Semgrep rule already prevents ConvertIA's own spawn code from opening the UNO loopback socket, so first-party code needs no localhost literal at all). A first-party `127.0.0.1`/`localhost` outside `#[cfg(test)]` fails; a literal inside the excluded bundled-LibreOffice config tree does not. A **G24 positive+negative self-test** machine-tests the carve-out itself (a planted first-party localhost literal FAILS; a literal under the excluded LibreOffice config path passes). The structural CSP/capability invariant is its own gate **G47** (it parses, not greps). **Delivered P0.3.10** (`scripts/check-repo-invariants`, stdlib): all six invariants authored as a committed non-empty data set — **(e) is LIVE now** over the existing `.github/workflows/*.yml` + `scripts/` (zero real mutator-invocations today); **(a)/(b)/(c)/(d)/(f) are target-absent** (no `design/`/`src/`/`src-tauri/`/`bundle/` yet) — patterns + carve-outs authored + self-tested, fail-closed the moment their source lands in P1. The **(e) prose-vs-invocation discriminator is structural per file type** (the load-bearing design): YAML strips only `#` comments and KEEPS `run:`/scalar values (a `run: cargo vet update` step fails; a `# …` comment does not); Python/TOML/shell blank `#` comments AND every string literal (`'`/`"`/`'''`/`"""`), so the gate's own pattern definition + docstring, this gate's self-test fixtures, and other gates' prose descriptions of the rule (all string/comment-resident) yield ZERO hits when scanned live — a self-test reads the gate's OWN source AND the self-test's OWN source and asserts 0 hits; the one residual (a Python `os.system("cargo vet update")` string) is accepted as covered downstream by **G18a**'s `imports.lock`-in-the-diff check (any real trust-set mutation reddens the push as an `imports.lock` drift). **(d)** blanks COMMENTS but KEEPS strings (a `"http://localhost"` literal is caught, a `// mentions localhost` comment is not — the `//`-in-URL is correctly not a comment) and exempts `#[cfg(test)]` blocks via a string-aware brace match robust to `println!("{}")` format braces; the LibreOffice carve-out is the three pinned bundle globs. **(b)** matches only the QUALIFIED spawn forms (`(std\|tokio)::process::Command::new`), NOT a bare `Command::new` (a clap CLI-builder also writes that — the bare imported form is **G29**'s import-resolving job, this grep is the cheap belt-and-suspenders net). **Carve-out paths** [Build-Session-Entscheidung: P0.3.10]: (b) any `…/isolation(/\|.rs)` segment, (c) any `…/ffi(/\|.rs)` segment, (f) the pinned `FC_WRAPPER_PATHS` — confirmed when the real P1 code lands (a carve-out change is itself a reviewed L(-1) edit). `g24-repo-invariants.py` = 112 legs; canary 21/21. Wired L1 (pre-commit) + L4 (`gate-tooling`). `check-repo-invariants` + its self-test are L(-1) (`scripts/check-*` / `scripts/gate-selftests/**`, already caged). **G1 review (volle Härte) — 4 NOGO rounds → fixed → GO.** *Round 1* — the security-skeptic found a **P0** in the LIVE (e) invariant: a real `cargo vet update` SPLIT across physical lines (a standard YAML folded `run: >-` / a plain multiline scalar / a shell `\`-line-continuation) folds into the executable command, yet the per-physical-line scan saw nothing — and the claimed **G18a** backstop is itself target-absent in P0, so there was NO compensating control. Plus four converged **P1**s in the (then target-absent) invariants, all fixed (not deferred): (d) `#[cfg(not(test))]` was mistaken for a test gate (a `test` inside `not(...)` no longer exempts); (d) a non-block `#[cfg(test)]` leaked `pending` into the next production block (cleared on a depth-0 `;`); (a)/(c) scanned RAW text and false-positived on `href="#fff"`/`url(#…)` fragment-refs, comment PR-refs/SHAs, and a `<T: Send>` generic BOUND — now (a)/(b)/(c)/(f) blank comments (keep strings) and (c) anchors `Send`/`Sync` to the implemented-trait position; shell `.sh` is a command-lang (string-wrapped `eval` caught); (f) scope broadened to `.test.tsx`/`.spec.tsx`/`__tests__/`. *Round 2* — the round-1 (e) fix (span any newline + per-line comment strip) introduced **two NEW defects**, both fixed: a **P0** re-bypass (a `#` on a continuation line of an OPEN quote was mis-stripped, truncating a multiline-quoted scalar and dropping a trailing `; cargo vet update`) and a **P1** cross-line FALSE-join (a blanked comment line / a same-indent sibling let the matcher bridge `cargo vet` + a later standalone `update`). *Round 3* — the round-2 rewrite introduced two FALSE-POSITIVES (both fixed): a **P1** where a word-internal apostrophe (an English contraction `it's`/`don't` in a scalar/word) opened a phantom run-to-EOF quote that kept a downstream prose `# … cargo vet … …` comment (a common CI edit), and a sibling false-join where shell indented lines were folded as if YAML (the indent-fold is now **YAML-only** (`fold_indent`), shell not being indentation-sensitive — only a trailing `\` continues). *Round 4* — the round-3 apostrophe guard was found to **re-open a P0 false-NEGATIVE**: in SHELL a quote opens word-adjacent (`X=a"b # c"` quotes `b # c`, so the `#` is DATA and a trailing `; cargo vet update` after the closing quote EXECUTES), but the "not after a word char" guard suppressed the opener and stripped the real invocation as a comment. Fixed by making the opener rule **language-specific**: in SHELL `'`/`"` ALWAYS open (a word-internal `'` in shell code is itself an unbalanced-quote syntax error, so over-flagging it blocks no legit push); in YAML they open only at a scalar start (≈ not after a word char), which both fixes the contraction FP and is correct because a YAML ` #` after a plain scalar is a real comment. Final (e) design: comment stripping is **quote-aware across physical lines** (`_strip_command_comments`, language-specific opener, fail-safe on an unbalanced quote) and matching is **per LOGICAL line** (`_logical_lines` folds only a genuine shell `\`-continuation (an odd trailing-backslash count) or — YAML-only — a more-indented scalar body), never an arbitrary newline. So a real invocation (single-line, string-wrapped, folded, or word-adjacent-quoted) is caught while contractions, unrelated/comment lines, and cosmetically-indented shell lines never false-join. (P2/P3 recorded: YAML narrow-to-`run:`-keys, literal-`|` block over-join, a `\`+trailing-whitespace over-fold, Rust raw/byte-string brace handling, deeper nested generics, the absurd `cfg(not(not(test)))` double-negation — all fail-safe over-flags, refinements when the real source lands in P1) | commit | by glob; fail-closed |
| **G10** | Fastpath self-tests | `test-*-fastpath-pattern` smoke tests (script naming convention documented in P0.2; → scripts authored in P0.2) | commit | only when a fastpath detector is edited; positive **and** negative self-tests required |
| **G47** | WebView CSP + capability structural lint *(mirror — L1 cheap, L4 structural)* | parse `tauri.conf.json` `app.security.csp` + `src-tauri/capabilities/*.json` with `jq`/`serde_json` (**not** regex) and assert **structural equality per directive against the LITERAL locked §0.10 CSP object** (00-architecture.md §0.10 is the single source — the gate diffs the whole shape, not a deny-list of a few directives). The **permitted non-`'self'` tokens are recorded explicitly per directive** so a future injected token has an explicit golden to diff against: `connect-src` ALLOW-list = `{ipc:, http://ipc.localhost}` (any `blob:`/`data:`/`https://…` fails); `script-src` = `'self'` only (**no remote origin, no `unsafe-eval`**); `img-src` non-`'self'` = `{data:, blob:}` (**NO `asset:`**, deliberate v1 prohibition); `media-src` non-`'self'` = `{blob:}` (**NO `asset:`**); `style-src` non-`'self'` = `{'unsafe-inline'}` (Tailwind needs; **no `unsafe-eval`**); `font-src = 'self'`; `object-src = 'none'`, `form-action = 'self'`, `base-uri = 'self'`, `frame-src = 'none'`, **`frame-ancestors 'none'`** (added to both the §0.10 block and the asserted set this round), `webrtc = 'block'` (already in the §0.10 golden — enforced, not a follow-up). **`frame-ancestors` honesty residual (r6, documentation-only):** like `webrtc 'block'` (which the doc already notes WKWebView/WebKitGTK ignore), `frame-ancestors` is a CSP directive **not universally honoured by WebKit** — its primary value is on WebView2 (Windows); the structural offline guarantee on macOS/Linux rests on the CSP **host restrictions** (the closed `connect-src`/`script-src` allow-list), not on `frame-ancestors`. Also FAIL on: any `fs:`/`http:`/`shell:allow-execute`/`opener:*`/`dialog:` grant in a capability file (and assert `opener:` permission scopes are denied by **PREFIX** — `opener:allow-open-url` etc. — not only the `opener:*` glob, so a specific-permission grant can't slip the glob check); any `updater`/bundle-updater block or updater pubkey in the conf; **`app.security.dangerousRemoteDomainIpcAccess` absent/empty** (a Tauri v2 `app.security` knob — NOT `app.windows[]` — that, if set, lets ANY remote origin invoke registered IPC commands directly, collapsing the T2 CSP boundary); **and `app.security.assetProtocol.enable` being present/true** (if a dev enables the asset protocol without adding `asset:` to the CSP, the WebView can still read bundled files via `asset://localhost` despite the CSP host token being blocked — so this must be asserted absent/false); **and `bundle.createUpdaterArtifacts` absent/false** (a Tauri v2 BUNDLE-side knob INDEPENDENT of `plugins.updater` — with it `true`/`"v1Compatible"`, `tauri build` emits updater `.sig` + manifest artifacts, a parallel signing surface OUTSIDE the minisign anchor, contradicting the §7.6 no-update posture, even with no `plugins.updater` block; same cheap jq/`serde_json` check as the three §0.10 hardening keys). **DNS-prefetch side-channel (spec §0.10 names DNS-prefetch as a CSP-uncovered leak):** the gate also asserts the `index.html` shell carries `<meta http-equiv="x-dns-prefetch-control" content="off">` — WKWebView/WebKitGTK honour this meta tag (unlike `webrtc 'block'`, which they ignore), so for a zero-egress product it removes a DNS leak on e.g. the About→Releases link hover; the `index.html` shell is authored in P1, but the asserted-present policy is G47-owned here. **Three by-construction release-hardening keys are ALSO asserted (spec §0.10, real Tauri v2 knobs that widen T2 if flipped):** **`app.withGlobalTauri` absent/`false`** (when `true` it injects the FULL Tauri API onto `window.__TAURI__`, so any XSS / supply-chained frontend dep can invoke IPC commands directly from JS, not only through the app's own React code); **`app.security.dangerousDisableAssetCspModification` absent / `false` / empty-array** (it suppresses Tauri's built-in CSP modification for the listed directives, which could silently strip the `script-src 'self'`/`connect-src` enforcement from the injected CSP even when this §0.10 object declares them — defeating the offline CSP proof this gate asserts); and **`devtools` NOT enabled in the release/bundle profile** (`app.windows[].devtools` / the bundle devtools feature — devtools open in a shipped build is an inspection/injection surface on the untrusted WebView). All three are cheap structural checks in the same jq/`serde_json` parse. **Deep-link / URL-scheme absence + local-`url` assertion (r7 — §7.8.2 claims "no URL scheme" but NO gate verified it, and G47 checked capabilities/CSP not deep-link registration):** assert `tauri.conf.json` has **no `plugins.deep-link` block** and **no custom URL-scheme registration** in any `Info.plist` / `.desktop` / `.reg` under `src-tauri/` (the macOS deep-link-from-external-process vector + the §7.8.2 negative); and assert **`app.windows[].url` resolves to a LOCAL/bundled URI** (`index.html` or a `tauri://localhost/…`), **failing any `http(s)` value** — closing the "the loop edits the startup `url` to a dev server" regression. Two-line jq additions in the same parse. Verifies the §7.6.1 updater-absence claim structurally; the concrete instantiation of the G9 invariant placeholder for T2/T2a/T2c/T9a. **Delivered P0.3.2** (`scripts/check-csp-capabilities`, stdlib `json` parse): `LOCKED_CSP` (13 directives) per-directive structural equality (widened-token / missing / unexpected-extra all fail) + every dangerous-knob / `url`-http(s) / dns-prefetch-meta (matched as one element, either attr order) / URL-scheme assertion above. The capability check is an **ALLOW-LIST** — a permission must be `core:`/`log:`/`store:`, so `fs:`/`http:`/`opener:`/`dialog:`/`updater:` AND every `shell:` grant (execute/spawn/**default**/**sidecar** — §3.3.3) AND any unknown-dangerous permission fail by exclusion; a capability **`remote`/`urls`** grant (v2 remote-origin IPC) is denied; and **inline** `app.security.capabilities[]` / `app.capabilities[]` object entries are evaluated too, not only `capabilities/*.json`. Target-absent ⇒ skip-with-warning (fail-open, P0 bootstrap), **fail-closed once `tauri.conf.json` lands (P1)**; `g24-csp-capabilities.py` = 41 planted-positive legs; wired L1 + L4. **G1 review (volle Härte) caught 3 real-v2 P1 widenings now fixed:** the shell deny was `execute`-only (missed spawn/sidecar/default) → broadened to the allow-list; `remote.urls` was uninspected → now denied; inline conf capabilities were untraversed → now evaluated. The release-profile `devtools` is asserted at the conf `app.windows[].devtools` level — the Cargo build-feature form is a P0.4/P1 build-profile concern outside this conf-parse gate | commit | always when the conf/capabilities glob matches; fail-closed |
| **G49** | Workflow lint (fast) *(mirror)* | **`actionlint`** — YAML/expression/shell lint of `.github/workflows/*` (catches syntax, bad `${{ }}` expressions, shellcheck issues in `run:` steps) | commit | on `.github/` glob; fail-closed |
| **G51** | Prose typo gate *(mirror)* | **`typos`** (`typos-cli`) — curated-list typo finder (not a dictionary spell-checker; near-zero false positives on Rust/TS identifiers) over public-facing prose: `SECURITY.md`/`PRIVACY.md`/`TRADEMARK.md`, **`docs/security/security-concept.md` + `docs/security/build-gates.md`** (both are referenced from `SECURITY.md` and carry the `minisign` verify recipe + gate descriptions), the verify-your-hash recipe, the user-facing error/string catalog — a typo in the security policy or the `minisign` verify recipe is a trust-damaging defect G8/G21 (markers, not prose) miss. **Delivered P0.3.11** (`scripts/run-typos` + pinned `typos` 1.47.2 in `gate-tools.toml`): the wrapper runs typos with the committed `.typos.toml` allowlist over the public-facing-prose `SCOPE` filtered to existing files (live now = the two security docs; `SECURITY.md`/`PRIVACY.md`/`TRADEMARK.md` + the user-facing string catalog auto-join as they land in P0.6/P1). `.typos.toml` whole-word-allowlists `mis` + `unparseable` (the valid `mis-` prefix + the alt-spelling), each a deliberate suppression-ledger entry; `.typos.toml` is L(-1). **G1 review P2 (honest ledger):** the whole-word `mis` entry DOES mask a standalone `mis`→`miss` typo — a prefix-only `extend-ignore-re` was tried but FALSE-POSITIVES on the prose scope, which legitimately discusses the bare token "mis" (these notes do) and would block doc commits; the whole-word form is accepted because such a typo in technical security prose is implausible AND the G1 dual-review reads the prose (the semantic backstop). Narrow to a `[type.md]` scope if app source ever enters the typos scope. `g24-typos.py` proves real misspellings ARE flagged, the allowlist (not typos) is what suppresses `mis`, and the live scope is clean. typos pin: no checksums.txt → per-archive sha256 computed + corroborated by Scoop (identical win sha) + Homebrew (version 1.47.2). Wired L1 + L4 | commit | on docs/strings glob; fail-closed |
| **G52** | Cross-platform EOL/charset hygiene *(mirror)* | committed **`.editorconfig`** + **`editorconfig-checker`** — EOL/charset/final-newline guard for `.toml`/`.yaml`/`.md`/shell scripts (G3 covers only `cargo fmt` + `prettier`-managed TS/CSS/JSON, leaving these unguarded; a CRLF drift in a `.sh` gate-script/hook is a real Windows footgun). **Delivered P0.3.11** (`scripts/check-editorconfig` + pinned `editorconfig-checker` 3.7.0): the committed `.editorconfig` sets `charset=utf-8`/`end_of_line=lf`/`insert_final_newline`/`trim_trailing_whitespace` (with `[*.md]` trailing-ws exempted for Markdown hard-breaks; indentation deliberately NOT asserted — not in the G52 contract), paired with a committed **`.gitattributes` (`* text=auto eol=lf`)** so `core.autocrlf` on a Windows checkout cannot drift a tracked file to CRLF (without it the local L1 ec run would false-flag a fresh-clone CRLF worktree). The gate caught a real footgun on its first run: with `core.autocrlf=true` this Windows WORKTREE held `security-concept.md` + the gitleaks fixture as CRLF (a past checkout had converted the LF-committed blobs) — exactly the drift G52 targets; without `.gitattributes` the local L1 ec run would block every Windows commit. The committed blobs were already LF (no committed content changed for EOL); `.gitattributes eol=lf` keeps the worktree LF on every future checkout, and the worktree was normalised to LF so the current ec run passes. `editorconfig-checker` reads `.editorconfig`, scans from root, respects `.gitignore` (skips `.gate-tools/`) + auto-skips binaries. `.editorconfig` is L(-1). `g24-editorconfig.py` proves CRLF / trailing-ws / missing-final-newline are each caught (runtime temp fixtures — a CRLF file can't be committed under `eol=lf`) + the real tree is clean. ec pin: official checksums.txt verified + Homebrew 3.7.0 (version); the `editorconfig-checker-*` archives (clean binary name) are used, not the platform-named `ec-*`. Wired L1 + L4 | commit | by glob; fail-closed |

## 3. L3 — commit-msg

> *(Ordering note — the catalogue is grouped by GIT TRIGGER, not by plane number:
> §2 (L1) and §3 (L3) are the two **`git commit`-time** planes (grouped here), and
> §4 (L2) is the **`git push`-time** plane (follows). So L3 is documented right after
> L1 because both fire at commit time; L2 fires at push time. The defense-in-depth
> plane list in [security-concept.md §3](security-concept.md#3-defense-in-depth--the-enforcement-planes)
> remains in strict L0→L5 order.)*

| ID | Gate | Tool / mechanism | Blocks | Scope / fail-mode |
|---|---|---|---|---|
| **G11** | Conventional-commit format | regex `^(feat\|fix\|chore\|docs\|refactor\|test\|perf\|ci\|build)(\([a-z0-9._-]+\))?: .*\S` (first line; the description must hold a non-whitespace char); merge/revert/fixup/squash/amend exempt. Solo-on-`main` rollback convention: `chore(scope): roll back — <reason>` (no `revert` type for build-session commits). **Delivered P0.3.3** (`scripts/check-commit-msg`): wires the commit-msg hook (L3) for the first time; `SUBJECT_RE` matches this row byte-for-byte (`[a-z0-9._-]` scope, no `!`); merge/revert/fixup/squash/amend exempt; lefthook passes the msg-file as `{1}`; `g24-commit-msg.py` = 29 legs | commit | always; fail-closed |

> **G12 is an L2 (pre-push) gate — documented in §4, not here.** It was previously
> listed in this §3 block for ID locality; it has been **moved to §4 (L2)** because
> that is the plane it fires at (a range-scoped conditional over the whole push range).
> Its ID is unchanged.

## 4. L2 — pre-push (heavier, < 3 min, parallel; cheap-commit fastpath)

| ID | Gate | Tool / mechanism | Blocks | Scope / fail-mode |
|---|---|---|---|---|
| **G12** | Dual-review trailer present + well-formed *(L2 — moved here from §3 in P0 review r3)* | the build commit body carries a trailer matching `^Dual-Review: opus=(GO\|NOGO) sonnet=(GO\|NOGO)$`; skipped for check-off/`[!extern]`. Runs at **pre-push** (not commit-msg) because it needs **conditional skip logic over the whole push range** — the bodies are read via `git log --format=%B <old>..<new>` and the check-off/`[!extern]` commits in the range are excluded. **Skip-regex parity (mandatory):** the exact check-off/`[!extern]` subject-pattern regex is a **single committed shared script** that BOTH the L2 pre-push hook AND the L4 CI mirror call (a drift between the German/English commit-subject pattern the Build-Loop produces and the regex would false-pass or false-fail valid check-off commits) — extended into the G54 two-plane parity assertion (any conditional-skip logic must be byte-identical across both planes, extracted to the shared script, mirroring the fastpath-commit pattern). **The check-off shape is a DOUBLE predicate** (pinned in build-loop.md so the loop emits exactly what the skip-regex recognises): subject matches **`chore(todo): .* (abgehakt|done)`** AND the diff is **markdown-only** (`git diff --name-only <commit>^..<commit>` has no non-`.md` path) — closing the subject-only vs diff-shape ambiguity; a **G54 planted-positive** asserts a check-off subject+md-only-diff skips while a build-commit subject or a non-md diff does not. **Findings-block-presence sub-check (turns the §4 "auditable smell" into a machine invariant):** G12 today checks only that the trailer is well-formed; security-concept §4 admits it "never checks that two live models actually ran", so the sole defence against a well-formed-but-unbacked `GO` is a PROSE rule in build-loop.md — and in a single-session autonomous loop, silently emitting `GO` without calling the API is the highest-value cheat. So G12 additionally asserts that **whenever the trailer says `opus=GO sonnet=GO`, the commit body carries each reviewer's NON-EMPTY findings block** (the §1 verbatim per-reviewer findings + convergence/divergence record): a **both-GO + empty/absent findings block on a non-trivial (non-check-off, non-`[!extern]`) diff FAILS the gate**, it is no longer merely a smell. (This does not make G1 a security control — a determined cheat can fabricate a findings block — but it raises the cost from "emit one trailer line" to "fabricate a plausible per-reviewer finding set", and a fabricated block remains an auditable Co-Pilot spot-audit target.) **Delivered P0.3.3** (`scripts/check-dual-review`): the SINGLE shared script both planes invoke — L2 pre-push (`resolve_base()..HEAD`) + the L4 mirror in the `workflow-security` CI job (`--base ${{ github.event.before }}` via env, push-only, `fetch-depth: 0`), so the check-off skip-regex (the `chore(todo): … (abgehakt|done)` + md-only-diff double predicate) lives in ONE place and cannot drift across planes (the mandated parity is structural). Findings-block-presence is a body-marker presence heuristic over the BODY only (the SUBJECT is excluded — a box-id like `(P0.3.3)` must not satisfy the `P[0-3]` marker; raises the cost of a fabricated GO, not a strict per-reviewer parse). An absent/unresolvable full-hex `--base` (a force-pushed `github.event.before`) routes to the HEAD-tip-only check via a `^{commit}` existence peel. `g24-dual-review.py` = 13 legs (incl. the double-predicate: md-only exempt, a `.rs` file / empty file-list NOT; the box-id-in-subject regression guard; the absent-base tip-only fallback). The explicit gate-planes parity meta-assertion is unnecessary given the single-script design. **NB the "check-off/`[!extern]`" wording above:** `[!extern]` is a plan-BOX status marker that yields NO commit (the loop skips + collects it, build-loop.md P0.6), so there is no `[!extern]` commit-subject to skip — only the check-off double-predicate is a real commit-level exemption | push | conditional; fail-closed |
| **G13** | Full type-check *(mirror)* | `tsc --noEmit` whole project. **Delivered (P0.4.7):** the whole-project leg is `check-ts-gate --full` (L2 pre-push + L4 `gate-tooling`); target-absent until the P1 `package.json`/`tsconfig.json` land, then fail-closed. | push | always; fail-closed |
| **G14** | Full lint *(mirror)* | `clippy --all-targets --all-features -D warnings` (incl. the G4 no-panic-sloppiness `unwrap_used`/`expect_used`/`panic`/`indexing_slicing` deny set + the exhaustive-match deny) + `eslint` whole tree. **Delivered (P0.4.1):** the Rust whole-tree leg is `scripts/check-rust-lint-contract --full` (L2 pre-push + L4 CI; target-absent until the P1 crates); the eslint leg is P0.4.7 | push | always; fail-closed |
| **G15** | Unit + integration tests *(mirror)* | `cargo test` (incl. real-file round-trips) + `vitest run`. **Atomicity-under-interruption (§6.4.2):** the kill is injected **specifically in the post-`sync_all()`-pre-`rename` window** (a `#[cfg(test)]` fence in `crate::fs_guard::atomic_publish`, all 3 OS) so it exercises the §2.1.3 two-state invariant at the critical boundary, not an uninteresting pre-sync kill. **Scoped mutation-testing sub-leg (release-tier, owner-decidable required-vs-informational):** `cargo-mutants` over `crate::fs_guard` + `crate::detect` + `crate::outcome` (the no-harm/atomicity/no-misroute kernel) — line coverage proves a line executed, not that a test would CATCH a regression there. **Detection-layer KAT (known-answer test, §6.4.1 made machine-enforceable at L2 cost):** a committed `tests/detect-kat.toml` pins canonical files to their EXACT `FormatId` — one entry per ambiguous case (DOC vs XLS from the same OLE2 magic; detected-but-unsupported; uncertain) — read by the G15 unit test, so a toolchain bump (`quick-xml`) or a detect refactor that silently changes the result for an ambiguous signature is caught at **L2**, not only at L4 by the corpus | push (mutants: release-tier) | always; fail-closed |
| **G16** | Property + fuzz smoke *(mirror)* | property tests — **`proptest`** (Rust, macro-based shrinking, no manual `Shrink` impls — satisfies the §P0.5 "shrinking mandatory" rule) + **`fast-check`** (TS, custom `Arbitrary` must delegate to built-in shrinkers — **ban `fc.gen()` without a shrink wrapper**); plus a fast **deterministic** fuzz leg here (a saved-crash-corpus replay / `proptest` smoke over `crate::detect` — **not** an instrumented libFuzzer build, which is L4-only and Unix-nightly-only). **Determinism (property tests are non-deterministic by design):** a **pinned CI seed**, a **case-count floor** above the thin default 256 (the adversarial path/budget space is large), and a property failure is **NEVER retried to pass** (only infra/timeout flakiness is retryable, E2E-only — §P0.5 flaky policy). The coverage-guided `cargo-fuzz` harness is **G48** (in-core detector + `fs_guard` + CSV/TSV) + **G26** (full pass) | push | always; coverage-guided fuzz at L4 |
| **G17** | Dependency-vuln audit *(mirror)* | **`cargo audit`** (Rust) — **plain, NO `--locked` (r7 tool-fact fix):** `cargo audit` reads `Cargo.lock` BY DEFAULT and has NO `--locked` flag — `--locked` is a cargo BUILT-IN for `build`/`test`/`run`/`fetch`, **not** a `cargo audit` argument, so `cargo audit --locked` makes clap reject an unknown argument and the gate hard-fails spuriously on the pinned binary (the §0 footgun). The complete `cargo audit` flag set is `-c/--color`, `--db`, `-D/--deny`, `-f/--file`, `--ignore`, `--no-yanked`, `-n/--no-fetch`, `--stale`, `--target-arch`, `--target-os`, `-u/--url`, `-q/--quiet`, `--format`, `--json` (+ global `-v`) — re-verified against the pinned cargo-audit `--help` on every bump exactly as G2 mandates for gitleaks. So the check is **plain `cargo audit`**, with **`cargo audit --no-fetch`** for the offline/airgapped leg (check against the local/vendored DB, refresh decoupled warn-only per §0), and the existing `cargo audit --json .database.last-updated` for the release-tier staleness floor below (already correct). Lockfile-integrity is owned by G18a (`git diff --exit-code Cargo.lock`), not by a non-existent `cargo audit --locked`. **JS/npm leg = `osv-scanner` over `pnpm-lock.yaml`, NOT `pnpm audit` (corrected r6):** `pnpm audit` queries the npm registry advisory endpoint **online at gate time** (no real offline mode) — violating §0's "decouple the warn-only refresh from a fail-closed check against a local/vendored DB, honour an offline env flag" discipline — **and is broken on the entire pnpm 10.x line** (the npm registry retired the legacy audit endpoint → HTTP 410; the pinned `pnpm@10.13.1` per CLAUDE.md is affected, verified against the pnpm issue tracker), so it would fetch-live-or-fail-open in airgapped CI. `osv-scanner` natively parses `pnpm-lock.yaml` and runs **`--offline`/`--offline-vulnerabilities` against a local/vendored OSV DB** (the same offline-tolerant posture the Rust leg has, and the same tool family as G17b), with the refresh decoupled warn-only and an offline env flag honoured. **The npm advisory-DB staleness floor is asserted at the release tier mirroring G17's RustSec floor** (the OSV DB timestamp vs the committed `MAX_ADVISORY_DB_STALENESS`). **NB:** these cover only the Rust crate + npm graph — the bundled-engine CVE surface is **G17b**. **Advisory-DB staleness floor (release-tier — for a fully-offline app whose entire CVE story rides a vendored DB, a silently-stale DB makes "green = no CVEs" false confidence):** at the **release tier** assert the RustSec advisory DB's age via `cargo audit --json` `.database.last-updated` against a committed `MAX_ADVISORY_DB_STALENESS` (e.g. 7 days) and fail (or loudly warn) the release if exceeded. This is a post-refresh assertion over a committed/vendored timestamp, **not** a live lookup, so it stays offline-tolerant (the per-push leg keeps the refresh warn-only). **Delivered (P0.4.1):** the Rust `cargo audit` leg is wired into `scripts/check-rust-lint-contract` (PLAIN — no `--locked`; `--no-fetch` offline; skipped until cargo-audit's binary pin + the P1 lockfile land); the JS `osv-scanner` leg + the release-tier staleness floor are later boxes | push | always; check fail-closed, refresh fail-open; release-tier DB-staleness floor |
| **G17b** | Bundled-engine CVE awareness *(informational per-push; release-tier escalation)* | feed the **PURL-keyed** `engines.lock` components (see G35 — each row carries a `pkg:generic/<name>@<version>` PURL, a CPE where one exists) to **`osv-scanner`** (consumes OSV — indexes FFmpeg/poppler/LibreOffice/x265 advisories) **or** `grype` (consumes the G35 CycloneDX SBOM); both match advisories **by PURL/ecosystem, not by a bare `(name, version)` string**, so a PURL-less manifest would silently match **nothing** and a green report would be a green-but-**empty** report masquerading as "no known CVEs". A **planted-positive self-test** (a deliberately-old pin that MUST surface a known historical CVE) guards against that empty-report failure — and the planted-positive uses a known **historical CVE in an INTERNAL FFmpeg decoder** (the highest-CVE surface, below), so the CPE path is proven to surface decoder-level advisories, not just a top-level package CVE. **Coverage caveat stated honestly:** `pkg:generic` PURLs miss CVEs that only a CPE indexes, and the genuinely highest-CVE surface inside the GPL FFmpeg binary is its **enabled internal decoders/demuxers** — the spec deliberately ships a large legacy decode-only surface (§3.4.3 VC-1/MPEG-2/H.263/MPEG-4 Part 2, plus WMA §3.1), exactly the T1 untrusted-input attack surface where FFmpeg CVEs land, and these are NOT separate `engines.lock` rows. So the **FFmpeg row's CPE (`cpe:2.3:a:ffmpeg:ffmpeg:<ver>`, which indexes the bulk of A/V decoder CVEs) is MANDATORY, not "where one exists"**, and a CPE is likewise stated for **poppler / libheif / libde265 / libvips / LibreOffice** (the §6.1.3 `-decoders` assertion already enumerates the CVE-relevant decoder inventory). **Severity escalation rule (release-tier, NOT per-push):** a CVE with **CVSS ≥ 7 on an engine code path ConvertIA actively exercises for a §04 format** → the Build-Loop **escalates to Co-Pilot and blocks the next release** until bumped or triaged not-exercised (threshold stated in `SECURITY.md` so users know the effective turnaround). The per-push leg stays **non-blocking** (honours SSOT §3.8 "engine currency is best-effort, not a gate"); offline-tolerant (vendored DB, refresh warn-only). Two halves of the offline "audit-it-yourself" story: G17b (dated open-CVE report) + **G55** (embedded-SBOM auditable binary). **Advisory-DB staleness floor (release-tier, mirrors G17):** the OSV/grype DB used here also carries a last-updated timestamp — assert it against the same committed `MAX_ADVISORY_DB_STALENESS` at the release tier and fail/loudly-warn if exceeded; the **dated open-CVE report records the DB age it was generated against** (so a reader knows whether "no known CVEs" was checked against a current or a stale DB) | informational (release CVSS≥7 escalation blocks) | report only per-push; never `--no-verify` |
| **G18** | License + supply-chain policy *(mirror)* | `cargo deny check` with an **explicit** `deny.toml`: `[bans]` deny-list for **`tauri-plugin-updater`** + the common HTTP-client crates (`reqwest`/`ureq`/`hyper`/`isahc`/`curl`) — no socket-opening dep enters the core (T2/T9a); `[licenses]` GPL/AGPL **denied** for the Rust crate graph **and set fail-closed on unresolved/low-confidence detection** (symmetry with G36); `[advisories]` `yanked = "deny"`; `[sources]` populated allow-registry/allow-git list (the `sources` check is a no-op without it). **Two-Rust-advisory-scanner reconciliation (r7 — G17 `cargo audit` and G18 `cargo-deny [advisories]` both scan the SAME RustSec DB and the catalogue never stated which is authoritative on disagreement or that their ignore/exception sets stay in sync, the exact divergence class the design obsesses over elsewhere — cdxgen-not-cyclonedx-npm, G55↔G35):** **`cargo-deny [advisories]` is the AUTHORITATIVE advisory axis** (it already carries `[bans]`/`[licenses]`/`[sources]` here, so it is the natural home), and `cargo audit`'s role narrows to the **release-tier staleness floor** (`cargo audit --json .database.last-updated`, G17) — its advisory axis is redundant with `cargo-deny [advisories]`. **If both advisory axes are kept hot, a G9/plan-lint sub-check asserts no advisory is suppressed in one tool's ignore set but live in the other** (the `[advisories].ignore` list in `deny.toml` ↔ the `cargo audit --ignore`/`audit.toml` set must agree), so a CVE silenced in `cargo audit` but flagged by `cargo-deny` (or vice-versa) cannot create a false-green. *(name-based `[bans]` is the baseline T9a "opens no socket" control; the behavioural upgrade — `cargo-acl`/`cackle`, a per-crate `std::net`/`std::process` capability policy that catches a renamed/transitive network crate — is flagged as a §8 forward idea, Linux-only, policy-authoring-intensive.)* **Delivered P0.3.6:** `deny.toml` (cargo-deny v2 — 8 forbidden crates denied incl. `tauri-plugin-http`; allow-list-only licenses so GPL/AGPL/LGPL + low-confidence fail closed; `yanked=deny`; crates.io-only `[sources]`) + `scripts/check-supply-chain`, a stdlib structural growth-guard freezing the policy intent (incl. the `[advisories].ignore` <-> `cargo audit` reconciliation); the live `cargo deny check` is target-absent (skip-with-notice) until P1's `Cargo.lock`. | push | always; fail-closed |
| **G18a** | Lockfile integrity *(mirror)* | CI builds/tests/audits with `--locked` (Rust) and `pnpm install --frozen-lockfile`; a post-install `git diff --exit-code Cargo.lock pnpm-lock.yaml **imports.lock**` so a drifted lockfile **FAILS** rather than auto-resolving a different graph than the audited/SBOM'd one (§3.8 pin-everything) — **`imports.lock` is in the diff set** so a `cargo vet sync`/`update` that silently refreshed the committed cargo-vet trust DB mid-box (passes locally, drifts from the CI tree) FAILS on the same push; the trust set only changes via a Co-Pilot-reviewed `imports.lock` commit, never an auto-fetch. **Delivered P0.3.6:** the `--locked`/`--frozen-lockfile` build flags + the `git diff --exit-code Cargo.lock pnpm-lock.yaml imports.lock` step are authored as the P1 CI build-step slot (target-absent until the lockfiles exist); `imports.lock` is in the diff set. | push | always; fail-closed |
| **G19** | Generated-artifact drift *(mirror)* | regenerate Tauri→TS bindings (`tauri-specta` + `specta`) / CLI `--help` / asset manifest, then `git diff --exit-code`; + structural (parsed, not regex) non-empty sanity. *(The concrete codegen command — `cargo xtask codegen` or the project-specific invocation — + the generated file path are filled in P1; named so the gate cannot silently pass on a stale generated file via a wrong codegen invocation.)* **Delivered P0.3.9:** `scripts/check-generated-drift` — a stdlib framework (regen → `git diff --exit-code` → parsed non-empty validator per artifact); the `ARTIFACTS` registry is empty/target-absent until each producer registers its artifact in P1. Wired L2 + L4. | push | by glob; fail-closed |
| **G20** | `plan-lint`/`spec-lint` full *(mirror)* | the G7 script, verbose, all checks. **Delivered P0.3.5:** the same `scripts/plan-lint` (full, all 35 checks) wired at L2 pre-push AND the L4 `gate-tooling` ci.yml step — one shared script, fail-closed | push | always; fail-closed |
| **G21** | Deferral gate full *(mirror)* | the G8 scan. **Crisp fail-mode (P0 review r3):** deferral gates are diff-scoped at **L1** only (realtime; fail-open w/o base acceptable there). The **L2 pre-push leg AND every L4/clean-checkout mirror run FULL-TREE** (so "no diff base" is unreachable) and **fail CLOSED** — consistent with §0's "a gate that cannot resolve its input fails CLOSED in CI". A no-diff-base deferral scan on a push (the common autonomous-loop case on a fresh `main` early in P0/P1) therefore runs the full tree, not a skip. **Delivered P0.3.4:** the same `scripts/check-deferral --full` (see G8), wired at the L2 pre-push hook AND the L4 `gate-tooling` CI step — one shared scanner, fail-closed | push | full-tree at the L2/CI mirror; **fail-closed** |
| **G61** | Defaults-registry "no required choices" guard *(mirror — Lane-A, §6.7.1 step 4a)* | **adopted from the reserved range (was RESERVED-unassigned; now a control, so it becomes a row per the reserved-id discipline below).** An **engine-free `cargo run`/xtask Rust bin** that **GENERATES the §1.6 consolidated `OptionDecl.default` index** by parsing **every §04 `(source,target)` matrix + the registered `OptionDecl` set** and **FAILS the build if any §04-offered pair lacks a `default` for any of its declared options** (every declared option of every offered pair must carry a default) — the single machine-checkable home of the SSOT *v1 DoD* "no required choices" gate (spec §1.6 lines 727-740, §6.7.1 step 4a, §6.10 row 7). The **values** stay owned by §04; this is the assembled/verified index, not a second source of truth. Runs **every push** (Lane-A, engine-free, fast) so a pair/option shipping without a default reddens `main` and is fixed immediately. **Bootstrap annotation (mirror gate over app code absent in P0):** the §04 matrices + the `OptionDecl` registrations do not exist until P3+ — so G61 **fail-opens / skips-with-warning when no §04-offered pair is registered yet (before app code) and fail-closes as soon as the first §04 pair + its `OptionDecl` set land → activated as each phase registers declarations** (the identical bootstrap annotation G22/G23/G47 carry). Ships a **G24 positive+negative self-test** (delete a `default` from a declared option of an offered pair ⇒ MUST fail; a complete registry ⇒ MUST pass) registered in `scripts/gate-selftests/`. Distinct from G22 (format∈matrix∧fixture∧test) and the §6.4.3a corpus↔pair bijection (G22/G23, step 4) — this is the **defaults**-completeness axis those do not cover. Sibling of the §6.7.1 Lane-A bijection guard (step 4) | push | by glob (§04 matrices / `OptionDecl` registry); fail-closed (bootstrap fail-open before app code) |
| **G22** | Schema/membership parity *(mirror)* | "every supported format ∈ the §04 category format matrices ∧ has a fixture ∧ has a round-trip test" — the **§04 per-category format matrices** (`docs/spec/04-formats/`, the per-format detection/targets/engine/options home) are the authoritative artifact the `scripts/check-corpus-coverage.rs` bijection guard reads, NOT a separate "README support matrix" (no box authors a README table; the §04 matrices + the corpus manifest are the real artifacts — conflict order spec > security/process docs > plan). *(NB: v1 is **English-ONLY with no i18n runtime** — there is no locale-file key parity; the inverse "assert NO locale framework import + every `ui.ts` key non-empty English" is its own gate **G57**, not a parenthetical here.)* **Bootstrap annotation (mirror gate over app code absent in P0):** the format registry, the fixtures, and the §04 category matrices do not exist until P1 — so G22 **fail-opens / skips-with-warning when the registry/§04-matrix target is absent (before P1 app code) and fail-closes as soon as a registry entry / §04 matrix row exists → activated in P1** (the identical bootstrap annotation G47/G27/G33a/G57 carry, so the P0 green-L4 exit criterion is not blocked by a gate targeting absent code, and the gate is never silently fail-open once its target lands) | push | by glob; fail-closed (bootstrap fail-open in P0 only) |
| **G23** | "every X has a Y" completeness *(mirror)* | e.g. every `convert_*` command has a test (via `git ls-files`) — caveat: tracking-aware, stage partner file together. **Bootstrap annotation (mirror gate over app code absent in P0):** the `#[tauri::command]` handlers do not exist until P1 — so G23 **fail-opens / skips-with-warning when the `convert_*` target is absent (before P1 app code) and fail-closes as soon as a `convert_*` command exists → activated in P1** (the identical bootstrap annotation G47/G27/G33a/G57 carry, so the empty P0 tree neither wedges the exit criterion nor leaves the gate silently fail-open once its target lands) | push | by glob; fail-closed (bootstrap fail-open in P0 only) |
| **G68** | Doc-graph integrity & freshness *(mirror)* | the **GENERAL "no source-of-truth drifts from the doc graph" enforcement** — a single `plan-lint`-family stdlib-only script (a peer of G7/G20, run at the same L1/L2/L4 call sites) that treats the WHOLE canonical doc graph as one machine-checkable object. The **gates→`.md` consistency case is ONE instance, not the scope** — the general control catches a drift between ANY authoritative source and EVERY doc that references it. **Three legs.** **(1) REACHABILITY / no-orphan** — every **repo-tracked** `.md` under `docs/` (+ the root governance docs `CLAUDE.md`/`README.md`, and `CONTRIBUTING.md` once it lands in P1.42 — the "over the docs that EXIST" hedge means a not-yet-authored doc is simply not in scope, never a false-fail) is reachable by a link from its proper index (the spec category files from [`docs/spec/README.md`](../spec/README.md), the plan phase files from [`docs/plan/README.md`](../plan/README.md), the process/security docs from `CLAUDE.md §9` + each other), so a doc that exists but is linked from nowhere — an orphan a reader/loop never finds — **fails**; a NEW `.md` added without an index link **fails**. **(2) CROSS-DOC REFERENCE RESOLUTION** — every cross-doc reference across the full **repo-scoped** graph (`CLAUDE.md` ↔ SSOT ↔ `docs/spec/` ↔ `docs/security/` ↔ `docs/process/` ↔ `docs/plan/`) resolves to a real file/heading/anchor — a relative link to a moved/renamed/deleted doc, or a `§`/`Gnn`/box-id pointer into a doc that no longer carries it, **fails** (generalising check 2's spec-internal `§X.Y` resolution to the *inter-document* edges the existing checks do not span). **Scope note — the persistent-memory channel is NOT a G68 node:** the `~/.claude` memory channel (its `MEMORY.md` index + the memory files) lives OUTSIDE the repo and outside `git ls-files`, so a repo-scoped `plan-lint`-family gate cannot mechanically reach it; its correctness/population is audited **once, manually**, in the P0.1.7 documentation-wiring audit (a human check), NOT by this continuous gate — G68 names only nodes it can actually resolve, so the "every cross-doc reference resolves" claim stays sound. **(3) FRESHNESS — the load-bearing leg: a change to an authoritative source MUST be reflected in every doc that describes it, in the same commit.** When an authoritative source changes — a **gate** added/renamed/removed in this catalogue, an **enum** variant, a **path**/directory, a **version pin**, a **convention**, a **decision** — every `.md` that DESCRIBES it the old way is stale and **fails**. The gates→`.md` instance: a `Gnn` row added/renamed/removed here while a referencing `.md` (security-concept §5, a plan box, CLAUDE.md §7) still names the old shape **fails** (the reachability + resolution legs already catch a deleted `Gnn`; this leg catches the *described-the-old-way* drift the resolution leg cannot, by asserting the per-source fingerprint each describing doc was reasoned against still matches — the same content-fingerprint discipline the G29 suppression ledger uses, applied to cross-doc source descriptions). It does **not** duplicate the specific parity checks (8 §0.11↔§5, 14 DoD-copies, 22 gate-ID gap-freeness) — those are precise single-source assertions; G68 is the **graph-wide** orphan/resolution/described-the-old-way net no single check spans. Ships a **G24 positive+negative self-test** (plant an orphan `.md` with no index link ⇒ MUST fail; plant a `.md` describing a deleted gate / a renamed enum ⇒ MUST fail; a clean graph ⇒ MUST pass) registered in `scripts/gate-selftests/`. **Bootstrap annotation:** the doc graph already exists (the seven P0 docs + the spec + the plan), so G68 is **fail-closed from P0** over the docs that exist — it does not wait on app code (distinct from G69, whose CLAUDE.md map target is authored at P1-end). **Delivered (P0.3.5 + P0.3.12):** `scripts/plan-lint` check 25 (`25:doc-graph`). P0.3.5 landed legs (1) reachability/no-orphan, (2) cross-doc resolution (forward-target allowlist + code-span/fence-aware), and the (3) freshness NAME-matching axis (a doc still naming a removed/renamed `Gnn`/threat-class fails). **P0.3.12** completes leg (3) with the **per-source content-fingerprint** axis — the committed L(-1) ledger `scripts/doc-fingerprints.toml` (`[[source]]` = `kind` file\|section + region locators + the sha256 the describing docs were reasoned against); `doc25_doc_graph` recomputes each region (LF-normalized, fence-aware section extraction) and FAILS on a mismatch, catching the same-name "described-the-old-way" drift the resolution + name-matching legs cannot. A re-bless is an owner-acked L(-1) edit (G71), the G29 ledger discipline. Missing/unparseable ledger, unresolved region, malformed fingerprint, unknown kind all fail closed. Seeded LIVE (`l-neg1-files.toml` → its four prose copies, the sync this row names) + DORMANT-until-P1.64 (§0.7 physical tree → CLAUDE.md §1a, the freshness companion to check 26, skipped while §1a is the placeholder stub). `g24-plan-lint.py` 51→65 legs (the +14 fingerprint cases). The ledger grows in P1 as more describing relationships stabilize | push | by docs glob; fail-closed |
| **G69** | Structural-map integrity *(mirror)* | every repo **directory** (top-level + every significant subdir) appears in the **CLAUDE.md "Repo layout" map** (the operational per-dir map — §1a) AND every mapped dir exists — a **bidirectional** structural assertion (the generalization of "Claude makes a new `test/` folder because it doesn't know one exists" to ANY structural drift: nothing structural lives outside the map). A directory present on disk but ABSENT from the map **fails** (a folder created without updating the map); a dir NAMED in the map but MISSING on disk **fails** (a stale map entry). Parses the CLAUDE.md "Repo layout" block (a fenced/list map, not a regex over prose) and diffs its dir set against a `git ls-files`-derived directory set (so untracked scratch dirs do not false-fail), with a committed ignore-list for the genuinely-out-of-scope trees (`target/`, `node_modules/`, `dist/`, `.git/` — the same set `.gitignore` covers). **Dual-authority bind (§1a is a §0.7 projection, NOT a competing source — r13 reconcile):** spec [§0.7](../spec/00-architecture.md) "Physical tree" is the HIGHER source per the repo conflict rule **SSOT > spec > docs**, so this gate additionally asserts the **CLAUDE.md §1a dir set is a faithful projection of the §0.7 physical tree** — every dir in the §1a map ∈ the §0.7-enumerated tree (§1a may not invent a dir §0.7 does not home), so a §1a-vs-§0.7 divergence **fails** (the freshness companion is G68 check 25, which fingerprints §0.7 as an authoritative source so a §0.7 tree edit not mirrored into §1a is stale). This closes the one structural source-of-truth pair the map gate would otherwise leave unguarded: §1a ↔ disk AND §1a ↔ §0.7. **`→ activated in P1`** — the full repository folder structure + the authoritative CLAUDE.md "Repo layout" map (the §0.7 projection) are authored at **P1-end** (the structure-establishment box), so G69 **fail-opens / skips-with-warning until the CLAUDE.md map block exists, then fail-closes** (the identical bootstrap annotation G47/G22/G23 carry, **tolerant of the already-existing `docs/` and `assets/` dirs** while the placeholder map in CLAUDE.md is still a stub). Ships a **G24 positive+negative self-test** (plant an on-disk dir absent from the map ⇒ MUST fail; remove a mapped dir from disk ⇒ MUST fail; plant a §1a dir absent from §0.7 ⇒ MUST fail; map==disk==§0.7-projection ⇒ MUST pass) registered in `scripts/gate-selftests/`. **Delivered (P0.3.13):** `scripts/plan-lint` check 26 (`doc26_struct_map`) — a width-agnostic tree-art parser (`_parse_tree_dirs`, an indent stack robust to the §1a/§0.7 connector widths, mining same-line sibling dirs + embedded-path ancestors), `_fenced_block_after` to locate both blocks, `_git_tracked_dirs` (`git ls-files`-derived, minus the committed `_STRUCT_IGNORE` set), and the pure set-only `_struct_map_relations` asserting all three edges (disk⊆map, map⊆disk, map⊆§0.7). **`→ activated in P1`:** skips while the §1a SECTION carries the word `PLACEHOLDER` (a section-scoped signal, not a whole-file scan, so a `[P1.64](…)` provenance link cannot pin it dormant forever; the G68 §0.7-fingerprint dormancy was aligned to `["PLACEHOLDER"]` for the same reason — both flip live together at P1.64), then fail-closes — a missing/unparseable map-or-§0.7 block or an unavailable git all fail closed once active. `g24-plan-lint.py` 71→93 legs. **G1 (volle Härte): 3 reviewers + synthesizer all GO, no P0/P1** (every mis-parse fails safe/false-positive); hardened this commit — annotation-stripping before tokenizing (no phantom dirs from prose), embedded-mining only on the tree node, `git -c core.quotepath=false ls-files -z`, the §1a-section-scoped skip signal, a heading-anchored §0.7 header regex. Recorded P1.64 obligations: (1) expand §0.7 to the complete tracked tree (incl. `docs/`/`assets/`/`supply-chain/`/the test+selftest subdirs) before §1a can project from it; (2) author §1a + §0.7 with every dir as an EXPLICIT row (the parser infers ancestors, so an omitted parent with a listed child would not raise disk-not-in-map) | push | by repo-tree + CLAUDE.md + §0.7 glob; fail-closed (bootstrap fail-open until the P1 map exists) |
| **G70** | Test-suppression-marker gate (the "no green-by-rewrite" mechanical signal leg) *(mirror)* | the **G8-deferral analogue applied to TESTS** — a `plan-lint`-family / diff-based scan (the same diff-scoped-at-L1, fail-open-without-base / full-tree-fail-closed-at-L2-and-L4 posture as G8/G21) that catches a diff **ADDING a test-suppression marker** to a test file (`*.test.ts`/`*.spec.ts`/`*.rs` under `tests/` or a `#[cfg(test)]` block) — **`#[ignore]`, `#[cfg(ignore)]`, `it.skip`/`describe.skip`/`.only`, `test.skip`, `xfail`, a `#[should_panic]` ADDED to a previously-real assertion, or a REMOVED / commented-out assertion in a changed test** — and **FAILS UNLESS a justification tag is present within ±N lines** (the same ±-line-window discipline as G8's `[Build-Session-Entscheidung]` suppressor): a `[Test-Change: <box-id> — old-obsolete+new-correct, §ref]` tag naming the box-id + the (1) old-obsolete + (2) new-correct proof + the spec-`§` that obsoleted the old expectation. **Two tag shapes** (a marker can appear for a CHANGED expectation or in a genuinely NEW test): the changed-expectation form `[Test-Change: <box-id> — old-obsolete+new-correct, §ref]` (the green-by-rewrite case the rule targets), and the net-new form `[Test-Change: <box-id> — new-test:<reason>, §ref]` for a marker that is part of a brand-NEW test with no prior expectation to obsolete (a legitimate net-new `#[should_panic]` panic-path test, a deliberately-`#[ignore]`d not-yet-runnable harness scaffold) — the scan flags BOTH the same way and BOTH pass with their respective tag, so a net-new panic-path test is documented, not awkwardly forced into "old-obsolete" wording. **This is the mechanical SIGNAL leg of the "no green-by-rewrite" discipline** ([test-strategy.md](../process/test-strategy.md) §8): the catastrophic failure mode is a model that bluntly rewrites/relaxes a red test until green and HIDES a real regression — G70 surfaces every test-suppression so it cannot slip in silently. **It FLAGS + REQUIRES JUSTIFICATION, it does NOT forbid** — a marker carrying the `[Test-Change]` tag passes normally (changing a test is necessary in the common case and stays a first-class move; the gate only fires on the UNJUSTIFIED suppression). **The SEMANTIC question ("is this rewrite legitimate, or is it suppressing a real regression?") is NOT G70's job — it is the G1 dual-review's test-integrity scrutiny item** (§1, a P0/P1 finding if the (1)+(2) justification is missing); G70 is the cheap deterministic canary that makes the change visible to that review (mirroring the G8↔G1 split: G8 flags a deferral marker, G1 judges the design). `#[should_panic]` on a genuinely-new panic-path test is legitimate (the tag documents it); the targeted abuse is `#[should_panic]` retro-fitted onto an assertion that used to check a value, to swallow a now-failing case. Ships a **G24 positive+negative self-test** (plant an `#[ignore]`/`it.skip`/a removed assertion in a changed test WITHOUT a `[Test-Change]` tag ⇒ MUST fail; the same marker WITH a well-formed `old-obsolete+new-correct` `[Test-Change]` tag ⇒ MUST pass; a net-new `#[should_panic]` test carrying the `new-test:<reason>` `[Test-Change]` tag ⇒ MUST pass; a clean test diff ⇒ MUST pass) registered in `scripts/gate-selftests/`, so plan-lint check 16 is satisfiable for G70. **Delivered (P0.3.14):** `scripts/check-test-suppression` (`--diff`/`--full`) — `test_scope_lines` (whole `*.test.*`/`*.spec.*`/`tests/**.rs` + the brace-matched `#[cfg(test)]` blocks of any other `.rs`, via `_cfg_test_ranges`), the Rust + TS/JS marker sets (incl. `#[cfg(any())]`/`#[cfg_attr(…,ignore)]`/`fit`/`fdescribe`/`skipIf`/`runIf`), `_scan_hunks` for the diff-only removed/commented-assertion signal (assert/`prop_assert!`/`assert_matches!`/`ensure!`/chained-`.toBe`), `TAG_RE` enforcing the two sanctioned `[Test-Change]` shapes **+ a real `P<n>` box-id** within ±6 lines, and `_line_comment_stripped` so a marker mentioned in a `//` comment is not flagged. `g24-test-suppression.py` = 43 legs (incl. a real temp-git `--diff` E2E + the `#[cfg(test)]` scoping both ways); canary 23→24. **G1 (volle Härte) NOGO → fixed → re-reviewed:** the `--full` `#[cfg(test)]` brace matcher gained `_rs_structural` (stateful multi-line string/raw-string/**nest-aware**-block-comment + lifetime-aware char-literal handling, the G8 `_comment_spans` discipline) so a `}` in a multi-line snapshot string / a `{` eaten by a lifetime / a `}` inside a nested `/* … */ } */` no longer closes the scope early and ejects a later `#[ignore]` from the fail-closed plane; plus `_rs_attr_joined` collapses a **line-wrapped attribute** (rustfmt's `#[cfg_attr(\n …, ignore)]`) onto its opener so the marker scan catches it; `TAG_RE` gained the box-id requirement; `ASSERT_RE` gained the property-test assertion families. Wired L1 (`--diff`) + L2/L4 (`--full`); already L(-1) via `scripts/check-*`. **Live now (not target-absent):** the 2 committed `tests/g53-fixture/**` crate-src `.rs` files are in scope (clean) | commit (L1 diff-scoped, new-marker only, fail-open w/o base) / push (L2 + L4 mirror full-tree) | by test-file glob; **fail-closed at the L2/CI mirror** (the L1 leg fails open w/o a diff base, identical to G8/G21) |
| **G71** | L(-1) security-critical-file change-control — owner-ack pre-push gate (DECISION D1; "the loop never edits the cage") *(mirror)* | the machine-checkable half of **owner decision D1**. A `plan-lint`-family / `git`-based **pre-push** check over the push range (`git log --format=%B <old>..<new>` + `git diff --name-only`) that, for **every commit whose diff touches an L(-1) security-critical file**, asserts the commit body carries a trailer matching `^L-neg1-ack: owner$` (the trailer is the **only** escape — a commit touching no L(-1) file needs none; there is **no** check-off / `[!extern]` exemption for an L(-1) edit, since that would waive the ack on exactly the gate catalogue / hooks / toolchain it guards — the §2-vs-plan conflict resolves to §2). The **authoritative L(-1) set is a committed machine-readable glob list `scripts/l-neg1-files.toml`** (itself an L(-1) file, so the cage cannot be silently narrowed) — the security-concept §2 prose list is its human description, held in sync by G68. The set is the cage the gates live in: gate scripts + their self-tests, the G29 SAST rule corpus `scripts/semgrep-rules/**` (project rules + vendored packs + the net-allow-list + planted-positive fixtures), `lefthook.yml`, the CI workflows (`.github/**`), `deny.toml`, `.gitleaks.toml` + allowlist/baseline, the pip gate-toolchain pin `requirements-ci.txt`, the JS supply-chain pin `.npmrc`, the EOL/charset policy `.editorconfig` (G52) + the prose-typo allowlist `.typos.toml` (G51), the `cargo-vet` exemption set + `imports.lock`, `.gitattributes`/`.lfsconfig`, the G56b SSH allowed-signers file, the `rust-toolchain.toml` channel pin, the `rustfmt.toml`/`clippy.toml` lint/format policy (G3/G4), the `coverage-floors.toml` coverage-floor ratchet (G27, increase-only), the Tauri `capabilities`, `engines.lock`, the reviewer-rubric block, `scripts/l-neg1-files.toml` itself, and the security/process docs. **The autonomous Build-Loop NEVER edits an L(-1) file (security-concept §2): a needed L(-1) edit is a hard-stop + escalation to Co-Pilot, where the owner makes/approves it and adds the trailer** — so the trailer records an OWNER decision (the one ownership control above the deterministic gates) and G71 mechanically AUDITS the trailer's presence, it does not judge the owner's intent. Independent of the G1 `Dual-Review:` trailer; both may co-occur on one commit. **Bootstrap (the G56a fail-soft→hard pattern):** G71 **fail-opens / skips-with-warning during the P0 bootstrap** (the P0 build itself authors the gates / lefthook / `deny.toml` / security docs — all L(-1) — under owner drive) **and fail-closes from P1 onward**, so once the autonomous loop starts every unacked L(-1) edit blocks the push. Ships a **G24 positive+negative self-test** (an L(-1)-touching commit WITHOUT the trailer ⇒ MUST fail post-P0; the same WITH a well-formed `L-neg1-ack: owner` trailer ⇒ MUST pass; a non-L(-1) diff ⇒ MUST pass) registered in `scripts/gate-selftests/`. **Delivered P0.2.14:** `scripts/check-l-neg1-ack` (the gate, gitignore-style glob matcher over the cage; the trailer is the ONLY escape — no check-off/`[!extern]` exemption for an L(-1) edit) + the committed `scripts/l-neg1-files.toml` cage + the `g24-l-neg1-ack.py` self-test (53 legs incl. a real temp-git-repo E2E); wired **L2** (lefthook pre-push) + the **L4 mirror** (ci.yml `workflow-security`, ranging `github.event.before..HEAD` with `fetch-depth: 0`), fail-soft in P0 | push | by L(-1) glob (`scripts/l-neg1-files.toml`); **fail-closed from P1** (bootstrap fail-open in P0 only) |
| **G24** | Gate-script self-tests *(mirror)* | run the custom-gate unit tests (positive + the G24-class planted-positive negative cases). **At L2 the self-tests run only when a gate script DIFFS (cheap); but the L4 clean-checkout mirror runs EVERY custom gate-script's planted-positive self-test as a PRELUDE on every run, not only when the script diffs** — so a tool-version bump (a pinned-tool upgrade) that breaks a STABLE script's parsing is caught even though the script itself did not change (the continuous-armed-canary pattern G42/G42b already use, applied to the whole gate-script set; complements plan-lint check 16 which asserts the self-tests EXIST) | push (L2 diff-scoped; L4 mirror runs all as a prelude) | fail-closed |
| **G24a** | Corpus / crash-fixture integrity *(mirror)* | the adversarial/decompression-bomb fixtures, the committed `fuzz/corpus/`+`fuzz/crashes/` set, and the LFS-backed `corpus-large` are **untrusted-input files fed to the highest-privilege Lane-B VPS leg** (the bundled C/C++ decoders) — a poisoned/swapped fixture (or an LFS pointer redirected to a tampered object) is an unguarded path to feed a crafted exploit into the test runner, and a swapped crash-replay fixture silently masks a regression. A committed **SHA-256 manifest of every tracked corpus/crash fixture** (and the LFS-resolved `corpus-large` objects) is verified in CI **before** the corpus runs, plus **`git lfs fsck`** on the Lane-B leg — any unreviewed fixture-bytes change surfaces as a manifest diff (a dual-review item), mirroring the `engines.lock` SHA discipline. **LFS-resolution-governing files are themselves guarded (r7 — `git lfs fsck` only inspects what is STILL `filter=lfs`-tracked; the files that govern that resolution were unguarded, one level up from the "redirect the LFS pointer" threat this gate names):** a tampered **`.gitattributes`** can silently un-track a corpus path (its bytes become a plain blob `git lfs fsck` no longer inspects) and a **`.lfsconfig`** edit can repoint the LFS endpoint so a fresh `git lfs pull` on the highest-privilege Lane-B VPS fetches tampered objects. So G24a additionally **asserts every corpus path in the manifest is actually `filter=lfs`-tracked per the EFFECTIVE `git check-attr filter <path>`** (an un-tracking `.gitattributes` edit then surfaces as a manifest-vs-attribute MISMATCH, not a silent skip), and **asserts `.lfsconfig` (if present) names only the canonical GitHub LFS endpoint**; `.gitattributes` + `.lfsconfig` join the L(-1) security-critical-file set (an owner-acked edit carrying the `L-neg1-ack: owner` trailer, G71 — like `engines.lock`). **Corpus-provenance sub-check (§6.4.5):** assert **every corpus manifest `[[file]]` entry carries a non-empty licence/provenance field from the allowed redistributable set (public-domain / CC0 / self-produced / synthetic)** BEFORE these untrusted files are fed to the highest-privilege Lane-B decoders — mirroring the engine-source allow-list discipline for the corpus (a fixture of unknown provenance is both a redistribution risk and an un-vouched untrusted input). **Auditable-provenance + manifested + ship-integrity legs (r15 — the licence CATEGORY alone is unverifiable from media bytes, so the gate enforces an auditable RECORD, not a content-licence inference):** additionally assert **(a)** every `[[file]]` entry carries a **non-empty `provenance` source** (a URL for a public-domain/CC0 file, a generator-script reference for a synthetic file, or `self-produced`) so each licence declaration is human-auditable; **(b)** every committed file under `tests/corpus/` has a manifest entry AND every manifest entry resolves to a real file (no un-manifested corpus byte, no dangling row); **(c)** the corpus is a **repo/CI-only test asset — never bundled into the installed application** (a `tests/corpus/` path appearing in the staged app bundle is caught by the G35 test-asset-exclusion sub-check — a path-prefix deny, since a corpus DATA file is not a shared object the binary-scoped diff catches). Activated as the §6.4.5 corpus fills (P3–P7) | push (verify) / red `main` | fail-closed |
| **G72** | engines.lock-bump corpus re-validation | **the genuinely-new control behind the §5a Dependabot / engine-bump rule** — an `engines.lock` engine-version/SHA edit (a CVE-driven or Dependabot engine bump) must RE-VALIDATE the bumped engine against the §6.5 reliability corpus before it lands, not merely pass the G37 hash/provenance check (a new version can pass its hash yet regress a conversion, change determinism, or break a pair). G72 asserts that any commit changing an `engines.lock` engine-version/SHA row carries a **`Reliability-Gate: <ledger-ref>` proof** that the §6.5.2 pair-status ledger (P4.61) was regenerated **GREEN** for the bumped engine's pairs (or a CI re-run of those pairs is attached). **Distinct from G37** (binary hash + provenance) — G72 is the OUTPUT-reliability axis of an engine bump. **Bootstrap:** the §6.5 machinery (the §6.4.3 runner + the P4.61 ledger) does not exist until P4 — so G72 **fail-soft / skip-with-warning during P1–P3** (an `engines.lock` bump in that window is HELD as a Co-Pilot review item, surfaced not auto-merged, per roles §5a) and **fail-closed from P4** (once the ledger exists, an unvalidated bump blocks). Ships a **G24 positive+negative self-test** (an `engines.lock` version-row edit WITHOUT the `Reliability-Gate:` proof post-P4 ⇒ MUST fail; WITH it ⇒ MUST pass; a non-`engines.lock` diff ⇒ MUST pass) registered in `scripts/gate-selftests/`. Wired in P4.61 | push (`engines.lock` glob) / red `main` | **fail-closed from P4** (bootstrap fail-soft P1–P3) |
| **G18b** | First-party crate-trust audit *(mirror)* | **`cargo-vet`** — records per-crate trust audits and **fails when an unvetted/changed crate enters the tree** (closes the gap G17 leaves: a *new* malicious/typosquatted crate has no advisory yet, so `cargo audit` can't see it); offline-friendly (audits are committed). **Bootstrap protocol (P0.3):** `cargo vet init`; `cargo vet import` ≥1 trusted external DB (Mozilla / Google supply-chain / ISRG) as baseline; `cargo vet suggest` then certify/exempt the initial tree with documented reasons; criteria policy = `safe-to-run` for build-only deps (incl. proc-macros) vs `safe-to-deploy` for runtime deps; a **new unvetted dep requires certify-or-documented-exemption and the Build-Loop ESCALATES a block** (never silently exempts). P0 exit includes a clean `cargo vet check` on the initial `Cargo.lock`; the audit-diff (which crates newly entered the tree) is surfaced to the dual review. **`imports.lock` is COMMITTED and treated like a lockfile (no live-registry trust):** the ≥ 1 imported external audit DB (Mozilla/Google/ISRG) is FETCHED over the network, which would violate §0's "offline-tolerant, pinned, no live-registry trust" discipline if done at gate time — so `cargo vet check` runs `--locked` offline against the **committed `imports.lock`**, any `imports.lock` refresh is a **Co-Pilot-reviewed change (never auto-fetched at gate time)**, mirroring the G18a `--locked` discipline; **≥ 2 distinct import sources are REQUIRED (decided r5 — Mozilla + Google supply-chain as the baseline pair, ISRG optional third)** so one compromised audit DB cannot single-handedly vouch a malicious crate, and a plan-lint sub-check asserts `imports.lock` is a tracked file naming ≥ 2 distinct `[imports.*]` sources. The committed-and-current enforcement is G18a (`imports.lock` ∈ the `git diff --exit-code` set) + the G9 invariant (e) (no `cargo vet update`/`sync`/`import` in CI/scripts). **cargo-vet exemption growth-lint (symmetry with the G2 allowlist + the G29 suppression ledger — r6):** the `[[exemptions.*]]` set in `supply-chain/config.toml` is **growth-guarded** like the `.gitleaks.toml` allowlist — each exemption carries a box-id + Dual-Review note, exemption-set growth is a Co-Pilot review item, and a `cargo vet diff <old-lockfile> <new-lockfile>` (cargo-vet 0.9+) on any `Cargo.lock`-touching commit surfaces which crates' vet status CHANGED (newly-unvetted / version-bumped / **exemption added or expiring**) as a human-readable diff for the dual review (so a bulk `cargo update` naming 30 crates is dual-review-efficient, not a wall of unstructured failure text). **Delivered P0.3.6:** `supply-chain/config.toml` declares the >=2 import sources (Mozilla + Google) + the safe-to-run/safe-to-deploy criteria + exemption protocol; `check-supply-chain` asserts the >=2 floor + growth-guards the exemption set (frozen count 0). `cargo vet init`/`import`/`check --locked` run in P1; `imports.lock` is committed then (Co-Pilot-reviewed, never auto-fetched). | push | always; fail-closed |
| **G18c** | JS registry pin + resolution-URL guard *(mirror)* | a committed **`.npmrc`** pins the registry, and a check asserts **every `pnpm-lock.yaml` resolution URL is from the allowed registry** (the `[sources]` analogue for the JS tree — dependency-confusion / source-substitution defence; the WebView IS the entire T2 attack surface, so its supply chain must match the Rust-side discipline). **Delivered P0.3.8:** `.npmrc` pins `registry.npmjs.org` (+ no scoped `@scope:registry` override to another origin); `scripts/check-js-supply-chain` enforces the pin now + (once `pnpm-lock.yaml` lands, P1) a fail-closed resolution scan rejecting any foreign host AND any non-registry scheme (`git+`/`ssh:`/`file:`/protocol-relative `//` = clone-and-build). Also scans the pnpm-workspace `registries:` block (default + per-scope) + forbids a committed `.pnpmfile.cjs` (install-time re-source code). | push | always; fail-closed |
| **G18d** | JS install-lifecycle-script lockdown *(mirror)* | a committed **minimal `onlyBuiltDependencies` allowlist** (pnpm 10 blocks build/lifecycle scripts by default; this asserts + pins that posture) + a lint that **fails if the allowlist grows** or if `enable-pre-post-scripts`/`unsafe-perm` is set — a malicious dep otherwise runs arbitrary code via `postinstall` the moment `pnpm install` runs in CI (which holds the signing secrets at release time). **Delivered P0.3.8:** `.npmrc` keeps the lifecycle posture locked (`enable-pre-post-scripts=false`, `unsafe-perm=false`, `frozen-lockfile=true`); `scripts/check-js-supply-chain` growth-guards the `onlyBuiltDependencies`/`allowBuilds` allowlist (frozen count 0; fail-closed on an unparseable form) once a pnpm manifest exists (P1), FAILS a committed manifest that has no lockfile, and forbids committed install-time code / source-mutation (`.pnpmfile.cjs`, `patchedDependencies`+`patches/`, source-redirecting `overrides`/`resolutions`, root install-lifecycle scripts) + any non-root `.npmrc`. | push | always; fail-closed |
| **G53** | Core-crate forbidden-dependency *(mirror)* | **`cargo-deny [bans]` with a workspace-member-scoped deny list** (OR a `cargo metadata`/xtask dep-tree walk) asserting the core crate's Cargo dependency closure does **NOT** contain the image-worker-only C libs (`libvips`/`libheif`/`libde265`/`librsvg`/`libimagequant`, + the ImageMagick delegate) — the build-time analogue of the §3.6 "LGPL must not link into the MIT core" assertion, the **explicit G53 verifying gate for the §5 T6 row**; a careless refactor that pulls a copyleft C lib into the core fails here. **Delivered P0.3.7:** `scripts/check-core-deps` — the `cargo metadata` core-closure walk (the cargo-deny `[bans]` route is rejected: it cannot scope a deny to one member, and `libvips` is legitimately present in `convertia-imgworker`); pure closure-walk + forbidden-stem matcher (unit-tested against synthetic metadata), live `cargo metadata --locked` target-absent until P1; negative fixture `tests/g53-fixture/` (planted core->libvips-sys), `g24-core-deps.py` 11 legs. | push | always; fail-closed |
| **G54** | Gate-plane integrity assertion (hooks-installed + not-redirected + two-plane parity + push-from-stale-base) *(mirror)* | a **Lane-A step runs `lefthook install` then `git diff --exit-code "$(git rev-parse --git-path hooks)"`** — it resolves the **EFFECTIVE hooks dir** via `git rev-parse --git-path hooks` (which honours a `core.hooksPath` override) instead of hardcoding `.git/hooks/`, so a `core.hooksPath` pointing away from `.git/hooks` cannot make the diff inspect an inert directory and pass while NO hook fires (the silent `core.hooksPath` bypass §0 forbids). **The same step also asserts `git config --get core.hooksPath` is either unset OR resolves to the lefthook-managed path** (a redirection to any other dir is a hard fail) — so the hooks-are-installed proof and the hooks-aren't-redirected proof are the same gate. (Mechanism note — a `post-checkout` hook can't be distributed because the hooks dir is untracked, so that option is circular; a single fresh-install hash is OS-specific because lefthook generates per-OS scripts.) A clone that skipped `lefthook install` has no local L1–L3 protection and (single-Build-Loop model) **no PR gate to catch it**; "`lefthook install` is mandatory after clone" is documented in `build-loop.md`/`CONTRIBUTING.md` (cannot be enforced technically). **Two-planes parity (machine-true):** a parity check asserts every L1/L2 lefthook command id has a corresponding CI invocation — drive CI from `lefthook run pre-commit`/`pre-push`, or diff the lefthook command set against the CI job set — so a hook wired in `lefthook.yml` but never wired into CI can't silently have only one plane. **Push-from-stale-base guard (the single-branch model's only unguarded push-from-stale-base gap):** a pre-push assertion that **`git merge-base HEAD origin/main == origin/main`** (HEAD is a descendant of the current remote tip) — so a push from a stale local base (which would overwrite a gate fix already pushed by Co-Pilot/owner mid-session, the §P0.6 step-1a concern at push time) is caught before it lands, not after. Pairs with the P0.6 step-1a session-start gate-currency fetch (this is its verifying pre-push enforcement) | push | always; fail-open only if Lefthook absent by design (CI never absent) |
| **G54b** | Gate-plane CONFIG integrity *(mirror)* | a stdlib-only (`tomllib`) check (`scripts/check-gate-planes`) over `scripts/gate-planes.toml` — the machine-readable source-of-truth for the enforcement-plane **STRUCTURE** (the seven planes `L(-1)`/`L0`–`L5`, each with trigger/enforcement/mirror, the two-enforcement-planes principle) + the fail-open/closed **POLICY**. Asserts: **`default_posture == "fail-closed"`**; all seven planes present + each fully-fielded (id/name/trigger/enforcement/mirror), no duplicate id, **no unknown key** (a stray key — e.g. an inline-array `fail_open` TOML-mis-scoped into the last `[[plane]]` — is rejected, not silently absorbed); and **every fail-OPEN exception is GENUINELY justified** — a `[[fail_open]]` array-of-tables row (NOT a plain `[fail_open]` table, which would otherwise crash, NOR an inline array scoped away to a zero-exception silent pass) naming `gate`/`fail_open_at`/`covered_by`/`reason`, with **`fail_open_at` referencing only DEFINED planes** and **`covered_by` naming no plane that is itself in `fail_open_at`** (a plane cannot cover its own fail-open — multi-plane- and whitespace-robust), and any plane-SHAPED `covered_by` token resolving to a defined plane (no dangling `L99`); a non-plane PHASE `covered_by` (e.g. `P10 / P3+ activation`, `P0.7.17 provisioning`) is allowed (its plane-self-cover is vacuously impossible). The **"empty-but-wired contract"** each P0.3/P0.4 gate adds its `[[fail_open]]` row to as it is built; the **plane-DEFINITION-integrity sibling of G54's plane-EXECUTION integrity** (hooks-installed/not-redirected). The per-gate plane assignment + posture lives in this §5 catalogue, kept in sync with `gate-planes.toml` + security-concept §3 by plan-lint (P0.3.5). Wired at **L1** (lefthook pre-commit) + the **L4 CI mirror**; self-tested by `scripts/gate-selftests/g24-gate-planes.py` (18 legs incl. the multi-plane/whitespace self-cover + dangling-plane + inline-array-mis-scope + plain-table-fails-cleanly adversarial shapes). L(-1) security-critical | commit | always; fail-closed |
| **G56** | CI runner-host integrity *(L4-authored; runs in CI)* | a workflow lint (zizmor custom check / a small jq-over-parsed-YAML script) that **fails any secret-using job bound to a self-hosted runner label — keyed on ALL THREE named secrets `MINISIGN_SECRET_KEY`/`MINISIGN_PASSWORD`/`ANTHROPIC_API_KEY` (r7 — was keyed only on the minisign pair, asymmetric with the §5 three-secret enumeration)** — the secret-bearing signing/release step must run on an **ephemeral GitHub-hosted runner**, host-isolated from the Lane-B untrusted-corpus/fuzz jobs that run on the shared VPS (spec §6.1.4/§6.7.2). (In practice `ANTHROPIC_API_KEY` is build-session-local and present in NO CI job — security-concept principle 11 — but the lint keys on it regardless so a future workflow that DOES read it inherits the same self-hosted-label ban + host-isolation as the minisign pair, not a silent asymmetry.) Also asserts the corpus/fuzz job and the signing job declare **disjoint runner hosts**, and the **GitHub-HOSTED signing job uses `step-security/harden-runner` (BLOCK mode)** — its assertion is **scoped to GitHub-hosted jobs ONLY**, because harden-runner's free/Community tier works only on GitHub-hosted runners (self-hosted requires a StepSecurity Enterprise license), so requiring/asserting it on the shared self-hosted VPS leg would either falsely pass (action present but inert) or be unsatisfiable; on the self-hosted leg the egress enforcement is **G42/G42b + the VPS egress allowlist + an ephemeral/JIT low-priv runner**, and G56 instead asserts that self-hosted leg declares no persistent shared workspace. **Corpus-job ephemerality as a MACHINE-CHECKED property + a per-job clean-checkout on the untrusted-corpus leg (r7 — principle 11 host-isolates the SIGNING secret from the corpus, but the corpus job itself runs the highest-privilege C/C++ decoders on adversarial bytes on a PERSISTENT shared VPS; a crafted-corpus RCE inside a decoder (the honestly-stated no-runtime-containment residual) can POISON the next job's vendored gate-tool binaries / Cargo cache / fuzz corpus BEFORE the G37 cache-restore re-verify runs — and that re-verify reads a hash the host-owner can also rewrite):** G56 asserts (a) the corpus/fuzz job runs on a **JIT / ephemeral runner with no persistent workspace reuse** — a runner-config attestation recorded like the §6.1.4 kernel version (`runner ephemeral: true` / a per-job `--ephemeral` registration), machine-checked, not prose; and (b) the untrusted-corpus leg restores **NO cache for the gate-tool binaries** — a **per-job clean-checkout** of the vendored, checksum-verified gate tools specifically on that leg, so a poisoned-by-a-prior-corpus-job cache cannot pre-seed the very tools that would catch the next finding. **Harden-runner-ENFORCED assertion via an ACTIVE PROBE (Community-tier silent-degrade residual; r7 — the mechanism made implementable):** the free/Community tier carries a ~10k-runs/week ceiling above which it silently degrades to no-enforcement; the rare/tag-only signing job won't bind it, but the release job must prove BLOCK is live. **step-security/harden-runner exposes NO step output for block/enforcement status (verified — status surfaces only via the job log, the markdown summary, and the StepSecurity dashboard), so "assert harden-runner reported ENFORCED" cannot be a step-output read and a log-string parse is fragile.** Instead a **post-step in the signing job actively attempts a KNOWN-BLOCKED outbound connection and asserts it FAILS** (a refused/timed-out connect proves BLOCK is live, not merely that the action ran), with a **G24-style self-test** that the probe itself is armed (a probe that always "passes" because it points at an allow-listed endpoint is a no-op). A degraded-to-audit / no-enforce status (the probe SUCCEEDS in connecting) is a release-blocking fail. Re-verify the action's actual output API at the exact pinned version before the P0.2 fill pass and use a real programmatic enforcement output if one is added upstream (preferring it over the probe). **Plus the CI-config hygiene assertions:** `dependabot.yml` contains a `package-ecosystem: github-actions` entry with `directory: /` and a schedule (if deleted/missing, the SHA pins silently stop updating and miss security updates); every PUSH workflow declares a **`concurrency: {group: ci-${{github.ref}}, cancel-in-progress: true}`** (rapid autonomous-loop pushes otherwise spin overlapping full matrices — macOS bills ~10× Linux — and a "red main fixed immediately" model wants the superseded run cancelled) — **never** on the release/tag workflow (never cancel a release mid-sign) **and never on the G42/G42b egress-deny job group** (a cancel mid-window yields a `cancelled` conclusion the build-loop step-2a would otherwise treat as the expected G56 cancel, silently skipping the egress/read-half proof — so the G42/G42b job group sets `cancel-in-progress: false`, and G56 asserts it); and **explicit `timeout-minutes` per job** (esp. the WebView/LibreOffice legs, which can otherwise run to the 6 h default). Also asserts `dependabot.yml` additionally carries **`package-ecosystem: cargo` AND `package-ecosystem: npm` AND `package-ecosystem: pip`** entries (directory `/`, with a schedule) — OR a committed `renovate.json` covering all four — so the Rust crate graph, the pnpm graph (the trees G17/G18 actually CVE-scan), **AND the pip-installed gate-toolchain layer (`requirements-ci.txt` — Semgrep et al., which read CI secrets AND produce findings, so a newly-published vulnerable version is a trust-boundary event the `--require-hashes` discipline VERIFIES but only a pip dependabot entry SURFACES — r7)** all get automated bump PRs, not only the actions graph (the pin-and-watch discipline is symmetric; bump PRs reviewed by Co-Pilot/owner, not auto-merged). Also an **actionlint sub-rule that any Linux CI AppImage invocation uses `--appimage-extract-and-run`** (a bare `./ConvertIA.AppImage` needs FUSE on the runner, which the GitHub Linux runner may lack). **Plus the `id-token` scope sub-rule (the OIDC token is as valuable as a long-lived secret for impersonation/Sigstore minting):** a one-line jq-over-parsed-YAML assertion that the **workflow-level `permissions:` block sets `id-token` absent or `none`** AND **`id-token: write` appears ONLY on the release/attestation job** (G59) — zizmor's excessive-permissions check (G50) targets the `GITHUB_TOKEN` scope, not the id-token scope specifically across all pinned versions, so this closes the class explicitly rather than assuming G50 covers it. **Plus the `pull_request_target` safe-handler sub-rule (the canonical Actions secret-exfiltration vector — a `pull_request_target` workflow that checks out fork `github.head_ref` code and runs it in the privileged base context with secrets):** assert **no workflow uses `pull_request_target` without an Environment-protection gate (required reviewers)**, OR that any `pull_request_target` handler only reads PR metadata and **never checks out / runs `head_ref` code** (G50 flags the trigger as dangerous but does not constrain the handler shape); any such trigger is audited at the engines.lock-SHA-edit review tier. **Plus the `bundle.createUpdaterArtifacts` absent/false assertion** is delegated to G47 (a parallel updater-signing surface — see G47). **Plus the cache-hygiene sub-rule (build-cache poisoning defence):** the secret-bearing release/signing job restores no writable shared cache (cache disabled OR read-only/lockfile+toolchain-derived key); fork-PR workflows share no cache-key namespace (no `restore-keys` prefix bleed) with release/`main`; the signed artifact builds `--locked`/`--frozen-lockfile` from a cache-cold or integrity-verified tree — generalising G37's "an Actions cache is not integrity-protected" rule beyond the engine-asset cache to the Cargo/pnpm/sccache/gate-tool/fuzz-corpus caches that also feed the minisigned bytes. **Plus the `build.rs`/proc-macro no-network-after-fetch sub-rule (r7 — the Rust analogue of G18d's JS lifecycle-script lockdown, which the JS side has and Rust LACKS):** `build.rs` scripts and proc-macros execute arbitrary native code during `cargo build`/`test` in the SAME secret-bearing release job with full network by default, and `cargo-vet` (G18b) is a TRUST signal not an execution sandbox (a vetted-then-compromised or not-yet-vetted-but-allowed crate's `build.rs` can open a socket / exfiltrate `MINISIGN_SECRET_KEY` at build time) — so a jq/yq-over-parsed-YAML assertion that the secret-bearing release/signing job sets **`CARGO_NET_OFFLINE=true`** for all `cargo build`/`test` steps AFTER an explicit `cargo fetch --locked`, reinforced by the harden-runner BLOCK egress allow-list already on that hosted job. **Residual stated HONESTLY: cargo cannot fully sandbox build scripts the way pnpm blocks lifecycle scripts** — this bounds egress (offline-after-fetch + harden-runner), not in-process behaviour; the full per-crate `std::net`/`std::process` cap is the owner-decidable `cargo-acl`/cackle (§8). **Plus the secret-in-Actions-log sub-rule (r6 — the most direct leak of `MINISIGN_SECRET_KEY`/`MINISIGN_PASSWORD`/`ANTHROPIC_API_KEY` into a PUBLIC repo's world-readable Actions log, which G2 does NOT look at):** GitHub auto-masks only EXACT registered-secret strings — a transformation (base64, substring, here-doc, a `set -x` expanding an env-interpolated arg, a debug `env | sort`, piping a secret through a tool that re-emits it) defeats masking and the secret is world-readable forever. In a loop that authors its OWN workflows, a stray `run: echo "$MINISIGN_SECRET_KEY"` or `set -x` in the sign step is exactly the self-inflicted leak the cage must catch. A **jq/yq-over-parsed-YAML workflow-lint** asserts the secret-bearing release/signing job's steps contain **no `set -x`/`set -o xtrace`**, **echo or pipe no secret-named env var**, and any secret-derived value is wrapped in `echo "::add-mask::…"`; **plus a release-tier post-run scan of the captured job log** for the secret SHAPES (reuse the G2 minisign-key + `sk-ant-` + `MINISIGN_PASSWORD` rules against the captured log), **fail-closed if a secret pattern appears** in the log. **Plus the `release` GitHub Environment sub-assertion (r7 — ADOPTED, the structural closure for the one irreversible key-bearing action):** G56b leg-2 ("abort before reading the secret") runs INSIDE a job where GitHub has ALREADY injected the secret at job start — "abort before read" is not "never injected". The native mechanism that keeps the three release secrets (`MINISIGN_SECRET_KEY`/`MINISIGN_PASSWORD`/the signing-relevant set) **structurally UNREACHABLE** by the autonomous loop until a human approves is a **GitHub Environment** (`release`) holding them as ENVIRONMENT secrets with **required-reviewers + a `v*` deployment-branch/tag policy** — the job cannot start (secret never injected) until the owner approves, exactly the human-in-the-loop a "two sessions, one branch, autonomous commits to main" model wants on its one irreversible action, and it is free + GitHub-native. So G56 asserts via **`gh api repos/:owner/:repo/environments/release`** that the `release` environment EXISTS with `protection_rules` containing a `required_reviewers` rule AND a `deployment_branch_policy` restricting deployments to the `v*` tag pattern, and that the secret-bearing signing/release job declares `environment: release`. **Fail-soft in the P0 bootstrap box, then hard** (mirroring G56a / G56b leg 1). Provisioned in P0.7. This turns G56b leg-2's "abort before reading the secret" into "secret never injected without approval" — strictly additive to G56b, not a replacement. **Plus the container/service image-digest sub-rule (r6 — zizmor G50 enforces ACTION SHA-pins but NOT image digests, and §0/principle 10 claim the CI base image is digest-pinned with no NAMED gate enforcing it):** a jq/yq-over-parsed-YAML assertion that every `jobs.<job>.container.image` and `services.*.image` value is a **digest-pinned `<registry>/<image>@sha256:<hash>`** form (a mutable tag fails). **Plus a transitive-action-pin check** — a SHA-pinned composite action can itself `uses:` a nested action by mutable tag in its own `action.yml`, so surface this via **`pinact`** (pins transitively) OR the reserved **G67 OSSF-Scorecard Pinned-Dependencies** check as the corroborator. Implements security-concept principle 11 / the §5 CI-runner-host-integrity row. **Delivered (P0.4.4):** the `build.rs`/proc-macro no-network-after-fetch sub-rule is **check (8)** of [`scripts/check-ci-supply-chain`](../../scripts/check-ci-supply-chain) (`check_secret_offline_build`): a secret-reading job (reused rule-(6) secret detection — `${{ secrets.* }}` per-job or a workflow-level env secret) that runs a COMPILING command (cargo `build`/`test`/`run`/`bench`/`check`/`install`/`doc`/`publish`/`package`/`clippy`/`rustc`/`miri` OR a `tauri build` wrapper — `pnpm`/`npx`/`yarn`/`bunx tauri build`, `cargo tauri build`, `tauri-apps/tauri-action` — each executes `build.rs` + proc-macros; the compile scan is scoped to the `steps:` section so a job-level env value embedding "cargo build" is not a phantom compile) must set `CARGO_NET_OFFLINE=true` AND carry an explicit `cargo fetch --locked` BEFORE the build (else the build script could fetch-and-exfiltrate `MINISIGN_SECRET_KEY`). **Fail-open until the P10 signing job lands** (no secret-bearing build job exists yet; per §6.7.2 the secret-bearing signing job is sign-only + disjoint from the build job, so this binds only if that design is violated). `g24-ci-supply-chain.py` +11 legs (113→124: + the tauri-wrapper, cargo-install, toolchain-override, and env-value-phantom cases). The §3.8 residual stands honestly — offline-after-fetch + the harden-runner BLOCK in check (6) bound EGRESS, not in-process behaviour; the full per-crate `std::net`/`std::process` cap is the owner-decidable P0.4.5 `cargo-acl` upgrade. | push (workflow glob) / red `main` | by `.github/` glob; fail-closed |
| **G56a** | GitHub branch-protection / required-status-checks config *(L4-authored; runs in CI on schedule + every push)* | in the single-branch direct-to-`main` model there is no PR and no second reviewer, so the **only** thing that turns a red CI run into an actual block is **GitHub repo config**, which is **invisible to the codebase** and silently relaxable in the UI — G56 lints workflow FILES but cannot see repo settings. This gate queries the GitHub **rulesets API** for `main` (`gh api repos/:owner/:repo/rulesets`, then a GET per active ruleset for its rules + `bypass_actors`) and **FAILS** if (a) the agreed required status checks are not **all present and required** — asserted on a **GitHub ruleset**, not classic branch protection, because classic required-checks block **all** direct pushes incl. admins (GH006), incompatible with the §2 direct-to-`main` "red `main`, fix immediately" model; so the config is **two rulesets** and (a) checks the required-checks ruleset covers the agreed checks **with its bypass set restricted to the repo-ADMIN role ONLY** (the trusted direct-pusher — a fork-PR stays gated, the owner/Build-Loop can direct-push), (b) the **`non_fast_forward` (force-push) + `deletion`** rules are absent **or carry any bypass actor** (history protection must apply to everyone incl. admins — **NO** bypass), (c) a **non-admin / wider actor** can bypass the required-checks rule (this REPLACES the old "enforce_admins must be true / no admin-bypass" assertion, which the GH006 direct-push reality made unsatisfiable — the resolution is owner-decided, security-concept §3). Runs on **schedule + every push** to catch UI drift; **fail-soft** ("repo not yet configured") **only** during the P0 bootstrap box, then **hard**. **Two further invisible repo-config settings are asserted as G56a sub-checks (same fail-soft-in-bootstrap-then-hard posture), because for a PUBLIC repo holding the most-damaging secret with no PR review they are free, load-bearing, and equally invisible to workflow-file analysis:** **(d)** GitHub native **secret-scanning + push-protection** is ENABLED (`gh api repos/:owner/:repo` → `.security_and_analysis.secret_scanning.status == "enabled"` + `.secret_scanning_push_protection.status == "enabled"`) — a free backstop to G2 for `MINISIGN_SECRET_KEY`/`MINISIGN_PASSWORD`/`ANTHROPIC_API_KEY` in a public repo; **(e)** the repo's **default workflow permissions** are read-only (`gh api repos/:owner/:repo/actions/permissions/workflow` → `default_workflow_permissions == "read"` AND `can_approve_pull_request_reviews == false`) — a workflow that omits a `permissions:` key inherits this default, so a permissive repo default silently widens every such workflow's `GITHUB_TOKEN`, invisible to G50's per-file scan; **(f) inter-procedural TAINT depth for the T2/WebView surface — an OR-GATE over the two sanctioned, owner-decidable options (r7 fix — was a self-contradicting hard CodeQL assert):** the Rust side has a deep bespoke SAST layer (`deny(unsafe_code)` + Semgrep `p/rust` + the project-local rules + `cargo-fuzz`) while the TS/WebView side — which IS the entire T2 attack surface — had only structural/pattern checks with **no inter-procedural taint** from a WebView-supplied value to a dangerous DOM/eval sink or an `invoke()` IPC argument. Sub-check (f) **PASSES if EITHER** (i) CodeQL default setup is configured — `gh api repos/:owner/:repo/code-scanning/default-setup` → `state == "configured"` for the `javascript-typescript` language pack (free + GitHub-native for the public `Ne-IA/convertia` repo, silently disable-able like secret-scanning) **OR** (ii) the **G29 rule (i)** committed Semgrep `mode: taint` ruleset is present under `scripts/semgrep-rules/` with its planted-positive registered in `scripts/gate-selftests/`; the fail message distinguishes the two cases ("neither CodeQL default-setup configured NOR the G29 rule-(i) taint ruleset present"). The two are **owner-decidable which ships — exactly ONE is required, not both, not neither** (the prior wording hard-asserted CodeQL `state == "configured"`, which permanently red-failed the build the moment the owner chose the explicitly-sanctioned G29-rule-(i) Semgrep path — an internal contradiction with the SAME gate's "owner-decidable, both not required" framing and the §5 T2 row). **plan-lint check 21 machine-enforces the XOR** (exactly one of {CodeQL-enabled-marker, G29-rule-(i)-present} is true) so the owner decision is not doc-only. **(g) require-signed-commits on `main` (r7 — closes the integrity axis tag-only signing leaves open):** today only `v*` release tags are signed (G56b leg 3) and every direct-to-`main` Build-Loop commit is UNSIGNED with a self-attested `Dual-Review:` trailer — an attacker with a leaked `GITHUB_TOKEN` could push a commit with a forged trailer + spoofed author that feeds the next legitimate release. The loop already provisions an SSH signing key + a committed allowed-signers file for tags (P0.7), so it REUSES that key to **sign its own loop commits** (`git config commit.gpgsign true` + the SSH signing key) and this sub-check asserts main's ruleset has a **`required_signatures` rule** (`gh api repos/:owner/:repo/rulesets`) — a free GitHub knob, the same invisible-config class as secret-scanning; **fail-soft in the P0 bootstrap box (commit-signing is wired mid-P0), then hard** (if the owner declines, commit authenticity is named as an explicit accepted residual in P0.6 rather than left unaddressed — but the key already exists, so adopting it is near-free). Without G56a, the autonomous Build-Loop keeps pushing to a red `main` and "CI is the backstop" is fiction with no gate noticing (security-concept §3 L4 / principle 10) | push + schedule | fail-soft only in P0 bootstrap, else fail-closed |
| **G56b** | Release-tag trust gate (the secret-bearing `v*` trigger that G56a does NOT cover) *(L5-authored; runs in CI on tag-creation, and the policy half on schedule)* | the minisign release job — the **only** thing holding `MINISIGN_SECRET_KEY`/`MINISIGN_PASSWORD` — fires on a **`v*` tag** (§3 L5; security-concept §3 L5 / spec §6.7.2). **G56a guards only the `main` branch ref (its rulesets) — it says NOTHING about tags.** In the autonomous direct-to-`main` model the loop (or a compromised `GITHUB_TOKEN`, or a stale/forced tag) can create a `v*` tag on ANY commit — **including one that never passed L4 green** — and mint a signed artifact: the exact "a red run must actually block" gap G56a closes, applied to the higher-value secret-bearing axis. **THREE legs:** **(1) tag-protection ruleset** — a GitHub **tag-protection ruleset on `v*`** is asserted via `gh api repos/:owner/:repo/rulesets` exactly as G56a queries branch protection (only the owner / a protected actor may create release tags; bypass actors enumerated and asserted minimal); runs on schedule + on tag, **fail-soft in the P0 bootstrap box, then hard** (the policy half). **(2) ancestry + green-history assertion (the release workflow's FIRST step, fail-CLOSED always)** — the tagged commit MUST be an **ancestor of `origin/main`** (`git merge-base --is-ancestor <tag-sha> origin/main`) AND **main's required checks were GREEN for that exact SHA** (`gh api repos/:owner/:repo/commits/<tag-sha>/check-runs` / the combined-status API — every G56a-required check `conclusion == "success"`), the release **aborts before any secret is read** otherwise. **(3) signed/annotated-tag verify — MADE REAL (r6, reconciled with spec §6.7.1):** the prior row required `git verify-tag` against "the committed signer key" but NOTHING in P0 ever provisioned, committed, or named that key, and the Build-Loop signs neither its commits nor its tags — so on a clean checkout leg 3 was **unsatisfiable**, yet it was labelled both "(recommended)" in the row AND "fail-closed always" in the fail-mode column (an internal disagreement on top of missing machinery). It is now CONCRETE and self-consistent: the Build-Loop **signs every `v*` tag** (`git tag -s` / an SSH-signed annotated tag) and the repo commits a **public SSH allowed-signers file** (`git config gpg.ssh.allowedSignersFile <committed file>`); the release workflow's first step runs **`git verify-tag <v* tag>`** against that committed allowed-signers file and aborts before any secret is read on failure, so a lightweight/unsigned tag planted by a compromised `GITHUB_TOKEN` cannot trigger a release. **This is distinct from the spec §6.7.1 DCO posture** — that is `Signed-off-by` *text* on external contributors' COMMITS, "requested not required, CI does not hard-block" (unchanged); leg 3 is the LOOP's own cryptographic signature on its OWN release TAGS (a different artifact, a different actor, no conflict). Legs 1+2 (tag-protection ruleset + ancestry/green-history) remain the primary load-bearing controls; leg 3 is the cheap belt-and-suspenders that is now actually runnable. The committed allowed-signers file is in the L(-1) security-critical-file set (an edit is a Co-Pilot escalation). Home the ruleset POLICY + the allowed-signers provisioning in P0.7, and the workflow ancestry/green-history + `verify-tag` ASSERTION in P0.2 (the release-workflow skeleton). Pairs with G56a (branch ref) as the tag-ref half of "the invisible repo config that makes a release actually trustworthy" | tag-creation + schedule | leg 1 fail-soft only in P0 bootstrap, else fail-closed; legs 2/3 fail-closed always |

**Fastpath / skip (L2 only).** Expensive hooks (G15/G16/G17/G18 + heavy lint) are
skipped **only** when provably irrelevant, via detectors that each default to
*run* on ambiguity:
- **Docs-only push** — the hard safety guard is the **RANGE diff over ALL unpushed
  commits** (`git diff --name-only @{u} HEAD`, fallback chain below), **not** the
  HEAD-commit subject: if every changed path across the unpushed range is markdown
  ⇒ no Rust/TS/lockfile to scan ⇒ safe to skip the byte-scanning gates. A code
  change in *any* unpushed commit forces the full gate even if HEAD is docs-only.
- **Check-off fastpath** — `chore(todo): … abgehakt`-style subject **AND** a
  markdown-only diff.
- Detector fallback chain: `@{u}` → `origin/<branch>` → `origin/main` →
  `origin/HEAD`; **no base / 0 unpushed commits ⇒ run the full gate.** Skipping is
  opt-in; anything ambiguous runs everything. **First-push-to-an-empty-remote note:**
  on the very first push (no `@{u}`/`origin/main`/`origin/HEAD` yet) the detector
  correctly fails **open** to "run everything" — this is the **normal** no-base case,
  **not** a hook failure, so the Build-Loop must not misread it as a red push and trip
  the 3-failures escalation on session 1.

> **Delivered (P0.2.13):** the byte-scan docs-only guard is [`scripts/fastpath-docs-only`](../../scripts/fastpath-docs-only)
> — pure `is_skip_eligible`/`is_docs_only` over the **net range diff** with the documented base
> fallback chain (`@{u}` → `origin/<branch>` → `origin/main` → `origin/HEAD`); an explicit
> **allowlist, default-DENY** (only `docs/**/*.md` + top-level `*.md` + `LICENSE`/`NOTICE` are skip-
> eligible — **`.md`-only by path** per the §4 invariant: a non-`.md` file even under `docs/` still
> forces the heavy gates, and any `..` path segment is denied); **exit 0 = docs-only/skip-eligible,
> exit 1 = must-run** (code-touching OR ambiguous OR
> 0-unpushed-commits OR no-base). It is the empty-but-wired contract the heavy L2 hooks (G13–G18+,
> P0.3/P0.4) consult; the **G2 gitleaks range leg is never gated by it**. The **check-off fastpath**
> is `is_docs_only ∧ a check-off subject` — the diff side reuses this detector, so the subject test
> is a 1-line add wired with the L2 cheap-commit consumer (P0.3+). Its G10 positive+negative smoke
> test is [`scripts/gate-selftests/test-docs-only-fastpath-pattern.py`](../../scripts/gate-selftests/test-docs-only-fastpath-pattern.py).
> **The L4 continuous-armed-canary** ([`scripts/run-gate-selftests`](../../scripts/run-gate-selftests))
> auto-discovers + runs EVERY `gate-selftests/*` self-test as a 3-OS prelude on every CI run (the
> former per-self-test ci.yml steps are consolidated into it), so a pinned-tool bump that breaks a
> stable script's parsing — or a new gate whose self-test was never wired — is caught (complements
> plan-lint check 16). Both new scripts are L(-1) security-critical.
- **The docs-only guard is `.md`-only by path, and the security-load-bearing NON-`.md`
  files are deliberately NOT skippable** — `tauri.conf.json`, `src-tauri/capabilities/*.json`,
  `engines.lock`, `deny.toml`, `.gitleaks.toml`, `.npmrc`, `.editorconfig`, `.typos.toml`, `.github/**`, the SBOM inputs
  are non-`.md`, so any change to them already forces the full gate via the `.md`-only
  test. This is stated explicitly (docs-only-push-detector style) so a future "docs-only
  also covers `.json` docs" relaxation cannot silently disable G47/G37/G18c.
- Cheap structural gates (G13/G20/G21/...) and glob-gated gates always run when
  their glob matches — they have nothing expensive to skip.

## 5. L4 — CI (GitHub Actions, post-push) & L5 — Release (`v*` tag)

### L4 — CI (clean checkout; mirrors L1–L2 + the heavy gates)
| ID | Gate | Tool / mechanism | Blocks |
|---|---|---|---|
| **G25** | All L1–L2 gates re-run on clean checkout | the same hooks/scripts in CI | red `main` |
| **G26** | Engine-side corpus fault-injection (T1 through the §2.12 boundary) | the §6.4.2 corpus/no-harm fault-injection **through the §2.12 isolation boundary** (truncated/0-byte/fuzzed-header/decompression-bomb inputs → one plain message, no crash, batch continues). It is a **fixed adversarial corpus, not coverage-guided** — so "fuzz" is reserved for **G48** (the real libFuzzer gate) and the engine-side coverage-guided idea is the §8 AFL++/radamsa forward item (reserved G65). *(`cargo-fuzz`/libFuzzer is in-process Rust and CANNOT reach the isolated C/C++ engines — the in-core fuzzable surface is **G48**; this row is the engine-side T1 control.)* **Positive output-validity leg (r7 — G26 asserted ONLY the negative "no crash"; the positive output-validity lives in G31, which activates per-phase as engines are staged, so between engine wiring and G31 corpus population an engine could emit structurally-INVALID outputs with no gate catching it):** G26's non-adversarial fixture set additionally asserts that, **for every staged engine, ≥1 NON-adversarial input produces output passing the same per-format STRUCTURAL readers G31 uses** (ffprobe decodable+codec / `vipsheader` decode+dims / poppler opens / `unzip`+`[Content_Types].xml` / the `csv` crate RFC-4180 parse) — not just "no crash". The fixtures already exist; only the structural readers on the RESULT path are added, closing the engine-staging activation-gap window. **engines.lock↔adversarial-fixture bijection guard** (every staged engine has ≥1 fault-injection fixture targeting its type, hard-fail otherwise — incl. ImageMagick, the densest-CVE decoder). **CI-HOST resource bound (r7 — the decompression-bomb fixtures feed the REAL C/C++ decoders on the SHARED self-hosted VPS four other projects use; G48's libFuzzer legs pin `-rss_limit_mb`/`-max_len`/`-timeout`/`-max_total_time` so an OOM is a committed finding not a runner kill, but the engine-side corpus had NO equivalent host bound, and §2.12.3's memory-cap-kill is an APP behaviour not a CI-host bound):** the G26/G31 corpus jobs (and the G65 black-box engine fuzz) apply G48's pinned-resource discipline at HOST level — explicit per-job **memory / disk / wall-clock bounds via cgroup or `ulimit`** (`ulimit -v`/`-f`/`-t`, or `systemd-run --scope -p MemoryMax=…` / `docker --memory --pids-limit` on the VPS leg) + the G56 `timeout-minutes` — so a corpus-induced host OOM/disk-fill is a CONTAINED finding (job fails clean, host recovers), not a shared-host outage. | red `main` |
| **G27** | Coverage — per-domain floors | **`cargo-llvm-cov`** (Rust, LLVM branch) **and** **vitest v8** (TS) — **separate** floors, **per-domain = per-crate** (Rust) / per-package (TS), fail if **ANY** is below its floor (never averaged); ratchet **50 % → 70 %** stored in a tracked file (can only increase; a commit that lowers it fails; raises are deliberate committed config changes — no auto-increment). **BRANCH-coverage floor for the three security-critical crates (r7 — line coverage is the wrong metric where the consequence is a missed BRANCH: a `fs_guard` path-traversal guard rejecting `../` but not `..\` has 100 % LINE coverage with only Unix tests yet leaves the Windows-separator branch untested):** `crate::detect` (in-core untrusted bytes), `crate::fs_guard` (the no-harm kernel), and `crate::isolation` (the subprocess wrapper) additionally carry a **BRANCH-coverage floor** via **`cargo-llvm-cov --branch`** (the branch metric, not line), ratcheting like the line floor (50→70, increase-only) — so an untested platform/error branch in exactly the highest-consequence surfaces is a deterministic floor failure, not a line-coverage false-green. **Initial value + activation:** the tracked file is created at **0 %** in P0 (there is no app code yet) and **enforces from P1** (annotated `→ activated in P1`) so it does not trip on the empty P0 tree; **gate scripts are excluded from the coverage floors** (they are G24-self-tested instead — the explicit exclusion glob `scripts/` + `.github/` is wired into the `cargo-llvm-cov` and vitest v8 invocations from first activation, so the exclusion is code-enforced, not a prose promise that would otherwise inflate the initial P1 percentage). **Shard-merge determinism (3-OS matrix):** each leg emits a **named partial** report, merged in a **fixed order**, and the floor is applied to the **merged** report only (avoids the last-writer-wins race). The G48 saved-crash-corpus replay does **not** count toward the Rust floor. **Delivered (P0.4.8):** [`scripts/check-coverage`](../../scripts/check-coverage) (stdlib, the P0.4.1 two-tier pattern) + the tracked [`coverage-floors.toml`](../../coverage-floors.toml) (created at 0 %, the increase-only ratchet file, **L(-1)** — added to the cage, the `l-neg1-cage` fingerprint re-blessed `b27d2c66…`→`e5e4cbde…` + the §2/G71 prose synced). FREEZE: `[meta].diff_floor`==80 + `target_line_floor`==70 + the `BRANCH_FLOOR_DOMAINS` contract (`crate::detect`/`fs_guard`/`isolation`). DECREASE-GUARD (increase-only): a lowered `[line]`/`[branch]` floor vs the prior committed version (`git show <base>`) fails — the authoritative leg is L4 `workflow-security` (`--base github.event.before`, full history); empty floors in P0 ⇒ no-op. LIVE per-domain/diff tier (`check_floor`, never averaged) is target-absent until the P1 cargo-llvm-cov/vitest reports. `g24-coverage.py` = 22 legs; canary 30→31; L1+L2+L4. G1 (volle Härte) GO; the P2 fixed: `decrease_guard` also fails a REMOVED floor (not just a lowering) + `freeze_floors` rejects a RETYPED (non-numeric) floor — both un-gate a domain. | red `main` |
| **G28** | Coverage — diff gate | **≥ 80 %** on changed lines (change-only) so new code can't dilute the floor. **Delivered (P0.4.8):** the `diff_floor`==80 is frozen by `check-coverage` (`coverage-floors.toml [meta]`); the live changed-lines coverage check activates in P1 with the reports. | red `main` |
| **G29** | SAST / static security | **unsafe-policy gate (primary):** `#![deny(unsafe_code)]` at the crate root of **every first-party Rust crate (the core AND `convertia-imgworker`)**, with a **single narrowly allow-listed FFI module** carrying `#[allow(unsafe_code)]` (each `unsafe extern` with a `// SAFETY:` comment). `#![forbid(unsafe_code)]` is **NOT usable here** — `forbid` is deliberately un-overridable (a module cannot re-permit `unsafe` via `#[allow(unsafe_code)]` under `forbid`; that is the whole semantic difference from `deny`), so an FFI-linking crate (imgworker → libvips/libheif/libde265/librsvg/libimagequant; the core → the §2.1/§2.3 OS primitives `renameat2`/`MoveFileExW`/`GetFileInformationByHandle` + the §0.9 Job-Object kill) under `forbid` **would not compile**; `forbid` is reserved ONLY for a pure-logic sub-crate with ZERO unsafe. The imgworker is the **densest unsafe surface** in the product (the first Rust code touching untrusted image bytes across a C/C++ FFI boundary). The gate is "**no new `unsafe` outside the allow-listed FFI module**", enforced by **deny-at-root + a grep/Semgrep check that `#[allow(unsafe_code)]` appears on EXACTLY the one allow-listed module path** (a new `#[allow(unsafe_code)]` anywhere else fails). **Semgrep** (pinned by version in `requirements-ci.txt`; **rulesets committed under `scripts/semgrep-rules/` at the pinned registry version**, regenerated on bump — `p/security-audit` is a managed registry pack fetched live from `semgrep.dev`, which **breaks under network isolation**, so the offline gate uses the community packs **`p/rust` + `p/typescript` + `r/bash` + `p/python` + `p/owasp-top-ten`** vendored as committed files and invoked via **`semgrep --config scripts/semgrep-rules/` (NEVER `--config p/rust`, which network-fetches)** — the distinction between the **Semgrep BINARY version** (pinned by the `pip --require-hashes`) and the **rule-pack version** (pinned by VENDORING) is explicit so a fill-pass cannot use the live-fetch form, with a **`semgrep --validate scripts/semgrep-rules/` self-test on any rule/version change** (the OWASP pack vendored under `scripts/semgrep-rules/` at the pinned registry version exactly like the others — its injection / path-traversal / SSRF taint rules directly reinforce T2a/T2b/T9b; ~500 rules mapped to the OWASP Top 10) plus the committed **project-local ruleset**: (a) a `#[tauri::command]` taking `PathBuf`/string-as-path from the WebView **without** an adjacent `fs_guard` validation call fails (T2a/T2b "forgot to validate"); (b) any `std`/`tokio` `process::Command::new` in `crate::isolation`/`crate::engines` **without** a `.env_clear()`/`.envs([])` in the same chain fails, and a `pandoc`/`soffice`/`ffmpeg` argv missing its mandatory safety flag fails: **pandoc `--sandbox` AND `--resource-path`** where the **`--resource-path` VALUE must resolve to the per-job scratch variable** (a presence-only check is insufficient — `--resource-path /` or `~/.local/share/pandoc` / `%APPDATA%` / `%USERPROFILE%` would pass a name-based rule while pandoc reads out-of-input user data, so the rule REJECTS an absolute literal / `~/` / `/home/` / `%APPDATA%` / `%USERPROFILE%` value and requires the scratch variable; planted-positive: a literal absolute `--resource-path` MUST fail; note per §3.5.4 this is belt-and-suspenders read-scoping, not a live-filter-execution closure — ConvertIA configures no Lua/JSON filters); **`soffice` `-env:UserInstallation=file://<per-job scratch>`** PRESENT with a value pointing into the per-job kind-2 scratch dir (planted-positive: a `soffice` spawn missing the flag MUST fail) **and NO `--accept`** (which would open the UNO loopback socket on every machine); **FFmpeg `-protocol_whitelist file,pipe`**; **the imgworker `MAGICK_CONFIGURE_PATH` is set to the bundle policy dir** (so MagickCore reads the bundled hardened `policy.xml`, not the system/no policy — §3.5.5/G38; a positive-presence assertion in the imgworker's `.envs([])` chain, planted-positive: an imgworker spawn without it MUST fail) — making the §5 T9b/G42 "every spawn is sanitized" a **structural** invariant, not just the one unit-tested spawn; (c) `process::Command` constructed outside `crate::isolation`; (d) **macOS T11 — every `Command::new` in `crate::isolation` under `cfg(target_os="macos")` is preceded by the `stage_for_tcc`/scratch-staging call** (so the engine never first-touches a TCC-protected path; spec §3.5.0/§7.2.6); (e) **single-store-name (T2c) — every `tauri_plugin_store` / `Store.load(...)` call references the single compile-time store-name constant and there is exactly ONE `Store.load` call site** (an XSS/supply-chained second store call is otherwise granted by the `store:default` capability, spec §0.10); (f) **no `name`/`rename_all` override on any `#[tauri::command]`** — Tauri allows it, but it would make the registered IPC name differ from the Rust function name and silently defeat plan-lint check 12's function-name diff, so it is forbidden project-policy and the function name IS always the IPC name); (g) **T9a per-push Rust-source structural net-ban — any `use std::net::` / `use tokio::net::` (or a fully-qualified `std::net::TcpStream`/`tokio::net::TcpListener` reference) outside a committed allow-list fails** (an initially-EMPTY `scripts/semgrep-rules/net-allow-list.txt`, so ANY match = unconditional fail), with a planted-positive (a fixture file containing `use std::net::TcpStream;` MUST fail G29). This is the **only per-push, Rust-source-level proof that first-party Rust opens no socket** — G18's name-based `[bans]` is defeated by a renamed/transitive crate reaching `std::net::TcpStream` directly, G47 is the Tauri-config side, and G42 is release-only; rule (g) closes the gap between them. **Honest-limitation note (r6):** rule (g) proves no socket in pure-Rust code **at `std::net`/`tokio::net` import sites only** — a first-party crate CAN open a socket WITHOUT those imports via `libc::socket()` raw syscall / `nix::sys::socket` / an unsafe FFI call (which the imgworker's unsafe surface allows); that FFI path is now closed per-push by **rule (j)** below (promoted from "optionally" to REQUIRED in r7), with the G26/G42 egress corpus + the owner-decidable `cargo-acl`/cackle cap (§8) as the release-tier / behavioural backstops. (The behavioural `cargo-acl`/cackle upgrade — a per-crate `std::net`/`std::process` capability policy over the whole dep graph — remains the §8 owner-decidable upgrade; rules (g)+(j) are the cheap cross-platform per-push baseline that ships now.) **(j) raw-socket FFI net-ban (r7 — REQUIRED, closes the rule-(g) honest-limitation hole on exactly the densest-unsafe crate):** a project-local rule flagging **`libc::socket` / `libc::connect` / `libc::bind` / `nix::sys::socket::*`** anywhere outside a committed allow-list (the same initially-empty-allow-list discipline as rule (g)), so a first-party crate — notably the imgworker's `#[allow(unsafe_code)]` FFI surface — cannot open a socket via the raw-syscall path that rule (g)'s `std::net`/`tokio::net` import-site scan structurally misses, with a planted-positive (a fixture calling `libc::socket(...)` MUST fail G29). Rules (g)+(j) together are the per-push structural proof that first-party Rust opens no socket by EITHER the std/tokio import path OR the raw libc/nix FFI path. **(h) opener-egress constant-only (r6) — every `OpenerExt::open_url`/opener call passes a COMPILE-TIME CONSTANT** (the only sanctioned egress path is the user-initiated Releases link, which G42's socket-based egress check structurally cannot see — a Rust-side `opener::open` of a WebView-supplied or computed URL would be an un-observed egress), with a planted-positive (an `open_url` of a non-constant value MUST fail). **(i) TS/WebView TAINT-mode ruleset for the T2 surface (r7 — the deepest threat class deserves a real verifying gate; the equivalent depth to CodeQL if G56a's CodeQL default-setup is rejected, owner-decidable which one ships — both are not required):** a committed Semgrep **`mode: taint`** project-local ruleset vendored under `scripts/semgrep-rules/` whose **sources** are WebView-controlled values (the `@tauri-apps/api` `invoke(...)` inputs + DOM `event` values) and whose **sinks** are dangerous DOM/eval-adjacent ops (`innerHTML`/`outerHTML`/`eval`/`Function(...)`/`document.write`) AND the IPC arg-marshalling sites where a WebView value crosses into Rust as a path/argv — closing the "no inter-procedural taint from a WebView value to a sink/IPC arg" gap that the structural rules (a)/(f) cannot reach (they prove the shape, not the flow); ships its OWN planted-positive in the G29 prelude (a tainted `invoke` input reaching `innerHTML` MUST be flagged). **Per-Semgrep-RULE planted-positives (the project-local rules (a)-(g) are the load-bearing T9b/T11/T2c/T9a structural controls, and a Semgrep version bump could silently stop a rule MATCHING — G24/check-16 cover gate SCRIPTS, but the RULES need the same armed-canary):** each project-local rule ships a **deliberately-violating fixture** (a `#[tauri::command]` without `fs_guard`; a `Command` without `.env_clear()`; a `pandoc` argv with `--resource-path` pointing at an absolute/home path; a LibreOffice argv with `--accept`; a LibreOffice argv MISSING `-env:UserInstallation`; an imgworker spawn MISSING `MAGICK_CONFIGURE_PATH`; a macOS `Command::new` without `stage_for_tcc`; a second `Store.load`; a `use std::net::TcpStream`), run in the **L4 self-test prelude** (the `--self-test` discipline — run as the FIRST step of the SAME CI job, same pinned-tool/runner image, immediately before the gate's real invocation; a different-job self-test does NOT satisfy G24/check-16) — a rule that stops matching its own planted violation FAILS, even on a clean run where no real code changed. **`shellcheck` runs over ALL committed `.sh` gate-scripts/hooks** (`actionlint` only shellchecks workflow `run:` steps, not the standalone scripts, which are themselves code per G24) and an `r/bash`/`p/python` leg over `scripts/`+hooks (the registry has **no** `p/bash` pack — HTTP 404 — so `r/bash`, its bash ruleset, is the faithful realization, P0.4.2). **`cargo-geiger` is INFORMATIONAL only** (a census, not an enforcer — version-fragile; never a required green check). *(Windows caveat: `.env_clear()` does NOT address PATH/DLL-search-order / `APPINIT_DLLS`/IFEO — those are the T3a side-loading controls, G37/G37b; G42's `.env_clear()` invariant is scoped cross-platform but the Windows DLL-search defence is the bundle-only `PATH`.)* **Delivered (P0.4.2):** the **unsafe-policy primary gate** [`scripts/check-unsafe-policy`](../../scripts/check-unsafe-policy) (pure-Python, cross-platform, the P0.3.6 target-absent pattern: a frozen *narrow* FFI-module allow-list + the deny-at-root / allow-only-in-the-one-FFI-module / forbid-has-no-allow / `// SAFETY:`-comment scan that fail-closes once the P1/P4 crate roots land — seeded with the §0.7 core `platform/` + imgworker `ffi` surfaces; a catch-all allow-list is rejected by a non-FFI-core sentinel set) wired **L1+L2** (lefthook) + **L4** (gate-tooling, 3-OS), with `g24-unsafe-policy.py` (17 legs). **Semgrep `1.167.0`** is hash-pinned with its full 66-package transitive tree in `requirements-ci.txt` (the FIRST pip gate tool; the `--require-hashes` install proven clean-room in pinned ubuntu/py3.12) and **shellcheck `0.11.0`** in `gate-tools.toml`. The 5 packs are vendored offline under `scripts/semgrep-rules/vendor/` (`--validate` = 583 rules) and the **15 project-local rules (a–j)** under `scripts/semgrep-rules/project/` each ship a planted-positive fixture under `scripts/semgrep-rules/fixtures/` — all 15 fire on their canary. The Semgrep+shellcheck driver [`scripts/check-sast`](../../scripts/check-sast) (the L4 `sast` CI job, ubuntu-only — a Linux source-scan, G56 cost discipline) runs the **armed-canary self-test prelude** (every project rule MUST fire on its planted-positive, FIRST in the same job) → `--validate` → the offline `--config scripts/semgrep-rules/` scan (target-absent until P1; net-ban findings filtered through the empty `net-allow-list.txt`) → shellcheck over committed `.sh`; `g24-sast.py` (30 legs) proves the pure logic in the 3-OS canary. `scripts/semgrep-rules/**` was added to the L(-1) cage (the G68 `l-neg1-cage` fingerprint re-blessed `745b0ea2…`→`b27d2c66…`); `check-branch-protection`'s G56a taint-OR-gate now recurses and **finds the rule-(i) taint ruleset** (`project/webview-taint.yaml`), and `sast` was added to `EXPECTED_REQUIRED_CHECKS` (the owner adds the context to branch protection). **Forced deviation (DoD item 2):** the contract named `p/bash`, which does NOT exist in the Semgrep registry (HTTP 404) — realized with `r/bash` (the registry's bash ruleset) and this row updated in the same commit. **Soundness (G1, 4 rounds — every spawn-hardening rule's suppression is CHAIN-ANCHORED-ONLY** so no region/fn/sequence not-inside can swallow a sibling/interleaved spawn (each fail-OPENED in review); **rule (d) has NO suppression** (stage_for_tcc is off-builder → any preceded-by form fail-OPENs) and is fails-SAFE over-strict (fires on every spawn; the precise per-path staging dataflow + `cfg(macos)` scope are P1); a `Command::new` nested in a macro arg is a Semgrep-matcher blind spot, backstopped by `scripts/check-sast`'s grep `macro_nested_commands` (the common no-internal-`;` expression-macro forms). Residuals are fails-SAFE over-strict (split-builders redden; (d) fires on all), NEVER fail-OPEN **except** the documented `Command::new` preceded by an internal `;` inside a macro-argument block/closure (any delimiter) — a matcher-gap the grep's `;`-boundary also misses, closed by the **P1** grep-every-`Command::new`↔rule-(d)-matched-set reconciliation (cannot run target-absent).) → live deny/allow + the Semgrep `--config` scan over real source **activate in P1**. | red `main` (Semgrep + unsafe-policy + shellcheck block; geiger informational) |
| **G30** | Cross-platform build matrix | native build on Windows / macOS / Linux runners (no cross-compile); macOS universal `lipo`. **Universal-sidecar fat-Mach-O assertion (the silent single-arch case neither Tauri nor the CI matrix surfaces):** Tauri's macOS universal build requires each sidecar pre-`lipo`'d and named `<name>-universal-apple-darwin`, but Tauri does **NOT** itself call `lipo` (spec §6.1.3) — so if `scripts/stage-engines` produces a single-arch sidecar yet names it `*-universal-apple-darwin`, Tauri bundles it without error and the user on the OTHER architecture gets a silent illegal-instruction crash, indistinguishable from a working build at CI level. So before `tauri build` on the macOS universal leg, run **`lipo -info <binary>` per staged `*-universal-apple-darwin` sidecar and assert BOTH `arm64` AND `x86_64` slices are present** (a single-arch fat-named binary hard-fails). One line per sidecar | red `main` |
| **G31** | Per-pair corpus + reliability | real-file round-trips per `(source→target)` pair per platform; **output validated by a REAL per-format structural check** (re-detect output magic via §1.2 **plus** `ffprobe` decodable+correct codec for A/V · decode+nonzero dimensions via `vipsheader` for images · `pdftotext`/poppler opens for PDF · `unzip` + well-formed `[Content_Types].xml` for OOXML · for CSV/TSV: **parse the output with a real RFC-4180 reader** — the `csv` crate, already in-workspace for the §3.5.6 engine — not a bare field-count parity, which passes on mis-quoted/embedded-newline output that is unparseable, AND assert the corpus's leading `=`/`+`/`@` injection cells are **preserved literally as text** = CSV-injection non-execution on the output side) — **not** a bare magic-sniff or "file exists"; reliability threshold. **Non-empty / output≠input / size-plausibility sub-assertions (close the "exit 0 but produced an empty / passthrough / 4-byte-stub output" class — a valid-header blank/all-grey image, a zero-text image-only PDF where text was expected, or the engine silently passing the input bytes through all pass a structural-openability check):** per pair, additionally assert **(1)** output file size `> 0` and within a sane factor of source for the relevant pairs (e.g. DOCX→PDF `> 1 KB`; lossless PNG→BMP within ~10× of source); **(2)** where `src_format != tgt_format`, `sha256(output) != sha256(input)` (catches passthrough); **(3)** for a text-source→PDF, `pdftotext` returns a **nonzero** byte count, and for an image-heavy PDF `pdfimages -list` reports ≥ 1 image; **(4)** for images, an "output has content" proof (`vipsheader`/`vips stats` pixel-variance above a threshold — NOT PSNR/SSIM, which compares to a reference; this is a self-contained non-blank check). **(5) DOCUMENT→IMAGE pairs need an ACTUAL CONTENT check — the size-floor fallback is REMOVED (r7):** a near-white rasterised text page (PDF→PNG via poppler, DOCX→PNG via LibreOffice) passes the pixel-variance threshold (4) trivially while being content-less, and a size floor — even a per-fixture one — passes on a correctly-SIZED image of entirely WRONG content (a font-subsetting failure, a headless-display-absent blank raster — plausible for bundled LibreOffice headless). So for every document→image pair the corpus fixture carries a **required `expected_text` field in the corpus manifest** (a known source text string) and the rasterised output is verified to **CONTAIN it** via **OCR (`tesseract <out.png> - --psm 6`)** — or `pdftotext` where a PDF intermediate exists — asserting the committed known-text string appears in the output. The **size-floor "OR at minimum" fallback is dropped** (it could not distinguish correct rendering from a valid-but-wrong-content raster); the alternative is always an actual content check, never a size floor. If per-push OCR cost is a concern, **scope the OCR leg to L4** with a committed min-confidence threshold — but it stays a content check. `expected_text` is a **required corpus-manifest field per document→image fixture** (a document→image fixture without it fails the corpus-manifest lint). One-liners per pair; they turn "structurally valid" into "structurally valid AND non-trivial AND content-bearing". **Cross-library decode validation for the headline formats (break the circular libheif→libheif check):** AVIF/HEIC outputs are validated a SECOND time with a **DIFFERENT decoder family** — **`ffprobe`** (FFmpeg decodes AVIF via the dav1d/AV1 path and HEIC via the HEVC path, distinct from the libvips/libheif that produced the file; verify the staged FFmpeg build actually carries these decoders, else skip that pair's cross-check rather than false-pass) — and **animated WEBP via `ffprobe`** (NOT `dwebp`, which explicitly does not decode animated WebP), so a libheif/libvips encoder bug producing a structurally-valid-but-subtly-wrong file that the producing library tolerates but other readers reject is caught (additive, low cost — `ffprobe` is already bundled). **macOS T11 first-accessor sub-test:** assert the **Rust core PID (not the engine PID) is the first process to access** a TCC-protected source path, and the engine receives a per-job **kind-2 scratch** path (spec §3.5.0/§7.2.6) — pairs with the G29 macOS Semgrep rule. **Hosts these adversarial/security integration assertions when their input phase exists:** the fs-safety/membership/freeze/redaction/temp-mode/resource-budget assertions (T2a/T2b/T4/T9b/T10); **T8 self-feeding / batch-expansion (the live-walk leg — G15 is the data-structure unit leg, this is the end-to-end proof)** — a batch whose conversion writes outputs INTO the same watched/dropped source folder mid-run asserts the fresh outputs are **NOT** in the run's ingest/result set (snapshot-not-live-iteration, §2.4.2), and a **two-instance fixture** where instance B drops a `file`/`*.part` into a shared folder mid-run asserts instance A's frozen set never grows (§2.4.3 concurrent-instance hand-off); **T9b sentinels** — a `.docm`/`.xlsm`/`.pptm` with an `AutoOpen`/`Workbook_Open` macro writing a canary file inside the egress-deny window → canary **NOT** created (proves LibreOffice macro-suppression), a `WEBSERVICE()` `.xlsx` → no egress + no out-of-input read, and a **crafted BMP (the densest-CVE ImageMagick decoder path) + an SVG carrying an MSL/URL-coder reference → no egress + no out-of-input read inside the G42/G42b window** (the ImageMagick coder-class sentinel, proving the §3.5.5 hardened-policy/coder-exclusion is effective) — all pulled forward from §6.4.2 into the per-push leg; **a MIXED-BATCH LibreOffice state-leak fixture (r7 — the corpus covers single malformed inputs + T8 self-feeding but NOT a mixed batch where file N is malformed and file N+1 is valid through a SHARED `soffice`-headless process; whether file N's error leaks state into N+1 — a shared instance, a corrupted isolation wrapper — is only testable with a mixed batch):** a batch where file N is malformed and N+1 is valid asserts **(1)** all valid files produce valid outputs, **(2)** malformed files fail cleanly, **(3)** the valid-file results are IDENTICAL to an isolated single-file run (no cross-file state leak through the shared LibreOffice process); **T7 INPUT-side symlink/junction** — a dropped folder containing `innocent.jpg -> /etc/passwd` (or a Windows junction → System32) is `resolve_identity`-resolved before the engine sees it: assert the engine receives the **resolved real path**, the frozen-set records the resolved identity (symlink+target dropped together convert once), a resolved non-convertible target fails clearly per §2.8; **Windows AV-retry** (§2.1.2) — a fault-injection test thread holding a handle on the publish target asserts the bounded `MoveFileExW`/`ERROR_ACCESS_DENIED` retry fires and recovers or fails to a clean `WriteFailed`, never a panic/silent discard; **privilege-drop-tier-applied regression** — a positive assertion per platform that the §2.12.3 tier actually FIRED (a denied syscall/socket/exec is refused inside the engine's own sandbox profile) so the silent-by-design degrade can't disable seccomp/Landlock/AppContainer on every run unnoticed (the §6.4.2 probe asserts availability; this asserts application), recording "tier applied vs degraded" per platform in the release evidence; **T10 process-group/Job-Object reap** — a deliberately-hanging/child-spawning sidecar is reaped by the §0.9 Job-Object/process-group kill with no orphan/zombie left and handle count returning to baseline; **§2.12.3 memory-cap kill** — a fixture making an isolated engine exceed its §2.12.3 Job-Object/`RLIMIT` MEMORY budget mid-conversion MUST be killed to a clean `Failed(TooBig|EngineHang)` (the batch continues, host RSS returns to baseline) — the memory-budget analogue of the privilege-drop-tier-applied and Job-Object-reap positive assertions | red `main` |
| **G32** | Source-unchanged + output-validity invariant | replaces the methodologically-vacuous "A→B→A byte-stable / within tolerance" (most of the catalogue is **one-way** — HEIC→JPG has no JPG→HEIC; XLSX→CSV→XLSX routes the reverse through LibreOffice, lossy — so a naive round-trip is either trivially-true under any tolerance or always-false). **Two sharp invariants instead:** (a) **SOURCE-UNCHANGED** — `sha256(source_before) == sha256(source_after)` on **every** corpus file (the no-harm proof, T2/T7); (b) **OUTPUT-VALIDITY** — the produced output passes the **real per-format structural check** of G31 (not magic-sniff). The literal A→B→A byte-stable check is **scoped explicitly to the small truly-lossless invertible set** (e.g. PNG→BMP→PNG, FLAC↔WAV if the pair exists). **The covered set is MACHINE-ENUMERATED, not a prose promise:** a committed **machine-readable lossless-round-trip-pairs file** (a section in `tests/corpus/manifest.toml`) enumerates every byte-stable pair + a one-line rationale; G32 READS it and FAILS if a listed pair does not round-trip exactly, OR if a pair is added without a rationale — so the Build-Loop cannot assert a round-trip for a pair that is NOT truly lossless (e.g. PNG→TIFF→PNG where TIFF strips ICC) with no gate catching the misclassification. **Plus a pure-logic lossy-disclosure property test (separate from the per-corpus-file integration check):** a property test in `crate::outcome`/`crate::format_registry` iterates the COMPLETE `FormatId × FormatId` product asserting `lossy_disclosure(src, tgt) == is_lossy(src, tgt)` — catching "added a pair but forgot the lossy flag" over the whole matrix without needing a corpus file per combination (the §6.4.3 per-corpus-file check only proves it for the files present). Everything asymmetric relies on (a)+(b). "Tolerance" is no longer an undefined fudge — there is no tolerance band, only structural validity. **(c) CONVERSION-OUTPUT DETERMINISM** — run the same source + same settings TWICE and assert `sha256(out1) == sha256(out2)`. **The floor is ≥ 1 pair per engine PER OUTPUT-FORMAT CATEGORY (r7 — "≥1 pair per engine" was too weak: FFmpeg/LibreOffice each emit MANY distinct formats and determinism in one pair does not imply it in another — a different muxer/writer can embed a timestamp or uninitialized padding, a privacy + §2.5 re-run-equivalence concern):** for FFmpeg = audio / video / container; for LibreOffice = word-processing / spreadsheet / presentation; for libvips/ImageMagick = per-colour-space — each category enumerated in the corpus manifest so plan-lint can check the floor is met **per platform** — the enumerated determinism pairs run on all three native CI legs (§6.4.4), and a category whose only pair is §3.4-unavailable on a platform is covered by another available pair in that category on that platform, so a platform-specific non-determinism (an OS encoder build embedding a timestamp the others do not) cannot hide. Non-determinism signals uninitialized memory, an embedded-timestamp leak (a privacy concern for an offline app), or a randomized path that breaks §2.5 re-run-equivalence. **Plus a "no embedded conversion timestamp" assertion via `diffoscope` (the reserved G60 tool) on the double-run output pair, asserting an EMPTY diff** — `sha256` inequality alone tells you outputs differ but not WHY; `diffoscope` localises it to the embedded timestamp / XMP creation-time / padding bytes so the leak is diagnosable and a PDF XMP `CreateDate` leak (which a naive sha-compare would also catch but not explain) is named. The empty-diff assertion is the positive determinism proof; the per-category sha-pair is the cheap front. For known-non-deterministic encoders (VP9 rate-control, AVIF variable encode) the exception is **documented in the corpus manifest** and the byte-level check skipped for that pair while structural validity (b) is still asserted. **Non-vacuous-exception guard (r6 — so the exception list cannot silently mask a regression):** for EVERY pair listed as a known-non-deterministic exception, the double-run MUST actually produce DIFFERENT `sha256` values at listing time — a planted-positive: if `sha256(out1) == sha256(out2)` for an "excepted" pair, the exception is **vacuous and must be removed** (the pair became deterministic after an encoder upgrade, or was added as a workaround for a real non-determinism bug — e.g. an embedded-timestamp privacy leak that should be FIXED, not excepted). Run at L4 on a schedule. Per-corpus-file cost: one extra conversion | red `main` |
| **G33a** | a11y — ARIA/role/focus (per-push) | **`vitest-axe`** (axe-core under jsdom) over the rendered React tree: ARIA-role/state validity + focus-order / roving-tabindex sanity. Lane-A per-push (jsdom cannot compute contrast). **`→ activated in P1` (the rendered React tree does not exist until P1+; carries the same bootstrap annotation as G47/G27/G42 — fail-open/skip-with-warning when the target is absent in P0, fail-closed once the React tree exists, so the P0 green-L4 exit criterion is not blocked by a gate targeting absent code)** | red `main` |
| **G48** | In-core untrusted-byte fuzz | **`cargo-fuzz`** (libFuzzer) targets over the in-core surfaces that process untrusted bytes **outside** the §2.12 boundary (§2.12.4), so a panic/OOM/UB lands in the core: **(1) `crate::detect`/sniff** on a hostile corpus (malformed ZIP central-directory, OLE2/CFB, gzip/svgz, XML) — no panic/abort, the §1.2 decompression-ratio cap (≤100×) + `MAX_SVGZ_SNIFF` (≤64 KiB) bound actually fire, the XML reader has **DTD/external-entity resolution disabled by construction** (`quick-xml`/`roxmltree` with entity resolution off — defeats XXE / billion-laughs in the `xl/workbook.xml`/ODS `content.xml` peek); **(2) `crate::fs_guard::resolve_identity`/`is_safe_output`** on untrusted PATHS from WebView/OS drag-drop (null bytes, overlong UTF-8, max-length, symlink chains, `..`) — no panic, structured `Err` on bad input, **never `Ok` on a null-byte path** (T7+T2a). **Deterministic bound-firing fixtures (its sibling targets have these; this one must too — libFuzzer "no crash" cannot prove a guard is REACHABLE):** two committed G16 fixtures — a **NUL-byte path MUST return `Err`** (every platform incl. Windows) and a **`PATH_MAX`+1 path MUST return `Err`** — so the null-byte and path-limit guards are structurally proven reachable, not fuzzer-hoped. **Windows dangerous-path classes (absent from the POSIX path space target — add ≥1 deterministic fixture per class):** **device paths** (`\\.\`, `\\?\`), **reserved filenames** (`CON`/`NUL`/`COM1-9`/`LPT1-9` with ANY extension — `CON.jpg` opens the console device, not a file), **drive-relative** (`C:foo` vs `C:\foo`), **UNC** (`\\server\share`), **trailing dots/spaces**; **(3) the in-core CSV/TSV native engine** (§3.5.6 `EngineProgram::InProcessNative` — memory-safe ≠ panic/OOM-safe; gigabyte quoted fields, recursive quoting, NUL bytes) — no panic, bounded output relative to input, clear failure beyond a column floor; **(4) archive-entry-name (zip-slip) target** — a crafted OPC/ODF/ZIP whose central-directory **entry names** contain `../`, absolute paths, NUL / overlong-UTF-8: the in-core peek never resolves or writes outside its bounded buffer, never panics, and (if any extract path exists) never escapes the per-job scratch root (distinct from (1) which mutates the directory STRUCTURE and (2) which fuzzes OS-supplied PATHS — this fuzzes attacker-controlled archive entry names) — **with a deterministic zip-slip bound-firing G16 fixture** (a ZIP whose central-directory entry is `../../etc/passwd` ⇒ the in-core §1.2 OPC peek produces only a *detection result* and reads/writes nothing at the traversal path). **(5) each `#[tauri::command]` handler's serde deserialization boundary** — a `proptest`/`cargo-fuzz` target driving each command handler directly with **malformed `serde_json`** (null-byte strings, `MAX_USIZE` arrays, deeply-nested JSON, NaN/Inf) asserting a **structured `Err`, never a panic** — this is the WebView→Rust type boundary that G29's *structural* Semgrep cannot catch (it proves the shape, not the runtime deserialization behaviour). **Sanitizers (memory-safe Rust ≠ UB-free across FFI):** `cargo-fuzz` builds with **AddressSanitizer ON** (libFuzzer's default — kept explicit and asserted-not-disabled, since `--sanitizer none` would silently drop it) on the Linux+macOS nightly legs, **plus a UBSan leg** where the toolchain supports it; a small **`cargo miri` leg** over the pure-logic in-core CSV/TSV + detect paths catches integer-overflow/wrapping/UB that `--release` suppresses (Miri does not run the C/C++ FFI, so it covers the safe-Rust side; ASAN covers the FFI boundary). **Bound-firing must be STRUCTURALLY proven, not fuzzer-hoped** (libFuzzer "no crash" cannot distinguish "cap fired" from "cap code never reached"): two deterministic **G16 saved-corpus fixtures** assert the bounds — a gzip whose uncompressed size is exactly 101× compressed ⇒ bounded `Err`, not a full inflate; a svgz exactly `MAX_SVGZ_SNIFF + 1` compressed ⇒ the sniff stops at the limit. **Crash-corpus persistence + the CONCRETE cross-platform replay mechanism (r7 — the "runs on every platform incl. Windows" claim named no HOW; NB the premise that libFuzzer can't build on Windows is WRONG — libFuzzer works on Windows via the MSVC AddressSanitizer, cargo-fuzz adds the `/include:main` linker arg — but the chosen design is plain replay, which must still be mechanically concrete for the fill pass):** every libFuzzer-found crash is minimized and committed under tracked `fuzz/corpus/` + `fuzz/crashes/`, and the deterministic replay is a **plain `cargo test` integration test (`tests/fuzz_replay.rs`)** that reads EVERY file under `fuzz/corpus/` + `fuzz/crashes/` and feeds each directly to the target function (`crate::detect`/`fs_guard`/the CSV-TSV engine/the command-handler serde boundary) **with NO libFuzzer harness**, asserting no panic/abort — so it **compiles and runs on ALL platforms (incl. Windows) under the stable toolchain**, no nightly/instrumentation needed. So: **Windows / all platforms: plain `cargo test` replay of the committed corpus + crash set (stable toolchain); Linux + macOS additionally: the instrumented `cargo-fuzz` run (date-pinned nightly).** A fixed crash can't silently regress on any OS. **G24 planted-positive:** a committed crash fixture MUST fail the `tests/fuzz_replay.rs` replay if the fix that closed it is reverted (the replay is armed, not a no-op). The committed corpus/crash fixtures are themselves integrity-pinned by the G24a fixture manifest. **Determinism:** a pinned CI seed; a case-count floor above libFuzzer's thin default; a property failure is **NEVER** retried to pass. **libFuzzer resource bounds are PINNED (a decompression-bomb / recursive-quote input can otherwise OOM/hang the runner and surface as flaky infra rather than a finding — a denial-of-CI vector on the shared VPS):** every fuzz leg pins **`-rss_limit_mb`** (so an OOM is a REPORTED crash committed to `fuzz/crashes/`, not a runner kill), **`-max_len`**, **`-timeout`** per input, **`-max_total_time`** per job, plus the G56 `timeout-minutes` on the fuzz job; a libFuzzer **OOM or timeout is a FINDING** (minimized + committed to `fuzz/crashes/`), **never retried**. **(6) `convertia-imgworker`'s own Rust→FFI surface** — a `cargo-fuzz` target over the imgworker's in-process Rust shim **linked against the staged `libvips`/`libheif`/`librsvg`** with **ASAN on** (the densest unsafe surface in the product, the first Rust touching untrusted image bytes across the C/C++ boundary) — distinct from G26 (the IPC/isolation-boundary corpus, which cannot reach in-process) and the §8 AFL++/G65 idea (the C/C++ subprocess black-box): this target exercises the FFI marshalling itself. **ASAN-coverage HONESTY (the wording must not overclaim):** `cargo-fuzz` builds a fresh instrumented binary from the Rust source, but the staged engines are **pre-compiled `.so`/`.dylib`/`.dll` that CANNOT be ASAN-instrumented** — so with the staged libs, ASAN catches **only Rust-side heap violations + FFI-boundary crossings, NOT bugs INSIDE libvips/libheif/librsvg** (accurate and valuable as a boundary test, but not a decoder-internals fuzz — those are G65's black-box subprocess job). The catalogue states this explicitly; an alternative that fully ASAN-instruments the C/C++ internals would require a **from-source build of the libs with ASAN, asserted same-version as the `engines.lock` pin** (an owner-decidable upgrade, not the default). Constrained to where libFuzzer is reliable (**Linux + macOS, nightly toolchain** — date-pinned `nightly-YYYY-MM-DD`, §0); the L2 leg is the deterministic G16 replay, never an instrumented Windows build. **Delivered (P0.4.3):** [`scripts/check-fuzz-contract`](../../scripts/check-fuzz-contract) (the P0.3.6 target-absent pattern): a STRUCTURAL FREEZE of the 6-entry libFuzzer in-core G48 target set (detect · fs_guard resolve_identity + is_safe_output · CSV/TSV · zip-slip · the imgworker Rust→FFI surface) + the 2 P2 IPC proptest contracts (the per-`#[tauri::command]` serde boundary + the per-numeric-IPC-arg overflow), the 4 mandatory libFuzzer resource bounds (`-rss_limit_mb`/`-max_len`/`-timeout`/`-max_total_time` — an OOM/timeout is a committed FINDING, never a runner kill), the 8 deterministic G16 bound-firing fixtures (NUL-path · PATH_MAX+1 · the 5 Windows dangerous-path classes · zip-slip entry), and the per-numeric-IPC-arg overflow boundary set, plus the `gate-tools.toml [toolchain] fuzz_nightly` **DATE-pin** assertion (`nightly-YYYY-MM-DD`, never a floating bare `+nightly`). The live tier binds each target to a REAL artifact — `fuzz/fuzz_targets/<key>.rs` with a comment-stripped `fuzz_target!` (a hollow stub or comment-only mention is CAUGHT — the P0.4.1 deny-context lesson, NOT a bare stem/substring), each fixture to a committed corpus FILE, each bound to a comment-stripped committed fuzz config — and skips-with-notice while `fuzz/` is absent, fail-closing when the harness lands; the target BODIES land across **P3–P9**, the IPC proptests in **P2** (the activation note). Wired **L1+L2** (lefthook) + **L4** (gate-tooling, 3-OS); `g24-fuzz-contract.py` = 22 legs (incl. the hollow-stub + comment-only regression guards); canary 27→28. The workflow-invocation exact-date cross-check (`check-ci-supply-chain` check (7): a `cargo +nightly-DATE fuzz` invocation's DATE == `fuzz_nightly`) is the **P3** refinement — there is no fuzz invocation to cross-check until the harness job lands. | red `main` |
| **G50** | Workflow security lint | **`zizmor`** (Rust GH-Actions static analyzer) — flags unpinned actions (mutable tags vs full commit SHA), dangerous `pull_request_target`, template-injection via untrusted `${{ github.event.* }}` in `run:` steps, and excessive `GITHUB_TOKEN` scope. **`zizmor` (G50) is the AUTHORITATIVE enforcing gate for ACTION SHA-pinning** (the `uses:` axis; G56 lints other CI-config hygiene but defers to G50 on action-pinning) — **but zizmor does NOT cover `jobs.<job>.container.image` / `services.*.image` digest-pinning NOR transitive action pins**, so those two are explicit **G56 sub-checks** (see G56), not assumed-covered here; a **dependabot bump PR that re-pins an action to a new SHA re-runs G49/G50 before the owner merges it — never auto-merged** | red `main` |
| **G57** | Principle-11 English-only / string-ownership lint *(Lane-A; contract in P0.4, activated in P1)* | spec §6.7.1 Lane-A + §6.10 row 23 require this DECIDED gate; v1 is **English-ONLY with NO i18n runtime**, so the gate is the INVERSE of locale-key parity: **(a)** FAIL on any locale-switch / i18n-runtime library import (`i18next`/`react-intl`/`@lingui`/`vue-i18n`/etc.); **(b)** every `strings/ui.ts` key resolves to a **non-empty English value**; **(c)** every user-facing literal lives in `strings/ui.ts` (no inline user-facing string in a component). Closes the spec-DECIDED-but-catalogue-less drift P0 exists to prevent (cite spec §6.10 row 23). **Delivered (P0.4.6):** [`scripts/check-english-only`](../../scripts/check-english-only) (stdlib, the P0.4.1/P0.4.7 two-tier pattern; self-contained — reads the same `src/strings/ui.ts` + eslint config the P0.4.7 TS toolchain establishes, `needs: P0.4.7`). FREEZE: the banned-i18n set (`i18next`/`react-intl`/`@lingui/*`/`@formatjs/*`/`vue-i18n`/… — exact + scoped-prefix), the required eslint rules (`react/jsx-no-literals` for leg (c) + `no-restricted-imports` for leg (a)), and the §6.10 PINNED_KEYS drift check (`idle_reassurance` verbatim). TARGET-ABSENT legs: (a) no banned i18n dep in `package.json` (any tier) + no banned import in `src/**` (comment-stripped) + the eslint i18n restriction names a banned lib; (b) every `src/strings/ui.ts` key non-empty + each pinned key present with exact text; (c) the eslint config carries `react/jsx-no-literals` (the precise AST enforcement, not a FP-prone JSX-text regex). `g24-english-only.py` = 29 legs; canary 29→30; L1+L2+L4. G1 (volle Härte) NOGO→fixed→re-reviewed: the leg-(b) `_ENTRY_RE` line-anchor fail-open (a 2nd-on-the-line / minified empty value shipped undetected) is closed (object-entry-boundary `[{,]` anchor), `assert_eslint_rules` gained the `_rule_is_off` off-detection (a `"react/jsx-no-literals": "off"` is caught, ported from the sibling check-ts-gate), + a bare `import "x"` side-effect form. | red `main` |

> **G49 (`actionlint`) is mirrored in CI on a clean checkout** — it is defined as a single L1
> row in §2 and runs again at L4 like every other *(mirror)* gate; it is intentionally NOT
> repeated as a second `| **G49** |` table row here (r7 nit — a membership scan for `| **G49** |`
> must see exactly one row), so this is a note, not a row. *(Same plane-mirror convention as the
> other §2/§4 mirror gates re-run at L4 without a duplicate L4 row.)*

**Per-push adversarial-egress pull-forward.** On runners that support the §6.7.3
enforcement path, the §6.4.2 adversarial-egress corpus (HLS `m3u8`, DASH `mpd`,
`concat` script, external-`href` SVG, remote-`<img>` pandoc, `WEBSERVICE()` xlsx)
runs in the per-push **L4** integration leg (under G42's egress-deny window) so a
**T9b** egress regression introduced in P6/P7 is caught on the push that introduced
it; **G42** is the final release confirmation. (The macOS WebView leg degrades to
the §6.6 walkthrough — see G42/G33b.)

> **G34 is intentionally vacated.** A screenshot/visual-regression gate has **no
> §-home in the spec** (§6.4.6 is the WebDriver flow; §6.4.6a is axe) — a
> release-blocking gate with no spec home is not added. If visual-regression is
> wanted, **add a spec §6.4.6-family entry first, then assign an id from the vacant
> range above G56**; the id `G34` stays reserved/unused so existing references do not
> renumber.

> **Vacated / reserved gate IDs — the complete annotated map of the integer space (r7, extended through r15).**
> The catalogue thesis is that **every `Gnn` IS a real security/quality control**, so a
> silently-missing integer ID (a fill-pass author could mis-read a gap as "three gates
> removed", or collide with it) is an audit-trail defect for a P0 whose bar is *exhaustive*.
> plan-lint **check 22** asserts every integer `N` in `2..max(Gnn)` is either a `| **Gnn** |`
> table row OR carries a documented vacated/reserved/deleted/split note here, so no ID can be
> invisible. The non-row IDs and their reasons:
> - **G33 — SPLIT (sub-letter-only, no plain row).** Integer 33 exists ONLY as the a11y sub-letter
>   pair: **G33a** (the jsdom/`vitest-axe` ARIA leg, Lane-A) and **G33b** (the release-tier
>   `@axe-core/webdriverio` WCAG-AA contrast leg, L5). There is no plain `| **G33** |` row — so,
>   unlike G18/G24/G42/G56 (each of which has a plain integer row covering its integer), 33 would be
>   invisible to check 22's integer scan without this note (sub-letter rows are "outside check 22's
>   integer scope", per the G18e note below). This `split` note is integer 33's coverage.
> - **G34 — VACATED** (no spec §-home for a visual-regression gate; see the note above).
> - **G40 — DELETED** (was a `cosign`/SLSA binary-signing gate; removed because binary
>   code-signing/notarization is **SSOT *Out of Scope*** — only minisign over `SHA256SUMS`
>   is in scope, G39). The id `G40` stays reserved/unused so existing references do not
>   renumber; P0.7 names the same deletion.
> - **G61 — ADOPTED** (was RESERVED-unassigned): the **defaults-registry "no required choices" guard**
>   (the §1.6 consolidated `OptionDecl.default` index + the §6.7.1 Lane-A guard that fails the build if any
>   §04-offered pair lacks a default; §6.10 row 7). Now a `| **G61** |` table row (Lane-A), so check 22 sees
>   the row instead of this note. (A reserved id becomes a row only when its control is adopted — see below.)
> - **G62, G63 — RESERVED, unassigned.** Held vacant so a future gate insertion in the
>   release-tier range does not renumber G64/G65/G66/G67; no control is attached today.
>   (A fill-pass author must NOT read the G60→G64 jump as "deleted gates".)
> - **G65 — RESERVED + pre-committed** (engine-subprocess coverage-guided fuzz — `radamsa`/
>   AFL++/`zzuf`; a REQUIRED SCHEDULED issue-opener now, an owner decision on per-push later;
>   homed in P0.7, §8).
> - **G66 — RESERVED** (scheduled engine-version-currency poller — an issue-opener for the
>   silent-upstream-fix case; homed in P0.7, §8).
> - **G67 — RESERVED** (OSSF Scorecard informational corroboration; homed in P0.2, §8).
> - **G18e — RESERVED (sub-letter id)** (committed npm-package name-trust allowlist — the `cargo-vet`
>   analogue G18c lacks; the lightweight closure of the T2 JS name-trust gap, §8). Sub-letter reserved ids
>   are outside check 22's integer scope but are listed here so no `Gnn` reference dangles.
> - **G68, G69 — ADOPTED rows (the living-doc-system enforcement pair).** **G68**
>   doc-graph integrity & freshness (the general "no source-of-truth drifts from the doc graph" net — orphan /
>   cross-doc-resolution / described-the-old-way-freshness; the gates→`.md` case is one instance) and **G69**
>   structural-map integrity (every repo dir ∈ the CLAUDE.md "Repo layout" map ∧ every mapped dir exists,
>   bidirectional, the map a §0.7 projection). They are `| **G68** |` / `| **G69** |` table rows (L2
>   *(mirror)*), NOT reserved notes — so check 22 reads them as rows and they need no reserved-note entry;
>   they are recorded here purely for **navigational completeness** so a reader scanning the reserved-id map
>   past the former `max(Gnn) == G64` is not surprised by the new rows (the integer-space completeness
>   rationale below applies to reserved NOTES, not to these rows). The release-tier reserved ids G65/G66/G67
>   sit BELOW them in number but remain prose-only reserved notes (a reserved id stays a note until its
>   control is adopted, regardless of a higher id becoming a row).
> - **G70 — ADOPTED row (the "no green-by-rewrite" mechanical signal leg, appended in r14, below `max(Gnn) == G71`).**
>   The **test-suppression-marker gate** — the G8-deferral analogue applied to TESTS: a diff adding an
>   `#[ignore]`/`it.skip`/`describe.skip`/`.only`/`test.skip`/`xfail` / a retro-fitted `#[should_panic]` / a
>   removed assertion to a test FAILS unless a `[Test-Change: <box-id> — old-obsolete+new-correct, §ref]`
>   justification tag (or the net-new variant `[Test-Change: <box-id> — new-test:<reason>, §ref]` for a marker in
>   a brand-new test) sits within ±N lines (it FLAGS + REQUIRES justification, it does NOT forbid — a justified
>   change passes). The SEMANTIC "is this suppressing a real regression" call stays the G1 dual-review's
>   test-integrity scrutiny item (§1). It is a `| **G70** |` table row (L2 *(mirror)*), NOT a reserved note —
>   so check 22 reads it as a row; recorded here for navigational completeness (now below `max(Gnn) == G71`).
> - **G71 — ADOPTED row (the L(-1) security-critical-file change-control gate, owner decision D1, r15; below `max(Gnn) == G72`).**
>   The owner-ack pre-push gate: any commit touching an L(-1) security-critical file (authoritative set
>   `scripts/l-neg1-files.toml`) must carry a `L-neg1-ack: owner` trailer; the autonomous Build-Loop never
>   edits an L(-1) file (security-concept §2). It is a `| **G71** |` table row (L2 *(mirror)*), NOT a reserved
>   note — so check 22 reads it as a row; recorded here for navigational completeness (now below `max(Gnn) == G72`).
> - **G72 — ADOPTED row (engines.lock-bump corpus re-validation gate, r15; now `max(Gnn)`).** A CVE/Dependabot
>   `engines.lock` version bump must carry a regenerated-green §6.5.2 pair-status-ledger proof before it lands;
>   **fail-soft P1–P3** (the §6.5 machinery lands in P4 — the bump is HELD as a Co-Pilot item, roles §5a) **then
>   fail-closed from P4** (wired in P4.61). It is a `| **G72** |` table row, NOT a reserved note — check 22 reads it
>   as a row; recorded here as the new `max(Gnn)`.
> A reserved id becomes a `| **Gnn** |` row only when its control is adopted (then check 22
> sees the row instead of the note); a deleted/vacated/split id stays a note forever.

### L5 — Release (tag-triggered; release-blocking)
| ID | Gate | Tool / mechanism | Blocks |
|---|---|---|---|
| **G33b** | a11y — WCAG-AA contrast (release-tier) | **`@axe-core/webdriverio`** against the live WebView (`tauri-driver`) — WCAG 2.1 AA `color-contrast`, **both** themes, on the **Linux + Windows** legs (jsdom cannot compute contrast). **macOS is the acknowledged automated gap** (`tauri-driver` has no WKWebView driver, §6.4.6) → satisfied by the §6.6 human walkthrough's readable-contrast check, recorded in `docs/usability-floor.md` | release |
| **G35** | SBOM generation + completeness | **generation:** `cargo cyclonedx` (Rust — the §0 pinned-tool manifest names the CRATE/binary as **`cargo-cyclonedx@<ver>`** distinct from the invocation `cargo cyclonedx --spec-version 1.5`, the same crate-vs-subcommand distinction spec §6.3 draws, so the pinned-and-verified list names the actual crate) + **`@cyclonedx/cdxgen`** for the frontend (`--spec-version 1.5`) — **NOT `@cyclonedx/cyclonedx-npm`**, which runs `npm ls` against an npm `package-lock.json`/`node_modules` tree and does **not** natively understand pnpm's `.pnpm` symlink structure; ConvertIA is a pnpm project (G18a freezes `pnpm-lock.yaml`), so the npm tool would either error or SBOM an npm-resolved tree that **diverges** from the pnpm-locked graph G18a froze and G17 audited — and feed `grype` (G17b) the wrong component set. `cdxgen` has native `pnpm-lock.yaml` support (the frontend SBOM is **generated from `pnpm-lock.yaml`** so it matches the G18a-frozen graph). Merged via §3.7.2 `cargo xtask sbom` with `engines.lock`. **Each `engines.lock` row carries a mandatory `purl` (`pkg:generic/<name>@<version>` minimum, a CPE where one exists) + a SHA-256** (§3.7.2 schema reconciled in the same change), emitted into the CycloneDX component so G17b/G37 have a named key, not an implied one. **completeness (MANDATORY):** **`Syft`** scans the staged bundle so every shipped executable/lib/font **AND every bundled security-CONFIG file** ∈ `engines.lock`+declared sub-components, no `UNKNOWN`/`NOASSERTION` (except the §6.3.3 `LicenseRef` carve-out); **backed by** a deterministic stage-tree file-manifest diffed against `engines.lock` (Syft can miss libs statically compiled INTO FFmpeg/LibreOffice) so an unexpected `.so`/`.dll`/`.dylib` **hard-fails**, AND a staged shared object **not matching its `engines.lock` row** (the T3a side-loading guard) hard-fails. **Test-asset-exclusion sub-check (r15):** the stage-tree manifest additionally **hard-fails on any staged path under a repo test-asset prefix** (`tests/`, `tests/corpus/`, `fuzz/corpus/`, `fuzz/crashes/`) — a corpus/fixture DATA file is NOT a shared object the binary-scoped diff above would catch, so this path-prefix deny is what keeps the repo/CI-only corpus (§6.4.5, G24a) out of the installed application. **Transitive-closure leg (the dangerous case for prebuilt C binaries):** Syft + the file-manifest catch only files PRESENT in the tree — the genuinely dangerous case is a transitive shared lib an engine **LOADS** that was NOT staged (a system/Homebrew/distro lib present on the build runner but absent on the user's machine — simultaneously an offline-floor break AND an SBOM hole), so G37b's `ldd`/`otool -L`/`dumpbin` closure check fails the build if any non-system dep resolves outside the bundle. **Static-link blind-spot leg (a statically-linked component leaves NO `.so` and NO dynamic-link entry — invisible to BOTH Syft AND `ldd`):** for any statically-built engine (carve-out iii), capture the configure record / `ffmpeg -version` build flags / linker map and assert every enabled external lib has a CycloneDX **sub-component** row; since v1's stated path is **dynamic-beside-the-exe**, **G37 asserts the dynamic path was actually taken** (the expected `.so` siblings exist) so a silent static switch cannot drop a per-`.so` completeness row. **DERIVED static-link closure (G35a, r7 — the static analogue of G37b's dynamic-dependency-closure check; the densest-CVE surface `convertia-imgworker` STATICALLY links libvips+libheif+libde265+librsvg+libimagequant+ImageMagick+x265, none of which are in `Cargo.lock`, so today completeness rests on the HAND-MAINTAINED `engines.lock` C-library rows with NO proof the DERIVED closure equals that row set — a new transitive C dep (libheif pulling a codec, a libvips loader compiled in by default) would ship with no SBOM row, no license row, no G17b PURL CVE coverage, and NOTHING fails):** a gate that DERIVES the static-link closure FROM THE BUILD — parse the static archive symbol table / object set via **`llvm-nm`/`nm -A`** over the staged `.a`/the worker's intermediate objects, OR the **`cargo-auditable` (G55) embedded data**, OR the captured **linker map** (`-Map=`) — and **HARD-FAILS on any library present in the static link but ABSENT from `engines.lock`** (and the reverse: an `engines.lock` static-component row with no symbol evidence in the link is a stale row). This makes the imgworker static-link closure a build-DERIVED machine invariant, not a prose promise, and feeds the missing library's row into G36 (license), G17b (PURL CVE), and the NOTICE-parity leg. Linux/macOS via `nm`/`llvm-nm`; the same symbol-table derivation runs on each platform's staged worker. Planted-positive: an `engines.lock` missing a library that the link demonstrably pulls in MUST fail G35a. **SPDX-expression VALIDATION leg:** validate the `engines.lock`+SBOM license expressions with a real SPDX-expression parser (the `spdx` crate / `cargo-about`), not a substring scan, and assert the named compatibility couplings the spec flags — poppler `GPL-2.0-only OR GPL-3.0-only`, x265/x264 must be `-or-later` for the LGPL-3.0 libheif host, libaom's `LicenseRef-AOMPL-1.0` carve-out — so G36's family-substring scan cannot pass a mis-scoped expression the spec calls a build-breaker. **Generated-vs-committed NOTICE parity sub-assertion:** every `engines.lock` row with a license has a corresponding `NOTICE`/`THIRD-PARTY-LICENSES` entry (a staged engine whose license text was never added to NOTICE passes the file-completeness check yet ships a legally-incomplete attribution). **Corresponding-source-POINTER parity (the load-bearing GPL/LGPL discharge mechanism, §3.6.2):** for every GPL/LGPL/AGPL `engines.lock` row, assert `THIRD-PARTY-LICENSES` carries **BOTH** its full license text **AND** a corresponding-source-pointer line (upstream URL + the pinned source revision/commit). For the **GPL FFmpeg binary** specifically, §3.6.2 discharges the corresponding-source obligation via the public-repo + canonical-release model — the SBOM records the pinned source revision + upstream URL/commit as a valid written offer and `THIRD-PARTY-LICENSES` carries the full text + the source-pointer line — so this row is NOT covered by G38b (which is scoped to the statically-linked LGPL worker stack + x265 physical-source bundle); without this parity assertion the GPL FFmpeg row's load-bearing pointer line could silently go missing. **For from-source engines** (where ConvertIA IS the distributing party) assert the offer additionally references our exact pinned source tarball + the §3.8 toolchain record. libmp3lame's LGPL written offer is asserted explicitly here (not left "subsumed" in prose). **Bundled FONTS are first-class `engines.lock` rows (not an afterthought):** the ~30–120 MB of redistributable bundled fonts (§3.7.2/§6.3.3) are third-party surface with a copyleft-adjacent license trap + an offline-floor/integrity surface (a swapped/corrupt font ships silently), so **each bundled font file carries its own `engines.lock` row (SHA-256 + SPDX id + source URL)**, verified by G37 before staging and **counted by this completeness diff** (Syft + the stage-tree manifest) exactly like every `.so`/`.dll`/`.dylib`. The SPDX-expression validation leg adds an explicit **OFL-1.1 handling rule** and a **Liberation-provenance check** (Liberation ≥ 2.00 is **OFL-1.1**, but the *older* build was **GPL-2.0 with the font exception** — the same provenance trap §3.1 flags for the libimagequant fork; assert the staged Liberation is the OFL release, not the old GPL+exception build; Carlito/Caladea = Apache-2.0, Noto CJK = OFL-1.1), so G36/G36b's GPL/AGPL family-substring scan cannot mis-handle the OFL (permissive) or silently pass an old-GPL Liberation; and a **G38b-style assertion that the OFL reserved-font-name / required license notice ships in `THIRD-PARTY-LICENSES`** | release |
| **G35a** | DERIVED static-link closure (the static analogue of G37b) | the densest-CVE surface `convertia-imgworker` **STATICALLY links** libvips+libheif+libde265+librsvg+libimagequant+ImageMagick+x265 — NONE in `Cargo.lock`, so the G35 static-link-blind-spot leg's completeness rests on the **hand-maintained `engines.lock` C-library rows with no proof the DERIVED closure equals that row set** (a new transitive C dep — libheif pulling a codec, a libvips loader compiled in by default — would ship with no SBOM/license/G17b-CVE coverage and nothing fails). G35a **DERIVES the static-link closure FROM THE BUILD** — the static-archive symbol table / object set via **`llvm-nm` / `nm -A`** over the staged `.a` (or the worker's intermediate objects), OR the **`cargo-auditable` (G55) embedded data**, OR the captured linker map (`ld -Map=`) — and **HARD-FAILS on any library present in the static link but ABSENT from `engines.lock`** (and flags a stale `engines.lock` static-component row with no symbol evidence). The derived set feeds G36 (license), G36b parity, G17b (PURL CVE), and the NOTICE/corresponding-source-pointer parity, so a statically-absorbed C lib can no longer be invisible to all three. Linux/macOS via `nm`/`llvm-nm`; runs per platform on the staged worker. **Planted-positive:** an `engines.lock` missing a library the link demonstrably pulls in MUST fail | release |
| **G35b** | SBOM-diff between releases *(informational, Co-Pilot-signed)* | diff each release's CycloneDX against the previous, surfacing added/removed/changed components — a careless P5–P7 stage commit adding a transitive `.so` would otherwise enter the bundle unreviewed; the diff is a Co-Pilot review item, **non-blocking** | informational |
| **G36** | License hard-fail (Rust + bundled) | SBOM scanned for forbidden families (GPL/AGPL in MIT core / static Rust binary) → exit 1 | release |
| **G36b** | License hard-fail (frontend) | the `cdxgen` frontend SBOM (G35) is scanned for **GPL/AGPL over the pnpm dependency graph** → exit 1 (`cargo-deny [licenses]` is Rust-only — nothing otherwise stops an AGPL/GPL npm dep tainting the MIT WebView). Runs `jq`/a license filter over the CycloneDX components. **Authored P0.3.8 (slot):** the frontend GPL/AGPL hard-fail consumes the G35 cdxgen SBOM — target-absent until G35 lands; `scripts/check-js-supply-chain` carries the slot. | release |
| **G37** | Engine checksum / integrity build gate | pinned-version + SHA-256 verify of every bundled engine **AND every staged codec shared object beside it** (`.dll`/`.dylib`/`.so` — each individually `engines.lock`-rowed, the T3a side-loading guard, not just the primary `.exe`) **AND every bundled security-CONFIG file (r7 — the load-bearing T9b/T1 STRUCTURAL controls are CONFIG, not binaries): the ImageMagick hardened `policy.xml` (the ONLY structural coder-lockdown — §2.12.3 isolation is the degradable tier), the LibreOffice `registrymodifications.xcu` (MacroSecurityLevel=3 / DisableMacrosExecution / LinkUpdateMode=0 — the only structural macro-RCE control), and any bundled fontconfig / coder config — each gets its own `engines.lock` SHA-256 row** (symmetric with the bundled-fonts decision) so a partial-extract / truncation / AV-gutting / attacker-writable-portable-dir swap (the T3a threat model) cannot silently disarm the coder-lockdown / macro-suppression at runtime: a 0-byte `policy.xml` makes MagickCore fall back to NO policy (all coders enabled); a damaged `.xcu` makes LibreOffice fall back to the macro-firing default profile. G38 still asserts their CONTENT at build time; G37 here asserts their BYTES match the `engines.lock` row, and Syft (G35) counts them) **against the change-reviewed in-repo `engines.lock`** *before* staging **AND re-verified on cache-restore** (an Actions cache is not integrity-protected — on mismatch, delete + refetch from the pinned upstream URL); build-time in-bundle hash manifest generated. **Pin-establishment provenance — references the spec §3.8 acquisition-mode decision (P0 review r3):** the corroboration source MUST be **satisfiable per engine per platform**, because prebuilt and from-source have different ground truths. **From-source** ⇒ the binary SHA-256 here is a **build-output stability check, NOT a provenance anchor**; the anchor is the **signed source tarball** verified with a NAMED tool against a PINNED key — **not a bare hash compare** (a bare hash of a tarball is the xz/liblzma class §3.8 itself rejects; the xz backdoor rode a signed-tarball-that-differed-from-git path, so *which key / pinned where / verified how* is the load-bearing detail): each from-source engine's **upstream signing key(s) are pinned in-repo** (a committed keyring / fingerprint list on the engine-source allow-list), verified with **`gpg --verify`** / **`sq verify`** (Sequoia) at pin-establishment, the verification (not just the resulting SHA) **recorded in `engines.lock`**, and a key/fingerprint change treated as the **same hard Co-Pilot escalation** as a SHA edit — **+ the digest-pinned build toolchain/base image**. **VCS-tag anchor — the xz lesson made explicit (r7 — a validly-signed tarball is NOT sufficient): the xz/liblzma backdoor rode a VALIDLY-SIGNED release tarball whose autotools-GENERATED files (`m4/build-to-host.m4`, the generated `configure`) DIFFERED from the upstream git tree and were absent from it, so "verify the tarball's signature against the pinned key" would have PASSED on the actual attack.** So the from-source anchor **PREFERS building from a VCS tag/commit** (`git archive` of the GPG-signed git tag) with **`configure`/`m4` REGENERATED locally** (`autoreconf -fi` / the project's bootstrap) rather than trusted from the tarball; **where the release tarball must be used**, additionally **diff the tarball's non-generated sources against the upstream VCS tag and regenerate `configure`/`m4` locally** (never trust the tarball's generated build scripts). **Record BOTH the signed-tarball SHA AND the upstream VCS tag/commit in `engines.lock`** (a tarball-only provenance is the xz class; the tag/commit is the ground truth the tarball is diffed against). **Prebuilt** ⇒ corroborate against the upstream's published checksum/signature where one exists; **where the upstream publishes NEITHER** (notably **FFmpeg** — the common gyan.dev/BtbN Windows prebuilts are unsigned), corroborate by **cross-checking the same artifact hash across ≥ 2 independent mirrors/providers** OR by pinning via a **distro GPG-signed package + signed repo metadata** as the anchor (a bare hash of one unsigned download is **rejected** — it launders provenance, the xz/liblzma class). The chosen mode + corroboration URLs are recorded in `engines.lock`; any `engines.lock` SHA edit is a **hard Co-Pilot escalation** (highest-value place to plant a backdoor). **Engine-source allow-list (the `[sources]` analogue for engine binaries, P0 review r3):** every `engines.lock` source URL — **both** the pin/download URL **and** the corroboration-checksum URL — must resolve to a committed per-engine origin allow-list (`ffmpeg.org`/FFmpeg GH org, TDF/`libreoffice.org`, freedesktop/poppler, strukturag/libheif, videolan/dav1d, the chosen distro/Homebrew origins), and where the upstream publishes its checksum on a separate channel the two must be on **independent** origins (so a single poisoned host cannot serve both artifact and matching hash). **Static-link reconciliation (SBOM/license blind spot):** a STATICALLY-linked sub-component (libmp3lame/libvorbis/libvpx absorbed into one binary) leaves NO `.so` and NO dynamic-link entry — invisible to BOTH Syft (G35) AND `ldd` (G37b). Since v1's stated path is **dynamic-beside-the-exe** (§3.9.1), G37 **asserts the dynamic path was actually taken** (the expected `.so`/`.dll`/`.dylib` siblings exist per `engines.lock`) so a silent switch to static cannot bypass the per-`.so` completeness rows; **if** an engine is built static (carve-out iii), G37 instead captures the configure record / `ffmpeg -version` build flags / linker map and asserts every enabled external lib has a CycloneDX **sub-component** row (G35). **Manifest cross-check:** the in-bundle hash manifest is generated **after** final staging and each staged binary's SHA-256 is re-read vs the just-generated manifest (so it can't be stale). On **Linux**, assert every staged engine binary has the exec bit (`test -x` per `engines.lock` entry) before AppImage assembly (a dropped `+x` otherwise surfaces as a misleading startup "integrity failed", not a packaging error). **Cache-restore re-verify is machine-checked:** a G56 workflow-lint sub-rule (jq/yq over parsed YAML, NOT actionlint — actionlint lints syntax, not step-ordering) asserts every workflow restoring an engine-asset cache has a subsequent SHA-256 verify step **before** `scripts/stage-engines`, with a G24 self-test that the verify fires on a planted mismatch. **General build-cache poisoning is ALSO gated (not just the engine-asset cache):** the trust boundary is "everything that touches the bytes that get minisigned", so the GENERAL caches that feed the signed artifact — the Cargo registry+build cache, the pnpm store, any `sccache`, the vendored gate-tool binaries, the `cargo-fuzz` corpus — must not be a poisoning vector (GitHub Actions cache poisoning is a mainstream supply-chain class — Cacheract 2024, TanStack 2025 — and ConvertIA is a PUBLIC OSS repo that keeps the external fork-PR concept, plus a SHARED self-hosted VPS where another tenant's or a prior corpus job's cache write is a host-local vector). The **G56 cache-hygiene workflow-lint sub-rule** asserts: **(a)** the secret-bearing release/signing job restores **NO writable shared cache** (cache disabled, OR read-only / scoped to a key derived only from committed lockfiles + the digest-pinned toolchain); **(b)** fork-PR workflows do NOT share a cache-key namespace with release/`main` (no `restore-keys` prefix bleed across the trust boundary); **(c)** the signed artifact is built `--locked`/`--frozen-lockfile` from a cache-cold or integrity-verified tree. The policy is homed in P0.7, the lint in P0.2 | release |
| **G37b** | Dynamic-dependency-closure assertion | at staging, run `ldd`/`readelf -d` (Linux) · **`otool -L` AND `otool -l` (macOS)** · `dumpbin /dependents` (Windows) on every staged engine binary/`.so`/`.dylib`/`.dll`: **every non-system dependency must resolve INSIDE the bundle** — fails the build otherwise. **macOS `otool -l` is mandatory alongside `otool -L` (the `-L`-only leg is asymmetrically weaker than the Linux `readelf -d` leg it claims parity with):** `otool -L` lists the install-names a Mach-O references but **NOT the `LC_RPATH` search paths** that resolve `@rpath/…` install-names at load time — a bundled engine/dylib that loads `@rpath/libfoo.dylib` with an `LC_RPATH` pointing OUTSIDE the bundle (e.g. an absolute `/usr/local` or `/opt/homebrew` rpath baked in by a Homebrew build) is exactly the T3a side-loading + offline-floor break this gate claims to catch, yet `otool -L` alone shows only `@rpath/libfoo.dylib`. So `otool -l` enumerates every **`LC_RPATH`** and **`LC_LOAD_DYLIB`**, and the gate asserts each `@rpath`/`@loader_path`/`@executable_path`-relative load resolves to a file INSIDE the bundle **and that no `LC_RPATH` points outside the bundle** — making the macOS closure check actually equivalent to the Linux `readelf -d` one. Generalises the §3.5.5 "x265 plugin must resolve from the bundle" concern to every staged binary; catches both a **T3a side-loading** vector and an **offline-floor break** (an engine linking a Homebrew/distro lib present only on the build runner). On **Windows**, engines are additionally spawned with an explicit **minimal `PATH` (the engine's own bundle dir only)** so the OS DLL search starts inside the bundle | release |
| **G37c** | Linux glibc symbol-version floor *(the other half of the AppImage offline-floor)* | the §3.5 offline-floor also requires that the bundle's engines do **not** require a NEWER glibc than the oldest supported target distro — a prebuilt FFmpeg/LibreOffice built on a too-new runner links e.g. `GLIBC_2.38` symbols and **fails at the loader on older user systems** (the same "works on the build runner, broken on the user machine" class G37b catches, on the symbol-version axis). Extract the max `GLIBC_x.y` referenced by each staged engine binary (`readelf --version-info | grep GLIBC_ | sort -V | tail -1`, or `objdump -T`) and **fail if it exceeds a declared floor recorded in a committed `scripts/glibc-floor.toml`** (e.g. `[linux] max_glibc = "GLIBC_2.31"`, Ubuntu 20.04 LTS) — named explicitly so the fill pass does not invent an ad-hoc location and the lint is concrete (the extracted value vs the committed `max_glibc`). **Limitation (G37c is a STATIC symbol-version proxy only):** it cannot catch a `dlopen`-at-runtime versioned symbol, a non-glibc shared dep (`libstdc++`/`libssl`) missing on the old distro, or a LibreOffice UNO component load. **TWO concrete sub-assertions close that gap (r7 — adopted as REQUIRED, matching the §12 changelog "G37c gained a runtime launch-smoke + macOS minos analogue"; previously a hedged "forward idea" parenthetical that conflicted with the changelog's "gained"):** **(1) release-tier runtime launch-smoke** of each staged engine (`--version` + one trivial conversion) inside a **digest-pinned MINIMUM-target container (Ubuntu 20.04 LTS)** as the AUTHORITATIVE offline-floor proof (G37c's static `GLIBC_x.y` extraction stays the cheap fast-fail front of it, with a planted-positive — an engine requiring a too-new `GLIBC` MUST fail); **(2) macOS `minos` analogue** — `vtool -show-build` / `otool -l LC_BUILD_VERSION minos` ≤ the §0.3.1 macOS 11.0 floor for staged sidecars (built separately from the app, so they may differ from the app's `MACOSX_DEPLOYMENT_TARGET`), with a planted-positive — a sidecar with `minos` > 11.0 MUST fail. Both are now required G37c sub-assertions, not §8 ideas | release |
| **G41b** | Pre-publish archive validity | before publishing, a <30 s leg asserts each artifact is a **valid OPENABLE archive** (not just size-checked, G41): `unzip -t` (Windows `.zip`), `hdiutil verify` (macOS `.dmg`), `--appimage-extract-and-run`/`file`+`sha256sum` (Linux AppImage) — a corrupt artifact passing the size check is otherwise discovered only by users | release |
| **G38** | Per-engine build assertions | FFmpeg `-protocols`/`-demuxers` curated + required-codec lock + `concat -safe 1`; **FFmpeg configure-flag / whole-binary-license assertion (r7 — the whole-binary license claim was a hand-entered SPDX id trusted blindly):** §3.6.1 hinges the FFmpeg binary's whole-binary license on its configure flags — `--enable-gpl` → `GPL-2.0-or-later`, but `--enable-nonfree` (e.g. a "full" gyan/BtbN build with `libfdk-aac`/some nonfree codecs) → **NON-DISTRIBUTABLE**, and a nonfree prebuilt would pass G36 with a wrong `GPL-2.0-or-later` `engines.lock` row and become legally undistributable with nothing failing. So parse the STAGED binary's `ffmpeg -version` **`configuration:`** line and **(a)** assert `--enable-gpl` is PRESENT (the row's SPDX id is GPL, so the binary must actually be GPL-configured), **(b)** **HARD-FAIL on `--enable-nonfree`** (and on the specific nonfree decoders — `--enable-libfdk-aac`, `--enable-nonfree`-gated `--enable-openssl`, etc.) — a nonfree build is undistributable, a release-blocker, not a warning, **(c)** record the exact `configuration:` line in `engines.lock` so a flag change is a hard Co-Pilot escalation like an `engines.lock` SHA edit (the configure flags are part of the FFmpeg binary's provenance, not just its SHA); **FFmpeg enabled-decoder ALLOW-LIST (the other direction — G38 today asserts the REQUIRED decoders are PRESENT but nothing fails if the build ACCIDENTALLY enables an EXTRA legacy/CVE-heavy decoder, a T1 attack-surface expansion):** extract `ffmpeg -decoders` (and `-demuxers`) from the STAGED binary and assert the enabled set **equals** a COMMITTED golden allow-list (`ffmpeg-allowed-decoders.lock`) — an unexpected decoder (RealMedia, WMV9, a recently-CVE-heavy codec) **hard-fails** and requires a Co-Pilot review like an `engines.lock` SHA edit; the golden is regenerated-and-`git diff --exit-code`'d at L4 (it bounds the T1 surface, so the authoritative proof is regenerate-and-diff, not an mtime heuristic — see plan-lint check 10); librsvg no-base-URL API + version-floor ≥ 2.56.3; libvips no-copyleft-PDF-loader (no `pdfload`/`poppler`/`mupdf` loader); LGPL shared-object-or-fail (carve-out i); libimagequant BSD-2-Clause leg-text + lockfile pin; the §6.1.3/§0.10 `tauri-plugin-store`-cannot-escape-`config_dir` assertion (T2c). **poppler `pdftotext` (PDF→TXT, §3.5.3, T9b — previously unnamed):** build-introspection that the staged `pdftotext` was built **WITHOUT** network/HTTP support (no `curl`/`libsoup` in its `ldd`/`otool -L` closure — a crafted PDF can carry `GoToR`/remote-action / annotation URIs) + a G31 corpus sentinel (a crafted PDF with a remote-URI annotation → **no packet** inside the egress-deny window). **pandoc (T9b):** assert the staged `pandoc --version` **≥ 2.15** (`--sandbox` shipped in pandoc 2.15 and is **honoured from 2.15 on**, enforced by pandoc's type system — verified against the pandoc 2.15 release notes + spec §3.5.4 line 1126; the earlier "≥ 2.17, older silently ignore `--sandbox`" claim was a baked-in WRONG fact corrected in r6 — `--sandbox` is available and effective since 2.15, and under SSOT > spec > docs the spec's ≥2.15 governs); the corpus sentinel (`$include(sentinel)` / `.. include:: /etc/passwd` under `--sandbox` inside the egress-deny window → output must NOT contain the sentinel) is G31. The presence of **`--resource-path <scratch-only>`** on every pandoc argv is **structurally enforced by the G29 argv Semgrep rule** (the rule asserts the `--resource-path` VALUE resolves to the per-job scratch variable, not an absolute literal / home path — see G29). **Rationale stated honestly (r6 nit fix):** spec §3.5.4 lines 1137–1138 state ConvertIA configures **NO** pandoc Lua/JSON filters and uses pandoc for **no** PDF production, so `--resource-path <scratch-only>` is **belt-and-suspenders scoping of template/data-file READS (defense-in-depth, bounding the T9b read half), NOT closure of a live filter-execution vector** (that vector does not exist in this project); the prior "closes the user-Lua-filter/template execution vector" wording is corrected so a reviewer is not misled. **LibreOffice (T1 macro / T9b, the previously-ungated control):** parse the shipped `registrymodifications.xcu` (xmllint / `quick-xml`) and assert `org.openoffice.Office.Common/Security/Scripting/MacroSecurityLevel = 3` + `DisableMacrosExecution = true`, `LinkUpdateMode = 0`, and the Calc external-data / external-reference / `WEBSERVICE()` refresh-on-load keys are disabled (§3.5.2) — a profile-existence check is insufficient; LibreOffice headless executes embedded Basic/VBA `AutoOpen`/`Workbook_Open` macros by default unless the profile blocks them (an RCE path on untrusted office files); the **`--convert-to`-only invocation shape (NO `--accept`, which would open the UNO loopback socket on every machine, §3.5.2) AND the per-job disposable-profile flag `-env:UserInstallation=file://<per-job kind-2 scratch>` (PRESENT on every `soffice` spawn — without it LibreOffice falls back to `~/.config/libreoffice` / `%APPDATA%`, which (a) violates T9b read-half by reading out-of-input user config, (b) violates T8 by writing persistent config surviving across jobs, (c) on macOS can trigger a home-dir TCC prompt) are enforced by the G29 argv Semgrep rule** (positive-presence + scratch-path-value check, with a planted-positive fixture where a spawn missing the flag MUST fail); an optional G31 sub-test confirms the working config dir during conversion is the scratch path. **ImageMagick (T9b/T1 — the densest-CVE decoder family, statically linked inside `convertia-imgworker` per §3.5.5):** assert the bundle ships a **hardened `policy.xml`** — parse it (xmllint / `quick-xml`, like the `registrymodifications.xcu`) and assert it denies the dangerous coders `<policy domain="coder" rights="none" pattern="{URL,HTTPS,HTTP,FTP,EPHEMERAL,MVG,MSL,TEXT,LABEL,SHOW,WIN,PLT}">` (the ImageTragick CVE-2016-3714 + URL/MSL/MVG SSRF/LFR/RCE coder class) plus a path-rights deny on `@`-indirect reads — **OR**, if the trimmed IM was built `--without-modules`/`--enable-zero-configuration` (which can bypass `policy.xml`, libvips build caveat §3.5.5), assert the build EXCLUDED those coders/delegates (introspect `convert -list coder` / `convert -list policy` from the staged build, and confirm the GPL/network optional delegates §3.6.1 were excluded). Because libvips invokes ImageMagick via MagickCore which reads `policy.xml` only from `MAGICK_CONFIGURE_PATH`, the **imgworker MUST set `MAGICK_CONFIGURE_PATH` to the bundle policy dir** (else the system / no policy is used) — folded into the G29 `.env_clear()`/argv-safety Semgrep rule for the imgworker (a positive-presence assertion). Corpus sentinel (a crafted BMP / an SVG-via-MSL/URL-coder → no egress + no out-of-input read inside the G42/G42b window) is G31. **Mitigation census (informational-then-ratcheted, like G17b — you don't control upstream build flags):** run `checksec`/`hardening-check` (Linux ELF: RELRO/NX/stack-canary/PIE) · **`winchecksec` (Trail of Bits, cross-platform PE analyzer, `-j` structured JSON, pinnable per §0)** for Windows PE mitigations (DEP/ASLR/HEASLR/CFG/SafeSEH/GS — `checksec` has NO native PE support and `dumpbin /headers` alone does not surface Control Flow Guard reliably; `dumpbin /headers /loadconfig` is the fallback if `winchecksec` is unavailable) · `otool`/`codesign` hardened-runtime inspection (macOS Mach-O) over every staged engine binary/`.so`/`.dylib`/`.dll`, reporting which mitigations are present — non-blocking, surfaced as a Co-Pilot review item + a pin-establishment data point, so a regression to a non-hardened upstream build of an untrusted-input decoder is visible | release |
| **G38b** | Copyleft corresponding-source bundle present | the §6.1.3 carve-out ii/iii **bundle-presence** assertion: for the static image-worker (LGPL §6) ship its complete corresponding source + LGPL object files / relink recipe, **and** because it links GPL x265 ship the **x265 GPL §3 complete corresponding source + written offer** — the stage step **fails the build if the source bundle is missing**. Maps to the §5 **T6** row | release |
| **G39** | Checksums + minisign | per-file SHA-256 + minisign detached signature **over `SHA256SUMS`** (pubkey at `docs/minisign.pub`, private key = `MINISIGN_SECRET_KEY` CI secret, rotation policy §6.2.3). **`minisign -Sm SHA256SUMS`** is the actual sign step — **provision the key AND wire the step**. **Executable verify-recipe assertion (turns "recipe present" into "recipe correct and working"):** the release job then **RUNS the exact documented recipe** `minisign -Vm SHA256SUMS -p docs/minisign.pub` (lowercase **`-p` = public-key FILE PATH**; uppercase `-P` expects an inline base64 **string** and would FAIL on a path — this is the literal copy-paste recipe a user pastes, so the gate must run the literal form) against the just-produced `SHA256SUMS` + `.minisig` + the committed pubkey and **fails the release on non-zero**. This is the **only** signing in scope (§6.2.3) — **not** binary code-signing/notarization (SSOT *Out of Scope*) | release |
| **G55** | Auditable Rust binary | build the shipped Rust core with **`cargo auditable build --release`** so the dependency list is embedded in the binary (~4 KB, zero CI cost) — a portable, no-auto-update artifact can be audited **from the binary alone** long after a CVE drops (`cargo audit bin` / grype consume it); a strong fit for an offline "audit it yourself" MIT product. **G55 + G17b are the two halves of the offline "audit-it-yourself" story** (embedded SBOM in the binary + the dated open-CVE report). **Embedded-list ↔ CycloneDX-SBOM agreement (r6 — the two halves are generated by INDEPENDENT tools and were never proven to agree, exactly the divergence class G18a/the cdxgen-not-cyclonedx-npm choice hardened against elsewhere):** a release-tier sub-assertion (folded into G35/G58) **extracts the `cargo-auditable` embedded list from the just-built binary** (`cargo audit bin` / the auditable JSON) and asserts the Rust-component set **==** the CycloneDX SBOM's Rust components (name+version), so a user running `cargo audit bin` and a user reading the SBOM cannot get different component sets and a stale/wrong SBOM asset is caught; it **also runs `cargo audit bin`/grype against the shipped binary** and fails on a new advisory — the same "generated → also VERIFIED on the runner" upgrade already applied to minisign (G39), the SBOM (G35), and the attestation (G59). Near-zero cost; both artifacts already exist | release |
| **G41** | Artifact size budget | per-platform compressed artifact ≤ budget (≤400 MB target) — measured after bundle | release |
| **G42** | Offline-egress: active-deny + observe-the-attempt | mirror spec §6.7.3 — an **OS-level egress-DENY window** (the enforcement) **plus** a packet monitor (the proof) on each platform: **Linux** netns/nftables drop with `iptables -j LOG`/`NFLOG` + an `strace -e trace=network` `connect()`/`getaddrinfo` leg (a *blocked-but-attempted* connection is caught, so a silent DROP can't make "zero packets" prove nothing); **macOS** `pf` `block log` → `pflog0` read by `tcpdump`; **Windows** outbound firewall block **with** dropped-packet logging via a **named ETW consumer** (`netsh trace start provider=Microsoft-Windows-Windows-Firewall-With-Advanced-Security … convert` then `Get-WinEvent`, not a vague "ETW"). **PLANTED-POSITIVE / armed-window self-test (mandatory — the gate's own G24-class control):** inside the **same** deny window a deliberate outbound attempt to a **harness-controlled sink** MUST be (a) **blocked** by the deny layer (proves DENY is armed) AND (b) **recorded** in the observe-the-attempt log (proves the monitor captures). If the canary's attempt is NOT recorded, the monitor is misconfigured (wrong interface, NFLOG group not bound, `pflog0` not read, ETW consumer never started, `strace` on the wrong PID) and the gate **FAILS CLOSED** with the same diagnosable `::error::` pattern the spec mandates for the ptrace/Landlock fail-closed (§6.4.2) — a mandatory gate that observes "zero attempts" because it is silently watching nothing is worse than a visible red. **macOS pf-availability residual (honest):** on GitHub-hosted (virtualized) macOS runners `pfctl`/`pflog` may require root/`visudo` entitlements not present by default, and pf in a VM may miss host-virtualization-layer traffic; the gate **verifies pf/pflog availability at P9 build time** and, if unavailable, falls back to **interface-level packet capture with `tcpdump` — a background capture on loopback `tcpdump -i lo0 -n` AND the active interface `tcpdump -i en0 -n` (started → run the conversion + the canary → stopped → the pcap analysed)**, NOT `nettop`. `tcpdump` ships on the macOS GitHub-hosted runner and captures at the *interface* level, so it records the **SYN of a connect() ATTEMPT (and the RST/refusal)** AND **UDP/DNS** packets — which is exactly what G42's mandatory armed-window canary needs: a deliberate outbound attempt that is *blocked* by the deny layer never reaches `ESTABLISHED`, so `nettop -m tcp` (which shows per-process socket STATE — `ESTABLISHED`/`CLOSE_WAIT` — not the attempt, and `-m tcp` blinds it to UDP/DNS entirely) would record **nothing** for a blocked canary and the gate's own armed-window self-test would then spuriously **FAIL CLOSED** ("monitor misconfigured, observed zero attempts") on every macOS fallback run. `tcpdump` produces an observable packet for the canary even when the connection is blocked, so the armed-window self-test is recordable on macOS. (If `tcpdump` itself is absent, **fail closed** with the same diagnosable `::error::` pattern — a monitor that cannot be armed is a red, not a skip. `dtrace` is not used as a fallback — SIP blocks it on the default runner. The invocation is re-verified against the pinned-runner `man tcpdump` on the P9 build, since interface names can differ in the runner VM.) **Windows loopback gap (explicit):** Windows Firewall outbound rules do **NOT** apply to loopback (`127.0.0.1`/`::1`), so if any engine (e.g. LibreOffice headless's local UNO socket server) opens a loopback connection the firewall block silently misses it and the DROP log won't record it — the Windows leg therefore **also** takes a **process-scoped** socket-STATE snapshot during the window: **`Get-NetTCPConnection -OwningProcess <ConvertIA-PID + spawned engine PIDs> | Where-Object State -in Established,CloseWait`** (a PID-filtered ETW consumer is the alternative) — **NOT a bare `netstat -an`**, which enumerates ALL processes on the machine so on a busy/shared runner a background service's `ESTABLISHED` connection causes a false positive or a real LibreOffice loopback socket hides among legitimate system entries — asserting no TCP/UDP socket of the ConvertIA process tree is in `ESTABLISHED`/`CLOSE_WAIT`, **plus enumerates named pipes (`\\.\pipe\`)** since LibreOffice headless IPC uses **named pipes** that the TCP/UDP snapshot misses. **Named-pipe enumeration names a concrete tool** (`netstat -an` does NOT enumerate named pipes): **`Get-ChildItem -Path \\.\pipe\ | Where-Object Name -match 'LibreOffice|soffice|uno'`** (PowerShell) or pinned Sysinternals **`handle.exe -a -p <soffice_pid>`**, asserting **no `soffice`-related named pipe persists beyond process exit** (a transient internal pipe closed at exit is acceptable; a persistent listening pipe is not). **Document the ETW sequence + privilege requirement:** `netsh trace start … → run conversion → netsh trace stop → convert the ETL → Get-WinEvent` requires **elevated privileges**, and a missing converted ETL is treated as **fail-closed** (the monitor was not armed). (Or runs inside Windows Sandbox, where loopback is also blocked.) A LibreOffice UNO loopback socket/pipe is decided as either a suppressed false-positive or a real gap fixed via the hardened profile (**the G29 no-`--accept` argv check is the PRIMARY control that keeps the UNO socket from being opened at all**). **DNS-only egress sub-assertion (r7 — promoted from a §8 forward idea to a REQUIRED G42 leg; a real low-bandwidth exfil channel an nftables/pf TCP/UDP DROP does NOT close):** a DNS A-record lookup (a crafted SVG hostname, a LibreOffice network resource, a pandoc remote `href`) can resolve via the OS stub resolver even while TCP/UDP is dropped — a 63-byte label per query is enough exfil — and G47's `x-dns-prefetch-control` covers only the WebView, not engine PIDs. So G42 additionally runs **`tcpdump -i any port 53` (Linux/macOS — the macOS leg already runs `tcpdump`, capturing UDP/53) / the Windows ETW `Microsoft-Windows-DNS-Client` provider** over the engine-PID scope and asserts **ZERO DNS queries** from the engine subprocesses in the egress-deny window, with the **same armed-window canary** as the packet leg (a deliberate lookup MUST be observed, else the DNS monitor is misconfigured → fail-closed). **Flush the resolver cache before arming** (`systemd-resolve --flush-caches` / `dscacheutil -flushcache` / `ipconfig /flushdns`) so a cross-job cache hit cannot mask a real lookup. The §7.2.3 startup engine smoke probes run **inside the same window**; engine spawns assert `.env_clear()` (no inherited `http_proxy`/`HTTPS_PROXY`/`*_PROXY`/`LD_PRELOAD`/`DYLD_*`) — a structural Semgrep-enforced invariant (G29), not just one unit-tested spawn. Zero egress except the user-initiated releases-page. **macOS WKWebView leg is driver-gapped (§6.7.3)** — core/engine egress is asserted there; the WebView's offline claim rests on the **platform-independent G47 structural proof** (a closed `connect-src` allow-list, no HTTP/updater plugin — a far stronger bound than the §6.6 human walkthrough) + static inspection | release |
| **G42b** | Offline-egress: the READ half (out-of-input file-read fs-audit) | the **symmetric sibling of G42** — G42 proves the egress half (zero outbound packets); **G42b proves the §0.11 T9b read half** (the engine reads/embeds nothing outside its declared input + scratch) with its own armed enforcement substrate, so the read half can never silently no-enforce. Mirrors spec §6.4.2 exactly: **(1) primary mechanism — `ptrace`** (an `strace`/`ptrace`-based fs-audit of the engine's `open`/`openat`), run with **`docker --cap-add SYS_PTRACE`** (commonly stripped inside CI containers) or natively on the §6.1.4 self-hosted VPS leg; **(2) fallback — the §2.12.3 Linux Landlock tier** restricting the decoder to `{input ro, scratch rw}` where **the grant IS the enforcement** (an out-of-input `open` is kernel-denied as the engine's `EACCES`); **(3) MANDATORY Landlock-availability probe** before relying on it (ABI ≥ 1, **kernel ≥ 5.13**, and the ruleset actually applied — Landlock is a silent-degrade tier in production, so the gate must NOT assume the grant took); **(4) FAIL CLOSED (hard red) when NEITHER `ptrace` NOR Landlock is available** — a mandatory check that silently no-enforces is worse than a visible red — with the spec-mandated diagnosable annotation `::error::fs-audit cannot enforce: neither ptrace (SYS_PTRACE) nor Landlock (kernel ≥ 5.13) available on this runner — see §6.4.2`; **(5) an out-of-input SENTINEL fixture** (a known file OUTSIDE the declared input + scratch that the engine MUST NOT read or embed into output — the adversarial corpus's `WEBSERVICE()`/`$include`/external-`href` cases reach for it); **(6) a PLANTED-POSITIVE self-test (G24-class)** — a deliberately-reaching engine invocation (an argv/config that DOES open the sentinel) MUST be CAUGHT by the armed fs-audit, proving the read-half monitor is armed and not silently watching nothing (the read-half analogue of G42's egress canary). **§6.1.4 records the Lane-B VPS runner kernel version** as a prerequisite so Landlock availability is a known fact, not a runtime surprise. **Platform asymmetry stated honestly (r6 — the armed fail-closed read-half substrate is LINUX-ONLY):** on macOS/Windows the read half is asserted via the G31 out-of-input sentinel corpus + the per-OS §2.12.3 Seatbelt/AppContainer profile — **but §2.12.3 Seatbelt/AppContainer IS the silently-degrading best-effort tier (the T1 honest residual)**, so on 2 of 3 platforms the read-half "enforcement" rests on the mechanism the docs elsewhere say cannot be relied on, with **only a corpus oracle (not an armed fail-closed substrate) behind it** — the very "monitor that can silently no-enforce" class G42b eliminates on Linux, left live there. So the absolute "the read half can never silently no-enforce" is **scoped to the LINUX leg** (the ptrace/Landlock fail-closed substrate); for macOS/Windows either (a) add an armed planted-positive — a fixture that DOES reach the sentinel MUST be caught by the Seatbelt/AppContainer profile, fail-closed if the profile silently did not apply (the read-half analogue of G31's privilege-drop-tier-APPLIED assertion; macOS `fs_usage` is SIP-blocked so use **`lsof +D`** over the scratch root or **ETW `Microsoft-Windows-Kernel-File`** / Procmon over the engine PID tree as the concrete observers), or (b) record it as an **owner-accepted residual** (see §10/§11). The ptrace/Landlock fail-closed substrate is the Linux leg's load-bearing enforcement | release (per-push L4 leg shares the G31 sentinel corpus) |
| **G43** | No-system-pollution audit | **the snapshot-diff is the load-bearing leg (CLI-automatable on every OS); the live monitor is best-effort and per-OS tool-gated.** The live-monitor half names a syscall/fs monitor during a conversion — **Linux** `strace`+inotify (CLI, reliable, the authoritative leg) — but the macOS/Windows live monitors named earlier are **NOT CLI-automatable in CI**: macOS `fs_usage` is DTrace-backed and **DTrace is SIP-blocked on macOS 11+** without special entitlements (not available on the default runner), and Procmon is **GUI-first** (its `/BackingFile /Quiet` automation is awkward/unreliable in CI) — so on macOS/Windows the live monitor is informational-where-available and **the deterministic before/after STATE snapshot-diff is the gate's actual proof.** Snapshot-diff per OS, all CLI-automatable: **Windows** — `reg export` of `HKCU` + `HKLM\SOFTWARE` before/after + a file-system snapshot diff (run/file-association/uninstall keys); **macOS** — before/after enumeration of `~/Library/LaunchAgents` + `/Library/LaunchAgents` + `/Library/LaunchDaemons` and the Launch Services / file-association DB, plus `lsof +D <config/scratch roots>` to confirm no out-of-tree open during the conversion (NOT the SIP-blocked `fs_usage` live trace); **Linux** — `~/.local`+`~/.config`+desktop-dirs before vs after. Any new entry outside config+log+chosen-output+scratch fails. (The Windows leg shares the §6.10 row-21 no-pollution machinery, P10.) | release |
| **G44** | Governance completeness | every required governance doc present + non-stub; download/trust page complete — the **minisign verify recipe is asserted in the LITERAL correct form** (`minisign -Vm SHA256SUMS -p docs/minisign.pub`, lowercase `-p`; a `-P <path>` form fails the gate) and is the same recipe G39 actually runs; the **WebView2/libfuse2/Sequoia prerequisite notes are PARSE-checked** (assert the `libfuse2`/`WebView2`/`Sequoia` strings are present in their respective download-page sections — structural parse, not a loose grep, same principle as the rest of G44). **Minisign-pubkey-fingerprint consistency sub-check:** parse `docs/minisign.pub` for its fingerprint and assert the **same string appears verbatim in the README / download-page** — a key rotation that updates the `.pub` but leaves the README pointing at the old fingerprint would otherwise pass every current gate (the out-of-band fingerprint anchor is only useful if the in-repo `.pub` and the published fingerprint actually agree) | release |
| **G45** | Name/trademark clearance record | `docs/name-clearance.md` present, dated for the release line, verdict = clear; dormant rename-propagation + old-name grep gate | release |
| **G46** | Startup integrity (runtime, also a release acceptance) | engine presence + integrity verification; missing/corrupt engine → app-fault, not crash. **Bundled-config startup hashing (r7 — the §7.2.3 startup verifier hashes only the EXECUTABLE sidecars, leaving the load-bearing T9b/T1 CONFIG controls runtime-unguarded):** spec §7.2.3 startup integrity is extended (living-doc spec-sync, the spec owns §7.2.3) to **also hash the tiny bundled security-config files** (`policy.xml`, `registrymodifications.xcu`, any coder/fontconfig config) against their `engine-integrity.json` entry — cheap (a few KB), and it closes the gap where a corrupt/blanked config disarms the lockdown at RUNTIME with no gate noticing. **Blanked-policy MUST-fault sub-test (G46/G31-class):** a deliberately-blanked / 0-byte `policy.xml` (and a damaged `.xcu`) MUST surface as a startup integrity fault (`BundleDamaged`), **NOT** silently run with all coders enabled / macros firing. **`QuarantinedByOs` sub-test (§7.2.3/§7.2.4):** inject a quarantine-style spawn failure (mock the quarantine errno/xattr) and assert the result is the distinct **`QuarantinedByOs`** error kind (NOT `EngineMissing`/`BundleDamaged`) with the §7.2.3 message form naming the sidecar — the Sequoia per-sidecar-quarantine reality is fully simulable, so it is a unit/integration test, not only the §6.6 human walkthrough; optionally the macOS startup probe detects the `com.apple.quarantine` xattr and populates `EngineHealth` proactively | release acceptance |
| **G64** | Privilege-drop-tier ratchet (the most consequential runtime security property, tracked so it can't silently regress) | the §2.12.3 runtime containment of the untrusted C/C++ decoders is **best-effort and silently degrades** (T1 honest residual) — G31's privilege-drop-tier-applied assertion proves the tier FIRED on the CI runner, but **nothing tracks the tier as a TREND or flags a NET regression** (a later P-phase change dropping a platform from a full tier to a cheap one). G64 records the **exact tier achieved per platform** into a tracked **`privilege-drop-coverage.toml`**, **decrease-guarded** exactly like `max_survived_mutants.toml` / the coverage floor — a commit that lowers an achieved tier **fails / escalates** (raises are deliberate committed changes). Pairs with the §5 T1 honest-residual framing (the tier is degradable-by-design but its regression is now visible). **Owner-decidable required-vs-informational like G17b** (informational while the §2.12.3 tier matrix is still being filled in P4–P9, required once stable) | release (informational until the tier matrix stabilises) |
| **G58** | Release-artifact completeness meta-gate | the single backstop catching "the SBOM / CVE-report / source-bundle silently didn't get attached": **enumerate EVERY required release asset and fail if any is missing** — the per-OS bundle, `SHA256SUMS`, `SHA256SUMS.minisig`, the SBOM file(s) (G35), the dated open-CVE report (G17b), `NOTICE`/`THIRD-PARTY-LICENSES`, the copyleft corresponding-source bundle (G38b), the measured-sizes asset (G41), `docs/usability-floor.md`, `docs/name-clearance.md` (G45), the §6.5.3 CHANGELOG/release-notes (with the demoted/lossy pairs), **and (v1 OWNER DECISION, no longer deferred) the `attest-build-provenance` Sigstore bundle asset + its paired `trusted_root.jsonl` offline-verify asset (G59)**. **Every enumerated asset is also asserted to have a corresponding LINE in the signed `SHA256SUMS`** (not merely "is present"), so the audit-it-yourself files (SBOM, CVE report, NOTICE, source bundle) are authenticated by the one minisign signature already in scope — at zero new signing surface. *(The release-tier reproducibility-delta check is its own reserved id G60 — see G60.)* | release |

| **G59** | Build-provenance attestation (verify-on-runner + offline bundle asset) *(v1 OWNER DECISION — promoted from `[DEFER: post-v1]`)* | **`actions/attest-build-provenance`** on the release job — the one genuinely-free build-ORIGIN trust signal for an unsigned, no-notarization portable build: it binds the artifact to the runner + workflow + commit (so a silently re-signed release from a poisoned shared VPS is detectable **even if the minisign key leaked** — the natural completion of the principle-11 host-isolation investment). It is **NOT** binary code-signing/notarization (so it does not breach the SSOT *Out of Scope* line) and is strictly additive to minisign (which proves checksum-manifest integrity but says nothing about build origin). Needs only `id-token: write` on the release job (asserted scoped to that one job — see G56/P0.7). **VERIFIED, not just generated** (the "generated but never checked" class G35/G17b/G58/G39 were hardened against): a release-tier step runs **`gh attestation verify`** against the just-produced artifact on a clean runner and **fails the release on non-zero**; the attestation joins the **G58 completeness enumeration**; the **Sigstore bundle ships as a named release asset** so users verify build origin **OFFLINE**. **Offline recipe corrected (r6 — a bare `gh attestation verify --bundle --repo` is NOT air-gapped: it still reaches Sigstore for trust-root/TUF material, and there are open cli/cli issues (#10030/#10059) where workflow-generated attestations FAIL offline without it, verified against current `gh` docs):** ship **`trusted_root.jsonl`** (produced ONLINE via `gh attestation trusted-root > trusted_root.jsonl`) as a **paired named release asset**, and document the recipe as `gh attestation verify <artifact> --bundle <bundle> --custom-trusted-root trusted_root.jsonl --repo Ne-IA/convertia`; `trusted_root.jsonl` joins the **G58 completeness enumeration**. **Residual stated:** the trusted root is a point-in-time snapshot — anything signed before it was generated verifies indefinitely, but it will not reflect post-generation Sigstore key rotation (a few times/year), so the recipe is re-validated and the asset regenerated per release; re-verify the flag set against the pinned `gh` version at P10. (If a future owner re-defers it, revert this row to a §8 forward idea and restore the P0.7 hedge in the same commit — plan-lint check 17 asserts §8 / G59 / P0.7 / §10 agree on its status.) | release |
| **G60** | Third-party-reproducibility delta of the self-compiled layer *(informational, release-tier, reserved id)* | full bit-reproducibility is correctly best-effort (the vendored prebuilt engines make it unattainable, §6.2.5) — but the higher-value achievable target for an MIT "audit it yourself" product is THIRD-PARTY reproducibility of the parts we DO compile: the **Rust core + the WebView bundle** (NOT the vendored engines). Build that self-compiled layer **twice** (a documented rebuild-and-compare recipe) and run **`diffoscope`** over the delta, publishing the determinism number as an informational release asset. That is the trust property that survives a poisoned build host — complementing G59 (build ORIGIN) with build DETERMINISM (the bytes we own). Non-blocking (vendored-engine non-determinism cannot fail it); the recipe + the delta number are a Co-Pilot review item | informational (release-tier) |

## 6. The `plan-lint` / `spec-lint` doc-consistency gate (G7/G20)

A single **stdlib-only** script (no third-party deps → runs anywhere instantly),
treating our canonical docs as machine-checkable truth. Exit codes: `0` none,
`1` ≥1 finding (**any** severity), `2` target missing. CLI: `--check <ids>`,
`--json`, `--quiet`, `--max-per-check N`. Three call sites: L1 (`--quiet` on glob),
L2 (full), L4.

**Base-case meta-check (plan-lint is itself a script the autonomous loop can edit, so "plan-lint
still works" must be PROVEN each run, not assumed; r5):** a committed **golden fixture repo-state
that MUST make plan-lint exit 1** is run in the L4 self-test prelude (alongside the G24 gate-script
planted-positives) — so "plan-lint still detects a planted doc-consistency violation" is proven on
every clean-checkout run. This is the doc-linter analogue of G24's planted-positive discipline and
the one base-case answer to "the loop blunted its own consistency gate and self-attested green".

Invariant checks (initial set; expanded during P0 review):
1. **Membership / matrix parity** — every format named in prose ∈ the README
   support matrix, and every matrix row has a code registry entry + a fixture + a
   round-trip test (ties G22).
2. **Cross-reference validity** — every `§X.Y` / internal anchor resolves.
3. **Heading hierarchy** — no skipped levels.
4. **Numbering gap-freeness** — sub-sections run min→max, no gaps.
5. **Gate-catalogue integrity (ONE-directional)** — every gate **named in
   security-concept.md** exists here as a `Gnn` row (the catalogue is the
   **superset**; membership, not phrasing). It is **not** bidirectional: this
   catalogue legitimately defines gates (G3/G5/G6/G10/G19/G22/G23/G27/G28/… and
   many more) that security-concept.md never names because they are
   quality/structural gates, not security controls — a "vice-versa over the whole
   catalogue" check would always fail on a clean checkout and make the P0 exit
   criterion unsatisfiable. The reverse direction, **where it matters** (every §5
   threat row cites a real `Gnn`), is owned by check 8, which is correctly
   bidirectional but scoped to the §5 threat rows.
6. **No forbidden tokens** — banned stamps/strikethrough/stale-dates in doc bodies.
7. **Generated-file structural sanity** — when validating a generated file, parse
   it (not regex) and assert non-empty/well-formed.
8. **§0.11 ↔ §5 threat-map parity (bidirectional)** — every spec §0.11 class
   (`T1, T2, T2a, T2b, T2c, T3, T3a, T4, T5, T6, T7, T8, T9a, T9b, T10, T11, T12` — **17**
   classes (r3 added **T11** macOS engine-as-first-TCC-accessor; r15 added **T12** unsigned-distribution / download-MITM)) has
   exactly one row in security-concept.md §5, and every §5 threat row cites a `Gnn`
   that exists in this catalogue. Fails the build if a class loses its row or a row
   loses its gate (so the mapping can never silently drift). **The ONLY authoritative
   class enumeration is the §5 table + this check-8 canonical set (r7 — the security-concept
   §7 "FROZEN r1 14-class snapshot" is explicitly historical/superseded and a literal-string
   class scanner or a skimming fill-pass author could mis-read it as live):** this check reads
   the class set ONLY from the §5 table and the canonical 17-class list here, NEVER from the §7
   frozen snapshot, so the superseded enumeration cannot leak into the parity check.
9. **Inventory parity (membership checks)** — every IPC command named in prose ∈ the
   §0.4.1 **explicit enumerated set** (treated as a SET, **not** a contiguous `C1..C13`
   range — `C2` is split into **C2a `pick_for_intake` / C2b `pick_destination`** and
   there is **C13 `cancel_ingest`**, so a range check would miss C2a/C2b); every engine
   id in prose ∈ §3.1; the §0.4 error taxonomy (incl. the `QuarantinedByOs` kind) and
   the fixed-set enums (`FormatId`, `EngineProgram`, `PatentDisposition`, the
   lossy-catalog) are internally consistent across the files that reference them.
10. **Generated-security-manifest currency** — if any `04-formats/*.md` is **newer**
    than the generated `ffmpeg-required-decoders.lock` / `ffmpeg-required-encoders.lock` /
    `ffmpeg-allowed-decoders.lock` (the G38 security-assertion manifests), fail "regeneration
    needed": a format change that updated the spec but left the generated security manifest
    stale is a security-relevant drift. **The L1/L2 plan-lint leg uses the cheap `git
    log --format=%ct` mtime heuristic (stdlib-only, early-warning); but because these are
    SECURITY manifests that bound the T1 surface, the AUTHORITATIVE proof at L4 is
    regenerate-and-`git diff --exit-code`** (run the codegen, fail if the committed manifest
    differs — the mtime heuristic can pass on a same-timestamp-wrong-content file, which a
    regenerate-and-diff cannot, the same discipline G19 uses for codegen). The mtime stays the
    cheap L1 leg. **Non-vacuous-artifact leg (r6 — a regenerate-and-diff PASSES on an empty-but-in-sync
    golden: a codegen bug emitting an EMPTY `ffmpeg-allowed-decoders.lock` would be committed empty,
    regenerate to empty, diff clean, and silently disable the entire T1 FFmpeg-decoder-allow-list
    surface-expansion gate G38 — the `paths: {}` failure mode on a security manifest):** after
    regenerate-and-diff, assert each generated security manifest (the ffmpeg golden, the SBOM, the
    corpus manifest) parses to a **non-empty structure with ≥1 expected entry**, so an empty golden
    fails CLOSED (ties G19's non-empty sanity).
11. **Span-bound currency** — the security-vs-quality boundary prose ("every gate
    except G1") never claims a numeric span narrower than `max(Gnn)` in this catalogue,
    so a frozen `G2–Gxx` bound can never silently drift below a newly-added gate.
12. **IPC command-surface drift** *(glob-scoped to `src-tauri/`, L2 pre-push)* — the set
    of registered `#[tauri::command]` function names equals **exactly** the §0.4
    enumerated command set (the SET above incl. C2a/C2b/C13 — an AST/grep diff against
    the committed golden), so a spurious `#[tauri::command]` (a new WebView-reachable
    surface) is a deterministic gate failure, not a dual-review-only catch. (G22/G23 are
    format-matrix-scoped, not IPC-surface-scoped, so this closes the IPC drift they miss.)
    **`name`/`rename_all` caveat:** Tauri allows a `name`/`rename_all` override on
    `#[tauri::command]`, so the registered IPC name can differ from the Rust function name;
    a function-name diff would then pass while the real IPC surface drifted. ConvertIA makes
    it **project policy (a G29 Semgrep rule) that `name`/`rename_all` on `#[tauri::command]`
    are FORBIDDEN**, so the function name IS always the IPC name and this check's function-name
    diff is sound.
13. **Tauri plugin-surface drift** — the set of `tauri-plugin-*` crates in `Cargo.lock`
    equals an explicit committed allowlist (**single-instance, dialog, opener, store, log** —
    note that **dialog/opener/single-instance are deliberately Rust-side-ONLY with ZERO
    capability-file grants**, §0.10 lines 1359/1488: a `tauri-plugin-*` crate's presence in
    `Cargo.lock` does NOT imply a WebView capability entry); any unlisted `tauri-plugin-*`
    fails (the same deny-all-except-allowlist is also wired into `cargo-deny [bans]` at G18 so
    it fails at L2 before a capability JSON exists, closing the `cargo add` → G47 window).
    **Two INDEPENDENT assertions (the prior single "`Cargo.lock` set == capability-file plugin
    blocks" cross-check was logically unsatisfiable — it would hard-fail every clean checkout,
    because only `log`/`store` of the five allowlisted plugins carry a capability block while
    dialog/opener/single-instance carry none by §0.10 decision):** **(a)** the `Cargo.lock`
    `tauri-plugin-*` set == the committed allowlist (above); **(b)** every permission entry in
    every `capabilities/*.json` references only a plugin on the committed allowlist (a
    capability granting an off-allowlist plugin fails). The two are checked separately so
    (b) closes no real threat the deny-all-except-allowlist doesn't already close, but it can
    never produce the guaranteed false-positive the old phrasing did.
14. **DoD-list parity (machine invariant, not a prose promise)** — the 8-point Definition-of-Done
    exists in three copies (the canonical `build-loop.md` list, the G1 bullet in §1 here, and the
    P0.6 transcription); extract the ordered DoD items from all three and assert **the lettered
    sequence (a)-(h) is identical IN ORDER modulo whitespace (r7 — not merely the same SET: the
    8-point DoD is ordered, so a reordered copy must also fail)**, so a drift (the exact "8 vs 9 DoD points"
    class a prior gap-check had to fix) is a deterministic gate failure, not a dual-review-only
    catch. Stdlib-only (the loop reads `build-loop.md` and would otherwise silently operate on
    stale thresholds).
15. **Build-loop hard-stop-number parity** — the canonical hard-stop / token-Notbremse numbers
    stated in P0.6 (soft-stop ~8 boxes, hard-stop ~12 boxes, P0 cluster-boundary soft-stop ≥5
    committed boxes, 3 consecutive push failures = hard-stop) must appear verbatim in
    `build-loop.md`. **Canonical form: build-loop.md states the thresholds as EXACT integers with
    EXPLICIT comparison operators (r7 — not the tilde/`≥` prose forms), e.g. `soft-stop fires when
    committed-box-count >= 8`, `hard-stop at == 12`, `cluster soft-stop at >= 5 committed boxes`,
    `>= 3 consecutive push failures`** — so the verbatim match is unambiguous and the loop cannot
    read "~8" as 5 or 15; this check asserts those exact operator-anchored strings are present in
    `build-loop.md` so the autonomous loop cannot silently run on stale thresholds. Stdlib-only.
16. **Planted-positive coverage of every fail-closed security gate (turns the design's strongest
    idea from POLICY into a machine invariant)** — the catastrophic system failure mode is a
    security gate that is silently a no-op (wrong glob, parses-nothing-fails-open, watches the
    wrong PID) and reports green forever; the §0 policy is that every custom gate ships a G24
    negative self-test, and the critical gates do (G42/G42b canary, G17b planted-old-CVE, the
    checksum wrong-checksum test, G24a) — but it is asserted as policy, not enforced across the
    catalogue. This check asserts **every gate cited in security-concept §5 that is marked
    fail-closed has a REGISTERED negative self-test fixture** (a planted-positive in the
    committed `scripts/gate-selftests/` registry), else the build fails — the same "recipe
    present → recipe runs" upgrade the design applies everywhere else, applied to the gates
    themselves. **CORRECTNESS, not only LIVENESS, for the LOAD-BEARING custom gates (r7 — the
    regime proves a gate fails on its ONE planted violation, i.e. it is armed/not-a-no-op, but
    proves NOTHING about the OTHER members of the variant class it bounds: a G29 argv rule
    matching `--accept` but missing `-accept`/`--accept=…`; a G47 jq check passing a
    reordered-but-permissive CSP; a `gitleaks` rule matching the planted key but missing a
    base64-wrapped variant — all sail through forever):** for the named load-bearing custom gates
    — the **G29 project-local rules, the G2 custom rules, and the G47/G37/G42/G42b/G56 parsers** —
    this check requires **≥ N registered fixtures INCLUDING the named evasion variants** (the
    obvious bypasses of the match pattern), not merely ≥ 1, and/or **`cargo-mutants` over the
    gate-script crate** (the one detection-completeness tool — today scoped only to the three app
    kernel crates, NOT the gate scripts §0 calls the real security controls) as the
    owner-decidable correctness backstop. Stdlib-only for the fixture-count leg (reads the §5
    table + the self-test registry); `cargo-mutants` over the gate-script crate is the
    owner-decidable add-on. **API-introspection gates are CARVED OUT (r7 — G56a/G56b are
    fail-closed §5 gates that work by querying the GitHub API at RUNTIME, so a "registered
    planted-positive fixture" is un-automatable for them — there is no local repo-state that makes
    `gh api …/rulesets` deterministically fail):** check 16 EXEMPTS the
    API-introspection gates (G56a, G56b legs 1/2, the G56 repo-config sub-assertions) from the
    fixture requirement and instead records a **documented manual-verification step** (the owner
    toggles the setting off in a test repo / confirms the assertion fires) — so check 16 does not
    demand an impossible fixture, and the API gates' liveness is owner-verified, not falsely
    asserted as fixture-covered.
17. **§8-forward-idea ↔ adopted-gate status agreement (no cross-doc status drift)** — when a §8
    forward idea is PROMOTED to a real catalogue gate (it gains a `Gnn` row) its `[DEFER]`/`[DEFER:
    post-v1]` marker in §8 must be removed in the SAME commit, and its status must agree across all
    four homes that name it: the §8 entry, the catalogue `Gnn` row, the P0-build-and-security.md
    box, and the security-concept §10/§11 reconciliation note. The canonical case the check exists for
    is **attest-build-provenance (G59)**, which carried three contradictory statuses (§8 `[DEFER]` /
    P0.7 "promoted to a v1 OWNER DECISION" / §10 "Gate hardening this round = done") before r5. Asserts
    no gate that has a catalogue `Gnn` row also carries a `[DEFER]` marker in §8, and no §8 entry claims
    "promoted/adopted" while its P0.7 box still hedges "only if later adopted". Stdlib-only.
18. **Named-build-loop-procedure presence (so "currently absent" cannot survive into the final docs)** —
    every build-loop.md procedure that P0.6 names as a deliverable (initially: the **crash-recovery
    procedure** and the **dual-review divergence-resolution rule**) must actually exist in
    `build-loop.md`: a canonical verbatim phrase for each (check-15 style) must appear there, else the
    build fails. Together with the G8 deferral-vocabulary scan now including "currently absent" (so a
    leftover annotation fails G8 once the docs exist), this turns "the loop cannot safely operate without
    these two procedures" from a prose note into a machine invariant. Stdlib-only.
19. **Reviewer-rubric presence + sourced-verbatim + canonical-phrase coverage (r6 — the one piece of the
    G1 quality-amplifier the autonomous loop can silently weaken).** The self-trust story is "every Gnn
    except G1 is the real control; G1 is a quality amplifier whose auditable-smell spot-audit is the
    backstop" — but the loop's single biggest leverage point, the **review PROMPT/RUBRIC the two reviewers
    are given** (what they critique for: spec-conformance, the SPEC-CONTRADICTION-above-P0 class, severity
    ranking, divergence handling), was nowhere pinned as a committed drift-guarded artifact, so a loop that
    silently edits or under-specifies its own review prompt can blunt the amplifier the spot-audit depends
    on. This check asserts the reviewer rubric exists as a **committed fenced canonical block INSIDE `build-loop.md`**
    (the home DECIDED in r7 — NOT a separate `docs/process/review-rubric.md`, so the "seven P0 docs" exit
    criterion stays exact), is **non-empty**, the prompt the loop EMITS is **sourced from it verbatim**, and it
    contains the canonical **SPEC-CONTRADICTION-class + divergence-resolution** phrases P0.6 promises (the same
    canonical-verbatim-phrase discipline as checks 15/18). The rubric block joins the L(-1) security-critical-file
    set. Stdlib-only.
20. **Recorded reviewer-family decision present in `build-loop.md` (r6 — the one non-deterministic control's
    open residual is now forced to a recorded decision, not left open).** security-concept §4 states Opus+Sonnet
    share lineage so "both GO, 0 findings" is a CORRELATED signal and says the same-vendor choice is "an owner
    call recorded here, not silently assumed" — but it was neither closed nor decided. This check asserts
    `build-loop.md` carries the **verbatim recorded owner decision** (either "one reviewer is a different model
    family" + the IDs, OR an explicit owner ACCEPTANCE of the correlated residual) **plus a concrete spot-audit
    cadence** (e.g. every Nth box / every phase boundary), so the autonomous loop cannot silently ignore it.
    Stdlib-only.
21. **T2-taint-depth XOR (r7 — machine-enforces the G56a sub-check (f) owner decision so it is not doc-only).**
    The inter-procedural taint depth for the T2/WebView surface is owner-decidable between two sanctioned
    options — CodeQL `javascript-typescript` default-setup (G56a sub-check (f) leg i) OR the G29 rule (i)
    Semgrep `mode: taint` ruleset (leg ii) — and **exactly one must be live, not both, not neither**. This check
    asserts **EXACTLY ONE** of {a committed `codeql-default-setup` marker file recording the CodeQL choice, the
    presence of the G29 rule-(i) taint ruleset under `scripts/semgrep-rules/` with its registered planted-positive}
    is true — so neither a "both enabled" duplication nor a "neither, T2 taint silently uncovered" gap can survive,
    and the G56a sub-check (f) OR-gate can never be left permanently red by an undocumented choice. Stdlib-only.
22. **Gate-ID space gap-freeness (r7 — closes the catalogue-integrity hole the "every Gnn is the real control"
    thesis rests on).** check 5 is one-directional (catalogue ⊇ security-concept) and check 4 targets sub-section
    HEADINGS, not gate IDs, so NOTHING asserted the catalogue's own integer ID space is gap-free-or-annotated —
    and the catalogue silently jumped G60→G64 (G61/G62/G63 absent) and omitted any note for the deleted G40.
    This check enumerates every `| **Gnn** |` integer table row (the plain-integer rows only — a sub-letter id
    like `G33a` does NOT by itself satisfy its integer), computes `max(Gnn)`, and asserts **every integer
    `N` in `2..max(Gnn)` is EITHER a plain table row OR carries a documented vacated/reserved/deleted/split note** in the
    "Vacated / reserved gate IDs" blockquote (or the G34 note), so a fill-pass author can never collide with or
    misread an invisible ID. An integer present ONLY as sub-letter rows (`G33` → `G33a`/`G33b`, with no plain
    `| **G33** |` row) MUST carry a `split` note, so it is never invisible. A new gap (an inserted-then-removed gate) fails the build unless its note is added in
    the same commit. Stdlib-only.
23. **Informational→required ratchet decision-log presence (r7 — a silent status flip is invisible).** The
    informational-then-ratcheted gates (`cargo-mutants` G15-leg, G17b, G64, G65, the owner-decidable `cargo-acl`/
    Kani/`cargo-careful` contracts; plus `cargo-geiger`, which carries a ledger row too but is informational-forever
    — a census tool that never ratchets to required) transition informational→required via a ratchet/owner decision,
    but no check
    recorded the decision AT the transition — a silent informational→required flip (or the reverse) had no audit
    trail. This check asserts that **every §8-informational / owner-decidable gate has a dated status entry in a
    committed `docs/process/gate-status.md`** and that the entry's status agrees with the gate's effective
    required-vs-informational posture (mirroring the dual-review correlated-residual recorded-decision pattern and
    plan-lint check 17's cross-doc status agreement), so a status change is a dated committed line, never an
    invisible flip. **Delivered (P0.4.5):** the ledger lands (`docs/process/gate-status.md`) with the four
    over-assurance contracts; `plan-lint` check 23 gains its real logic — `_OWNER_DECIDABLE_GATES` (the
    effective-posture registry, seeded with the four, appended by later boxes) + the ledger-table parse +
    presence/well-formedness/posture-agreement assertions; a flip edits BOTH the ledger row AND the registry in
    one owner-acked commit. Stdlib-only.
24. **P0-completion-record format (r7)** — once `docs/process/p0-completion.md`
    exists, assert its `run_url:` field matches
    `https://github.com/Ne-IA/convertia/actions/runs/<id>` (the immutable
    Actions-run URL recording the P0-completion gate run); **exit-2 / skip while the
    file is absent** (P0 not yet complete), **fail-closed once it lands**.
    Stdlib-only. (Promotes the r7 unnumbered P0-completion URL-format check to a
    numbered §6 check, so the plan's `check 24` reference resolves.)
25. **Doc-graph integrity & freshness (the G68 leg — generalizes checks 2/5/8 from
    single-source assertions to the WHOLE doc graph).** The general "no
    source-of-truth drifts from the doc graph" net, in three parts: **(a) no
    orphan** — every **repo-tracked** `.md` under `docs/` (+ the root governance docs
    `CLAUDE.md`/`README.md`, and `CONTRIBUTING.md` once it lands — the "over the docs
    that EXIST" hedge means a not-yet-authored doc is out of scope, not a false-fail)
    is reachable by a link from its proper index ([`docs/spec/README.md`](../spec/README.md)
    for the spec category files, [`docs/plan/README.md`](../plan/README.md) for the
    plan phase files, `CLAUDE.md §9` + the process/security docs' own `## References`
    for the process/security docs), so a doc linked from nowhere fails; **(b)
    cross-doc resolution** — every INTER-document reference across the **repo-scoped**
    graph (`CLAUDE.md` ↔ SSOT ↔ `docs/spec/` ↔ `docs/security/` ↔ `docs/process/` ↔
    `docs/plan/`) resolves to a real file/heading/anchor (the inter-document edges
    check 2's spec-internal `§X.Y` resolution does not span); **(c) freshness** — when
    an authoritative source changes (a `Gnn`, an enum variant, a path, a version pin,
    a convention, a decision — **including a spec [§0.7](../spec/00-architecture.md)
    physical-tree edit**, fingerprinted so a §0.7 change not mirrored into the
    CLAUDE.md §1a map surfaces as stale, the freshness companion to check 26's
    projection bind), every doc that DESCRIBES it the old way fails (the gates→`.md`
    case is one instance), asserted via a per-source content-fingerprint the
    describing doc was reasoned against, the same fingerprint discipline the G29
    suppression ledger uses. It does NOT subsume the precise single-source parity
    checks (8/14/22) — it is the graph-wide orphan/resolution/described-the-old-way
    net no single check spans. **Scope note:** the out-of-repo `~/.claude` memory
    channel (`MEMORY.md` + the memory files) is NOT a node here — it lives outside
    `git ls-files` and a repo-scoped gate cannot reach it; its population is audited
    once, manually, in P0.1.7, so this check names only nodes it can resolve.
    Stdlib-only; fail-closed from P0 over the docs that exist (the doc graph is live
    from the seven P0 docs). This is the G68 implementation; G68 is its catalogue row.
26. **Structural-map integrity (the G69 leg).** Parse the CLAUDE.md "Repo layout"
    map (a fenced/list block, not a regex over prose) and assert it is **bidirectional
    with the on-disk tree**: every repo directory (top-level + every significant
    subdir) in the `git ls-files`-derived directory set ∈ the map (a folder created
    without updating the map fails), AND every dir NAMED in the map exists on disk (a
    stale map entry fails). A committed ignore-list excludes the genuinely-out-of-scope
    trees (`target/`/`node_modules/`/`dist/`/`.git/`, the `.gitignore` set). **Plus the
    dual-authority bind:** assert the CLAUDE.md §1a dir set is a **faithful projection
    of the spec [§0.7](../spec/00-architecture.md) physical tree** (every §1a dir ∈
    the §0.7-enumerated tree — §0.7 is the higher source per SSOT > spec > docs, so
    §1a may not invent a dir §0.7 does not home; a §1a-vs-§0.7 divergence fails),
    closing the §1a↔§0.7 edge so the map gate guards both §1a↔disk AND §1a↔§0.7.
    Stdlib-only; **exit-2 / skip while the CLAUDE.md "Repo layout" map block is still
    the P1 placeholder stub, fail-closed once the authoritative map is authored at
    P1-end** (the G69 `→ activated in P1` bootstrap annotation), tolerant of the
    already-existing `docs/`/`assets/` dirs in the interim. This is the G69
    implementation; G69 is its catalogue row.

## 7. Reconciled during P0 review r1

- **Concrete tools picked (closed).** secrets-scanner = **`gitleaks`** (G2);
  CSP/capability-lint = **G47** (`jq`/`serde_json` structural); SAST = `#![forbid(unsafe_code)]`
  *(corrected to `#![deny(unsafe_code)]` in r3 — `forbid` is un-overridable; see §9)*
  + Semgrep packs (`cargo-geiger` informational only, G29); in-core fuzz harness =
  **G48** (`cargo-fuzz` over `crate::detect`, Linux+macOS nightly); per-OS observability =
  G42 (netns/nftables+strace · `pf` block-log+tcpdump · Windows firewall-block+ETW);
  CI hardening = `actionlint` (G49) + `zizmor` (G50) + lockfile integrity (G18a).
- **Every §0.11 class has a verifying gate (closed).** Enforced mechanically by
  plan-lint check 8 (§6); the new gates G47/G48/G38b/G17b close the previously
  runtime-only classes T2/T2a/T2c, the in-core T1 path, T6's source-bundle, and the
  T3 CVE-awareness signal.
- **Living-doc/spec-sync.** Gates added here that the spec only named as `[DEFER]`
  (CSP-lint) or did not name (in-core detector fuzz, the SAST layer, `actionlint`/
  `zizmor`/lockfile-integrity) are reconciled into the spec **in the same change**
  per the SSOT > spec > docs conflict order.

**Still owner-decidable (ratchet plan, not blocking the P0 *design*):**

- [ ] Which L4 gates are *required checks* vs informational on day one, so a
  half-built P1 isn't wedged (G17b/`cargo-geiger` are informational by design).
- [ ] **Required-vs-informational** for the new owner-decidable gates: `cargo-mutants`
  (G15 mutation-testing sub-leg, below), the AFL++/radamsa engine-subprocess fuzz
  (reserved **G65**, owner decides before P10), the `cargo-acl`/cackle capability upgrade
  (now an owner-decidable P0.4 contract, §8), the privilege-drop-tier ratchet (**G64**),
  and the r5 in-core over-assurance contracts **`cargo-careful` + Kani** (§8) — same class
  as G17b/geiger.
- [x] **Security-critical-file change-control L(-1) — DECIDED (owner decision D1, r15).**
  The open "whether to require an ack" question is resolved: **YES.** Any commit touching the
  L(-1) security-critical-file set is **owner-approved before push via a `L-neg1-ack: owner`
  commit-body trailer**, the autonomous Build-Loop **NEVER** edits an L(-1) file (a needed
  L(-1) edit is a hard-stop + escalation so the owner makes/acks it), and the pre-push gate
  **G71** (L2 row, below) enforces it (fail-soft during the P0 bootstrap, fail-closed from P1).
  The **authoritative file set is the committed glob list `scripts/l-neg1-files.toml`** (itself
  L(-1)); the gate + list + self-test are wired in **P0.2.14**. The full policy + the enumerated
  set live in **[security-concept §2](security-concept.md#2-working-model--two-sessions-one-branch)**
  and the **G71** catalogue row.

## 8. Reconciled during P0 review r2

- **CI runner-host integrity (G56, new).** The signing secret must never share a host
  with the untrusted Lane-B corpus/fuzz inputs on the shared VPS (spec §6.1.4/§6.7.2);
  a workflow lint fails any secret-using job bound to a self-hosted label, and the
  signing step is bound to an ephemeral GitHub-hosted runner (spec §6.7.2 synced).
- **T3a DLL/dylib side-loading (new §0.11 class).** Per-shared-object SHA-256 verify
  (G37), manifest-diff guard (G35), dynamic-dependency closure (G37b), bundle-only
  `PATH` on Windows. §0.11 → 15 classes (check 8 enumeration updated). *(r3 added T11 →
  16 classes; r15 added T12 unsigned-distribution/download-MITM → **17 classes** (current);
  see §9 / §16 of security-concept.)*
- **G2 secrets gate corrected.** The PEM-rule "catches a minisign key" claim was
  factually wrong (minisign keys have no `-----BEGIN-----` envelope) — a committed
  custom rule + a full-**history** scan leg + `MINISIGN_PASSWORD` added.
- **G35 SBOM tool corrected.** `@cyclonedx/cyclonedx-npm` (npm-only) → `@cyclonedx/cdxgen`
  (native `pnpm-lock.yaml`); mandatory `purl` + SHA-256 `engines.lock` schema fields so
  G17b/G37 match by PURL (not an empty match) and verify a named hash.
- **G37 pin provenance.** The recorded SHA-256 is corroborated against upstream's own
  published checksum/signature at pin time (the xz/liblzma class), the source URL
  recorded; any `engines.lock` SHA edit is a hard Co-Pilot escalation.
- **G38 LibreOffice + pandoc.** Added the LibreOffice `registrymodifications.xcu`
  macro/profile build assertion (the previously-ungated T1 macro RCE control) + the
  pandoc `--sandbox` version-floor (recorded as ≥ 2.17 at r2 — **corrected to ≥ 2.15 in r6**, a baked-in
  wrong fact: `--sandbox` shipped + is effective since 2.15, matching spec §3.5.4; see §12); corpus
  sentinels in G31.
- **G32 round-trip de-vacuumed.** Replaced with SOURCE-UNCHANGED + OUTPUT-VALIDITY;
  the literal byte-stable check is scoped to the small truly-invertible set.
- **plan-lint check 5 made satisfiable.** One-directional (catalogue is the superset);
  the reverse where it matters is check 8. New checks 10 (generated-manifest currency)
  + 11 (span-bound currency).
- **New gates this round:** G18c (`.npmrc` registry pin), G18d (`onlyBuiltDependencies`
  lockdown), G35b (SBOM-diff), G36b (frontend GPL/AGPL deny), G37b (dynamic-dep
  closure), G41b (pre-publish archive validity), G56 (CI runner-host integrity).
  Boundary statement de-frozen from "G2–G50" to "every gate except G1".
- **G29 broadened** to `convertia-imgworker` (densest unsafe surface), Semgrep
  offline-vendored, `shellcheck` over all `.sh`, the structural `.env_clear()`/argv
  Semgrep rule, the `#[tauri::command]` path-validation rule. **G4/G14** gained the
  no-panic-sloppiness deny set + exhaustive-match deny. **G48** gained `fs_guard` +
  CSV/TSV fuzz sub-targets + committed crash-corpus replay on all platforms.
- **Owner-decidable mutation gate.** A scoped `cargo-mutants` leg over
  `crate::fs_guard` + `crate::detect` + `crate::outcome` (the no-harm/atomicity kernel)
  is added as a release-tier informational-then-ratcheted gate — line coverage proves a
  line executed, not that a test would CATCH a regression; flagged required-vs-informational
  for the owner like G17b. The §6.4.2 atomicity-under-interruption test injects the kill
  **specifically in the post-`sync_all()`-pre-`rename` window** (a `#[cfg(test)]` fence in
  `crate::fs_guard::atomic_publish` on all 3 OS) so it exercises the §2.1.3 two-state
  invariant at the critical boundary, not an uninteresting pre-sync kill.
- **Living-doc/spec-sync (r2).** The §3.7.2 `engines.lock` schema (`purl` + SHA-256),
  the §3.5.2 LibreOffice profile assertion in §6.1.3, the pandoc version-floor, the
  §2.12.4 in-core-surface reconciliation (it must list the OLE2/CFB read + the bounded
  XML peeks that §1.2/G48 actually cover), the §5 T6→G53 explicit citation, and the
  §6.7.2 signing-runner binding are owned by the spec and synced in the same change.

### Forward ideas (non-blocking; reconcile into the spec FIRST per the conflict rule)

- **OSSF Scorecard informational corroboration (gate G67, reserved id; r5).** **`ossf/scorecard-action`**
  is a free, recognized public trust signal that independently corroborates exactly this repo's risk
  profile — branch-protection, pinned dependencies, dangerous workflows, token scope — the same axes
  the bespoke G50/G56/G56a assert. Add as an **informational scheduled workflow** (push + schedule on the
  default branch, per the action's supported triggers; a PAT is needed for the Branch-Protection check on
  a public repo, `id-token: write` only if `publish_results` is enabled) that cross-checks the bespoke
  gates — a divergence (Scorecard flags something G50/G56/G56a passed, or vice-versa) is a Co-Pilot review
  item, never a required green check (it is a third-party scanner, not a deterministic in-repo gate).
  Content-independent; homed in P0.2. Reserved id **G67** so adoption does not renumber.
- **`cargo-careful` + Kani for the in-core untrusted-byte kernel (owner-decidable; r5).** The §1.2
  detect/`fs_guard` path runs untrusted bytes OUTSIDE the §2.12 isolation boundary (principle 9) — the one
  place a panic/UB lands in the core, the highest-value Rust to over-assure. **(a) `cargo-careful`** runs
  the existing test/fuzz suite with extra std debug assertions + runtime UB checks at near-normal speed (a
  superset of what plain tests catch, complementing Miri which doesn't run the C/C++ FFI) — add as an
  in-core test/fuzz-leg wrapper on the Linux+macOS nightly legs (it needs the date-pinned nightly G48
  already pins). **(b) Kani** (bounded model checking) can PROVE — for all inputs, not fuzzer-hoped — the
  small, bounded, total-function load-bearing numeric caps: the ≤100× decompression-ratio bound, the
  `MAX_SVGZ_SNIFF ≤64 KiB` bound, the `fs_guard` path predicates. This takes the design's own "bound-firing
  must be structurally proven, not fuzzer-hoped" principle to actual proof for those few pure functions.
  Both **owner-decidable** (like G64/G65); neither replaces G48's fuzz, they harden the highest-value
  in-core surface above it.

- **`actions/attest-build-provenance` — ADOPTED as a v1 OWNER DECISION (gate G59; no longer
  `[DEFER]`).** *(Status note: r5 resolved a three-way drift — this idea was `[DEFER: post-v1]`
  here, "promoted to a v1 OWNER DECISION" in P0.7, and "Gate hardening this round" in
  security-concept §10. ONE status is now propagated: adopted-for-v1, catalogue row **G59**,
  P0.7 wiring unconditional. plan-lint check 17 asserts the four homes agree.)* GitHub's free
  build-provenance attestation is the ONE genuinely-free, in-scope-adjacent trust signal for an
  unsigned, no-notarization portable build — it is **not** binary code-signing (so it doesn't
  breach the SSOT out-of-scope line) and is strictly additive to minisign (which proves
  checksum-manifest integrity but says nothing about build ORIGIN). Needs only `id-token:
  write` on the release job (asserted scoped to ONLY that job, G56/P0.7), produces a
  Sigstore-verifiable attestation (`gh attestation verify`), no key to manage. **VERIFIED, not
  just generated** (the "generated but never checked" class G35/G17b/G58/G39 were hardened
  against): a release-tier step runs `gh attestation verify` against the just-produced
  artifact on a clean runner and fails the release on non-zero; the attestation joins the
  G58 completeness enumeration; the **Sigstore bundle ships as a named release asset** so
  users verify build origin OFFLINE (`gh attestation verify --bundle <file> --repo`) without
  Sigstore network access — the one corroboration that survives a leaked key from a poisoned
  shared host (the completion of the principle-11 host-isolation investment). See G59.
- **Engine-subprocess coverage-guided fuzz (the highest-risk surface).** Engine-side T1
  (the C/C++ decoders) is covered today only by a fixed fault-injected corpus through the
  §2.12 boundary (G26/G31) — regression samples, not coverage-guided exploration —
  while `cargo-fuzz` (G48) correctly only reaches the in-core Rust detector. So the
  highest-risk surface gets the weakest input generation. Add a **non-blocking CI-tier /
  periodic black-box mutational fuzz of the real sidecar** (AFL++ binary-only/QEMU mode,
  or a radamsa harness feeding the real engine through the isolation wrapper), reusing the
  §2.12 boundary + §6.4.2 oracles (no-crash-escapes-boundary + no-egress +
  no-out-of-input-read). **Constraint:** the harness MUST use the same **SHA-256-verified
  bundled engine binary G37 stages** (from the `engines.lock`-pinned cache), not a
  separately-compiled debug build. **This is the single biggest coverage asymmetry in the
  regime** — the HIGHEST-risk surface (bundled C/C++ decoders on untrusted bytes, the literal
  product premise) gets the WEAKEST input generation (G26/G31 fixed fault-injected corpus)
  while the lower-blast-radius in-core Rust detector gets real coverage-guided libFuzzer (G48).
  So this is **promoted from a bare forward idea to a NAMED owner-decidable gate with a
  RESERVED id `G65`** (required-vs-informational like G17b — even a non-blocking periodic CI
  run materially changes the risk posture vs a fixed corpus); it stays non-blocking-by-default
  so it can't wedge a half-built phase, and the id `G65` is reserved now so an adoption does
  not renumber. **Homed as a P0.7 box (r5)** with the AFL++ binary-only/QEMU-or-`radamsa`
  choice, the G37-staged-SHA-verified-engine constraint, and an **owner decision required BEFORE
  P10** — so the fill pass has an anchor and the decision is not orphaned downstream.
- **`cargo-acl` (cackle) capability policy — OWNER-DECIDABLE P0.4 gate (r5; the behavioural
  backstop to the name-based T9a ban and the new G29 rule (g)).** Structural upgrade to G18's
  name-based HTTP-client `[bans]` AND to G29 rule (g)'s first-party-source `std::net` ban: a
  per-crate `std::net`/`std::process` capability policy (a committed `cackle.toml` denying
  `std::net` to the WHOLE dep graph except an explicit nothing, and `std::process::Command` to
  `crate::isolation` only) enforced at build time over the entire graph — turning the T9a "opens
  no socket" claim from name-based into **behavioural** (a renamed/transitive network crate
  reaching `std::net::TcpStream` directly can't slip the list, which G18's name-based ban and even
  G29 rule (g) — first-party source only — miss for a third-party crate). Linux-only is acceptable
  (a build-time graph check). Made **owner-decidable informational-then-required like G64/G17b**;
  the name-based `[bans]` (G18) + the per-push G29 rule (g) stay the always-on baseline. Pairs with
  `cargo-vet` (who-do-I-trust) + cackle (what-can-this-crate-do).
- **Startup integrity tied to the signed `SHA256SUMS`.** The spec is honest that T3
  startup integrity gives no runtime tamper-resistance (an attacker who swaps a binary
  swaps the in-bundle manifest too). A near-free hardening: make the §7.2.3 startup
  verifier read engine hashes from the **minisign-signed `SHA256SUMS`** (or a signed
  subset bundled as a resource) and verify the minisign signature with the committed
  pubkey at startup — it still can't stop a whole-bundle-including-pubkey replacement, but
  it removes the trivial "swap the unsigned manifest too" path and reuses in-scope
  machinery. Owner strengthening, not a blocker.
- **Scheduled engine-version-currency poller — PROMOTED to a P0.7-owned earliness signal (gate
  G66, reserved id; r5).** A scheduled GitHub Actions workflow polls the upstream engine release
  endpoints (FFmpeg/LibreOffice/poppler/pandoc/libheif/dav1d) and **opens an issue when a newer
  version than the `engines.lock` pin exists**. For a no-auto-update offline app shipping
  known-vulnerable-CLASS C/C++ decoders, the DOMINANT real-world patch path is "upstream shipped a
  security fix but no CVE/OSV advisory exists yet" — G17b is CVE-driven and structurally blind to
  it, so this is the ONLY signal for the dominant silent-fix case. It is content-independent
  (`.github/` config only), an **issue-OPENER not a gate** (so it cannot wedge the autonomous loop),
  feeds the already-authored `vuln-response.md` runbook, and honours SSOT §3.8 best-effort currency.
  Promoted to a named P0.7 deliverable with the reserved id **G66** so plan-lint check 5 tracks it
  and it does not vanish back into §8.
- **Intermediate T9a structural `std::net` check — PROMOTED to G29 project-local rule (g) in r5
  (no longer a forward idea).** The G29 Semgrep rule failing on `use std::net::` /
  `use tokio::net::` outside a committed (initially-empty) allow-list — the cross-platform per-push
  Rust-source proof that first-party Rust opens no socket — is now a concrete catalogue rule, cited
  in the §5 T9a verifying-gate cell, with a planted-positive. (`cargo-acl`/cackle remains the
  behavioural §8 upgrade below.) The G18b `cargo-vet` + `--locked` checksum-anchor note still holds: a `cargo-vet`
  audit is sound only when paired with the G18a `--locked` checksum-pinned lockfile (Cargo
  verifies the registry SHA on download), so an audited version cannot be substituted
  post-audit. *(The advisory-DB staleness floor formerly flagged here as a forward idea
  is now PROMOTED to an owned release-tier sub-check in G17/G17b — RustSec via `cargo audit
  --json` `.database.last-updated`, OSV/grype via its DB timestamp, both against a committed
  `MAX_ADVISORY_DB_STALENESS` ≈ 7 days, with the dated open-CVE report recording the DB age
  it was generated against.)*

### Forward ideas surfaced in P0 review r6 (non-blocking; reconcile into the spec FIRST per the conflict rule)

- **CodeQL code-scanning (or a Semgrep taint-mode ruleset) for the TS/WebView T2 surface.** The Rust
  side has a strong bespoke SAST layer (`deny(unsafe_code)` + Semgrep `p/rust` + project-local rules +
  `cargo-fuzz`); the TS/WebView side — the entire T2 attack surface — gets only ESLint + Semgrep pattern
  packs, with **no inter-procedural taint analysis** from a WebView-supplied value to a dangerous sink (a
  string reaching a DOM/eval-adjacent path or an IPC arg). **CodeQL default setup (`javascript-typescript`)**
  is free + GitHub-native and fits the "free, load-bearing, invisible-config" philosophy — assert it ENABLED
  in the G56a sub-check set (like secret-scanning, since it is silently disable-able). If CodeQL is rejected,
  add at least a **Semgrep taint-mode project-local ruleset** (sources = IPC/WebView inputs, sinks = dangerous
  DOM/path ops) to G29 so the T2 surface has the Rust side's depth. (Rust CodeQL support is more limited, so
  the Rust side stays on the bespoke layer.)
- **JS-side name-trust (a committed npm-package allowlist — the `cargo-vet` analogue G18c lacks; reserved id G18e, r7).**
  G18c gives the JS tree a resolution-URL guard (every `pnpm-lock` URL ∈ the allowed registry) but **no
  name-based trust**: a typosquat or convincing-name malicious package hosted ON npmjs.com passes G18c
  entirely, whereas `cargo-vet` (G18b) closes that for Rust — and the WebView renderer is the entire T2 surface.
  The lightweight closure (cheaper than the Socket CLI) is a committed **`scripts/npm-allowlist.txt`**
  enumerating every name+scope in `pnpm-lock.yaml` + a pre-push diff: a NEW package requires an allowlist entry
  with a Dual-Review note before install (the same ledger pattern as the G29 suppression ledger + the cargo-vet
  `imports.lock`). **Reserved id `G18e`** so adoption does not renumber. The **`socket` CLI** (Socket Security,
  free for public OSS — flags typosquats, install-script anomalies, high-entropy strings, known-malicious
  packages) can be added LATER as a non-blocking corroborator. At minimum the gap is documented honestly in the
  §5 T2 row + G18c (already noted there as the accepted-for-v1 residual + the lightweight closure).
- **Bind the in-bundle engine-integrity manifest into the compiled binary (build.rs include) — closes the
  trivial "swap manifest alongside binary" path of the T3 residual.** The T3 honest residual is "a whole-bundle
  swap swaps the in-bundle manifest too". A near-free additional binding: `build.rs` computes
  `SHA-256(engine-integrity.json)` and embeds it as a `const`; the §7.2.3 startup verifier checks
  `SHA-256(read engine-integrity.json) == embedded_const` BEFORE any engine-hash lookup — so forging startup
  integrity now requires replacing BOTH the manifest AND the binary, not just the manifest file. Still cannot
  stop whole-binary replacement, but it is the lowest-cost step before the full "startup reads from the signed
  `SHA256SUMS`" upgrade (already listed above). Cost: one build.rs step + one startup comparison. Name it in the
  T3 row.
- **DNS-resolution observation leg for G42 — ADOPTED in r7 (now a REQUIRED G42 sub-assertion).** Promoted from
  a forward idea: a hostname lookup can succeed/be observed via the OS stub resolver even when TCP/UDP is blocked
  at nftables/pf, and a crafted SVG/HTML with an internal hostname could exfiltrate via a DNS-only side-channel
  (the G47 DNS-prefetch meta-tag covers the WebView, not the engine subprocesses). G42 now runs
  **`tcpdump -i any port 53`** (Linux/macOS) / the **Windows ETW `Microsoft-Windows-DNS-Client` provider** over
  the engine-PID scope, asserting **zero DNS queries** in the egress-deny window with the same armed-window canary
  and a resolver-cache flush before arming — see G42.
- **Bound the gate-infrastructure's own resource use on adversarial input (ReDoS / huge-diff denial-of-CI).**
  The product's untrusted-byte paths are fuzzed (G48) and engines fault-injected (G26), but the GATE
  infrastructure processes attacker-influenceable input on the highest-privilege host — commit subjects, branch
  names, diff content, fork-PR bytes flow through G11's regex, the G8/G21 marker scanner, the fastpath
  skip-regex, and plan-lint's parsers. A catastrophic-backtracking commit subject or a pathological diff can
  hang/OOM a gate (a denial-of-CI on the shared VPS, the class G48's `-rss_limit_mb`/`-timeout` pin for the
  fuzzer but NOT the gate scripts). Extend G24/§0: each custom gate / fastpath / plan-lint script carries a
  **resource-bound adversarial-input self-test** (a backtracking-bait subject, a huge synthetic diff, a
  deeply-nested doc) it must reject within a pinned wall-clock + memory bound; extend the G56 per-job
  `timeout-minutes` to the gate-script jobs; and **name a linear-time regex engine (RE2 / the Rust `regex`
  crate)** for any regex fed externally-authored text.
- **TruffleHog verified-secret mode as a release-tier corroborator to G2's full-history leg.** G2 is thorough
  but all PATTERN/entropy-based; for a PUBLIC repo where a leaked credential is irrecoverable the missing axis
  is VERIFICATION (does the matched string actually authenticate). Add **TruffleHog** (pinned + checksum-verified
  per §0) as a **non-blocking release-tier corroborator** on G2's full-history leg: VERIFIED findings = hard
  escalation (false-positive-free by construction), unverified = Co-Pilot review item — analogous to how
  G67/OSSF-Scorecard corroborates G50/G56. Keep `gitleaks` as the deterministic per-push baseline. **Egress
  interaction to document (r7):** TruffleHog VERIFICATION makes live API calls to the credential provider, so
  on the harden-runner BLOCK-mode signing runner the providers it must reach (`api.github.com`,
  `api.anthropic.com`) must be in the egress allowlist — name this in the G2 body if adopted (a verified-mode
  scan that silently can't reach the provider would degrade to unverified, masking a live key).
- **Move the minisign secret behind a GitHub Environment with required reviewers + a `v*` deployment-tag
  restriction — ADOPTED in r7 (no longer a forward idea).** Promoted from a forward idea to a recorded owner
  decision: the three release secrets move into a **`release` GitHub Environment** with **required-reviewers + a
  `v*` deployment-branch/tag policy** so the secret is structurally UNREACHABLE (never injected) until a human
  approves — closing the residual that G56b leg-2 ("abort before reading the secret") runs INSIDE a job that
  already had the secret injected at job start. It is now a **G56 sub-assertion** (`gh api
  repos/:owner/:repo/environments/release` → required_reviewers + the `v*` deployment_branch_policy + the job's
  `environment: release` binding, fail-soft in P0 bootstrap then hard) and a **P0.7 provisioning deliverable**.
  Complements G56b; turns "abort before reading the secret" into "secret never injected without approval".
- **Third-party-rebuild recipe (the human half of G60).** G60 (third-party reproducibility via `diffoscope`) is
  reserved/informational — but for an MIT "audit it yourself" product the more impactful achievable thing is the
  HUMAN-FACING half: a committed **`docs/reproduce.md`** recipe + a build-environment lock (pinned base-image
  digest, `rust-toolchain.toml`, Tauri CLI/bundler digest, exact build command, expected per-file SHA-256 of the
  Rust core) that an independent party can follow to rebuild the compiled layer and compare against the published
  hashes. G59 attestation proves "GitHub's runner built this from this commit" but does not let an outsider
  REPRODUCE and disagree — this turns G60 from a self-check into an actual third-party trust signal, completing
  the audit-it-yourself posture alongside G55/G17b.

## 9. Reconciled during P0 review r3

- **`forbid` → `deny` unsafe policy (G29).** `#![forbid(unsafe_code)]` is un-overridable
  and cannot coexist with the allow-listed FFI module — invalid Rust for the FFI-heavy
  imgworker/core. Corrected to `#![deny(unsafe_code)]` at root + `#[allow(unsafe_code)]`
  on exactly the one FFI module path (asserted); `forbid` reserved for a pure-logic
  zero-unsafe sub-crate. Spec §6.4.2 synced.
- **Minisign recipe corrected + executable (G39/G44).** `-P <docs/minisign.pub>` (the
  inline-string flag fed a file path → fails) → `-p docs/minisign.pub` (the file-path
  flag); G39 now RUNS the literal recipe, G44 asserts the literal form. Spec §6.2.3/§6.2.4
  + README synced.
- **gitleaks current subcommands + L2 leg (G2).** `protect`/`detect` were deprecated in
  v8.19 → `gitleaks git --staged` (L1) / `gitleaks git` over `@{u}..HEAD` (new L2 leg, the
  last local catch, excluded from the docs-only fastpath) / `gitleaks dir` (L4) /
  full-history `gitleaks git` (release).
- **New gates:** **G12** moved to its real L2 plane (skip-regex parity enforced via a
  shared script + G54); **G24a** corpus/crash-fixture integrity manifest + `git lfs fsck`;
  **G37c** Linux glibc symbol-version floor; **G56a** GitHub branch-protection config
  assertion (the invisible repo state that makes a red L4 actually block); **G57**
  Principle-11 English-only / string-ownership lint (spec §6.10 row 23, previously
  catalogue-less); **G58** release-artifact completeness meta-gate.
- **Gate hardening this round.** **G42** gained the mandatory planted-positive armed-window
  self-test + the honest macOS pf-availability fallback (in r4 stated as `nettop`; **corrected to
  `tcpdump` in r5** — `nettop` observes socket STATE not the connect() attempt, breaking the
  armed-window canary on a blocked connection; not `dtrace`, which SIP blocks) + Windows named-pipe
  enumeration; **G37** gained the spec §3.8 engine-acquisition
  provenance (the FFmpeg worst-case anchor) + the engine-source allow-list + the static-link
  reconciliation + machine-checked cache-restore re-verify; **G35** gained the static-link
  SBOM leg + SPDX-expression validation (`spdx` crate / `cargo-about`) + NOTICE parity;
  **G29** gained the macOS-T11 TCC-staging rule, the single-store-name (T2c) rule, the
  pandoc `--resource-path` + LibreOffice no-`--accept` argv rules; **G48** gained the
  zip-slip target, ASAN-asserted-on/UBSan/Miri legs, and the deterministic bound-firing
  fixtures; **G31** gained cross-library decode validation (`ffprobe` vs libheif), the
  RFC-4180 CSV parse + CSV-injection assertion, and the macOS T11 first-accessor sub-test;
  **G46** gained the `QuarantinedByOs` sub-test; **G38** gained poppler-no-network +
  the mitigation census (`checksec`/`dumpbin`/`otool`); **G9** concretized with a non-empty
  invariant list; **G18/check 13** added the Tauri plugin-surface deny-all-except-allowlist;
  **check 12** added IPC command-surface drift; **check 8** now **16** classes (T11);
  **check 9** treats the IPC command set as an explicit SET incl. C2a/C2b/C13.
- **Two-plane / fail-mode crispness.** G21 deferral gate is full-tree + fail-closed at the
  L2/CI mirror; the gate-tool checksum verify (§0) is promoted to a self-tested control with
  `pip --require-hashes`; the build toolchain (base image, C/C++ toolchain, Tauri bundler,
  date-pinned `cargo-fuzz` nightly) is brought into the pinned trust boundary.
- **Living-doc/spec-sync (r3).** Spec §6.7 (single-branch reconciliation), §6.4.2 (`deny`
  unsafe), §6.2.3/§6.2.4 (minisign `-p`), §3.8 (engine acquisition mode + engine-source
  allow-list), §0.10 (`frame-ancestors 'none'`), §0.11 (T11) are owned by the spec and
  edited in the same change (SSOT > spec > docs).

## 10. Reconciled during P0 review r4

- **New gates:** **G42b** (T9b read-half fs-audit — `ptrace`/Landlock, fail-closed when
  neither is available, out-of-input sentinel + planted-positive, the symmetric sibling of
  G42 so the read half can never silently no-enforce; spec §6.4.2); **G64** (privilege-drop-
  tier ratchet — record the achieved §2.12.3 tier per platform in `privilege-drop-coverage.toml`,
  decrease-guarded, so a net regression of the most consequential runtime property is visible;
  owner-decidable); **G65** (reserved — the §8 engine-subprocess coverage-guided fuzz, the
  biggest coverage asymmetry, owner-decidable). plan-lint **checks 14** (DoD-list parity),
  **15** (build-loop hard-stop-number parity), **16** (every fail-closed §5 gate has a
  registered planted-positive — policy → machine invariant).
- **plan-lint check 13 made satisfiable.** The "set == capability-file plugin blocks"
  cross-check was unsatisfiable (dialog/opener/single-instance carry zero capability grants by
  §0.10) → two independent assertions (allowlist membership + capability-entry membership).
- **G47 + spec §0.10** gained the three by-construction release-hardening keys
  (`withGlobalTauri` off, `dangerousDisableAssetCspModification` off, release `devtools` off);
  the stale spec §0.7 main.json directory comment corrected.
- **harden-runner runner-type reality.** Its free/Community tier works only on GitHub-hosted
  runners (self-hosted needs Enterprise) — G56/principle-11/§5/P0.2/spec corrected; the
  self-hosted VPS leg relies on G42/G42b + the VPS egress allowlist + an ephemeral/JIT low-priv
  runner.
- **Gate hardening this round.** **G37/G56** general build-cache-poisoning policy; **G17/G17b**
  advisory-DB staleness floor + mandatory FFmpeg CPE + internal-decoder planted-positive;
  **G35/G36/G36b** bundled fonts as first-class `engines.lock` rows + OFL/Liberation provenance;
  **G37** from-source `gpg`/`sq` signature verify against pinned keys; **G18b** committed
  cargo-vet `imports.lock`; **G48** fs_guard Windows dangerous-path classes + NUL/`PATH_MAX`+1 +
  zip-slip bound-firing fixtures + the IPC-handler serde-boundary target + the imgworker FFI
  target; **G29** the OWASP Semgrep pack + the `name`/`rename_all`-forbidden rule; **G8** the
  broadened deferral vocabulary; **G54** the push-from-stale-base guard + the double-predicate
  check-off shape; **G24** the L4 self-test-prelude; **G37b** macOS `otool -l` LC_RPATH; **G42**
  the process-scoped Windows socket snapshot + named-pipe tool + ETW privilege/fail-closed;
  **G44** the minisign-fingerprint-consistency sub-check; **G24a** the corpus-provenance
  sub-check; **G33a** the bootstrap annotation. The §8 attest-build-provenance idea gains a
  verify-on-runner step + an offline Sigstore bundle asset; the per-finding suppression ledger
  is homed in P0.6.
- **Stale-text fixes.** P0.5 line-113 CSV "field-count parity" → the real RFC-4180 reader (it
  contradicted line 121 + G31/G32); the §7 frozen r1 14-class list marked as a historical
  snapshot; the §0 pinned-tool list names cargo-mutants/cargo-auditable/minisign; P0.7
  plane-annotation line-number cross-refs → phase-name references.
- **Living-doc/spec-sync (r4).** Spec §0.10 (the three by-construction keys), §0.7 (main.json
  comment), §6.7.2 + §6 CI-hardening (harden-runner runner-type), §2.14 (scratch-residue
  accepted-residual) are owned by the spec and edited in the same change (SSOT > spec > docs).

## 11. Reconciled during P0 review r5

- **New gates:** **G56b** (release-tag `v*` trust gate — tag-protection ruleset + ancestry/green-history
  release-workflow first step + signed-tag verify, the secret-bearing trigger G56a did not cover, the
  highest-value gap closed this round); **G59** (`actions/attest-build-provenance` — PROMOTED from
  `[DEFER: post-v1]` to a v1 OWNER DECISION, verify-on-runner + offline Sigstore bundle asset, in the
  G58 enumeration); **G60** (reserved — third-party-reproducibility delta of the self-compiled
  Rust-core+WebView layer via `diffoscope`, informational); **G66** (reserved — scheduled
  engine-version-currency poller, the only signal for the "upstream fixed it, no CVE yet" case);
  **G67** (reserved — OSSF Scorecard informational corroboration of G50/G56/G56a).
- **G29 project-local rule (g)** added — the per-push `std::net`/`tokio::net`-outside-allow-list ban
  (the only per-push Rust-source proof that first-party Rust opens no socket), with an initially-empty
  `net-allow-list.txt` + a planted-positive; removed from §8 forward-ideas, cited in the §5 T9a row.
  `cargo-acl`/cackle promoted to an owner-decidable P0.4 contract (the behavioural backstop).
- **G42 macOS fallback corrected:** `nettop -m tcp -P` (socket STATE, misses the blocked connect()
  attempt + UDP/DNS) → interface-level `tcpdump -i lo0 -n` + `tcpdump -i en0 -n` so the armed-window
  canary is recordable on macOS even when the connection is blocked; same fail-closed posture if absent.
- **attest-build-provenance three-way status drift resolved** (§8 `[DEFER]` / P0.7 promoted / §10 done)
  → ONE status: adopted-for-v1 (G59), §8 `[DEFER]` removed, P0.7 hedge removed, `id-token: write`
  scoped to the release job only (a new G56 sub-rule). New plan-lint **check 17** enforces the agreement.
- **T8 integration gate** added to G31 (output-into-source-folder mid-batch + two-instance `*.part`
  hand-off) + the §5 T8 row + the G31 host-list + the P0.5 homes; G15 stays the unit leg.
- **plan-lint checks 17** (§8-forward-idea ↔ adopted-gate status agreement) **+ 18** (named-build-loop-
  procedure presence — crash-recovery + divergence-resolution must exist in `build-loop.md`); the G8
  vocabulary gained "currently absent"; a golden-fixture base-case meta-check proves plan-lint still
  detects a planted violation each run.
- **G56a sub-checks** (secret-scanning + push-protection enabled, default-workflow-permissions read-only);
  **G56 sub-rules** (`id-token` scope on the release job only, `pull_request_target` safe-handler shape,
  harden-runner-reported-ENFORCED on the signing job). **G64/G65 homed in P0.7** beside the other release
  ratchets (G64 moved from a P0.5 one-liner; G65 newly homed with the AFL++/radamsa + staged-engine
  constraint + owner-decision-before-P10).
- **Gate hardening this round.** G35 NOTICE-parity gained the GPL/LGPL corresponding-source-POINTER
  assertion (the §3.6.2 GPL-FFmpeg discharge mechanism) + the from-source pinned-source/toolchain
  reference + the SHA256SUMS-line assertion (G58); G18a added `imports.lock` to the lockfile diff +
  G9 invariant (e) bans `cargo vet update/sync/import` in CI/scripts + G18b decided ≥2 import sources;
  G31 gained the non-empty/output≠input/size-plausibility sub-assertions + the detection-layer KAT
  (`tests/detect-kat.toml`); G32 gained machine-enumerated lossless pairs + the lossy-disclosure
  property test + the conversion-output determinism sub-assertion; G48 clarified the ASAN-coverage
  honesty (staged `.so` → Rust/boundary only) + pinned libFuzzer resource bounds; G38 gained the FFmpeg
  enabled-decoder allow-list (`ffmpeg-allowed-decoders.lock`); G12 gained the findings-block-presence
  sub-check; G29 gained per-project-local-rule planted-positives; G47 gained
  `bundle.createUpdaterArtifacts`-absent + the DNS-prefetch meta-tag; check 10 promoted to
  regenerate-and-diff for the security manifests; the gate-quarantine procedure gained the
  self-referential L1/plan-lint bootstrap exception; the build-loop push-exit-code capture adopted the
  background-push marker-file pattern; G9 invariant (f) machine-enforces the `fc.gen()` shrink-wrapper rule.
- **Honesty / naming fixes.** `ANTHROPIC_API_KEY` named as the third real secret (G2 + §5 row); the
  `gitleaks-action` org-license footgun stated (CI uses the pinned binary, not the Marketplace action);
  `cargo-cyclonedx` crate-vs-subcommand naming; `scripts/glibc-floor.toml` (G37c) + the LibreOffice
  carve-out globs (G9) named concretely; the L1 budget restated as a SOFT target (§0); the
  two-enforcement-planes principle softened to "every gate that CAN run locally" (the L5/CI-only gates
  legitimately run one plane); harden-runner Community-tier silent-degrade residual + the ENFORCED
  assertion (principle 11(c) / G56).
- **Living-doc/spec-sync (r5).** Spec material r5 gates touch — §6.7.2/§6.7.3 (G56b tag-trust on the
  release trigger), §3.6.2 (the GPL FFmpeg corresponding-source discharge G35 asserts), §0.10
  (`bundle.createUpdaterArtifacts` + DNS-prefetch, G47), §6.4.1 (the detect-KAT, G15) — is owned by the
  spec and edited in the same change per SSOT > spec > docs; noted here so the fill pass syncs the spec.

## 12. Reconciled during P0 review r6

- **ImageMagick — the last untrusted-input decoder with no per-engine hardening (closed).** ImageMagick
  (REQUIRED bundled BMP load+save delegate, statically linked in `convertia-imgworker`, spec §3.5.5; the
  historically most CVE-dense decoder family) had no control, no G38 assertion, no G31 sentinel. **G38**
  gained the hardened-`policy.xml`-or-coder-excluded-build assertion; **G29** gained the imgworker
  `MAGICK_CONFIGURE_PATH`-points-at-the-bundle-policy rule (so MagickCore reads the bundled policy); **G31**
  gained the crafted-BMP / SVG-via-MSL/URL-coder sentinel; **G26** gained the `engines.lock`↔adversarial-
  fixture bijection guard. §5 T9b/T1 + P0.5/P0.7 + spec §3.5.5 (synced FIRST) updated.
- **pandoc `--sandbox` floor corrected ≥ 2.17 → ≥ 2.15 (a baked-in WRONG fact removed from G38).**
  `--sandbox` shipped + is effective since 2.15 (verified vs the release notes; spec §3.5.4 already says
  ≥ 2.15); the false "older silently ignore `--sandbox`" rationale removed. G38, G29, §5 T9b, P0.7 corrected.
  The pandoc `--resource-path` rationale corrected too — per spec §3.5.4 ConvertIA configures no Lua/JSON
  filters and no pandoc PDF path, so it is belt-and-suspenders read-scoping, NOT closure of a live
  filter-execution vector.
- **`core.hooksPath` named as a forbidden bypass + G54 fixed.** Added to the forbidden-bypass set (§0 +
  the §3 plane table + P0.6 step-0); **G54** now resolves the EFFECTIVE hooks dir
  (`git rev-parse --git-path hooks`) and asserts `core.hooksPath` is unset/lefthook-managed, instead of
  hardcoding `.git/hooks/`.
- **G53 homed in P0.3** (its `cargo-deny [bans]` workspace-member rule + a G24 negative self-test fixture),
  making plan-lint check 16 satisfiable for it; **`tauri-plugin-http` added to the G18 deny-list** (it wraps
  `reqwest` + registers an IPC HTTP client).
- **G56b leg 3 made real** (the loop signs `v*` tags; a committed SSH allowed-signers file backs
  `git verify-tag`; distinct from spec §6.7.1's requested-not-required DCO commit sign-off) — closing the
  prior unsatisfiable "verify against the committed signer key" + the "(recommended)" vs "fail-closed always"
  self-disagreement.
- **Verified tool corrections.** **G17** JS leg `pnpm audit` → `osv-scanner` over `pnpm-lock.yaml`
  (`pnpm audit` is online-only AND broken on pnpm 10.x — registry retired the legacy endpoint, HTTP 410);
  **G38** Windows PE census `checksec`/`dumpbin` → **`winchecksec`** (Trail of Bits — checksec has no native
  PE support); **G43** macOS/Windows live monitors are SIP-blocked (`fs_usage`/DTrace) / GUI-first (Procmon)
  → the CLI-automatable before/after snapshot-diff (`reg export`, LaunchAgents/file-assoc enumeration +
  `lsof +D`) is the load-bearing leg; **G59** offline attestation needs `--custom-trusted-root
  trusted_root.jsonl` (a bare `--bundle/--repo` reaches Sigstore) — the asset added to G58; **G30** macOS
  universal sidecars get a `lipo -info` both-slices (`arm64`+`x86_64`) assertion (Tauri does not itself
  `lipo`).
- **G42b read-half "can never silently no-enforce" scoped to the LINUX leg** (the armed `ptrace`/Landlock
  substrate is Linux-only; macOS/Windows rest on the degradable §2.12.3 tier + the G31 oracle, with an
  optional armed planted-positive or an owner-accepted residual).
- **Value-not-just-presence argv checks.** G29 now asserts the pandoc `--resource-path` VALUE resolves to
  scratch (presence-only passed `/`) and the LibreOffice `-env:UserInstallation` disposable-profile is
  PRESENT with a scratch value (a dropped flag re-used the user/home profile — T9b read-half + T8).
- **Generated/derived → also-VERIFIED upgrades.** G55↔G35 embedded-list-equals-SBOM agreement (+ `cargo
  audit bin` on the shipped binary); plan-lint check 10 gained a non-vacuous-artifact leg (an empty-but-in-
  sync golden would silently disable G38); G32 gained a non-vacuous-exception guard (an "excepted"
  non-deterministic pair must actually differ on a double-run, else the exception is removed).
- **Suppression-ratchet symmetry.** The `.gitleaks.toml` allowlist/baseline (G2) + the cargo-vet
  exemption set (G18b) gained the box-id + Dual-Review + rotating-fingerprint growth-guard; both join the
  L(-1) set; a **G56 secret-in-Actions-log scan** (`set -x`/echo/pipe of a secret-named var; post-run log
  shape-scan) + a GitHub-Environment-required-reviewers idea close the secret-leak axis.
- **Gate-tool checksum VALUE authenticity** (corroborated against the tool's own signature / ≥2 origins,
  G37-discipline applied to the gate tools; §0 + P0.2) — the wrong-checksum self-test alone proved nothing
  about authenticity. **cargo-fuzz invokes the date-pinned nightly explicitly** (`+nightly-YYYY-MM-DD`, never
  a bare `+nightly`, which ignores `rust-toolchain.toml`'s channel; P0.2).
- **Reviewer-prompt + reviewer-family pinned.** New plan-lint **check 19** (the reviewer rubric is a
  committed, sourced-verbatim, canonical-phrase artifact joined to the L(-1) set) + **check 20** (the
  recorded reviewer-family owner decision present in `build-loop.md`); security-concept §4 records the
  decision (accept-the-correlated-residual for v1 + a concrete spot-audit cadence; flip stays open).
- **build-loop soundness.** step-2a SHA-anchors the run-ID capture (a fast second push can't steal "the
  most recent run") and collapses the G42/G42b-window protection to ONE rule (defer the check-off push
  until the box-commit run is green; `cancel-in-progress: false` is belt-and-suspenders, not an
  alternative); the cluster-boundary soft-stop counter increments ONLY on committed boxes; the
  session-start CI-health check is workflow/event/branch-filtered + fires per-box; the `[!]` dep-unlock
  scan runs at startup before first-box selection.
- **vuln-response runbook** gained the dominant offline-app branch — a confirmed high-severity engine vuln
  with NO upstream fix yet (disable the decoder via the G38 allow-list / drop the format path / escalate),
  tied to a `SECURITY.md` known-issues line.
- **New forward ideas (§8 r6 block).** CodeQL/Semgrep-taint for the T2 surface; JS name-trust via
  Socket/an npm allowlist; the `build.rs` engine-integrity-manifest binding (T3 residual); a DNS-observation
  G42 leg; gate-infrastructure ReDoS/huge-diff resource bounds; TruffleHog verified-secret release-tier
  corroborator; the GitHub-Environment release gate; the `docs/reproduce.md` third-party rebuild recipe
  (the human half of G60). **G65 pre-committed to a REQUIRED SCHEDULED issue-opener** (`zzuf` named for
  LibreOffice). **G47** gained `dangerousRemoteDomainIpcAccess`-absent + opener-prefix-deny + G29 rule (h)
  (opener-egress constant-only); **G56** gained the container/service image-digest + transitive-action-pin
  sub-checks (zizmor/G50 covers neither). **G37c** gained a runtime launch-smoke + macOS `minos` analogue
  (it is a static symbol-version proxy only; in r7 BOTH were promoted from a hedged "forward idea"
  parenthetical to REQUIRED G37c sub-assertions with planted-positives, resolving the body-vs-changelog
  status conflict). The §6.10 row-21 G43 home (README P10) synced; the Windows
  ACL scratch + cleanup-on-kill homed in P0.5; "audit-WORM" → "immutable audit-log row" (P0.6).
- **Nits.** G29 Semgrep rule packs explicitly invoked via `--config scripts/semgrep-rules/` (never the
  live-fetch `--config p/rust`) + a `--validate` self-test; G29 rule (g) FFI honest-limitation note (it
  proves no socket at `std::net`/`tokio::net` import sites only — the raw `libc::socket` FFI path is the
  G26/G42 + cackle domain); the L3-before-L2 chronological note added to P0.2's lefthook bullet; the G47
  `frame-ancestors` honesty residual (WebKit may ignore it; the host restrictions are the structural bound);
  the security-concept §3 L1 budget marked SOFT; the G42b "can never silently no-enforce" scoped to the
  Linux leg in §5.
- **Living-doc/spec-sync (r6).** The ImageMagick coder/delegate hardening is owned by the spec (§3.5.5,
  edited FIRST this change). The pandoc ≥ 2.15 floor already matched spec §3.5.4 — the docs were corrected
  TO the spec (no spec edit needed there), per SSOT > spec > docs.

## 13. Reconciled during P0 review r7

- **G17 `cargo audit --locked` → plain `cargo audit` (tool-fact fix, mustFix).** `cargo audit` has NO
  `--locked` flag (it reads `Cargo.lock` by default; `--locked` is a cargo BUILT-IN, not a `cargo audit`
  argument), so `cargo audit --locked` makes clap reject an unknown argument and hard-fails spuriously on the
  pinned binary — the §0 footgun. Corrected to plain `cargo audit` (+ `--no-fetch` for the offline leg, keeping
  `cargo audit --json .database.last-updated` for the staleness floor); lockfile integrity stays owned by G18a.
  G17 + P0.4 corrected. The complete flag set is recorded for the on-bump re-verify.
- **G56a sub-check (f) self-contradiction resolved → OR-GATE (mustFix).** (f) hard-asserted CodeQL
  `state == "configured"` while the SAME gate + the §5 T2 row + P0.2 declared it owner-decidable with the G29
  rule-(i) Semgrep taint path as the equivalent — so choosing the sanctioned Semgrep path walked into a
  permanently-red required check. (f) is now an OR-gate (CodeQL configured **OR** the G29 rule-(i) taint ruleset
  present with its planted-positive), with **new plan-lint check 21** machine-enforcing the XOR (exactly one live).
  §5 T2, P0.2, P0.3 synced.
- **Invisible gate-ID gaps closed (mustFix).** The catalogue jumped G60→G64 (G61/G62/G63 absent, no note) and
  P0.7 said "the former G40 is deleted" with NO matching catalogue note. Added a consolidated **"Vacated /
  reserved gate IDs"** blockquote (G34 vacated, G40 deleted, G61–G63 reserved, G65/G66/G67 reserved + the
  G18e sub-letter reserved) and **new plan-lint check 22** asserting every integer N in `2..max(Gnn)` is a row
  OR a documented note — so an invisible ID can never survive and the "every Gnn is the real control" thesis is
  machine-backed.
- **shouldFix adopted.** `release` GitHub Environment (required-reviewers + `v*` deployment policy) for the three
  release secrets — a G56 sub-assertion + P0.7 provisioning (turns G56b leg-2 "abort before read" into "secret
  never injected without approval"). **G35a** DERIVED static-link closure (the static analogue of G37b — derive
  the imgworker static stack from the build via `llvm-nm`/`nm -A`/`cargo-auditable`/linker map, hard-fail any
  linked lib absent from `engines.lock`). **G38** FFmpeg configure-flag / whole-binary-license assertion (assert
  `--enable-gpl`, HARD-FAIL `--enable-nonfree`/`--enable-libfdk-aac`, record the `configuration:` line in
  `engines.lock`). **G29 rule (j)** raw-socket FFI net-ban (`libc::socket`/`nix::sys::socket`) promoted from
  "optionally" to REQUIRED, cited in §5 T9a. **`ANTHROPIC_API_KEY` host-isolation** stated honestly
  (build-session-local, in NO CI job; the G56 self-hosted-label lint keys on all three secrets).
  **G48 Windows crash-replay** mechanism named (`tests/fuzz_replay.rs` plain `cargo test`, all platforms; the
  "cargo-fuzz can't build on Windows" premise was WRONG — it works via MSVC ASan — but plain replay is the chosen
  design). **G56 harden-runner-ENFORCED** via an ACTIVE PROBE (the action exposes no enforcement-status output).
  **security-concept §3 gate-range labels** "G26–G34/G35–G67" → accurate prose (G34 vacated, L5 starts at G33b).
  **review-rubric home DECIDED** = a fenced canonical block in `build-loop.md` (not a separate file — keeps "seven
  P0 docs" exact); check 19 + P0.6/P0.3 synced. **G31 document→image** size-floor fallback REMOVED → mandatory OCR
  content check (`tesseract`/`pdftotext` for the committed `expected_text`). **G26** positive output-validity leg
  for non-adversarial inputs (closes the engine-staging activation-gap before G31 populates). **G32 determinism**
  floor raised to ≥1 pair per engine PER OUTPUT-FORMAT CATEGORY + a `diffoscope` empty-diff timestamp assertion.
  **G26/G31/G65 CI-HOST resource bound** (cgroup/`ulimit` memory/disk/time so a corpus bomb is a contained finding,
  not a shared-VPS outage). **G42 DNS-only egress leg** promoted to REQUIRED (`tcpdump port 53` / Windows ETW
  DNS-Client, armed canary + resolver-cache flush).
- **Ideas folded in.** G56a sub-check (g) require-signed-commits on `main` (the loop reuses the P0.7 SSH key to
  sign its own commits — closes the integrity axis tag-only signing left open). T10 output/scratch BYTE budget
  (disk-exhaustion sibling). check 16 CORRECTNESS-not-only-LIVENESS for load-bearing custom gates (≥N evasion-
  variant fixtures + `cargo-mutants` over the gate-script crate) + the API-introspection-gate carve-out (nit 7).
  minisign key GENESIS-and-CUSTODY policy (P0.7, distinct from rotation). TruffleHog `--only-verified` egress-
  interaction note (§8). **G18e** reserved id for the npm name-trust allowlist. Corpus-job ephemerality
  machine-checked + per-job clean-checkout on the untrusted-corpus leg (G56). Mixed-batch LibreOffice state-leak
  fixture (G31). Windows AV-lock cleanup-on-fault sub-case (P0.5). BRANCH-coverage floor for detect/fs_guard/
  isolation (`cargo-llvm-cov --branch`, G27/P0.4). Per-numeric-IPC-arg overflow `proptest` +
  `clippy::arithmetic_side_effects` deny on the IPC handler (G4/G48/P0.4). Deep-link/URL-scheme absence +
  local-`url` assertion (G47). pip dependabot entry for `requirements-ci.txt` (G56 + principle 10). Two-Rust-
  advisory-scanner reconciliation (`cargo-deny [advisories]` authoritative; G18). Informational→required ratchet
  decision-log `docs/process/gate-status.md` + **new plan-lint check 23**.
- **Nits.** §7 frozen-snapshot leak guarded (check 8 reads only §5 + the canonical set). poppler added to the §5
  T9b control + verifying-gates cells + the G31 sentinel host-list. G49 L4 mirror → a note (one `| **G49** |` row).
  check 14 asserts the lettered DoD sequence IN ORDER; check 15 prefers exact-integer operator-anchored thresholds
  over the tilde forms. G8 `[!extern]` suppressor restricted to `.md` under `docs/plan/` (production code suppresses
  only via `[Build-Session-Entscheidung]`), G24 negative self-test added. P0.7 heading renamed to include
  "security-ratchet". P0-exit L4 run-log URL recorded in a committed `docs/process/p0-completion.md` with a
  URL-format plan-lint check.
- **DECLINED.** None of the synthesis report's items were declined as wrong; weaker framings were tightened rather
  than dropped. (TruffleHog verified-mode + the JS name-trust allowlist stay forward ideas / a reserved id rather
  than adopted gates, matching the report's own "idea" framing.)
- **Living-doc/spec-sync (r7).** Where r7 gates reference spec material owned by the spec — §3.6.1 (the FFmpeg
  configure-flag / `--enable-nonfree` whole-binary-license assertion G38 adds), §7.8.2 (the deep-link/URL-scheme
  absence + local-`url` G47 asserts), §1.10 (the output/scratch byte budget), §7.2.3 (already extended for
  bundled-config startup hashing in r7's G46), §6.4.2 (the CI-host resource bound + the DNS-egress leg) — the spec
  is edited in the same change per the SSOT > spec > docs conflict order; those spec edits are noted here so the
  fill pass syncs them.
