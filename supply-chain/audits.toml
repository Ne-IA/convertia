# supply-chain/audits.toml — ConvertIA's own (first-party) crate-trust audits (G18b). Populated in
# P10 (P10.59) via `cargo vet certify` — the live run was deferred (P1.59); each entry records a
# per-crate audit at `safe-to-run` (build-only / proc-macro deps) or `safe-to-deploy` (runtime deps).
# Empty until then.
# L(-1) (supply-chain/**, G71).

[criteria]

[audits]
