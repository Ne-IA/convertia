# scripts/doc-fingerprints.toml — the G68 check-25 leg (c2) freshness ledger (P0.3.12).
#
# WHY: plan-lint check 25's resolution leg (b) and name-matching freshness leg (c1) catch a doc that
# links to a moved file or NAMES a removed/renamed gate-id. They CANNOT see the "described-the-old-way"
# drift where an authoritative SOURCE keeps its name but changes its CONTENT — a Gnn row reworded, the
# spec §0.7 physical tree edited, a §0.8 version pin bumped — silently stranding every doc that
# DESCRIBES it. This ledger is the per-source content-fingerprint that closes that gap (build-gates.md
# §6 check 25(c) / the G68 "load-bearing" freshness leg; the same content-fingerprint discipline the
# G29 suppression ledger uses).
#
# HOW: each [[source]] records the sha256 of an authoritative source REGION as of when the docs that
# DESCRIBE it were last reasoned against it. plan-lint recomputes the region's hash on every run; a
# MISMATCH fails check 25. The fix is never to silently re-hash: you re-VERIFY each `describes` doc
# against the new source content, update them where stale, THEN re-bless the fingerprint here — all in
# the SAME commit (DoD item 2). Because this file is L(-1) (matched by `scripts/*.toml`, G71), a
# re-bless is an owner-acked edit — the freshness check cannot be silently waived on the source it guards.
#
# COMPUTING a fingerprint: `sha256:` + sha256 of the LF-normalized region. For kind="file" the region
# is the whole file; for kind="section" it is the numbered-heading body (heading line through the line
# before the next heading of level <= its own, fence-aware). Always compute via plan-lint's own
# `_lf`/`_extract_section` so seed-time and check-time normalize identically (see the P0.3.12 box).
#
# FIELDS: id (stable label) · kind ("file" | "section") · file (repo-relative) · anchor (section number,
# kind="section" only) · describes (the docs reasoned against this source — informational, surfaced in
# the failure message) · fingerprint ("sha256:<64 lowercase hex>") · dormant (optional {file, contains}:
# the entry is SKIPPED while `file` still contains ANY string in `contains` (a str OR a list) — for a
# binding whose describing doc is not yet authored; matches the G69 check-26 OR-dormancy exactly).
#
# FLOOR: plan-lint's _REQUIRED_FP_IDS lists the seed ids that MUST be present here, else the freshness
# axis would silently no-op (a fail-OPEN hole). An empty/seedless ledger, or one a required seed was
# dropped from, FAILS check 25. Retiring a binding = edit BOTH this file AND _REQUIRED_FP_IDS in the
# same owner-acked L(-1) commit.
#
# SCOPE (P0.3.12): the mechanism is fail-closed from P0; the ledger is SEEDED with the live + dormant
# bindings whose source exists today and GROWS as P1 stabilizes more describing relationships (gate-row
# bindings once the catalogue settles, §0.8 version pins, the Rust dispatch enums). Adding a [[source]]
# is the normal ledger-growth discipline, never a deferral of the mechanism.

# (a) LIVE — the authoritative L(-1) cage glob-list. Its prose copies (security-concept §2, the
#     build-gates G71 row, CLAUDE.md §5, the docs-only-fastpath list) are its human descriptions; the
#     G71 row itself states this sync is "held in sync by G68". A cage edit (a glob added/removed) must
#     re-verify those four prose copies — this fingerprint forces it. Both the cage and this ledger are
#     L(-1), so a cage change + this re-bless ride the one owner-acked commit.
[[source]]
id = "l-neg1-cage"
kind = "file"
file = "scripts/l-neg1-files.toml"
describes = [
  "security-concept.md §2 (the L(-1) enumeration)",
  "build-gates.md G71 row (the cage description)",
  "CLAUDE.md §5 (the L(-1) anti-pattern bullet)",
  "the docs-only-fastpath L(-1) list (G10)",
]
fingerprint = "sha256:59cf428eea6d3c0ae12b6b2a02e153826f7d7002d174d2d2c5ac16717867fc60"
# ^ re-blessed 2026-07-21 (P3.72): the cage gained `max_survived_mutants.toml` (the G15 mutation
#   ratchet, the coverage-floors class); the §2 + G71-row enumerations were synced in the same
#   commit; CLAUDE §5 is deliberately non-exhaustive, and the docs-only fastpath's skip
#   eligibility is a hardcoded .md/LICENSE/NOTICE allow-list (it never parses the cage), so a
#   non-.md file like the ratchet is structurally ineligible — both re-verified, no edit needed.
# ^ re-blessed 2026-08-31 (the P4.56.1 pre-planned tail (b)): the bare `engines.lock` pattern was
#   re-spelled `src-tauri/engines.lock` — check-l-neg1-ack matches root-anchored (`^...$`), so the
#   bare spelling never matched the §3.7.2-homed manifest this act commits; the pattern rename is a
#   WIDENING onto the real path, not a narrowing (the old pattern matched no spec-sanctioned path —
#   §3.7.2 fixes the home in src-tauri/; a stray repo-root engines.lock would be an untracked
#   alien). All four describes copies re-verified: security-concept §2 and the G71
#   row name `engines.lock` path-free (still true of the file), CLAUDE §5 likewise (and is
#   deliberately non-exhaustive), and the docs-only fastpath's hardcoded .md allow-list makes a
#   .lock file structurally ineligible — no prose edit needed FOR THE RENAME (the G71 row and the
#   §2 sentence ARE edited in this same act, for the separate cage-liveness-audit passage).

# (b) LIVE since P1.64 — the spec §0.7 physical tree is the HIGHER source (SSOT > spec > docs); the
#     CLAUDE.md §1a "Repo layout" map is its operational projection. This fingerprint is the freshness
#     companion to G69 check 26's projection bind: a §0.7 tree edit not mirrored into §1a surfaces as
#     stale here. P1.64 authored §1a (deleting the bootstrap "PLACEHOLDER" stub — which lifted BOTH this
#     binding's former dormancy AND G69 check 26's skip, together), re-verified §1a as a faithful
#     projection of the expanded §0.7 (the check-26 bijection §1a == on-disk-tree ⊆ §0.7 passes), and
#     re-blessed the hash below in that commit. A later §0.7 tree edit not mirrored into §1a now fails
#     check 25; the `dormant` key was removed when PLACEHOLDER ceased to exist (the binding is permanent).
#     Re-blessed 2026-07-21 (the P3.87 bin+lib split): §0.7's physical tree gained the
#     `src-tauri/src/lib.rs` FILE row (no directory change — the G69 dir-set is untouched); §1a
#     re-verified as the faithful projection in the same commit (its `src/` row parenthetical re-cut).
#     Re-blessed 2026-07-21 (P3.73 — the fuzz/ tree + the P0 ruling): §0.7 gained the fuzz/ rows
#     (fuzz_targets/ + corpus/ + the two per-target corpus subdirs) and the P0-ruling re-cut of the
#     corpus-subdir descriptions (the win_* seeds re-homed onto fs_guard_resolve_identity/;
#     fs_guard_is_safe_output/ carries its own nul_output_path seed); §1a re-verified as the faithful
#     projection in the same commit (its fuzz/ / fuzz_targets/ / corpus/ rows land with the staged
#     tree; the corpus SUBDIR rows are §0.7-only detail — the §1a ⊆ §0.7 projection bind holds and
#     check 26 is green over the staged tree). Hash computed via plan-lint's own _extract_section.
#     Re-blessed 2026-08-31 (the P4.32 Co-Pilot residual): §0.7's engines/ file list gained the
#     program.rs FILE row (§3.3.3 resolution + the EngineId→binary-name table — no directory change,
#     the G69 dir-set untouched); §1a re-verified in the same commit: its engines/ row names
#     §-responsibilities, not files, and was extended with §3.3.3 in the same act, so the projection
#     holds. The P3.87 precedent is the PROCEDURE (a FILE row leaves the G69 dir-set untouched;
#     §1a is re-verified and edited iff its own text is touched by the change — P3.87 did re-cut
#     §1a's src/ parenthetical then), never a no-edit outcome.
#     Re-blessed 2026-08-31 (the P4.56.1 pre-planned tail (a)): §0.7's engines/ file list gained the
#     lock.rs FILE row (§3.7.2 engines.lock schema + validator, the (artifact, target-triple) row
#     law — no directory change, the G69 dir-set untouched) + the three engines container lines
#     (tree parent row, tier-2 logical block, responsibility bullet) extended with §3.7.2; §1a
#     re-verified in the same act: its engines/ row was ALREADY extended with the §3.7.2 schema by
#     the Loop-half commit (3fac83a — §1a is docs-layer, Loop-editable), so this §0.7 edit restores
#     the projection ORDER (§0.7 is the higher source; the two now agree) with no further §1a edit.
#     Re-blessed 2026-08-31 (the P4.56.1 pre-planned tail (b), batched per the tail-(a) review's
#     forward finding): §0.7's src-tauri/ file list gained the engines.lock MANIFEST row in the
#     same act that creates the file + re-spells its cage glob (one owner-acked commit re-blessing
#     BOTH ledger entries instead of a third §0.7 tail). A FILE row only — the G69 dir-set is
#     untouched; §1a re-verified: its src-tauri/ block lists DIRECTORIES only (no rows for files at
#     the src-tauri/ root — Cargo.toml/tauri.conf.json are likewise unlisted there), so the
#     projection holds unedited.
[[source]]
id = "spec-0.7-physical-tree"
kind = "section"
file = "docs/spec/00-architecture.md"
anchor = "0.7"
describes = ["CLAUDE.md §1a Repo-layout map (the §0.7 projection)"]
fingerprint = "sha256:39de2367239feed74d63bf2ec99d65ecab354b12621e3ad4a8a0384ac69c2c6f"
